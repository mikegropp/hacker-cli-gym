{
  "catalog_version": 2,
  "track": "linux",
  "platform": "linux",
  "shell": "bash",
  "command_count": 100,
  "stages_per_command": 5,
  "exercise_count": 500,
  "sections": [
    {
      "id": "01",
      "title": "Navigation and help",
      "start": 1,
      "end": 50
    },
    {
      "id": "02",
      "title": "Files and directories",
      "start": 51,
      "end": 100
    },
    {
      "id": "03",
      "title": "Reading content",
      "start": 101,
      "end": 150
    },
    {
      "id": "04",
      "title": "Text processing",
      "start": 151,
      "end": 200
    },
    {
      "id": "05",
      "title": "Composition and comparison",
      "start": 201,
      "end": 250
    },
    {
      "id": "06",
      "title": "Identity and permissions",
      "start": 251,
      "end": 300
    },
    {
      "id": "07",
      "title": "Processes and execution",
      "start": 301,
      "end": 350
    },
    {
      "id": "08",
      "title": "System and storage",
      "start": 351,
      "end": 400
    },
    {
      "id": "09",
      "title": "Archives, data, and automation",
      "start": 401,
      "end": 450
    },
    {
      "id": "10",
      "title": "Networking and services",
      "start": 451,
      "end": 500
    }
  ],
  "lessons": [
    {
      "id": "linux-pwd",
      "order": 1,
      "section": "01-navigation-and-help",
      "command": "pwd",
      "title": "Know where you are",
      "about": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain.",
      "example": "pwd",
      "example_output": "/home/learner/projects\n",
      "breakdown": [
        "pwd needs no arguments here.",
        "The result anchors every relative path that follows."
      ],
      "task": "Print the absolute path of this disposable practice workspace.",
      "solution": "pwd",
      "hints": [
        "The topic command prints the current working directory.",
        "Use the checker as a specification: The workspace path was printed.",
        "Try this command shape, replacing placeholders with fixture values: pwd"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The workspace path was printed.",
          "expected": "{{workspace}}",
          "normalize": "trim"
        }
      ],
      "completion": "Orient first. A correct command in the wrong directory can still produce the wrong result.",
      "command_order": 1,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "pwd"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-pwd-2",
      "section": "01-navigation-and-help",
      "command": "pwd",
      "title": "Anchor a nested workflow",
      "about": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. Run pwd after changing directories to verify exactly where a relative operation will begin.",
      "example": "cd projects && pwd",
      "example_output": "/work/projects",
      "breakdown": [
        "pwd is the featured command in the worked pattern.",
        "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. Run pwd after changing directories to verify exactly where a relative operation will begin.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Enter reports/daily and print its absolute path.",
      "solution": "cd reports/daily && pwd",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cd <value> && pwd",
        "Use man pwd or pwd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "reports/daily"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Anchor a nested workflow”.",
          "expected": "{{workspace}}/reports/daily",
          "normalize": "trim"
        }
      ],
      "completion": "Run pwd after changing directories to verify exactly where a relative operation will begin. You can now use this behavior as part of a larger command-line workflow.",
      "order": 2,
      "command_order": 1,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. Run pwd after changing directories to verify exactly where a relative operation will begin.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "pwd",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "cd"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-pwd-3",
      "section": "01-navigation-and-help",
      "command": "pwd",
      "title": "Resolve the physical directory",
      "about": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. The -P option follows symbolic links and prints the physical directory on disk.",
      "example": "cd current && pwd -P",
      "example_output": "/work/releases/2026",
      "breakdown": [
        "pwd is the featured command in the worked pattern.",
        "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. The -P option follows symbolic links and prints the physical directory on disk.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Enter the current symlink and print the physical release directory.",
      "solution": "cd current && pwd -P",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cd <value> && pwd -P",
        "Use man pwd or pwd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "releases/2026"
      ],
      "files": {},
      "symlinks": {
        "current": "releases/2026"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Resolve the physical directory”.",
          "expected": "{{workspace}}/releases/2026",
          "normalize": "trim"
        }
      ],
      "completion": "The -P option follows symbolic links and prints the physical directory on disk. You can now use this behavior as part of a larger command-line workflow.",
      "order": 3,
      "command_order": 1,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. The -P option follows symbolic links and prints the physical directory on disk.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "pwd",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "cd"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-pwd-4",
      "section": "01-navigation-and-help",
      "command": "pwd",
      "title": "Compare logical and physical paths",
      "about": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. pwd -L preserves the logical symlink path, while pwd -P reveals its physical target.",
      "example": "cd live && printf '%s\\n%s\\n' \"$(pwd -L)\" \"$(pwd -P)\"",
      "example_output": "/work/live\n/work/releases/v2",
      "breakdown": [
        "pwd is the featured command in the worked pattern.",
        "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. pwd -L preserves the logical symlink path, while pwd -P reveals its physical target.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Enter live and print its logical path followed by its physical path.",
      "solution": "cd live && printf '%s\\n%s\\n' \"$(pwd -L)\" \"$(pwd -P)\"",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cd <value> && printf <values>",
        "Use man pwd or pwd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "releases/v2"
      ],
      "files": {},
      "symlinks": {
        "live": "releases/v2"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Compare logical and physical paths”.",
          "expected": "{{workspace}}/live\n{{workspace}}/releases/v2",
          "normalize": "trim"
        }
      ],
      "completion": "pwd -L preserves the logical symlink path, while pwd -P reveals its physical target. You can now use this behavior as part of a larger command-line workflow.",
      "order": 4,
      "command_order": 1,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. pwd -L preserves the logical symlink path, while pwd -P reveals its physical target.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "pwd",
        "command chaining",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "cd",
        "printf"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-pwd-5",
      "section": "01-navigation-and-help",
      "command": "pwd",
      "title": "Record a canonical deployment path",
      "about": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. Command substitution lets a script capture pwd -P as reliable workflow data.",
      "example": "cd active && printf 'root=%s\\n' \"$(pwd -P)\" > /work/location.env",
      "example_output": "",
      "breakdown": [
        "pwd is the featured command in the worked pattern.",
        "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. Command substitution lets a script capture pwd -P as reliable workflow data.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Enter active and write root=<physical path> to location.env in the workspace root.",
      "solution": "cd active && printf 'root=%s\\n' \"$(pwd -P)\" > /work/location.env",
      "hints": [
        "Use the checker as a specification: The canonical deployment root was recorded.",
        "Try this command shape, replacing placeholders with fixture values: cd <value> && printf <values> > <value>",
        "Use man pwd or pwd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "deployments/blue"
      ],
      "files": {},
      "symlinks": {
        "active": "deployments/blue"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "The canonical deployment root was recorded.",
          "path": "location.env",
          "expected": "root={{workspace}}/deployments/blue\n"
        }
      ],
      "completion": "Command substitution lets a script capture pwd -P as reliable workflow data. You can now use this behavior as part of a larger command-line workflow.",
      "order": 5,
      "command_order": 1,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "pwd prints the absolute path of the shell's current working directory. Use it before a relative-path operation when location is uncertain. Command substitution lets a script capture pwd -P as reliable workflow data.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "pwd",
        "redirection",
        "command chaining",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "cd",
        "printf",
        "env"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ls",
      "order": 6,
      "section": "01-navigation-and-help",
      "command": "ls",
      "title": "List visible and hidden entries",
      "about": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe.",
      "example": "ls -a1",
      "example_output": ".\n..\n.env\nnotes.txt\n",
      "breakdown": [
        "-a includes names beginning with a dot.",
        "-1 uses one output line per entry."
      ],
      "task": "List every entry in the workspace, including hidden entries, one name per line.",
      "solution": "ls -a1",
      "hints": [
        "Combine the all and one-entry-per-line options.",
        "Use the checker as a specification: The hidden .env file was included. notes.txt was included.",
        "Try this command shape, replacing placeholders with fixture values: ls -a1"
      ],
      "files": {
        "notes.txt": "ready\n",
        ".env": "MODE=lab\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The hidden .env file was included.",
          "expected": ".env"
        },
        {
          "type": "stdout-contains",
          "description": "notes.txt was included.",
          "expected": "notes.txt"
        }
      ],
      "completion": "A plain ls can hide operationally important dotfiles. Ask for the view the task actually requires.",
      "command_order": 2,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "ls"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ls-2",
      "section": "01-navigation-and-help",
      "command": "ls",
      "title": "Include hidden entries",
      "about": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. The -A option includes hidden names but omits the special . and .. entries.",
      "example": "ls -A",
      "example_output": ".env\nnotes.txt",
      "breakdown": [
        "ls is the featured command in the worked pattern.",
        "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. The -A option includes hidden names but omits the special . and .. entries.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "List every name in the workspace, including .env, one per line.",
      "solution": "ls -1A",
      "hints": [
        "Use the checker as a specification: Both visible and hidden files were listed.",
        "Try this command shape, replacing placeholders with fixture values: ls -1A",
        "Use man ls or ls --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        ".env": "MODE=practice\n",
        "notes.txt": "ready\n"
      },
      "checks": [
        {
          "type": "stdout-unordered-lines",
          "description": "Both visible and hidden files were listed.",
          "expected": ".env\nnotes.txt"
        }
      ],
      "completion": "The -A option includes hidden names but omits the special . and .. entries. You can now use this behavior as part of a larger command-line workflow.",
      "order": 7,
      "command_order": 2,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. The -A option includes hidden names but omits the special . and .. entries.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "ls"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ls-3",
      "section": "01-navigation-and-help",
      "command": "ls",
      "title": "Select entries with a wildcard",
      "about": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Shell wildcards can narrow ls to names that match a useful filename pattern.",
      "example": "ls -1 *.log",
      "example_output": "app.log\naudit.log",
      "breakdown": [
        "ls is the featured command in the worked pattern.",
        "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Shell wildcards can narrow ls to names that match a useful filename pattern.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the .log filenames, one per line.",
      "solution": "ls -1 *.log",
      "hints": [
        "Use the checker as a specification: Only log files were listed.",
        "Try this command shape, replacing placeholders with fixture values: ls -1 <value>",
        "Use man ls or ls --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.log": "a\n",
        "audit.log": "b\n",
        "notes.txt": "c\n"
      },
      "checks": [
        {
          "type": "stdout-unordered-lines",
          "description": "Only log files were listed.",
          "expected": "app.log\naudit.log"
        }
      ],
      "completion": "Shell wildcards can narrow ls to names that match a useful filename pattern. You can now use this behavior as part of a larger command-line workflow.",
      "order": 8,
      "command_order": 2,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Shell wildcards can narrow ls to names that match a useful filename pattern.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "ls"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ls-4",
      "section": "01-navigation-and-help",
      "command": "ls",
      "title": "Sort files by size",
      "about": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Combining -S with -1 orders entries from largest to smallest in a script-friendly list.",
      "example": "ls -1S *.dat",
      "example_output": "large.dat\nsmall.dat",
      "breakdown": [
        "ls is the featured command in the worked pattern.",
        "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Combining -S with -1 orders entries from largest to smallest in a script-friendly list.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "List the three .dat files from largest to smallest.",
      "solution": "ls -1S *.dat",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: ls -1S <value>",
        "Use man ls or ls --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "tiny.dat": "1",
        "medium.dat": "12345",
        "large.dat": "1234567890"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Sort files by size”.",
          "expected": "large.dat\nmedium.dat\ntiny.dat",
          "normalize": "trim"
        }
      ],
      "completion": "Combining -S with -1 orders entries from largest to smallest in a script-friendly list. You can now use this behavior as part of a larger command-line workflow.",
      "order": 9,
      "command_order": 2,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Combining -S with -1 orders entries from largest to smallest in a script-friendly list.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "ls"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ls-5",
      "section": "01-navigation-and-help",
      "command": "ls",
      "title": "Inventory a project tree",
      "about": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Recursive listing with -R provides a quick inventory across nested directories.",
      "example": "ls -R project",
      "example_output": "project:\nREADME.md  src\n\nproject/src:\nmain.sh",
      "breakdown": [
        "ls is the featured command in the worked pattern.",
        "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Recursive listing with -R provides a quick inventory across nested directories.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Recursively list project and prove the output includes README.md, src, and main.sh.",
      "solution": "ls -R project",
      "hints": [
        "Use the checker as a specification: The root file was listed. The source directory was listed.",
        "Try this command shape, replacing placeholders with fixture values: ls -R <value>",
        "Use man ls or ls --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "project/README.md": "demo\n",
        "project/src/main.sh": "#!/bin/sh\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The root file was listed.",
          "expected": "README.md"
        },
        {
          "type": "stdout-contains",
          "description": "The source directory was listed.",
          "expected": "src"
        },
        {
          "type": "stdout-contains",
          "description": "The nested script was listed.",
          "expected": "main.sh"
        }
      ],
      "completion": "Recursive listing with -R provides a quick inventory across nested directories. You can now use this behavior as part of a larger command-line workflow.",
      "order": 10,
      "command_order": 2,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "ls lists directory entries. -a includes dotfiles and -1 prints one name per line, which makes the result easier to inspect or pipe. Recursive listing with -R provides a quick inventory across nested directories.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "ls"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-cd",
      "order": 11,
      "section": "01-navigation-and-help",
      "command": "cd",
      "title": "Change directory deliberately",
      "about": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution.",
      "example": "cd reports && pwd",
      "example_output": "/home/learner/reports\n",
      "breakdown": [
        "cd reports changes location only if it succeeds.",
        "&& runs pwd after a successful change."
      ],
      "task": "Change into the reports directory and print the resulting absolute path.",
      "solution": "cd reports && pwd",
      "hints": [
        "Use && to verify the change only after cd succeeds.",
        "Use the checker as a specification: The reports directory became the working directory.",
        "Try this command shape, replacing placeholders with fixture values: cd <value> && pwd"
      ],
      "files": {
        "reports/.keep": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The reports directory became the working directory.",
          "expected": "{{workspace}}/reports",
          "normalize": "trim"
        }
      ],
      "completion": "cd changes shell state; it normally prints nothing. Pair navigation with pwd when you need explicit confirmation.",
      "command_order": 3,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "cd",
        "command chaining"
      ],
      "prerequisites": [
        "pwd"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cd-2",
      "section": "01-navigation-and-help",
      "command": "cd",
      "title": "Navigate a nested relative path",
      "about": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. A multi-component relative path moves through several levels in one command.",
      "example": "cd projects/alpha",
      "example_output": "",
      "breakdown": [
        "cd is the featured command in the worked pattern.",
        "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. A multi-component relative path moves through several levels in one command.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Change into clients/acme/reports and print the resulting path.",
      "solution": "cd clients/acme/reports && pwd",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cd <value> && pwd",
        "Use man cd or cd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "clients/acme/reports"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Navigate a nested relative path”.",
          "expected": "{{workspace}}/clients/acme/reports",
          "normalize": "trim"
        }
      ],
      "completion": "A multi-component relative path moves through several levels in one command. You can now use this behavior as part of a larger command-line workflow.",
      "order": 12,
      "command_order": 3,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. A multi-component relative path moves through several levels in one command.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "cd",
        "command chaining"
      ],
      "prerequisites": [
        "pwd"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cd-3",
      "section": "01-navigation-and-help",
      "command": "cd",
      "title": "Move to a parent directory",
      "about": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. The .. component moves to the parent without requiring its name.",
      "example": "cd projects/alpha && cd .. && pwd",
      "example_output": "/work/projects",
      "breakdown": [
        "cd is the featured command in the worked pattern.",
        "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. The .. component moves to the parent without requiring its name.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Enter archive/2026, move back to archive with cd .., and print the path.",
      "solution": "cd archive/2026 && cd .. && pwd",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cd <value> && cd <value> && pwd",
        "Use man cd or cd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "archive/2026"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Move to a parent directory”.",
          "expected": "{{workspace}}/archive",
          "normalize": "trim"
        }
      ],
      "completion": "The .. component moves to the parent without requiring its name. You can now use this behavior as part of a larger command-line workflow.",
      "order": 13,
      "command_order": 3,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. The .. component moves to the parent without requiring its name.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "cd",
        "command chaining"
      ],
      "prerequisites": [
        "pwd"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cd-4",
      "section": "01-navigation-and-help",
      "command": "cd",
      "title": "Return to the previous directory",
      "about": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. cd - switches to OLDPWD, which is useful when alternating between two working directories.",
      "example": "cd one; cd ../two; cd -",
      "example_output": "/work/one",
      "breakdown": [
        "cd is the featured command in the worked pattern.",
        "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. cd - switches to OLDPWD, which is useful when alternating between two working directories.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Visit blue, then green, then use cd - to return to blue and print the final path only.",
      "solution": "cd blue && cd ../green && cd - >/dev/null && pwd",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cd <value> && cd <value> && cd <value> > <value> && pwd",
        "Use man cd or cd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "blue",
        "green"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Return to the previous directory”.",
          "expected": "{{workspace}}/blue",
          "normalize": "trim"
        }
      ],
      "completion": "cd - switches to OLDPWD, which is useful when alternating between two working directories. You can now use this behavior as part of a larger command-line workflow.",
      "order": 14,
      "command_order": 3,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. cd - switches to OLDPWD, which is useful when alternating between two working directories.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "cd",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [
        "pwd"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cd-5",
      "section": "01-navigation-and-help",
      "command": "cd",
      "title": "Enter a symlink target physically",
      "about": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. cd -P resolves symbolic links while changing directory so later relative paths use the physical location.",
      "example": "cd -P current && pwd",
      "example_output": "/work/releases/stable",
      "breakdown": [
        "cd is the featured command in the worked pattern.",
        "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. cd -P resolves symbolic links while changing directory so later relative paths use the physical location.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use cd -P on current and print the resulting physical directory.",
      "solution": "cd -P current && pwd",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cd -P <value> && pwd",
        "Use man cd or cd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "releases/stable"
      ],
      "files": {},
      "symlinks": {
        "current": "releases/stable"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Enter a symlink target physically”.",
          "expected": "{{workspace}}/releases/stable",
          "normalize": "trim"
        }
      ],
      "completion": "cd -P resolves symbolic links while changing directory so later relative paths use the physical location. You can now use this behavior as part of a larger command-line workflow.",
      "order": 15,
      "command_order": 3,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "cd is a shell builtin that changes the current working directory. This rep combines it with pwd so the changed location is observable in one shell execution. cd -P resolves symbolic links while changing directory so later relative paths use the physical location.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "cd",
        "command chaining"
      ],
      "prerequisites": [
        "pwd"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-basename",
      "order": 16,
      "section": "01-navigation-and-help",
      "command": "basename",
      "title": "Extract the final path component",
      "about": "basename removes leading directory components from a path string. It does not require the named path to exist.",
      "example": "basename reports/daily.txt",
      "example_output": "daily.txt\n",
      "breakdown": [
        "reports/ is discarded.",
        "daily.txt is the final path component."
      ],
      "task": "Print only the filename component of projects/alpha/notes.md.",
      "solution": "basename projects/alpha/notes.md",
      "hints": [
        "Pass the complete relative path to basename.",
        "Use the checker as a specification: Only notes.md was printed.",
        "Try this command shape, replacing placeholders with fixture values: basename <value>"
      ],
      "files": {
        "projects/alpha/notes.md": "alpha notes\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only notes.md was printed.",
          "expected": "notes.md",
          "normalize": "trim"
        }
      ],
      "completion": "basename transforms a path string; it does not inspect or validate the filesystem object.",
      "command_order": 4,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "basename removes leading directory components from a path string. It does not require the named path to exist.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "basename"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-basename-2",
      "section": "01-navigation-and-help",
      "command": "basename",
      "title": "Remove a known suffix",
      "about": "basename removes leading directory components from a path string. It does not require the named path to exist. A suffix argument removes a known extension from the final path component.",
      "example": "basename reports/daily.csv .csv",
      "example_output": "daily",
      "breakdown": [
        "basename is the featured command in the worked pattern.",
        "basename removes leading directory components from a path string. It does not require the named path to exist. A suffix argument removes a known extension from the final path component.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the filename report without the .json suffix from exports/report.json.",
      "solution": "basename exports/report.json .json",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: basename <values>",
        "Use man basename or basename --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "exports/report.json": "{}\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Remove a known suffix”.",
          "expected": "report",
          "normalize": "trim"
        }
      ],
      "completion": "A suffix argument removes a known extension from the final path component. You can now use this behavior as part of a larger command-line workflow.",
      "order": 17,
      "command_order": 4,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "basename removes leading directory components from a path string. It does not require the named path to exist. A suffix argument removes a known extension from the final path component.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "basename"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-basename-3",
      "section": "01-navigation-and-help",
      "command": "basename",
      "title": "Extract several filenames",
      "about": "basename removes leading directory components from a path string. It does not require the named path to exist. The -a option accepts multiple paths and prints one basename for each.",
      "example": "basename -a one/a.txt two/b.txt",
      "example_output": "a.txt\nb.txt",
      "breakdown": [
        "basename is the featured command in the worked pattern.",
        "basename removes leading directory components from a path string. It does not require the named path to exist. The -a option accepts multiple paths and prints one basename for each.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the filenames from src/app.js, config/app.ini, and docs/app.md.",
      "solution": "basename -a src/app.js config/app.ini docs/app.md",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: basename -a <values>",
        "Use man basename or basename --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "src/app.js": "",
        "config/app.ini": "",
        "docs/app.md": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Extract several filenames”.",
          "expected": "app.js\napp.ini\napp.md",
          "normalize": "trim"
        }
      ],
      "completion": "The -a option accepts multiple paths and prints one basename for each. You can now use this behavior as part of a larger command-line workflow.",
      "order": 18,
      "command_order": 4,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "basename removes leading directory components from a path string. It does not require the named path to exist. The -a option accepts multiple paths and prints one basename for each.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "basename"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-basename-4",
      "section": "01-navigation-and-help",
      "command": "basename",
      "title": "Strip one suffix from a batch",
      "about": "basename removes leading directory components from a path string. It does not require the named path to exist. Combining -a and -s removes the same suffix from every supplied path.",
      "example": "basename -a -s .log logs/app.log logs/auth.log",
      "example_output": "app\nauth",
      "breakdown": [
        "basename is the featured command in the worked pattern.",
        "basename removes leading directory components from a path string. It does not require the named path to exist. Combining -a and -s removes the same suffix from every supplied path.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print api, worker, and scheduler from their .service paths.",
      "solution": "basename -a -s .service units/api.service units/worker.service units/scheduler.service",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: basename -a -s <values>",
        "Use man basename or basename --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "units/api.service": "",
        "units/worker.service": "",
        "units/scheduler.service": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Strip one suffix from a batch”.",
          "expected": "api\nworker\nscheduler",
          "normalize": "trim"
        }
      ],
      "completion": "Combining -a and -s removes the same suffix from every supplied path. You can now use this behavior as part of a larger command-line workflow.",
      "order": 19,
      "command_order": 4,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "basename removes leading directory components from a path string. It does not require the named path to exist. Combining -a and -s removes the same suffix from every supplied path.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "basename"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-basename-5",
      "section": "01-navigation-and-help",
      "command": "basename",
      "title": "Label files from a path stream",
      "about": "basename removes leading directory components from a path string. It does not require the named path to exist. xargs can apply basename repeatedly when paths arrive as a stream from another command.",
      "example": "printf '%s\\n' logs/a.log logs/b.log | xargs -n1 basename -s .log",
      "example_output": "a\nb",
      "breakdown": [
        "basename is the featured command in the worked pattern.",
        "basename removes leading directory components from a path string. It does not require the named path to exist. xargs can apply basename repeatedly when paths arrive as a stream from another command.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read paths.txt and print each filename without its .bak suffix.",
      "solution": "xargs -n1 basename -s .bak < paths.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: xargs -n1 <value> -s <value> < <value>",
        "Use man basename or basename --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "paths.txt": "archive/db.bak\narchive/web.bak\narchive/mail.bak\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Label files from a path stream”.",
          "expected": "db\nweb\nmail",
          "normalize": "trim"
        }
      ],
      "completion": "xargs can apply basename repeatedly when paths arrive as a stream from another command. You can now use this behavior as part of a larger command-line workflow.",
      "order": 20,
      "command_order": 4,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "basename removes leading directory components from a path string. It does not require the named path to exist. xargs can apply basename repeatedly when paths arrive as a stream from another command.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "basename",
        "redirection"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "xargs"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-dirname",
      "order": 21,
      "section": "01-navigation-and-help",
      "command": "dirname",
      "title": "Extract the directory portion",
      "about": "dirname removes the final component from a path string and prints the directory portion.",
      "example": "dirname reports/daily.txt",
      "example_output": "reports\n",
      "breakdown": [
        "daily.txt is removed from the string.",
        "The remaining directory expression is printed."
      ],
      "task": "Print only the directory portion of projects/alpha/notes.md.",
      "solution": "dirname projects/alpha/notes.md",
      "hints": [
        "dirname complements basename.",
        "Use the checker as a specification: Only projects/alpha was printed.",
        "Try this command shape, replacing placeholders with fixture values: dirname <value>"
      ],
      "files": {
        "projects/alpha/notes.md": "alpha notes\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only projects/alpha was printed.",
          "expected": "projects/alpha",
          "normalize": "trim"
        }
      ],
      "completion": "dirname is string processing. Combine it with realpath when canonical filesystem resolution matters.",
      "command_order": 5,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "dirname removes the final component from a path string and prints the directory portion.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "dirname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-dirname-2",
      "section": "01-navigation-and-help",
      "command": "dirname",
      "title": "Find a parent path",
      "about": "dirname removes the final component from a path string and prints the directory portion. dirname removes the final component even when the path has several nested levels.",
      "example": "dirname app/config/settings.ini",
      "example_output": "app/config",
      "breakdown": [
        "dirname is the featured command in the worked pattern.",
        "dirname removes the final component from a path string and prints the directory portion. dirname removes the final component even when the path has several nested levels.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the parent path of clients/acme/reports/daily.csv.",
      "solution": "dirname clients/acme/reports/daily.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: dirname <value>",
        "Use man dirname or dirname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "clients/acme/reports/daily.csv": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Find a parent path”.",
          "expected": "clients/acme/reports",
          "normalize": "trim"
        }
      ],
      "completion": "dirname removes the final component even when the path has several nested levels. You can now use this behavior as part of a larger command-line workflow.",
      "order": 22,
      "command_order": 5,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "dirname removes the final component from a path string and prints the directory portion. dirname removes the final component even when the path has several nested levels.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "dirname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-dirname-3",
      "section": "01-navigation-and-help",
      "command": "dirname",
      "title": "Process multiple paths",
      "about": "dirname removes the final component from a path string and prints the directory portion. GNU dirname accepts several paths and emits one parent for each in input order.",
      "example": "dirname one/a.txt two/b.txt",
      "example_output": "one\ntwo",
      "breakdown": [
        "dirname is the featured command in the worked pattern.",
        "dirname removes the final component from a path string and prints the directory portion. GNU dirname accepts several paths and emits one parent for each in input order.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the parents of src/app.js, tests/app.test.js, and docs/readme.md.",
      "solution": "dirname src/app.js tests/app.test.js docs/readme.md",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: dirname <values>",
        "Use man dirname or dirname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "src/app.js": "",
        "tests/app.test.js": "",
        "docs/readme.md": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Process multiple paths”.",
          "expected": "src\ntests\ndocs",
          "normalize": "trim"
        }
      ],
      "completion": "GNU dirname accepts several paths and emits one parent for each in input order. You can now use this behavior as part of a larger command-line workflow.",
      "order": 23,
      "command_order": 5,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "dirname removes the final component from a path string and prints the directory portion. GNU dirname accepts several paths and emits one parent for each in input order.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "dirname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-dirname-4",
      "section": "01-navigation-and-help",
      "command": "dirname",
      "title": "Climb from a file to a project root",
      "about": "dirname removes the final component from a path string and prints the directory portion. Applying dirname more than once walks upward through a path string without touching the filesystem.",
      "example": "dirname \"$(dirname project/src/main.c)\"",
      "example_output": "project",
      "breakdown": [
        "dirname is the featured command in the worked pattern.",
        "dirname removes the final component from a path string and prints the directory portion. Applying dirname more than once walks upward through a path string without touching the filesystem.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Starting with services/api/config/app.ini, print the services/api project directory.",
      "solution": "dirname \"$(dirname services/api/config/app.ini)\"",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: dirname <values>",
        "Use man dirname or dirname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services/api/config/app.ini": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Climb from a file to a project root”.",
          "expected": "services/api",
          "normalize": "trim"
        }
      ],
      "completion": "Applying dirname more than once walks upward through a path string without touching the filesystem. You can now use this behavior as part of a larger command-line workflow.",
      "order": 24,
      "command_order": 5,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "dirname removes the final component from a path string and prints the directory portion. Applying dirname more than once walks upward through a path string without touching the filesystem.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "dirname",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-dirname-5",
      "section": "01-navigation-and-help",
      "command": "dirname",
      "title": "Summarize unique source directories",
      "about": "dirname removes the final component from a path string and prints the directory portion. dirname composes naturally with sort and uniq to summarize which directories contain a set of files.",
      "example": "xargs dirname < paths.txt | sort -u",
      "example_output": "app\nlib",
      "breakdown": [
        "dirname is the featured command in the worked pattern.",
        "dirname removes the final component from a path string and prints the directory portion. dirname composes naturally with sort and uniq to summarize which directories contain a set of files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read paths.txt and print the unique parent directories in sorted order.",
      "solution": "xargs dirname < paths.txt | sort -u",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: xargs <value> < <value> | sort -u",
        "Use man dirname or dirname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "paths.txt": "src/app.js\nsrc/util.js\ntests/app.test.js\ndocs/readme.md\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Summarize unique source directories”.",
          "expected": "docs\nsrc\ntests",
          "normalize": "trim"
        }
      ],
      "completion": "dirname composes naturally with sort and uniq to summarize which directories contain a set of files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 25,
      "command_order": 5,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "dirname removes the final component from a path string and prints the directory portion. dirname composes naturally with sort and uniq to summarize which directories contain a set of files.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "dirname",
        "pipelines",
        "redirection",
        "ordering and grouping"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "sort",
        "xargs"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-realpath",
      "order": 26,
      "section": "01-navigation-and-help",
      "command": "realpath",
      "title": "Resolve a canonical absolute path",
      "about": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way.",
      "example": "realpath notes.txt",
      "example_output": "/work/notes.txt\n",
      "breakdown": [
        "notes.txt begins as a relative path.",
        "realpath emits one unambiguous absolute path."
      ],
      "task": "Resolve notes.txt to its canonical absolute path.",
      "solution": "realpath notes.txt",
      "hints": [
        "Pass the relative filename to realpath.",
        "Use the checker as a specification: The canonical notes.txt path was printed.",
        "Try this command shape, replacing placeholders with fixture values: realpath <value>"
      ],
      "files": {
        "notes.txt": "resolved\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The canonical notes.txt path was printed.",
          "expected": "{{workspace}}/notes.txt",
          "normalize": "trim"
        }
      ],
      "completion": "Canonical paths remove ambiguity before a path is logged, compared, or passed into a larger workflow.",
      "command_order": 6,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "realpath"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-realpath-2",
      "section": "01-navigation-and-help",
      "command": "realpath",
      "title": "Normalize relative components",
      "about": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath removes . and .. components while resolving an existing path.",
      "example": "realpath project/./src/../README.md",
      "example_output": "/work/project/README.md",
      "breakdown": [
        "realpath is the featured command in the worked pattern.",
        "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath removes . and .. components while resolving an existing path.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Resolve app/./config/../README.md to its canonical absolute path.",
      "solution": "realpath app/./config/../README.md",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: realpath <value>",
        "Use man realpath or realpath --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "app/config"
      ],
      "files": {
        "app/README.md": "app\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Normalize relative components”.",
          "expected": "{{workspace}}/app/README.md",
          "normalize": "trim"
        }
      ],
      "completion": "realpath removes . and .. components while resolving an existing path. You can now use this behavior as part of a larger command-line workflow.",
      "order": 27,
      "command_order": 6,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath removes . and .. components while resolving an existing path.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "realpath"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-realpath-3",
      "section": "01-navigation-and-help",
      "command": "realpath",
      "title": "Print a path relative to a base",
      "about": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. The --relative-to option converts a canonical path into a path relative to a chosen directory.",
      "example": "realpath --relative-to=project project/src/main.sh",
      "example_output": "src/main.sh",
      "breakdown": [
        "realpath is the featured command in the worked pattern.",
        "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. The --relative-to option converts a canonical path into a path relative to a chosen directory.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print releases/2026/app.conf relative to releases.",
      "solution": "realpath --relative-to=releases releases/2026/app.conf",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: realpath --relative-to=releases <value>",
        "Use man realpath or realpath --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "releases/2026/app.conf": "mode=practice\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print a path relative to a base”.",
          "expected": "2026/app.conf",
          "normalize": "trim"
        }
      ],
      "completion": "The --relative-to option converts a canonical path into a path relative to a chosen directory. You can now use this behavior as part of a larger command-line workflow.",
      "order": 28,
      "command_order": 6,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. The --relative-to option converts a canonical path into a path relative to a chosen directory.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "realpath"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-realpath-4",
      "section": "01-navigation-and-help",
      "command": "realpath",
      "title": "Resolve a missing destination safely",
      "about": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath -m canonicalizes every component even when the final path does not exist yet.",
      "example": "realpath -m output/../build/result.txt",
      "example_output": "/work/build/result.txt",
      "breakdown": [
        "realpath is the featured command in the worked pattern.",
        "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath -m canonicalizes every component even when the final path does not exist yet.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Canonicalize staging/../artifacts/new.tar even though new.tar does not exist.",
      "solution": "realpath -m staging/../artifacts/new.tar",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: realpath -m <value>",
        "Use man realpath or realpath --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "staging",
        "artifacts"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Resolve a missing destination safely”.",
          "expected": "{{workspace}}/artifacts/new.tar",
          "normalize": "trim"
        }
      ],
      "completion": "realpath -m canonicalizes every component even when the final path does not exist yet. You can now use this behavior as part of a larger command-line workflow.",
      "order": 29,
      "command_order": 6,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath -m canonicalizes every component even when the final path does not exist yet.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "realpath"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "tar"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-realpath-5",
      "section": "01-navigation-and-help",
      "command": "realpath",
      "title": "Trace a deployment symlink chain",
      "about": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath follows every symbolic link in a chain to identify the actual versioned file.",
      "example": "realpath entry.conf",
      "example_output": "/work/releases/2026/config/app.conf",
      "breakdown": [
        "realpath is the featured command in the worked pattern.",
        "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath follows every symbolic link in a chain to identify the actual versioned file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Resolve entry.conf through all three symbolic links to the actual app.conf file.",
      "solution": "realpath entry.conf",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: realpath <value>",
        "Use man realpath or realpath --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "vault/releases/2026/config/app.conf": "mode=practice\n"
      },
      "symlinks": {
        "vault/current": "releases/2026",
        "active": "vault/current",
        "entry.conf": "active/config/app.conf"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Trace a deployment symlink chain”.",
          "expected": "{{workspace}}/vault/releases/2026/config/app.conf",
          "normalize": "trim"
        }
      ],
      "completion": "realpath follows every symbolic link in a chain to identify the actual versioned file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 30,
      "command_order": 6,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "realpath prints the canonical absolute path of a file, resolving relative components and symbolic links along the way. realpath follows every symbolic link in a chain to identify the actual versioned file.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "realpath"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-which",
      "order": 31,
      "section": "01-navigation-and-help",
      "command": "which",
      "title": "Locate an executable on PATH",
      "about": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V.",
      "example": "which grep",
      "example_output": "/usr/bin/grep\n",
      "breakdown": [
        "PATH directories are searched in order.",
        "The first matching executable path is printed."
      ],
      "task": "Print the executable path selected for grep.",
      "solution": "which grep",
      "hints": [
        "Give which the executable name, not a full path.",
        "Use the checker as a specification: The selected path ends with grep. grep was found on PATH.",
        "Try this command shape, replacing placeholders with fixture values: which <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The selected path ends with grep.",
          "expected": "grep"
        },
        {
          "type": "exit-code",
          "description": "grep was found on PATH.",
          "expected": 0
        }
      ],
      "completion": "which answers a PATH question. Use type when shell functions, aliases, and builtins are also possible.",
      "command_order": 7,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "which"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-which-2",
      "section": "01-navigation-and-help",
      "command": "which",
      "title": "Locate several required tools",
      "about": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. which accepts multiple command names, making it useful for quick prerequisite checks.",
      "example": "which grep sed",
      "example_output": "/usr/bin/grep\n/usr/bin/sed",
      "breakdown": [
        "which is the featured command in the worked pattern.",
        "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. which accepts multiple command names, making it useful for quick prerequisite checks.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the selected executable paths for grep and awk.",
      "solution": "which grep awk",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: which <values>",
        "Use man which or which --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Locate several required tools”.",
          "expected": "/usr/bin/grep\n/usr/bin/awk",
          "normalize": "trim"
        }
      ],
      "completion": "which accepts multiple command names, making it useful for quick prerequisite checks. You can now use this behavior as part of a larger command-line workflow.",
      "order": 32,
      "command_order": 7,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. which accepts multiple command names, making it useful for quick prerequisite checks.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "which"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "grep",
        "awk"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-which-3",
      "section": "01-navigation-and-help",
      "command": "which",
      "title": "Test whether a command is available",
      "about": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. The exit status from which is useful in a conditional even when its normal output is suppressed.",
      "example": "if which jq >/dev/null; then echo ready; fi",
      "example_output": "ready",
      "breakdown": [
        "which is the featured command in the worked pattern.",
        "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. The exit status from which is useful in a conditional even when its normal output is suppressed.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use which in a conditional and print available if rsync can be found.",
      "solution": "if which rsync >/dev/null; then echo available; fi",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: if <values> > <value> ; then <values> ; fi",
        "Use man which or which --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Test whether a command is available”.",
          "expected": "available",
          "normalize": "trim"
        }
      ],
      "completion": "The exit status from which is useful in a conditional even when its normal output is suppressed. You can now use this behavior as part of a larger command-line workflow.",
      "order": 33,
      "command_order": 7,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. The exit status from which is useful in a conditional even when its normal output is suppressed.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "which",
        "redirection",
        "command chaining",
        "filtering and conditions"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "echo",
        "rsync"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-which-4",
      "section": "01-navigation-and-help",
      "command": "which",
      "title": "Inspect every PATH match",
      "about": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. The -a option prints every matching executable instead of stopping at the first.",
      "example": "which -a sh",
      "example_output": "/usr/bin/sh\n/bin/sh",
      "breakdown": [
        "which is the featured command in the worked pattern.",
        "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. The -a option prints every matching executable instead of stopping at the first.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use which -a to print every PATH match for bash.",
      "solution": "which -a bash",
      "hints": [
        "Use the checker as a specification: At least the selected bash path was printed.",
        "Try this command shape, replacing placeholders with fixture values: which -a <value>",
        "Use man which or which --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "At least the selected bash path was printed.",
          "expected": "/usr/bin/bash"
        }
      ],
      "completion": "The -a option prints every matching executable instead of stopping at the first. You can now use this behavior as part of a larger command-line workflow.",
      "order": 34,
      "command_order": 7,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. The -a option prints every matching executable instead of stopping at the first.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "which"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-which-5",
      "section": "01-navigation-and-help",
      "command": "which",
      "title": "Build a prerequisite report",
      "about": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. A small loop can pair each required command name with the executable selected by PATH.",
      "example": "for tool in grep sed; do printf '%s=%s\\n' \"$tool\" \"$(which \"$tool\")\"; done",
      "example_output": "grep=/usr/bin/grep\nsed=/usr/bin/sed",
      "breakdown": [
        "which is the featured command in the worked pattern.",
        "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. A small loop can pair each required command name with the executable selected by PATH.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print tool=path lines for jq, curl, and tar in that order.",
      "solution": "for tool in jq curl tar; do printf '%s=%s\\n' \"$tool\" \"$(which \"$tool\")\"; done",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <values> ; done",
        "Use man which or which --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Build a prerequisite report”.",
          "expected": "jq=/usr/bin/jq\ncurl=/usr/bin/curl\ntar=/usr/bin/tar",
          "normalize": "trim"
        }
      ],
      "completion": "A small loop can pair each required command name with the executable selected by PATH. You can now use this behavior as part of a larger command-line workflow.",
      "order": 35,
      "command_order": 7,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "which searches PATH for an executable name. It is useful for a quick external-command location, but shell builtins and aliases require type or command -V. A small loop can pair each required command name with the executable selected by PATH.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "which",
        "command chaining",
        "command substitution",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "printf",
        "tar",
        "jq",
        "curl"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-whereis",
      "order": 36,
      "section": "01-navigation-and-help",
      "command": "whereis",
      "title": "Find command-related paths",
      "about": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths.",
      "example": "whereis bash",
      "example_output": "bash: /usr/bin/bash /usr/share/man/man1/bash.1.gz\n",
      "breakdown": [
        "bash is the lookup name.",
        "The labeled result may include a binary and documentation."
      ],
      "task": "Use whereis to locate files associated with bash.",
      "solution": "whereis bash",
      "hints": [
        "Pass the command name to whereis.",
        "Use the checker as a specification: The result was labeled for bash.",
        "Try this command shape, replacing placeholders with fixture values: whereis <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The result was labeled for bash.",
          "expected": "bash:"
        }
      ],
      "completion": "whereis uses known location databases, not an unrestricted filesystem walk. Use find for a scoped live search.",
      "command_order": 8,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "whereis"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-whereis-2",
      "section": "01-navigation-and-help",
      "command": "whereis",
      "title": "Find only a command binary",
      "about": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. The -b option limits whereis output to binary locations.",
      "example": "whereis -b grep",
      "example_output": "grep: /usr/bin/grep",
      "breakdown": [
        "whereis is the featured command in the worked pattern.",
        "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. The -b option limits whereis output to binary locations.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use whereis -b to locate the sed binary.",
      "solution": "whereis -b sed",
      "hints": [
        "Use the checker as a specification: The sed binary location was included.",
        "Try this command shape, replacing placeholders with fixture values: whereis -b <value>",
        "Use man whereis or whereis --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The sed binary location was included.",
          "expected": "/usr/bin/sed"
        }
      ],
      "completion": "The -b option limits whereis output to binary locations. You can now use this behavior as part of a larger command-line workflow.",
      "order": 37,
      "command_order": 8,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. The -b option limits whereis output to binary locations.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "whereis"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "sed"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-whereis-3",
      "section": "01-navigation-and-help",
      "command": "whereis",
      "title": "Search for several commands",
      "about": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. whereis can report locations for multiple names in one invocation.",
      "example": "whereis -b grep awk",
      "example_output": "grep: /usr/bin/grep\nawk: /usr/bin/awk",
      "breakdown": [
        "whereis is the featured command in the worked pattern.",
        "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. whereis can report locations for multiple names in one invocation.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print binary-location reports for bash and tar.",
      "solution": "whereis -b bash tar",
      "hints": [
        "Use the checker as a specification: The bash report was printed. The tar report was printed.",
        "Try this command shape, replacing placeholders with fixture values: whereis -b <values>",
        "Use man whereis or whereis --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The bash report was printed.",
          "expected": "bash:"
        },
        {
          "type": "stdout-contains",
          "description": "The tar report was printed.",
          "expected": "tar:"
        }
      ],
      "completion": "whereis can report locations for multiple names in one invocation. You can now use this behavior as part of a larger command-line workflow.",
      "order": 38,
      "command_order": 8,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. whereis can report locations for multiple names in one invocation.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "whereis"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "tar"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-whereis-4",
      "section": "01-navigation-and-help",
      "command": "whereis",
      "title": "Extract a binary path from the report",
      "about": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. Text tools can isolate the location field from whereis output for later scripting.",
      "example": "whereis -b jq | awk '{print $2}'",
      "example_output": "/usr/bin/jq",
      "breakdown": [
        "whereis is the featured command in the worked pattern.",
        "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. Text tools can isolate the location field from whereis output for later scripting.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the first binary path reported for curl.",
      "solution": "whereis -b curl | awk '{print $2}'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: whereis -b <value> | awk <values>",
        "Use man whereis or whereis --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Extract a binary path from the report”.",
          "expected": "/usr/bin/curl",
          "normalize": "trim"
        }
      ],
      "completion": "Text tools can isolate the location field from whereis output for later scripting. You can now use this behavior as part of a larger command-line workflow.",
      "order": 39,
      "command_order": 8,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. Text tools can isolate the location field from whereis output for later scripting.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "whereis",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "awk",
        "curl"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-whereis-5",
      "section": "01-navigation-and-help",
      "command": "whereis",
      "title": "Audit tool locations",
      "about": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. Combining whereis with a loop creates a compact inventory without searching the entire filesystem.",
      "example": "for t in jq tar; do whereis -b \"$t\"; done",
      "example_output": "jq: /usr/bin/jq\ntar: /usr/bin/tar",
      "breakdown": [
        "whereis is the featured command in the worked pattern.",
        "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. Combining whereis with a loop creates a compact inventory without searching the entire filesystem.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Produce whereis -b reports for ssh, scp, and rsync in that order.",
      "solution": "for tool in ssh scp rsync; do whereis -b \"$tool\"; done",
      "hints": [
        "Use the checker as a specification: The ssh inventory entry was printed. The scp inventory entry was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <value> -b <value> ; done",
        "Use man whereis or whereis --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The ssh inventory entry was printed.",
          "expected": "ssh:"
        },
        {
          "type": "stdout-contains",
          "description": "The scp inventory entry was printed.",
          "expected": "scp:"
        },
        {
          "type": "stdout-contains",
          "description": "The rsync inventory entry was printed.",
          "expected": "rsync:"
        }
      ],
      "completion": "Combining whereis with a loop creates a compact inventory without searching the entire filesystem. You can now use this behavior as part of a larger command-line workflow.",
      "order": 40,
      "command_order": 8,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "whereis searches standard locations for a command's binary, source, and manual-page files. Its result can contain several paths. Combining whereis with a loop creates a compact inventory without searching the entire filesystem.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "whereis",
        "command chaining",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "ssh",
        "scp",
        "rsync"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-type",
      "order": 41,
      "section": "01-navigation-and-help",
      "command": "type",
      "title": "Ask Bash how a name resolves",
      "about": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable.",
      "example": "type cd",
      "example_output": "cd is a shell builtin\n",
      "breakdown": [
        "cd is looked up using Bash's own resolution rules.",
        "The result identifies it as a builtin."
      ],
      "task": "Ask Bash how the name cd resolves.",
      "solution": "type cd",
      "hints": [
        "Use the shell builtin named in the topic.",
        "Use the checker as a specification: cd was identified as a shell builtin.",
        "Try this command shape, replacing placeholders with fixture values: type <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "cd was identified as a shell builtin.",
          "expected": "shell builtin"
        }
      ],
      "completion": "type is more complete than which for understanding what Bash will actually execute.",
      "command_order": 9,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "type"
      ],
      "prerequisites": [
        "cd"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-type-2",
      "section": "01-navigation-and-help",
      "command": "type",
      "title": "Identify a shell builtin",
      "about": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type recognizes shell builtins that do not have a standalone executable file.",
      "example": "type cd",
      "example_output": "cd is a shell builtin",
      "breakdown": [
        "type is the featured command in the worked pattern.",
        "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type recognizes shell builtins that do not have a standalone executable file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use type to identify pwd in this Bash session.",
      "solution": "type pwd",
      "hints": [
        "Use the checker as a specification: pwd was identified as a shell builtin.",
        "Try this command shape, replacing placeholders with fixture values: type <value>",
        "Use man type or type --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "pwd was identified as a shell builtin.",
          "expected": "shell builtin"
        }
      ],
      "completion": "type recognizes shell builtins that do not have a standalone executable file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 42,
      "command_order": 9,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type recognizes shell builtins that do not have a standalone executable file.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "type"
      ],
      "prerequisites": [
        "pwd"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-type-3",
      "section": "01-navigation-and-help",
      "command": "type",
      "title": "Print only the command category",
      "about": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type -t emits a compact category such as builtin, file, alias, or function.",
      "example": "type -t printf",
      "example_output": "builtin",
      "breakdown": [
        "type is the featured command in the worked pattern.",
        "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type -t emits a compact category such as builtin, file, alias, or function.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the type category for grep.",
      "solution": "type -t grep",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: type -t <value>",
        "Use man type or type --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print only the command category”.",
          "expected": "file",
          "normalize": "trim"
        }
      ],
      "completion": "type -t emits a compact category such as builtin, file, alias, or function. You can now use this behavior as part of a larger command-line workflow.",
      "order": 43,
      "command_order": 9,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type -t emits a compact category such as builtin, file, alias, or function.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "type"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-type-4",
      "section": "01-navigation-and-help",
      "command": "type",
      "title": "Show every command resolution",
      "about": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type -a reveals all matching builtins and executable files in command-resolution order.",
      "example": "type -a pwd",
      "example_output": "pwd is a shell builtin\npwd is /usr/bin/pwd",
      "breakdown": [
        "type is the featured command in the worked pattern.",
        "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type -a reveals all matching builtins and executable files in command-resolution order.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use type -a to show every available implementation of printf.",
      "solution": "type -a printf",
      "hints": [
        "Use the checker as a specification: The builtin implementation was shown. An executable implementation was shown.",
        "Try this command shape, replacing placeholders with fixture values: type -a <value>",
        "Use man type or type --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The builtin implementation was shown.",
          "expected": "shell builtin"
        },
        {
          "type": "stdout-contains",
          "description": "An executable implementation was shown.",
          "expected": "/usr/bin/printf"
        }
      ],
      "completion": "type -a reveals all matching builtins and executable files in command-resolution order. You can now use this behavior as part of a larger command-line workflow.",
      "order": 44,
      "command_order": 9,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. type -a reveals all matching builtins and executable files in command-resolution order.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "type"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "printf"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-type-5",
      "section": "01-navigation-and-help",
      "command": "type",
      "title": "Classify a mixed command set",
      "about": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. A loop around type -t can document whether dependencies are builtins or external files.",
      "example": "for c in cd grep; do printf '%s=%s\\n' \"$c\" \"$(type -t \"$c\")\"; done",
      "example_output": "cd=builtin\ngrep=file",
      "breakdown": [
        "type is the featured command in the worked pattern.",
        "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. A loop around type -t can document whether dependencies are builtins or external files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print name=category lines for cd, echo, and sed in that order.",
      "solution": "for c in cd echo sed; do printf '%s=%s\\n' \"$c\" \"$(type -t \"$c\")\"; done",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <values> -t <value> ; done",
        "Use man type or type --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Classify a mixed command set”.",
          "expected": "cd=builtin\necho=builtin\nsed=file",
          "normalize": "trim"
        }
      ],
      "completion": "A loop around type -t can document whether dependencies are builtins or external files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 45,
      "command_order": 9,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "type is a Bash builtin that explains whether a name resolves to an alias, function, builtin, keyword, or executable. A loop around type -t can document whether dependencies are builtins or external files.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "type",
        "command chaining",
        "command substitution",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [
        "cd"
      ],
      "introduced_inline": [
        "sed",
        "echo",
        "printf"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-man",
      "order": 46,
      "section": "01-navigation-and-help",
      "command": "man",
      "title": "Open local command documentation",
      "about": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager.",
      "example": "man --help | head -n 1",
      "example_output": "Usage: man [OPTION...] [SECTION] PAGE...\n",
      "breakdown": [
        "--help prints man usage without opening a pager.",
        "head keeps this rep short."
      ],
      "task": "Print the first line of man's own help output.",
      "solution": "man --help | head -n 1",
      "hints": [
        "Request --help, then limit the result to one line.",
        "Use the checker as a specification: A man help line was printed. man help completed successfully.",
        "Try this command shape, replacing placeholders with fixture values: man --help | head -n <value>"
      ],
      "files": {},
      "packages": [
        "man-db"
      ],
      "checks": [
        {
          "type": "stdout-nonempty",
          "description": "A man help line was printed."
        },
        {
          "type": "exit-code",
          "description": "man help completed successfully.",
          "expected": 0
        }
      ],
      "completion": "Manual pages are local, version-matched references. Search inside a page with /pattern and leave the pager with q.",
      "command_order": 10,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "man",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "head"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-man-2",
      "section": "01-navigation-and-help",
      "command": "man",
      "title": "Identify the installed man version",
      "about": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. The --version output confirms which man implementation and documentation behavior are available.",
      "example": "man --version | head -n 1",
      "example_output": "man 2.11.2",
      "breakdown": [
        "man is the featured command in the worked pattern.",
        "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. The --version output confirms which man implementation and documentation behavior are available.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the first line of man --version.",
      "solution": "man --version | head -n 1",
      "hints": [
        "Use the checker as a specification: The man version line was printed.",
        "Try this command shape, replacing placeholders with fixture values: man --version | head -n <value>",
        "Use man man or man --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The man version line was printed.",
          "expected": "man"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 1 nonblank output line was printed.",
          "expected": 1
        }
      ],
      "completion": "The --version output confirms which man implementation and documentation behavior are available. You can now use this behavior as part of a larger command-line workflow.",
      "order": 47,
      "command_order": 10,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. The --version output confirms which man implementation and documentation behavior are available.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "man",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "head"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-man-3",
      "section": "01-navigation-and-help",
      "command": "man",
      "title": "Render a local manual page",
      "about": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. man -l reads a page file directly, which is useful for project documentation not installed system-wide.",
      "example": "man -l tool.1",
      "example_output": "TOOL(1) ...",
      "breakdown": [
        "man is the featured command in the worked pattern.",
        "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. man -l reads a page file directly, which is useful for project documentation not installed system-wide.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Render the local gymtool.1 page and print it without an interactive pager.",
      "solution": "MANPAGER=cat man -l gymtool.1",
      "hints": [
        "Use the checker as a specification: The local page name section was rendered.",
        "Try this command shape, replacing placeholders with fixture values: <value> man -l <value>",
        "Use man man or man --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "gymtool.1": ".TH GYMTOOL 1\n.SH NAME\ngymtool \\- practice helper\n.SH SYNOPSIS\ngymtool [OPTIONS]\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The local page name section was rendered.",
          "expected": "practice helper"
        }
      ],
      "completion": "man -l reads a page file directly, which is useful for project documentation not installed system-wide. You can now use this behavior as part of a larger command-line workflow.",
      "order": 48,
      "command_order": 10,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. man -l reads a page file directly, which is useful for project documentation not installed system-wide.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "man"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "cat"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-man-4",
      "section": "01-navigation-and-help",
      "command": "man",
      "title": "Extract a manual-page section",
      "about": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. A noninteractive pager lets text tools search rendered manual content in scripts.",
      "example": "MANPAGER=cat man -l tool.1 | col -b | grep -A1 '^SYNOPSIS'",
      "example_output": "SYNOPSIS\n       tool [OPTIONS]",
      "breakdown": [
        "man is the featured command in the worked pattern.",
        "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. A noninteractive pager lets text tools search rendered manual content in scripts.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Render gymtool.1 and print the SYNOPSIS heading plus its following line.",
      "solution": "MANPAGER=cat man -l gymtool.1 | col -b | grep -A1 '^SYNOPSIS'",
      "hints": [
        "Use the checker as a specification: The synopsis invocation was extracted.",
        "Try this command shape, replacing placeholders with fixture values: <value> man -l <value> | col -b | grep -A1 <value>",
        "Use man man or man --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "gymtool.1": ".TH GYMTOOL 1\n.SH NAME\ngymtool \\- practice helper\n.SH SYNOPSIS\ngymtool [OPTIONS]\n.SH DESCRIPTION\nA local practice command.\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The synopsis invocation was extracted.",
          "expected": "gymtool [OPTIONS]"
        }
      ],
      "completion": "A noninteractive pager lets text tools search rendered manual content in scripts. You can now use this behavior as part of a larger command-line workflow.",
      "order": 49,
      "command_order": 10,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. A noninteractive pager lets text tools search rendered manual content in scripts.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "man",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "cat",
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-man-5",
      "section": "01-navigation-and-help",
      "command": "man",
      "title": "Answer a question from local documentation",
      "about": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. Manual pages become operational references when a pipeline extracts the exact option a workflow needs.",
      "example": "MANPAGER=cat man -l tool.1 | col -b | grep -- '--dry-run'",
      "example_output": "       --dry-run  preview changes",
      "breakdown": [
        "man is the featured command in the worked pattern.",
        "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. Manual pages become operational references when a pipeline extracts the exact option a workflow needs.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use man to render deploy.1, then print the line documenting --check. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "MANPAGER=cat man -l deploy.1 | col -b | grep -- '--check' && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: The --check option was found in the manual.",
        "Try this command shape, replacing placeholders with fixture values: <value> man -l <value> | col -b | grep -- <value>",
        "Use man man or man --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "deploy.1": ".TH DEPLOY 1\n.SH NAME\ndeploy \\- publish an artifact\n.SH OPTIONS\n.TP\n.B --check\nvalidate without publishing\n.TP\n.B --force\npublish without prompting\n",
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The --check option was found in the manual.",
          "expected": "--check"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "Manual pages become operational references when a pipeline extracts the exact option a workflow needs. You can now use this behavior as part of a larger command-line workflow.",
      "order": 50,
      "command_order": 10,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "man displays local manual pages. This noninteractive rep checks the help surface; in a normal terminal, man ls opens the complete page in a pager. Manual pages become operational references when a pipeline extracts the exact option a workflow needs.",
      "stage_kind": "workflow",
      "mode": "capstone",
      "concepts": [
        "man",
        "pipelines",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "cp",
        "cat",
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-mkdir",
      "order": 51,
      "section": "02-files-and-directories",
      "command": "mkdir",
      "title": "Create a nested directory tree",
      "about": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success.",
      "example": "mkdir -p evidence/screenshots",
      "example_output": "",
      "breakdown": [
        "-p creates evidence if needed.",
        "The final screenshots directory is created below it."
      ],
      "task": "Create reports/daily in one command.",
      "solution": "mkdir -p reports/daily",
      "hints": [
        "Use the parent-creation option.",
        "Use the checker as a specification: reports/daily exists as a directory.",
        "Try this command shape, replacing placeholders with fixture values: mkdir -p <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "path-exists",
          "description": "reports/daily exists as a directory.",
          "path": "reports/daily",
          "kind": "directory"
        }
      ],
      "completion": "mkdir -p is script-friendly, but a typo can still create the wrong tree. Verify the path.",
      "command_order": 11,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "mkdir"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mkdir-2",
      "section": "02-files-and-directories",
      "command": "mkdir",
      "title": "Create nested parents",
      "about": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. mkdir -p creates every missing parent and succeeds when directories already exist.",
      "example": "mkdir -p project/src/lib",
      "example_output": "",
      "breakdown": [
        "mkdir is the featured command in the worked pattern.",
        "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. mkdir -p creates every missing parent and succeeds when directories already exist.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create clients/acme/reports in one command.",
      "solution": "mkdir -p clients/acme/reports",
      "hints": [
        "Use the checker as a specification: clients/acme/reports exists.",
        "Try this command shape, replacing placeholders with fixture values: mkdir -p <value>",
        "Use man mkdir or mkdir --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "path-exists",
          "description": "clients/acme/reports exists.",
          "path": "clients/acme/reports",
          "kind": "directory"
        }
      ],
      "completion": "mkdir -p creates every missing parent and succeeds when directories already exist. You can now use this behavior as part of a larger command-line workflow.",
      "order": 52,
      "command_order": 11,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. mkdir -p creates every missing parent and succeeds when directories already exist.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "mkdir"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mkdir-3",
      "section": "02-files-and-directories",
      "command": "mkdir",
      "title": "Choose permissions at creation",
      "about": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. The -m option assigns a directory mode as it is created instead of requiring a later chmod.",
      "example": "mkdir -m 750 private",
      "example_output": "",
      "breakdown": [
        "mkdir is the featured command in the worked pattern.",
        "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. The -m option assigns a directory mode as it is created instead of requiring a later chmod.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create secrets with mode 700.",
      "solution": "mkdir -m 700 secrets",
      "hints": [
        "Use the checker as a specification: secrets exists. The directory mode is 700.",
        "Try this command shape, replacing placeholders with fixture values: mkdir -m <values>",
        "Use man mkdir or mkdir --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "path-exists",
          "description": "secrets exists.",
          "path": "secrets",
          "kind": "directory"
        },
        {
          "type": "file-mode",
          "description": "The directory mode is 700.",
          "path": "secrets",
          "expected": "700"
        }
      ],
      "completion": "The -m option assigns a directory mode as it is created instead of requiring a later chmod. You can now use this behavior as part of a larger command-line workflow.",
      "order": 53,
      "command_order": 11,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. The -m option assigns a directory mode as it is created instead of requiring a later chmod.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "mkdir"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mkdir-4",
      "section": "02-files-and-directories",
      "command": "mkdir",
      "title": "Build a standard project tree",
      "about": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. Brace expansion combines with mkdir -p to create sibling directories concisely.",
      "example": "mkdir -p app/{src,tests,docs}",
      "example_output": "",
      "breakdown": [
        "mkdir is the featured command in the worked pattern.",
        "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. Brace expansion combines with mkdir -p to create sibling directories concisely.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create project/src, project/tests, project/docs, and project/build.",
      "solution": "mkdir -p project/{src,tests,docs,build}",
      "hints": [
        "Use the checker as a specification: project/src exists. project/tests exists.",
        "Try this command shape, replacing placeholders with fixture values: mkdir -p <value>",
        "Use man mkdir or mkdir --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "path-exists",
          "description": "project/src exists.",
          "path": "project/src",
          "kind": "directory"
        },
        {
          "type": "path-exists",
          "description": "project/tests exists.",
          "path": "project/tests",
          "kind": "directory"
        },
        {
          "type": "path-exists",
          "description": "project/docs exists.",
          "path": "project/docs",
          "kind": "directory"
        },
        {
          "type": "path-exists",
          "description": "project/build exists.",
          "path": "project/build",
          "kind": "directory"
        }
      ],
      "completion": "Brace expansion combines with mkdir -p to create sibling directories concisely. You can now use this behavior as part of a larger command-line workflow.",
      "order": 54,
      "command_order": 11,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. Brace expansion combines with mkdir -p to create sibling directories concisely.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "mkdir"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mkdir-5",
      "section": "02-files-and-directories",
      "command": "mkdir",
      "title": "Create directories from a manifest",
      "about": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. xargs can feed a reviewed directory manifest to mkdir -p for repeatable setup.",
      "example": "xargs mkdir -p < directories.txt",
      "example_output": "",
      "breakdown": [
        "mkdir is the featured command in the worked pattern.",
        "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. xargs can feed a reviewed directory manifest to mkdir -p for repeatable setup.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read directories.txt and create every listed directory.",
      "solution": "xargs mkdir -p < directories.txt",
      "hints": [
        "Use the checker as a specification: services/api/config exists. services/web/public exists.",
        "Try this command shape, replacing placeholders with fixture values: xargs <value> -p < <value>",
        "Use man mkdir or mkdir --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "directories.txt": "services/api/config\nservices/web/public\nvar/cache/app\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "services/api/config exists.",
          "path": "services/api/config",
          "kind": "directory"
        },
        {
          "type": "path-exists",
          "description": "services/web/public exists.",
          "path": "services/web/public",
          "kind": "directory"
        },
        {
          "type": "path-exists",
          "description": "var/cache/app exists.",
          "path": "var/cache/app",
          "kind": "directory"
        }
      ],
      "completion": "xargs can feed a reviewed directory manifest to mkdir -p for repeatable setup. You can now use this behavior as part of a larger command-line workflow.",
      "order": 55,
      "command_order": 11,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "mkdir creates directories. -p creates missing parents and treats an existing requested directory as success. xargs can feed a reviewed directory manifest to mkdir -p for repeatable setup.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "mkdir",
        "redirection"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "xargs"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rmdir",
      "order": 56,
      "section": "02-files-and-directories",
      "command": "rmdir",
      "title": "Remove an empty directory",
      "about": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition.",
      "example": "rmdir old-empty-dir",
      "example_output": "",
      "breakdown": [
        "The target must be empty.",
        "A successful removal normally prints nothing."
      ],
      "task": "Remove the empty directory named retired.",
      "solution": "rmdir retired",
      "hints": [
        "The directory is empty, so recursive rm is unnecessary.",
        "Use the checker as a specification: The retired directory no longer exists.",
        "Try this command shape, replacing placeholders with fixture values: rmdir <value>"
      ],
      "directories": [
        "retired"
      ],
      "files": {},
      "checks": [
        {
          "type": "path-not-exists",
          "description": "The retired directory no longer exists.",
          "path": "retired"
        }
      ],
      "completion": "rmdir refuses nonempty directories. That refusal is useful evidence that the scope is broader than expected.",
      "command_order": 12,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "rmdir"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rmdir-2",
      "section": "02-files-and-directories",
      "command": "rmdir",
      "title": "Remove several empty directories",
      "about": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. rmdir accepts multiple empty directory operands and refuses to remove their contents.",
      "example": "rmdir old-a old-b",
      "example_output": "",
      "breakdown": [
        "rmdir is the featured command in the worked pattern.",
        "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. rmdir accepts multiple empty directory operands and refuses to remove their contents.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove the empty retired-a and retired-b directories.",
      "solution": "rmdir retired-a retired-b",
      "hints": [
        "Use the checker as a specification: retired-a no longer exists. retired-b no longer exists.",
        "Try this command shape, replacing placeholders with fixture values: rmdir retired-a retired-b",
        "Use man rmdir or rmdir --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "retired-a",
        "retired-b"
      ],
      "files": {},
      "checks": [
        {
          "type": "path-not-exists",
          "description": "retired-a no longer exists.",
          "path": "retired-a"
        },
        {
          "type": "path-not-exists",
          "description": "retired-b no longer exists.",
          "path": "retired-b"
        }
      ],
      "completion": "rmdir accepts multiple empty directory operands and refuses to remove their contents. You can now use this behavior as part of a larger command-line workflow.",
      "order": 57,
      "command_order": 12,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. rmdir accepts multiple empty directory operands and refuses to remove their contents.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "rmdir"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rmdir-3",
      "section": "02-files-and-directories",
      "command": "rmdir",
      "title": "Prune empty parent directories",
      "about": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. rmdir -p removes the named leaf and then each newly empty parent in the same path.",
      "example": "rmdir -p archive/2025/q4",
      "example_output": "",
      "breakdown": [
        "rmdir is the featured command in the worked pattern.",
        "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. rmdir -p removes the named leaf and then each newly empty parent in the same path.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove archive/2024/q1 and its now-empty parents.",
      "solution": "rmdir -p archive/2024/q1",
      "hints": [
        "Use the checker as a specification: archive no longer exists.",
        "Try this command shape, replacing placeholders with fixture values: rmdir -p <value>",
        "Use man rmdir or rmdir --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "archive/2024/q1"
      ],
      "files": {},
      "checks": [
        {
          "type": "path-not-exists",
          "description": "archive no longer exists.",
          "path": "archive"
        }
      ],
      "completion": "rmdir -p removes the named leaf and then each newly empty parent in the same path. You can now use this behavior as part of a larger command-line workflow.",
      "order": 58,
      "command_order": 12,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. rmdir -p removes the named leaf and then each newly empty parent in the same path.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "rmdir"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rmdir-4",
      "section": "02-files-and-directories",
      "command": "rmdir",
      "title": "Ignore non-empty directories safely",
      "about": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. --ignore-fail-on-non-empty lets a cleanup remove empty candidates without treating retained data as an error.",
      "example": "rmdir --ignore-fail-on-non-empty cache logs",
      "example_output": "",
      "breakdown": [
        "rmdir is the featured command in the worked pattern.",
        "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. --ignore-fail-on-non-empty lets a cleanup remove empty candidates without treating retained data as an error.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Attempt to remove cache and staging, keeping non-empty cache while removing empty staging.",
      "solution": "rmdir --ignore-fail-on-non-empty cache staging",
      "hints": [
        "Use the checker as a specification: cache exists. staging no longer exists.",
        "Try this command shape, replacing placeholders with fixture values: rmdir --ignore-fail-on-non-empty <values>",
        "Use man rmdir or rmdir --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "staging"
      ],
      "files": {
        "cache/keep.txt": "keep\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "cache exists.",
          "path": "cache",
          "kind": "directory"
        },
        {
          "type": "path-not-exists",
          "description": "staging no longer exists.",
          "path": "staging"
        }
      ],
      "completion": "--ignore-fail-on-non-empty lets a cleanup remove empty candidates without treating retained data as an error. You can now use this behavior as part of a larger command-line workflow.",
      "order": 59,
      "command_order": 12,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. --ignore-fail-on-non-empty lets a cleanup remove empty candidates without treating retained data as an error.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "rmdir"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rmdir-5",
      "section": "02-files-and-directories",
      "command": "rmdir",
      "title": "Prune empty branches in a tree",
      "about": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. find -depth can identify empty leaves before rmdir visits their parents, while non-empty branches remain untouched.",
      "example": "find tree -depth -type d -empty -exec rmdir {} \\;",
      "example_output": "",
      "breakdown": [
        "rmdir is the featured command in the worked pattern.",
        "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. find -depth can identify empty leaves before rmdir visits their parents, while non-empty branches remain untouched.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove every empty directory under workspace while preserving the branch containing keep.txt.",
      "solution": "find workspace -depth -type d -empty -exec rmdir {} \\;",
      "hints": [
        "Use the checker as a specification: workspace/tmp no longer exists. workspace/build no longer exists.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -depth -type <value> -empty -exec <values> ;",
        "Use man rmdir or rmdir --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "workspace/tmp/cache",
        "workspace/build/old"
      ],
      "files": {
        "workspace/data/keep.txt": "keep\n"
      },
      "checks": [
        {
          "type": "path-not-exists",
          "description": "workspace/tmp no longer exists.",
          "path": "workspace/tmp"
        },
        {
          "type": "path-not-exists",
          "description": "workspace/build no longer exists.",
          "path": "workspace/build"
        },
        {
          "type": "path-exists",
          "description": "workspace/data/keep.txt exists.",
          "path": "workspace/data/keep.txt",
          "kind": "file"
        }
      ],
      "completion": "find -depth can identify empty leaves before rmdir visits their parents, while non-empty branches remain untouched. You can now use this behavior as part of a larger command-line workflow.",
      "order": 60,
      "command_order": 12,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "rmdir removes empty directories only. That narrow behavior is safer than recursive removal when emptiness is a required precondition. find -depth can identify empty leaves before rmdir visits their parents, while non-empty branches remain untouched.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "rmdir",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "find"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-touch",
      "order": 61,
      "section": "02-files-and-directories",
      "command": "touch",
      "title": "Create an empty marker file",
      "about": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty.",
      "example": "touch ready.flag",
      "example_output": "",
      "breakdown": [
        "ready.flag is created when absent.",
        "No content is written."
      ],
      "task": "Create an empty file named complete.flag.",
      "solution": "touch complete.flag",
      "hints": [
        "Pass the new filename to touch.",
        "Use the checker as a specification: complete.flag exists as a file.",
        "Try this command shape, replacing placeholders with fixture values: touch <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "path-exists",
          "description": "complete.flag exists as a file.",
          "path": "complete.flag",
          "kind": "file"
        }
      ],
      "completion": "touch is also a timestamp tool. Inspect before assuming it created a new empty file.",
      "command_order": 13,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "touch"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-touch-2",
      "section": "02-files-and-directories",
      "command": "touch",
      "title": "Create several empty files",
      "about": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch accepts multiple names, making it convenient for scaffolding related files.",
      "example": "touch one.txt two.txt",
      "example_output": "",
      "breakdown": [
        "touch is the featured command in the worked pattern.",
        "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch accepts multiple names, making it convenient for scaffolding related files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create app.log, audit.log, and error.log.",
      "solution": "touch app.log audit.log error.log",
      "hints": [
        "Use the checker as a specification: app.log exists. audit.log exists.",
        "Try this command shape, replacing placeholders with fixture values: touch <values>",
        "Use man touch or touch --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "path-exists",
          "description": "app.log exists.",
          "path": "app.log",
          "kind": "file"
        },
        {
          "type": "path-exists",
          "description": "audit.log exists.",
          "path": "audit.log",
          "kind": "file"
        },
        {
          "type": "path-exists",
          "description": "error.log exists.",
          "path": "error.log",
          "kind": "file"
        }
      ],
      "completion": "touch accepts multiple names, making it convenient for scaffolding related files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 62,
      "command_order": 13,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch accepts multiple names, making it convenient for scaffolding related files.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "touch"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-touch-3",
      "section": "02-files-and-directories",
      "command": "touch",
      "title": "Set a specific timestamp",
      "about": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -t applies a compact [[CC]YY]MMDDhhmm timestamp for reproducible test fixtures.",
      "example": "touch -t 202601020304 sample && date -u -r sample '+%Y-%m-%d %H:%M'",
      "example_output": "2026-01-02 03:04",
      "breakdown": [
        "touch is the featured command in the worked pattern.",
        "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -t applies a compact [[CC]YY]MMDDhhmm timestamp for reproducible test fixtures.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set release.marker to 2025-12-31 23:59 UTC and print that timestamp.",
      "solution": "TZ=UTC touch -t 202512312359 release.marker && date -u -r release.marker '+%Y-%m-%d %H:%M'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <value> touch -t <values> && date -u -r <values>",
        "Use man touch or touch --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "release.marker": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Set a specific timestamp”.",
          "expected": "2025-12-31 23:59",
          "normalize": "trim"
        }
      ],
      "completion": "touch -t applies a compact [[CC]YY]MMDDhhmm timestamp for reproducible test fixtures. You can now use this behavior as part of a larger command-line workflow.",
      "order": 63,
      "command_order": 13,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -t applies a compact [[CC]YY]MMDDhhmm timestamp for reproducible test fixtures.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "touch",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "date"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-touch-4",
      "section": "02-files-and-directories",
      "command": "touch",
      "title": "Copy a reference timestamp",
      "about": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -r copies access and modification times from a reference file.",
      "example": "touch -t 202501010000 reference; touch -r reference target",
      "example_output": "",
      "breakdown": [
        "touch is the featured command in the worked pattern.",
        "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -r copies access and modification times from a reference file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Give target.txt the same timestamp as source.txt and print same when their epoch modification times match.",
      "solution": "touch -t 202402030405 source.txt && touch -r source.txt target.txt && test \"$(stat -c '%Y' source.txt)\" = \"$(stat -c '%Y' target.txt)\" && echo same",
      "hints": [
        "Use the checker as a specification: The target received the reference timestamp.",
        "Try this command shape, replacing placeholders with fixture values: touch -t <values> && touch -r <values> && test <value> -c <values> -c <values> && echo <value>",
        "Use man touch or touch --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "source.txt": "source\n",
        "target.txt": "target\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The target received the reference timestamp.",
          "expected": "same",
          "normalize": "trim"
        }
      ],
      "completion": "touch -r copies access and modification times from a reference file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 64,
      "command_order": 13,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -r copies access and modification times from a reference file.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "touch",
        "command chaining",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "stat",
        "echo",
        "source"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-touch-5",
      "section": "02-files-and-directories",
      "command": "touch",
      "title": "Refresh only existing files",
      "about": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -c updates named files when present but does not accidentally create missing ones.",
      "example": "touch -c existing.log absent.log",
      "example_output": "",
      "breakdown": [
        "touch is the featured command in the worked pattern.",
        "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -c updates named files when present but does not accidentally create missing ones.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Refresh app.log and reference missing.log with -c, without creating missing.log, then print refreshed.",
      "solution": "touch -c app.log missing.log && echo refreshed",
      "hints": [
        "Use the checker as a specification: app.log exists. missing.log no longer exists.",
        "Try this command shape, replacing placeholders with fixture values: touch -c <values> && echo <value>",
        "Use man touch or touch --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.log": "existing\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "app.log exists.",
          "path": "app.log",
          "kind": "file"
        },
        {
          "type": "path-not-exists",
          "description": "missing.log no longer exists.",
          "path": "missing.log"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Refresh only existing files”.",
          "expected": "refreshed",
          "normalize": "trim"
        }
      ],
      "completion": "touch -c updates named files when present but does not accidentally create missing ones. You can now use this behavior as part of a larger command-line workflow.",
      "order": 65,
      "command_order": 13,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "touch creates a missing file or updates timestamps on an existing one. It does not guarantee that an existing file is empty. touch -c updates named files when present but does not accidentally create missing ones.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "touch",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "echo"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cp",
      "order": 66,
      "section": "02-files-and-directories",
      "command": "cp",
      "title": "Copy without changing the source",
      "about": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first.",
      "example": "cp source.txt backup.txt",
      "example_output": "",
      "breakdown": [
        "source.txt is read.",
        "backup.txt receives the same content."
      ],
      "task": "Copy briefing.txt to briefing.backup.",
      "solution": "cp briefing.txt briefing.backup",
      "hints": [
        "Source comes before destination.",
        "Use the checker as a specification: The backup contains the briefing. The source remains unchanged.",
        "Try this command shape, replacing placeholders with fixture values: cp <values>"
      ],
      "files": {
        "briefing.txt": "scope\nevidence\ncleanup\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "The backup contains the briefing.",
          "path": "briefing.backup",
          "expected": "scope\nevidence\ncleanup",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "The source remains unchanged.",
          "path": "briefing.txt",
          "expected": "scope\nevidence\ncleanup",
          "normalize": "trim"
        }
      ],
      "completion": "Copying preserves the source path. Metadata preservation requires explicit options such as -a or -p.",
      "command_order": 14,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "cp"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cp-2",
      "section": "02-files-and-directories",
      "command": "cp",
      "title": "Copy a directory tree",
      "about": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -R recursively copies a directory and all of its descendants.",
      "example": "cp -R template project",
      "example_output": "",
      "breakdown": [
        "cp is the featured command in the worked pattern.",
        "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -R recursively copies a directory and all of its descendants.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Copy template to project, including its nested config file.",
      "solution": "cp -R template project",
      "hints": [
        "Use the checker as a specification: project/README.md has the expected content. project/config/app.ini has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: cp -R <values>",
        "Use man cp or cp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "template/README.md": "template\n",
        "template/config/app.ini": "mode=dev\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "project/README.md has the expected content.",
          "path": "project/README.md",
          "expected": "template\n"
        },
        {
          "type": "file-content",
          "description": "project/config/app.ini has the expected content.",
          "path": "project/config/app.ini",
          "expected": "mode=dev\n"
        }
      ],
      "completion": "cp -R recursively copies a directory and all of its descendants. You can now use this behavior as part of a larger command-line workflow.",
      "order": 67,
      "command_order": 14,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -R recursively copies a directory and all of its descendants.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "cp"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cp-3",
      "section": "02-files-and-directories",
      "command": "cp",
      "title": "Preserve file metadata",
      "about": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -p preserves mode and timestamps along with file content.",
      "example": "cp -p script.sh backup.sh",
      "example_output": "",
      "breakdown": [
        "cp is the featured command in the worked pattern.",
        "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -p preserves mode and timestamps along with file content.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set deploy.sh to mode 750, then copy it to deploy.backup while preserving that mode.",
      "solution": "chmod 750 deploy.sh && cp -p deploy.sh deploy.backup",
      "hints": [
        "Use the checker as a specification: deploy.backup has the expected content. The executable mode was preserved.",
        "Try this command shape, replacing placeholders with fixture values: chmod <values> && cp -p <values>",
        "Use man cp or cp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "deploy.sh": "#!/bin/sh\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "deploy.backup has the expected content.",
          "path": "deploy.backup",
          "expected": "#!/bin/sh\n"
        },
        {
          "type": "file-mode",
          "description": "The executable mode was preserved.",
          "path": "deploy.backup",
          "expected": "750"
        }
      ],
      "completion": "cp -p preserves mode and timestamps along with file content. You can now use this behavior as part of a larger command-line workflow.",
      "order": 68,
      "command_order": 14,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -p preserves mode and timestamps along with file content.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "cp",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "chmod"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cp-4",
      "section": "02-files-and-directories",
      "command": "cp",
      "title": "Keep numbered destination backups",
      "about": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. --backup=numbered preserves an existing destination before replacing it.",
      "example": "cp --backup=numbered new.conf app.conf",
      "example_output": "",
      "breakdown": [
        "cp is the featured command in the worked pattern.",
        "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. --backup=numbered preserves an existing destination before replacing it.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Copy candidate.conf over app.conf while retaining the old app.conf as app.conf.~1~.",
      "solution": "cp --backup=numbered candidate.conf app.conf",
      "hints": [
        "Use the checker as a specification: app.conf has the expected content. app.conf.~1~ has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: cp --backup=numbered <values>",
        "Use man cp or cp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "candidate.conf": "mode=new\n",
        "app.conf": "mode=old\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "app.conf has the expected content.",
          "path": "app.conf",
          "expected": "mode=new\n"
        },
        {
          "type": "file-content",
          "description": "app.conf.~1~ has the expected content.",
          "path": "app.conf.~1~",
          "expected": "mode=old\n"
        }
      ],
      "completion": "--backup=numbered preserves an existing destination before replacing it. You can now use this behavior as part of a larger command-line workflow.",
      "order": 69,
      "command_order": 14,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. --backup=numbered preserves an existing destination before replacing it.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "cp"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cp-5",
      "section": "02-files-and-directories",
      "command": "cp",
      "title": "Archive a tree faithfully",
      "about": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -a preserves a directory hierarchy, modes, timestamps, and symbolic links for a local archive copy.",
      "example": "cp -a release snapshot",
      "example_output": "",
      "breakdown": [
        "cp is the featured command in the worked pattern.",
        "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -a preserves a directory hierarchy, modes, timestamps, and symbolic links for a local archive copy.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Archive release as snapshot while preserving current as a symbolic link.",
      "solution": "cp -a release snapshot && readlink snapshot/current",
      "hints": [
        "Use archive mode rather than recursively dereferencing links.",
        "Run cp -a release snapshot; the filesystem checks grade the copy.",
        "Use the checker as a specification: snapshot/v2/app.txt has the expected content. The copied link still points to v2."
      ],
      "files": {
        "release/v2/app.txt": "version 2\n"
      },
      "symlinks": {
        "release/current": "v2"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "snapshot/v2/app.txt has the expected content.",
          "path": "snapshot/v2/app.txt",
          "expected": "version 2\n"
        },
        {
          "type": "stdout",
          "description": "The copied link still points to v2.",
          "expected": "v2",
          "normalize": "trim"
        },
        {
          "type": "path-exists",
          "description": "snapshot/current exists.",
          "path": "snapshot/current",
          "kind": "any"
        }
      ],
      "completion": "cp -a preserves a directory hierarchy, modes, timestamps, and symbolic links for a local archive copy. You can now use this behavior as part of a larger command-line workflow.",
      "order": 70,
      "command_order": 14,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "cp creates a destination from a source. A plain copy can overwrite an existing path, so verify both names first. cp -a preserves a directory hierarchy, modes, timestamps, and symbolic links for a local archive copy.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "cp",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "readlink"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mv",
      "order": 71,
      "section": "02-files-and-directories",
      "command": "mv",
      "title": "Rename a file",
      "about": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes.",
      "example": "mv draft.txt final.txt",
      "example_output": "",
      "breakdown": [
        "draft.txt is the source name.",
        "final.txt becomes the destination name."
      ],
      "task": "Rename report.draft to report.final.",
      "solution": "mv report.draft report.final",
      "hints": [
        "Use the old path first and new path second.",
        "Use the checker as a specification: report.final contains the report. The draft path is gone.",
        "Try this command shape, replacing placeholders with fixture values: mv <values>"
      ],
      "files": {
        "report.draft": "approved\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "report.final contains the report.",
          "path": "report.final",
          "expected": "approved",
          "normalize": "trim"
        },
        {
          "type": "path-not-exists",
          "description": "The draft path is gone.",
          "path": "report.draft"
        }
      ],
      "completion": "mv can overwrite a destination. Use -i or -n when accidental replacement is a concern.",
      "command_order": 15,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "mv"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mv-2",
      "section": "02-files-and-directories",
      "command": "mv",
      "title": "Move several files into a directory",
      "about": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. When the final operand is a directory, mv relocates every preceding file into it.",
      "example": "mv one.log two.log archive/",
      "example_output": "",
      "breakdown": [
        "mv is the featured command in the worked pattern.",
        "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. When the final operand is a directory, mv relocates every preceding file into it.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Move api.log, web.log, and db.log into logs/archive.",
      "solution": "mv api.log web.log db.log logs/archive/",
      "hints": [
        "Use the checker as a specification: logs/archive/api.log has the expected content. logs/archive/web.log has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: mv <values>",
        "Use man mv or mv --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "logs/archive"
      ],
      "files": {
        "api.log": "api\n",
        "web.log": "web\n",
        "db.log": "db\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "logs/archive/api.log has the expected content.",
          "path": "logs/archive/api.log",
          "expected": "api\n"
        },
        {
          "type": "file-content",
          "description": "logs/archive/web.log has the expected content.",
          "path": "logs/archive/web.log",
          "expected": "web\n"
        },
        {
          "type": "file-content",
          "description": "logs/archive/db.log has the expected content.",
          "path": "logs/archive/db.log",
          "expected": "db\n"
        }
      ],
      "completion": "When the final operand is a directory, mv relocates every preceding file into it. You can now use this behavior as part of a larger command-line workflow.",
      "order": 72,
      "command_order": 15,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. When the final operand is a directory, mv relocates every preceding file into it.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "mv"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mv-3",
      "section": "02-files-and-directories",
      "command": "mv",
      "title": "Avoid overwriting an existing destination",
      "about": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. mv -n keeps an existing destination untouched and leaves the source available for review.",
      "example": "mv -n candidate.txt final.txt",
      "example_output": "",
      "breakdown": [
        "mv is the featured command in the worked pattern.",
        "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. mv -n keeps an existing destination untouched and leaves the source available for review.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Try to move candidate.conf over existing app.conf without overwriting it, then print preserved.",
      "solution": "mv -n candidate.conf app.conf && echo preserved",
      "hints": [
        "Use the checker as a specification: app.conf has the expected content. candidate.conf has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: mv -n <values> && echo <value>",
        "Use man mv or mv --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "candidate.conf": "candidate\n",
        "app.conf": "production\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "app.conf has the expected content.",
          "path": "app.conf",
          "expected": "production\n"
        },
        {
          "type": "file-content",
          "description": "candidate.conf has the expected content.",
          "path": "candidate.conf",
          "expected": "candidate\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Avoid overwriting an existing destination”.",
          "expected": "preserved",
          "normalize": "trim"
        }
      ],
      "completion": "mv -n keeps an existing destination untouched and leaves the source available for review. You can now use this behavior as part of a larger command-line workflow.",
      "order": 73,
      "command_order": 15,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. mv -n keeps an existing destination untouched and leaves the source available for review.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "mv",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "echo"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mv-4",
      "section": "02-files-and-directories",
      "command": "mv",
      "title": "Back up a replaced destination",
      "about": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. mv --backup=numbered preserves the prior destination while installing the new file.",
      "example": "mv --backup=numbered new.txt current.txt",
      "example_output": "",
      "breakdown": [
        "mv is the featured command in the worked pattern.",
        "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. mv --backup=numbered preserves the prior destination while installing the new file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Move next.conf to current.conf and retain the old current.conf as current.conf.~1~.",
      "solution": "mv --backup=numbered next.conf current.conf",
      "hints": [
        "Use the checker as a specification: current.conf has the expected content. current.conf.~1~ has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: mv --backup=numbered <values>",
        "Use man mv or mv --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "next.conf": "next\n",
        "current.conf": "current\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "current.conf has the expected content.",
          "path": "current.conf",
          "expected": "next\n"
        },
        {
          "type": "file-content",
          "description": "current.conf.~1~ has the expected content.",
          "path": "current.conf.~1~",
          "expected": "current\n"
        },
        {
          "type": "path-not-exists",
          "description": "next.conf no longer exists.",
          "path": "next.conf"
        }
      ],
      "completion": "mv --backup=numbered preserves the prior destination while installing the new file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 74,
      "command_order": 15,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. mv --backup=numbered preserves the prior destination while installing the new file.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "mv"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-mv-5",
      "section": "02-files-and-directories",
      "command": "mv",
      "title": "Promote a staged release",
      "about": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. A sequence of mv operations can archive the current release before promoting a staged directory atomically within one filesystem.",
      "example": "mv current previous && mv staged current",
      "example_output": "",
      "breakdown": [
        "mv is the featured command in the worked pattern.",
        "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. A sequence of mv operations can archive the current release before promoting a staged directory atomically within one filesystem.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Move release/current to release/previous, then promote release/staged to release/current.",
      "solution": "mv release/current release/previous && mv release/staged release/current",
      "hints": [
        "Use the checker as a specification: release/previous/version.txt has the expected content. release/current/version.txt has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: mv <values> && mv <values>",
        "Use man mv or mv --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "release/current/version.txt": "v1\n",
        "release/staged/version.txt": "v2\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "release/previous/version.txt has the expected content.",
          "path": "release/previous/version.txt",
          "expected": "v1\n"
        },
        {
          "type": "file-content",
          "description": "release/current/version.txt has the expected content.",
          "path": "release/current/version.txt",
          "expected": "v2\n"
        },
        {
          "type": "path-not-exists",
          "description": "release/staged no longer exists.",
          "path": "release/staged"
        }
      ],
      "completion": "A sequence of mv operations can archive the current release before promoting a staged directory atomically within one filesystem. You can now use this behavior as part of a larger command-line workflow.",
      "order": 75,
      "command_order": 15,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "mv moves or renames a path. Within one filesystem, a rename usually changes directory metadata rather than copying file bytes. A sequence of mv operations can archive the current release before promoting a staged directory atomically within one filesystem.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "mv",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rm",
      "order": 76,
      "section": "02-files-and-directories",
      "command": "rm",
      "title": "Remove one disposable file",
      "about": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion.",
      "example": "rm obsolete.txt",
      "example_output": "",
      "breakdown": [
        "Only the named file is removed.",
        "No recursive or force option is needed."
      ],
      "task": "Remove trash.txt and nothing else.",
      "solution": "rm trash.txt",
      "hints": [
        "Name the one disposable file directly.",
        "Use the checker as a specification: trash.txt was removed. keep.txt remains unchanged.",
        "Try this command shape, replacing placeholders with fixture values: rm <value>"
      ],
      "files": {
        "trash.txt": "discard\n",
        "keep.txt": "retain\n"
      },
      "checks": [
        {
          "type": "path-not-exists",
          "description": "trash.txt was removed.",
          "path": "trash.txt"
        },
        {
          "type": "file-content",
          "description": "keep.txt remains unchanged.",
          "path": "keep.txt",
          "expected": "retain",
          "normalize": "trim"
        }
      ],
      "completion": "rm normally has no undo. Narrow the target, avoid unnecessary -r/-f, and verify what must remain.",
      "command_order": 16,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "rm"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rm-2",
      "section": "02-files-and-directories",
      "command": "rm",
      "title": "Remove several disposable files",
      "about": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm accepts multiple paths so related disposable files can be removed together.",
      "example": "rm one.tmp two.tmp",
      "example_output": "",
      "breakdown": [
        "rm is the featured command in the worked pattern.",
        "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm accepts multiple paths so related disposable files can be removed together.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove cache.tmp, session.tmp, and stale.tmp.",
      "solution": "rm cache.tmp session.tmp stale.tmp",
      "hints": [
        "Use the checker as a specification: cache.tmp no longer exists. session.tmp no longer exists.",
        "Try this command shape, replacing placeholders with fixture values: rm <values>",
        "Use man rm or rm --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "cache.tmp": "",
        "session.tmp": "",
        "stale.tmp": ""
      },
      "checks": [
        {
          "type": "path-not-exists",
          "description": "cache.tmp no longer exists.",
          "path": "cache.tmp"
        },
        {
          "type": "path-not-exists",
          "description": "session.tmp no longer exists.",
          "path": "session.tmp"
        },
        {
          "type": "path-not-exists",
          "description": "stale.tmp no longer exists.",
          "path": "stale.tmp"
        }
      ],
      "completion": "rm accepts multiple paths so related disposable files can be removed together. You can now use this behavior as part of a larger command-line workflow.",
      "order": 77,
      "command_order": 16,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm accepts multiple paths so related disposable files can be removed together.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "rm"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rm-3",
      "section": "02-files-and-directories",
      "command": "rm",
      "title": "Remove a disposable directory tree",
      "about": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm -r recursively removes a named tree; it should be scoped to an exact reviewed path.",
      "example": "rm -r build/old",
      "example_output": "",
      "breakdown": [
        "rm is the featured command in the worked pattern.",
        "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm -r recursively removes a named tree; it should be scoped to an exact reviewed path.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove the entire scratch directory while preserving project.",
      "solution": "rm -r scratch",
      "hints": [
        "Use the checker as a specification: scratch no longer exists. project/keep.txt has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: rm -r <value>",
        "Use man rm or rm --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "scratch/cache/a.tmp": "",
        "scratch/cache/b.tmp": "",
        "project/keep.txt": "keep\n"
      },
      "checks": [
        {
          "type": "path-not-exists",
          "description": "scratch no longer exists.",
          "path": "scratch"
        },
        {
          "type": "file-content",
          "description": "project/keep.txt has the expected content.",
          "path": "project/keep.txt",
          "expected": "keep\n"
        }
      ],
      "completion": "rm -r recursively removes a named tree; it should be scoped to an exact reviewed path. You can now use this behavior as part of a larger command-line workflow.",
      "order": 78,
      "command_order": 16,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm -r recursively removes a named tree; it should be scoped to an exact reviewed path.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "rm"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rm-4",
      "section": "02-files-and-directories",
      "command": "rm",
      "title": "Make cleanup idempotent",
      "about": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm -f does not fail when a file is already absent, which helps repeatable cleanup scripts.",
      "example": "rm -f generated.txt",
      "example_output": "",
      "breakdown": [
        "rm is the featured command in the worked pattern.",
        "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm -f does not fail when a file is already absent, which helps repeatable cleanup scripts.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove present.tmp and also name already-gone.tmp without producing an error.",
      "solution": "rm -f present.tmp already-gone.tmp",
      "hints": [
        "Use the checker as a specification: present.tmp no longer exists. The repeatable cleanup succeeded.",
        "Try this command shape, replacing placeholders with fixture values: rm -f <values>",
        "Use man rm or rm --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "present.tmp": "temporary\n"
      },
      "checks": [
        {
          "type": "path-not-exists",
          "description": "present.tmp no longer exists.",
          "path": "present.tmp"
        },
        {
          "type": "exit-code",
          "description": "The repeatable cleanup succeeded.",
          "expected": 0
        }
      ],
      "completion": "rm -f does not fail when a file is already absent, which helps repeatable cleanup scripts. You can now use this behavior as part of a larger command-line workflow.",
      "order": 79,
      "command_order": 16,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. rm -f does not fail when a file is already absent, which helps repeatable cleanup scripts.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "rm"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-rm-5",
      "section": "02-files-and-directories",
      "command": "rm",
      "title": "Delete files selected by a safe search",
      "about": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. A null-delimited find/xargs pipeline passes exact filenames to rm without breaking on spaces.",
      "example": "find cache -type f -name '*.tmp' -print0 | xargs -0 rm",
      "example_output": "",
      "breakdown": [
        "rm is the featured command in the worked pattern.",
        "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. A null-delimited find/xargs pipeline passes exact filenames to rm without breaking on spaces.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Delete every .tmp file under cache, including old session.tmp, while preserving .dat files.",
      "solution": "find cache -type f -name '*.tmp' -print0 | xargs -0 rm",
      "hints": [
        "Use the checker as a specification: cache/a.tmp no longer exists. cache/old session.tmp no longer exists.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -type <value> -name <value> -print0 | xargs -0 <value>",
        "Use man rm or rm --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "cache/a.tmp": "",
        "cache/old session.tmp": "",
        "cache/data.dat": "keep\n",
        "cache/nested/b.tmp": ""
      },
      "checks": [
        {
          "type": "path-not-exists",
          "description": "cache/a.tmp no longer exists.",
          "path": "cache/a.tmp"
        },
        {
          "type": "path-not-exists",
          "description": "cache/old session.tmp no longer exists.",
          "path": "cache/old session.tmp"
        },
        {
          "type": "path-not-exists",
          "description": "cache/nested/b.tmp no longer exists.",
          "path": "cache/nested/b.tmp"
        },
        {
          "type": "file-content",
          "description": "cache/data.dat has the expected content.",
          "path": "cache/data.dat",
          "expected": "keep\n"
        }
      ],
      "completion": "A null-delimited find/xargs pipeline passes exact filenames to rm without breaking on spaces. You can now use this behavior as part of a larger command-line workflow.",
      "order": 80,
      "command_order": 16,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "rm unlinks files. This rep is constrained to a synthetic temporary workspace that is discarded after completion. A null-delimited find/xargs pipeline passes exact filenames to rm without breaking on spaces.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "rm",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "xargs",
        "find"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ln",
      "order": 81,
      "section": "02-files-and-directories",
      "command": "ln",
      "title": "Create a hard link",
      "about": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies.",
      "example": "ln source.txt second-name.txt",
      "example_output": "",
      "breakdown": [
        "source.txt already names an inode.",
        "second-name.txt becomes another name for that inode."
      ],
      "task": "Create hardlink.txt as a hard link to source.txt.",
      "solution": "ln source.txt hardlink.txt",
      "hints": [
        "Do not add -s; this rep asks for a hard link.",
        "Use the checker as a specification: Both names reference the same inode.",
        "Try this command shape, replacing placeholders with fixture values: ln <values>"
      ],
      "files": {
        "source.txt": "one inode\n"
      },
      "checks": [
        {
          "type": "same-inode",
          "description": "Both names reference the same inode.",
          "path": "source.txt",
          "other_path": "hardlink.txt"
        }
      ],
      "completion": "Hard links share content and inode metadata. Removing one name does not remove the inode while another hard link remains.",
      "command_order": 17,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "ln"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "source"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ln-2",
      "section": "02-files-and-directories",
      "command": "ln",
      "title": "Create a symbolic link",
      "about": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. ln -s creates a symbolic link whose stored target can be a relative path.",
      "example": "ln -s releases/v1 current",
      "example_output": "",
      "breakdown": [
        "ln is the featured command in the worked pattern.",
        "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. ln -s creates a symbolic link whose stored target can be a relative path.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create latest as a symbolic link to releases/2026.",
      "solution": "ln -s releases/2026 latest",
      "hints": [
        "Use the checker as a specification: latest/version.txt has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: ln -s <values>",
        "Use man ln or ln --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "releases/2026/version.txt": "2026\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "latest/version.txt has the expected content.",
          "path": "latest/version.txt",
          "expected": "2026\n"
        }
      ],
      "completion": "ln -s creates a symbolic link whose stored target can be a relative path. You can now use this behavior as part of a larger command-line workflow.",
      "order": 82,
      "command_order": 17,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. ln -s creates a symbolic link whose stored target can be a relative path.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "ln"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ln-3",
      "section": "02-files-and-directories",
      "command": "ln",
      "title": "Create hard links in a backup directory",
      "about": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. A hard link gives the same file inode another directory entry without duplicating its data.",
      "example": "ln report.txt backups/report.txt",
      "example_output": "",
      "breakdown": [
        "ln is the featured command in the worked pattern.",
        "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. A hard link gives the same file inode another directory entry without duplicating its data.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create backups/inventory.csv as a hard link to inventory.csv.",
      "solution": "ln inventory.csv backups/inventory.csv",
      "hints": [
        "Use the checker as a specification: The backup and source share one inode.",
        "Try this command shape, replacing placeholders with fixture values: ln <values>",
        "Use man ln or ln --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "backups"
      ],
      "files": {
        "inventory.csv": "router,online\n"
      },
      "checks": [
        {
          "type": "same-inode",
          "description": "The backup and source share one inode.",
          "path": "inventory.csv",
          "other_path": "backups/inventory.csv"
        }
      ],
      "completion": "A hard link gives the same file inode another directory entry without duplicating its data. You can now use this behavior as part of a larger command-line workflow.",
      "order": 83,
      "command_order": 17,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. A hard link gives the same file inode another directory entry without duplicating its data.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "ln"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ln-4",
      "section": "02-files-and-directories",
      "command": "ln",
      "title": "Create links for several files",
      "about": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. With a directory destination, ln creates one hard link for each preceding source file.",
      "example": "ln one.txt two.txt snapshots/",
      "example_output": "",
      "breakdown": [
        "ln is the featured command in the worked pattern.",
        "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. With a directory destination, ln creates one hard link for each preceding source file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Hard-link api.conf, web.conf, and db.conf into snapshots.",
      "solution": "ln api.conf web.conf db.conf snapshots/",
      "hints": [
        "Use the checker as a specification: api.conf shares its source inode. web.conf shares its source inode.",
        "Try this command shape, replacing placeholders with fixture values: ln <values>",
        "Use man ln or ln --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "snapshots"
      ],
      "files": {
        "api.conf": "api\n",
        "web.conf": "web\n",
        "db.conf": "db\n"
      },
      "checks": [
        {
          "type": "same-inode",
          "description": "api.conf shares its source inode.",
          "path": "api.conf",
          "other_path": "snapshots/api.conf"
        },
        {
          "type": "same-inode",
          "description": "web.conf shares its source inode.",
          "path": "web.conf",
          "other_path": "snapshots/web.conf"
        },
        {
          "type": "same-inode",
          "description": "db.conf shares its source inode.",
          "path": "db.conf",
          "other_path": "snapshots/db.conf"
        }
      ],
      "completion": "With a directory destination, ln creates one hard link for each preceding source file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 84,
      "command_order": 17,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. With a directory destination, ln creates one hard link for each preceding source file.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "ln"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ln-5",
      "section": "02-files-and-directories",
      "command": "ln",
      "title": "Switch a stable release link",
      "about": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. ln -sfn replaces an existing symbolic-link destination without copying or moving release data.",
      "example": "ln -sfn releases/v2 current",
      "example_output": "",
      "breakdown": [
        "ln is the featured command in the worked pattern.",
        "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. ln -sfn replaces an existing symbolic-link destination without copying or moving release data.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Switch current from releases/v1 to releases/v2, then print its stored target.",
      "solution": "ln -sfn releases/v2 current && readlink current",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: ln -sfn <values> && readlink <value>",
        "Use man ln or ln --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "releases/v1/version": "v1\n",
        "releases/v2/version": "v2\n"
      },
      "symlinks": {
        "current": "releases/v1"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Switch a stable release link”.",
          "expected": "releases/v2",
          "normalize": "trim"
        }
      ],
      "completion": "ln -sfn replaces an existing symbolic-link destination without copying or moving release data. You can now use this behavior as part of a larger command-line workflow.",
      "order": 85,
      "command_order": 17,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "ln creates a hard link by default. Both directory entries refer to the same inode rather than storing two independent copies. ln -sfn replaces an existing symbolic-link destination without copying or moving release data.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "ln",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "readlink"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-readlink",
      "order": 86,
      "section": "02-files-and-directories",
      "command": "readlink",
      "title": "Inspect a symbolic-link target",
      "about": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist.",
      "example": "ln -s configs/app.conf current.conf && readlink current.conf",
      "example_output": "configs/app.conf\n",
      "breakdown": [
        "ln -s creates a symbolic link.",
        "readlink prints its stored target string."
      ],
      "task": "Create latest.log pointing to logs/app.log, then print the stored target.",
      "solution": "ln -s logs/app.log latest.log && readlink latest.log",
      "hints": [
        "Create a symbolic link with ln -s, then inspect it.",
        "Use the checker as a specification: The stored relative target was printed.",
        "Try this command shape, replacing placeholders with fixture values: ln -s <values> && readlink <value>"
      ],
      "files": {
        "logs/app.log": "ready\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The stored relative target was printed.",
          "expected": "logs/app.log",
          "normalize": "trim"
        }
      ],
      "completion": "readlink reports the link's stored target; realpath resolves the resulting path through the filesystem.",
      "command_order": 18,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "readlink",
        "command chaining"
      ],
      "prerequisites": [
        "ln"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-readlink-2",
      "section": "02-files-and-directories",
      "command": "readlink",
      "title": "Inspect a relative link target",
      "about": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink prints the target text stored in a symbolic link without resolving it.",
      "example": "readlink current",
      "example_output": "releases/v1",
      "breakdown": [
        "readlink is the featured command in the worked pattern.",
        "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink prints the target text stored in a symbolic link without resolving it.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the stored target of latest.",
      "solution": "readlink latest",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: readlink <value>",
        "Use man readlink or readlink --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "releases/2026/app": ""
      },
      "symlinks": {
        "latest": "releases/2026"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Inspect a relative link target”.",
          "expected": "releases/2026",
          "normalize": "trim"
        }
      ],
      "completion": "readlink prints the target text stored in a symbolic link without resolving it. You can now use this behavior as part of a larger command-line workflow.",
      "order": 87,
      "command_order": 18,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink prints the target text stored in a symbolic link without resolving it.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "readlink"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-readlink-3",
      "section": "02-files-and-directories",
      "command": "readlink",
      "title": "Resolve a complete link chain",
      "about": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink -f follows every link and prints the canonical absolute target.",
      "example": "readlink -f entry",
      "example_output": "/work/releases/v2/app",
      "breakdown": [
        "readlink is the featured command in the worked pattern.",
        "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink -f follows every link and prints the canonical absolute target.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Resolve entry through active and current to the actual app file.",
      "solution": "readlink -f entry",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: readlink -f <value>",
        "Use man readlink or readlink --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "releases/v2/app": "v2\n"
      },
      "symlinks": {
        "current": "releases/v2",
        "active": "current",
        "entry": "active/app"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Resolve a complete link chain”.",
          "expected": "{{workspace}}/releases/v2/app",
          "normalize": "trim"
        }
      ],
      "completion": "readlink -f follows every link and prints the canonical absolute target. You can now use this behavior as part of a larger command-line workflow.",
      "order": 88,
      "command_order": 18,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink -f follows every link and prints the canonical absolute target.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "readlink"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-readlink-4",
      "section": "02-files-and-directories",
      "command": "readlink",
      "title": "Require every component to exist",
      "about": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink -e resolves a canonical path only when every component, including the final one, exists.",
      "example": "readlink -e current/config.ini",
      "example_output": "/work/releases/v2/config.ini",
      "breakdown": [
        "readlink is the featured command in the worked pattern.",
        "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink -e resolves a canonical path only when every component, including the final one, exists.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Resolve stable/app.conf with -e and print its canonical target.",
      "solution": "readlink -e stable/app.conf",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: readlink -e <value>",
        "Use man readlink or readlink --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "versions/blue/app.conf": "blue\n"
      },
      "symlinks": {
        "stable": "versions/blue"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Require every component to exist”.",
          "expected": "{{workspace}}/versions/blue/app.conf",
          "normalize": "trim"
        }
      ],
      "completion": "readlink -e resolves a canonical path only when every component, including the final one, exists. You can now use this behavior as part of a larger command-line workflow.",
      "order": 89,
      "command_order": 18,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. readlink -e resolves a canonical path only when every component, including the final one, exists.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "readlink"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-readlink-5",
      "section": "02-files-and-directories",
      "command": "readlink",
      "title": "Audit several deployment links",
      "about": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. A loop around readlink can produce a concise name=target inventory of stable aliases.",
      "example": "for link in current backup; do printf '%s=%s\\n' \"$link\" \"$(readlink \"$link\")\"; done",
      "example_output": "current=releases/v2\nbackup=releases/v1",
      "breakdown": [
        "readlink is the featured command in the worked pattern.",
        "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. A loop around readlink can produce a concise name=target inventory of stable aliases.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print name=target lines for blue, green, and active in that order.",
      "solution": "for link in blue green active; do printf '%s=%s\\n' \"$link\" \"$(readlink \"$link\")\"; done",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <values> ; done",
        "Use man readlink or readlink --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "releases/blue/version": "",
        "releases/green/version": ""
      },
      "symlinks": {
        "blue": "releases/blue",
        "green": "releases/green",
        "active": "green"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Audit several deployment links”.",
          "expected": "blue=releases/blue\ngreen=releases/green\nactive=green",
          "normalize": "trim"
        }
      ],
      "completion": "A loop around readlink can produce a concise name=target inventory of stable aliases. You can now use this behavior as part of a larger command-line workflow.",
      "order": 90,
      "command_order": 18,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "readlink prints the target stored in a symbolic link. The stored target may itself be relative and may not exist. A loop around readlink can produce a concise name=target inventory of stable aliases.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "readlink",
        "command chaining",
        "command substitution",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "printf"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-file",
      "order": 91,
      "section": "02-files-and-directories",
      "command": "file",
      "title": "Identify content type from bytes",
      "about": "file examines content signatures and heuristics rather than trusting only a filename extension.",
      "example": "file -b mystery.txt",
      "example_output": "ASCII text\n",
      "breakdown": [
        "-b omits the filename label.",
        "The file's bytes drive the description."
      ],
      "task": "Ask file to identify sample.txt without printing its filename.",
      "solution": "file -b sample.txt",
      "hints": [
        "Use brief mode with -b.",
        "Use the checker as a specification: The data was identified as text.",
        "Try this command shape, replacing placeholders with fixture values: file -b <value>"
      ],
      "files": {
        "sample.txt": "plain ASCII sample\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The data was identified as text.",
          "expected": "text"
        }
      ],
      "completion": "file is a clue, not a security verdict. Treat its label as an observation to validate when stakes are higher.",
      "command_order": 19,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "file examines content signatures and heuristics rather than trusting only a filename extension.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "file"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-file-2",
      "section": "02-files-and-directories",
      "command": "file",
      "title": "Identify several file types",
      "about": "file examines content signatures and heuristics rather than trusting only a filename extension. file accepts multiple paths and reports a type for each.",
      "example": "file note.txt empty.bin",
      "example_output": "note.txt: ASCII text\nempty.bin: empty",
      "breakdown": [
        "file is the featured command in the worked pattern.",
        "file examines content signatures and heuristics rather than trusting only a filename extension. file accepts multiple paths and reports a type for each.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run file on note.txt and empty.bin.",
      "solution": "file note.txt empty.bin",
      "hints": [
        "Use the checker as a specification: The text file was classified. The empty file was classified.",
        "Try this command shape, replacing placeholders with fixture values: file <values>",
        "Use man file or file --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "note.txt": "hello world\n",
        "empty.bin": ""
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The text file was classified.",
          "expected": "note.txt:"
        },
        {
          "type": "stdout-contains",
          "description": "The empty file was classified.",
          "expected": "empty.bin: empty"
        }
      ],
      "completion": "file accepts multiple paths and reports a type for each. You can now use this behavior as part of a larger command-line workflow.",
      "order": 92,
      "command_order": 19,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "file examines content signatures and heuristics rather than trusting only a filename extension. file accepts multiple paths and reports a type for each.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "file"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-file-3",
      "section": "02-files-and-directories",
      "command": "file",
      "title": "Print only MIME types",
      "about": "file examines content signatures and heuristics rather than trusting only a filename extension. --mime-type and -b produce compact media types without filenames or encoding details.",
      "example": "file -b --mime-type note.txt",
      "example_output": "text/plain",
      "breakdown": [
        "file is the featured command in the worked pattern.",
        "file examines content signatures and heuristics rather than trusting only a filename extension. --mime-type and -b produce compact media types without filenames or encoding details.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the MIME type of data.csv.",
      "solution": "file -b --mime-type data.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: file -b --mime-type <value>",
        "Use man file or file --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "data.csv": "name,status\napi,up\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print only MIME types”.",
          "expected": "text/plain",
          "normalize": "trim"
        }
      ],
      "completion": "--mime-type and -b produce compact media types without filenames or encoding details. You can now use this behavior as part of a larger command-line workflow.",
      "order": 93,
      "command_order": 19,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "file examines content signatures and heuristics rather than trusting only a filename extension. --mime-type and -b produce compact media types without filenames or encoding details.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "file"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-file-4",
      "section": "02-files-and-directories",
      "command": "file",
      "title": "Classify piped content",
      "about": "file examines content signatures and heuristics rather than trusting only a filename extension. Using - as the path lets file inspect bytes arriving on standard input.",
      "example": "printf 'hello\\n' | file -b -",
      "example_output": "ASCII text",
      "breakdown": [
        "file is the featured command in the worked pattern.",
        "file examines content signatures and heuristics rather than trusting only a filename extension. Using - as the path lets file inspect bytes arriving on standard input.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Pipe payload.txt into file and print its brief classification.",
      "solution": "file -b - < payload.txt",
      "hints": [
        "Use the checker as a specification: The piped payload was recognized as text.",
        "Try this command shape, replacing placeholders with fixture values: file -b <value> < <value>",
        "Use man file or file --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "payload.txt": "{\"status\":\"ok\"}\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The piped payload was recognized as text.",
          "expected": "text"
        }
      ],
      "completion": "Using - as the path lets file inspect bytes arriving on standard input. You can now use this behavior as part of a larger command-line workflow.",
      "order": 94,
      "command_order": 19,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "file examines content signatures and heuristics rather than trusting only a filename extension. Using - as the path lets file inspect bytes arriving on standard input.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "file",
        "redirection"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-file-5",
      "section": "02-files-and-directories",
      "command": "file",
      "title": "Inventory MIME types across a directory",
      "about": "file examines content signatures and heuristics rather than trusting only a filename extension. A loop can turn file --mime-type into a stable filename=type report for validation workflows.",
      "example": "for f in assets/*; do printf '%s=%s\\n' \"$(basename \"$f\")\" \"$(file -b --mime-type \"$f\")\"; done",
      "example_output": "empty.bin=inode/x-empty\nreadme.txt=text/plain",
      "breakdown": [
        "file is the featured command in the worked pattern.",
        "file examines content signatures and heuristics rather than trusting only a filename extension. A loop can turn file --mime-type into a stable filename=type report for validation workflows.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print filename=MIME-type lines for assets/empty.bin and assets/readme.txt in sorted filename order.",
      "solution": "for f in assets/*; do printf '%s=%s\\n' \"$(basename \"$f\")\" \"$(file -b --mime-type \"$f\")\"; done",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <values> -b --mime-type <value> ; done",
        "Use man file or file --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "assets/empty.bin": "",
        "assets/readme.txt": "hello\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Inventory MIME types across a directory”.",
          "expected": "empty.bin=inode/x-empty\nreadme.txt=text/plain",
          "normalize": "trim"
        }
      ],
      "completion": "A loop can turn file --mime-type into a stable filename=type report for validation workflows. You can now use this behavior as part of a larger command-line workflow.",
      "order": 95,
      "command_order": 19,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "file examines content signatures and heuristics rather than trusting only a filename extension. A loop can turn file --mime-type into a stable filename=type report for validation workflows.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "file",
        "command chaining",
        "command substitution",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [
        "basename"
      ],
      "introduced_inline": [
        "printf"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-stat",
      "order": 96,
      "section": "02-files-and-directories",
      "command": "stat",
      "title": "Read exact file metadata",
      "about": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field.",
      "example": "stat -c '%s' sample.txt",
      "example_output": "4\n",
      "breakdown": [
        "-c chooses a custom format.",
        "%s is file size in bytes."
      ],
      "task": "Print only the byte size of bytes.txt.",
      "solution": "stat -c '%s' bytes.txt",
      "hints": [
        "Use GNU stat format token %s.",
        "Use the checker as a specification: The four-byte size was printed.",
        "Try this command shape, replacing placeholders with fixture values: stat -c <values>"
      ],
      "files": {
        "bytes.txt": "ABC\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The four-byte size was printed.",
          "expected": "4",
          "normalize": "trim"
        }
      ],
      "completion": "Metadata formats differ across Unix families. This rep deliberately targets GNU stat as found on Linux.",
      "command_order": 20,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "stat",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-stat-2",
      "section": "02-files-and-directories",
      "command": "stat",
      "title": "Print a file size",
      "about": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. stat -c accepts format tokens; %s expands to the size in bytes.",
      "example": "stat -c '%s' note.txt",
      "example_output": "6",
      "breakdown": [
        "stat is the featured command in the worked pattern.",
        "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. stat -c accepts format tokens; %s expands to the size in bytes.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the byte size of payload.bin.",
      "solution": "stat -c '%s' payload.bin",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: stat -c <values>",
        "Use man stat or stat --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "payload.bin": "1234567890"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print a file size”.",
          "expected": "10",
          "normalize": "trim"
        }
      ],
      "completion": "stat -c accepts format tokens; %s expands to the size in bytes. You can now use this behavior as part of a larger command-line workflow.",
      "order": 97,
      "command_order": 20,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. stat -c accepts format tokens; %s expands to the size in bytes.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "stat",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-stat-3",
      "section": "02-files-and-directories",
      "command": "stat",
      "title": "Inspect numeric permissions",
      "about": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. The %a format token prints the familiar octal permission bits.",
      "example": "stat -c '%a' script.sh",
      "example_output": "755",
      "breakdown": [
        "stat is the featured command in the worked pattern.",
        "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. The %a format token prints the familiar octal permission bits.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set deploy.sh to mode 750 and print only that numeric mode.",
      "solution": "chmod 750 deploy.sh && stat -c '%a' deploy.sh",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: chmod <values> && stat -c <values>",
        "Use man stat or stat --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "deploy.sh": "#!/bin/sh\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Inspect numeric permissions”.",
          "expected": "750",
          "normalize": "trim"
        }
      ],
      "completion": "The %a format token prints the familiar octal permission bits. You can now use this behavior as part of a larger command-line workflow.",
      "order": 98,
      "command_order": 20,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. The %a format token prints the familiar octal permission bits.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "stat",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "chmod"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-stat-4",
      "section": "02-files-and-directories",
      "command": "stat",
      "title": "Create a compact metadata record",
      "about": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. Several stat format tokens can produce a parseable name|size|mode record in one call.",
      "example": "stat -c '%n|%s|%a' app.conf",
      "example_output": "app.conf|12|644",
      "breakdown": [
        "stat is the featured command in the worked pattern.",
        "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. Several stat format tokens can produce a parseable name|size|mode record in one call.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print inventory.csv|18|640 after assigning mode 640.",
      "solution": "chmod 640 inventory.csv && stat -c '%n|%s|%a' inventory.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: chmod <values> && stat -c <value> | <value> | <values>",
        "Use man stat or stat --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "inventory.csv": "name,status\napi,up"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Create a compact metadata record”.",
          "expected": "inventory.csv|18|640",
          "normalize": "trim"
        }
      ],
      "completion": "Several stat format tokens can produce a parseable name|size|mode record in one call. You can now use this behavior as part of a larger command-line workflow.",
      "order": 99,
      "command_order": 20,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. Several stat format tokens can produce a parseable name|size|mode record in one call.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "stat",
        "pipelines",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "chmod"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-stat-5",
      "section": "02-files-and-directories",
      "command": "stat",
      "title": "Audit a set of deployment files",
      "about": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. stat can apply one custom format to several files, producing consistent metadata rows.",
      "example": "stat -c '%n %s bytes' one.txt two.txt",
      "example_output": "one.txt 3 bytes\ntwo.txt 5 bytes",
      "breakdown": [
        "stat is the featured command in the worked pattern.",
        "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. stat can apply one custom format to several files, producing consistent metadata rows.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print path:size rows for config/api.conf, config/db.conf, and config/web.conf in that order. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "stat -c '%n:%s' config/api.conf config/db.conf config/web.conf && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: stat -c <values>",
        "Use man stat or stat --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "config/api.conf": "api\n",
        "config/db.conf": "database\n",
        "config/web.conf": "web\n",
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Audit a set of deployment files”.",
          "expected": "config/api.conf:4\nconfig/db.conf:9\nconfig/web.conf:4",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "stat can apply one custom format to several files, producing consistent metadata rows. You can now use this behavior as part of a larger command-line workflow.",
      "order": 100,
      "command_order": 20,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "stat reports filesystem metadata. GNU stat -c accepts a format string so scripts can request only the needed field. stat can apply one custom format to several files, producing consistent metadata rows.",
      "stage_kind": "transfer",
      "mode": "capstone",
      "concepts": [
        "stat",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "cp"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cat",
      "order": 101,
      "section": "03-reading-content",
      "command": "cat",
      "title": "Print a short text file",
      "about": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source.",
      "example": "cat motto.txt",
      "example_output": "inspect before changing\n",
      "breakdown": [
        "motto.txt is opened for reading.",
        "Its content is sent to stdout."
      ],
      "task": "Print all lines from briefing.txt.",
      "solution": "cat briefing.txt",
      "hints": [
        "Pass the filename to cat.",
        "Use the checker as a specification: The complete briefing was printed.",
        "Try this command shape, replacing placeholders with fixture values: cat <value>"
      ],
      "files": {
        "briefing.txt": "observe\nverify\nrecord\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The complete briefing was printed.",
          "expected": "observe\nverify\nrecord",
          "normalize": "trim"
        }
      ],
      "completion": "Use less for long output and avoid sending unknown binary data directly to a terminal.",
      "command_order": 21,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "cat"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cat-2",
      "section": "03-reading-content",
      "command": "cat",
      "title": "Combine files in order",
      "about": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat accepts several files and streams their contents in the order given.",
      "example": "cat header.txt body.txt",
      "example_output": "HEADER\nbody",
      "breakdown": [
        "cat is the featured command in the worked pattern.",
        "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat accepts several files and streams their contents in the order given.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print header.txt, body.txt, and footer.txt as one continuous document.",
      "solution": "cat header.txt body.txt footer.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cat <values>",
        "Use man cat or cat --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "header.txt": "TITLE\n",
        "body.txt": "alpha\nbeta\n",
        "footer.txt": "END\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Combine files in order”.",
          "expected": "TITLE\nalpha\nbeta\nEND",
          "normalize": "trim"
        }
      ],
      "completion": "cat accepts several files and streams their contents in the order given. You can now use this behavior as part of a larger command-line workflow.",
      "order": 102,
      "command_order": 21,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat accepts several files and streams their contents in the order given.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "cat"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cat-3",
      "section": "03-reading-content",
      "command": "cat",
      "title": "Number every line",
      "about": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -n prefixes all output lines, including blank ones, with line numbers.",
      "example": "cat -n notes.txt",
      "example_output": "     1\talpha\n     2\t\n     3\tbeta",
      "breakdown": [
        "cat is the featured command in the worked pattern.",
        "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -n prefixes all output lines, including blank ones, with line numbers.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print plan.txt with every line numbered.",
      "solution": "cat -n plan.txt",
      "hints": [
        "Use the checker as a specification: The first line was numbered. The third line was numbered.",
        "Try this command shape, replacing placeholders with fixture values: cat -n <value>",
        "Use man cat or cat --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "plan.txt": "prepare\n\ndeploy\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The first line was numbered.",
          "expected": "1\tprepare"
        },
        {
          "type": "stdout-contains",
          "description": "The third line was numbered.",
          "expected": "3\tdeploy"
        }
      ],
      "completion": "cat -n prefixes all output lines, including blank ones, with line numbers. You can now use this behavior as part of a larger command-line workflow.",
      "order": 103,
      "command_order": 21,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -n prefixes all output lines, including blank ones, with line numbers.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "cat"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-cat-4",
      "section": "03-reading-content",
      "command": "cat",
      "title": "Number only nonblank lines",
      "about": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -b numbers nonblank lines and leaves blank separators unnumbered.",
      "example": "cat -b notes.txt",
      "example_output": "     1\talpha\n\n     2\tbeta",
      "breakdown": [
        "cat is the featured command in the worked pattern.",
        "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -b numbers nonblank lines and leaves blank separators unnumbered.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print checklist.txt with only nonblank lines numbered.",
      "solution": "cat -b checklist.txt",
      "hints": [
        "Use the checker as a specification: The third item received number 3.",
        "Try this command shape, replacing placeholders with fixture values: cat -b <value>",
        "Use man cat or cat --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "checklist.txt": "build\n\ntest\n\ndeploy\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The third item received number 3.",
          "expected": "3\tdeploy"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 3 nonblank output lines were printed.",
          "expected": 3
        }
      ],
      "completion": "cat -b numbers nonblank lines and leaves blank separators unnumbered. You can now use this behavior as part of a larger command-line workflow.",
      "order": 104,
      "command_order": 21,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -b numbers nonblank lines and leaves blank separators unnumbered.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "cat"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-cat-5",
      "section": "03-reading-content",
      "command": "cat",
      "title": "Collapse repeated blank lines",
      "about": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -s squeezes each run of blank lines into a single blank separator.",
      "example": "cat -s draft.txt",
      "example_output": "one\n\ntwo",
      "breakdown": [
        "cat is the featured command in the worked pattern.",
        "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -s squeezes each run of blank lines into a single blank separator.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print draft.txt with repeated blank lines collapsed.",
      "solution": "cat -s draft.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cat -s <value>",
        "Use man cat or cat --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "draft.txt": "intro\n\n\nsection\n\n\n\nend\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Collapse repeated blank lines”.",
          "expected": "intro\n\nsection\n\nend",
          "normalize": "trim"
        }
      ],
      "completion": "cat -s squeezes each run of blank lines into a single blank separator. You can now use this behavior as part of a larger command-line workflow.",
      "order": 105,
      "command_order": 21,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "cat writes file bytes to standard output. It is convenient for small known text files and as a pipeline source. cat -s squeezes each run of blank lines into a single blank separator.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "cat"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-less",
      "order": 106,
      "section": "03-reading-content",
      "command": "less",
      "title": "Page through long text",
      "about": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive.",
      "example": "less -F -X note.txt",
      "example_output": "short note\n",
      "breakdown": [
        "-F quits when one screen is enough.",
        "-X leaves the displayed text visible."
      ],
      "task": "Display note.txt through less and let it exit because the file is short.",
      "solution": "less -F -X note.txt",
      "hints": [
        "Combine -F and -X for this short noninteractive rep.",
        "Use the checker as a specification: The pager emitted the note content. less exited successfully.",
        "Try this command shape, replacing placeholders with fixture values: less -F -X <value>"
      ],
      "files": {
        "note.txt": "search with /pattern\nnext match with n\nquit with q\n"
      },
      "packages": [
        "less"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The pager emitted the note content.",
          "expected": "quit with q"
        },
        {
          "type": "exit-code",
          "description": "less exited successfully.",
          "expected": 0
        }
      ],
      "completion": "In a real interactive page, use /pattern to search, n for the next match, and q to quit.",
      "command_order": 22,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "less"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-less-2",
      "section": "03-reading-content",
      "command": "less",
      "title": "Show line numbers while paging",
      "about": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. less -N displays line numbers, which makes discussion and troubleshooting easier in long files.",
      "example": "less -N runbook.txt",
      "example_output": "      1 step one\n      2 step two",
      "breakdown": [
        "less is the featured command in the worked pattern.",
        "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. less -N displays line numbers, which makes discussion and troubleshooting easier in long files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use less -N to display runbook.txt with line numbers.",
      "solution": "less -N runbook.txt",
      "hints": [
        "Use the checker as a specification: The runbook content was displayed.",
        "Try this command shape, replacing placeholders with fixture values: less -N <value>",
        "Use man less or less --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "runbook.txt": "prepare\nvalidate\ndeploy\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The runbook content was displayed.",
          "expected": "validate"
        }
      ],
      "completion": "less -N displays line numbers, which makes discussion and troubleshooting easier in long files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 107,
      "command_order": 22,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. less -N displays line numbers, which makes discussion and troubleshooting easier in long files.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "less"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-less-3",
      "section": "03-reading-content",
      "command": "less",
      "title": "Open at a search match",
      "about": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. The +/pattern startup command opens a document at the first matching line in an interactive pager.",
      "example": "less +/ERROR app.log",
      "example_output": "... ERROR connection failed ...",
      "breakdown": [
        "less is the featured command in the worked pattern.",
        "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. The +/pattern startup command opens a document at the first matching line in an interactive pager.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Open app.log with less positioned at the first ERROR match.",
      "solution": "less +/ERROR app.log",
      "hints": [
        "Use the checker as a specification: The target error is present in the pager output.",
        "Try this command shape, replacing placeholders with fixture values: less <values>",
        "Use man less or less --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.log": "INFO start\nDEBUG ready\nERROR connection failed\nINFO retry\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The target error is present in the pager output.",
          "expected": "ERROR connection failed"
        }
      ],
      "completion": "The +/pattern startup command opens a document at the first matching line in an interactive pager. You can now use this behavior as part of a larger command-line workflow.",
      "order": 108,
      "command_order": 22,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. The +/pattern startup command opens a document at the first matching line in an interactive pager.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "less"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-less-4",
      "section": "03-reading-content",
      "command": "less",
      "title": "Preserve the terminal after exit",
      "about": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. The -X option prevents less from clearing its displayed content when the pager exits.",
      "example": "less -X report.txt",
      "example_output": "report contents",
      "breakdown": [
        "less is the featured command in the worked pattern.",
        "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. The -X option prevents less from clearing its displayed content when the pager exits.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Display report.txt with less -X so its contents remain visible after exit.",
      "solution": "less -X report.txt",
      "hints": [
        "Use the checker as a specification: The report contents were shown.",
        "Try this command shape, replacing placeholders with fixture values: less -X <value>",
        "Use man less or less --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "report.txt": "status: ready\nowner: ops\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The report contents were shown.",
          "expected": "status: ready"
        }
      ],
      "completion": "The -X option prevents less from clearing its displayed content when the pager exits. You can now use this behavior as part of a larger command-line workflow.",
      "order": 109,
      "command_order": 22,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. The -X option prevents less from clearing its displayed content when the pager exits.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "less"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-less-5",
      "section": "03-reading-content",
      "command": "less",
      "title": "Page a pipeline without wrapping",
      "about": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. less -S keeps long pipeline rows on one visual line so horizontal scrolling remains possible.",
      "example": "grep ERROR app.log | less -S",
      "example_output": "ERROR a long diagnostic row...",
      "breakdown": [
        "less is the featured command in the worked pattern.",
        "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. less -S keeps long pipeline rows on one visual line so horizontal scrolling remains possible.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Filter ERROR lines from app.log and send them to less -S.",
      "solution": "grep ERROR app.log | less -S",
      "hints": [
        "Use the checker as a specification: Only error rows reached the pager.",
        "Try this command shape, replacing placeholders with fixture values: grep <values> | less -S",
        "Use man less or less --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.log": "INFO start\nERROR api timeout request=1234567890\nWARN retry\nERROR db unavailable request=9876543210\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only error rows reached the pager.",
          "expected": "ERROR api timeout request=1234567890\nERROR db unavailable request=9876543210",
          "normalize": "trim"
        }
      ],
      "completion": "less -S keeps long pipeline rows on one visual line so horizontal scrolling remains possible. You can now use this behavior as part of a larger command-line workflow.",
      "order": 110,
      "command_order": 22,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "less is an interactive pager with search and navigation. -F exits when the content fits and -X avoids clearing the terminal, making this rep noninteractive. less -S keeps long pipeline rows on one visual line so horizontal scrolling remains possible.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "less",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-head",
      "order": 111,
      "section": "03-reading-content",
      "command": "head",
      "title": "Read the beginning",
      "about": "head prints the beginning of a file or stream. -n sets an exact line count.",
      "example": "head -n 2 queue.txt",
      "example_output": "first\nsecond\n",
      "breakdown": [
        "-n 2 selects two lines.",
        "The source remains unchanged."
      ],
      "task": "Print only the first three lines of events.txt.",
      "solution": "head -n 3 events.txt",
      "hints": [
        "Use -n with a value of 3.",
        "Use the checker as a specification: The first three events were printed.",
        "Try this command shape, replacing placeholders with fixture values: head -n <values>"
      ],
      "files": {
        "events.txt": "login\nquery\nexport\nlogout\narchive\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The first three events were printed.",
          "expected": "login\nquery\nexport",
          "normalize": "trim"
        }
      ],
      "completion": "head is ideal for quickly learning a file's header and record shape.",
      "command_order": 23,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "head prints the beginning of a file or stream. -n sets an exact line count.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "head"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-head-2",
      "section": "03-reading-content",
      "command": "head",
      "title": "Choose a line count",
      "about": "head prints the beginning of a file or stream. -n sets an exact line count. head -n sets the exact number of leading lines to print.",
      "example": "head -n 3 events.log",
      "example_output": "one\ntwo\nthree",
      "breakdown": [
        "head is the featured command in the worked pattern.",
        "head prints the beginning of a file or stream. -n sets an exact line count. head -n sets the exact number of leading lines to print.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the first four lines of queue.txt.",
      "solution": "head -n 4 queue.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: head -n <values>",
        "Use man head or head --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "queue.txt": "alpha\nbeta\ngamma\ndelta\nepsilon\nzeta\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Choose a line count”.",
          "expected": "alpha\nbeta\ngamma\ndelta",
          "normalize": "trim"
        }
      ],
      "completion": "head -n sets the exact number of leading lines to print. You can now use this behavior as part of a larger command-line workflow.",
      "order": 112,
      "command_order": 23,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "head prints the beginning of a file or stream. -n sets an exact line count. head -n sets the exact number of leading lines to print.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "head"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-head-3",
      "section": "03-reading-content",
      "command": "head",
      "title": "Read a byte prefix",
      "about": "head prints the beginning of a file or stream. -n sets an exact line count. head -c selects an exact byte prefix, useful when inspecting file signatures or fixed headers.",
      "example": "head -c 4 artifact.bin",
      "example_output": "MAGC",
      "breakdown": [
        "head is the featured command in the worked pattern.",
        "head prints the beginning of a file or stream. -n sets an exact line count. head -c selects an exact byte prefix, useful when inspecting file signatures or fixed headers.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the first eight bytes of packet.dat.",
      "solution": "head -c 8 packet.dat",
      "hints": [
        "Use the checker as a specification: The eight-byte header was printed.",
        "Try this command shape, replacing placeholders with fixture values: head -c <values>",
        "Use man head or head --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "packet.dat": "HEADER01payload-data"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The eight-byte header was printed.",
          "expected": "HEADER01",
          "normalize": "exact"
        }
      ],
      "completion": "head -c selects an exact byte prefix, useful when inspecting file signatures or fixed headers. You can now use this behavior as part of a larger command-line workflow.",
      "order": 113,
      "command_order": 23,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "head prints the beginning of a file or stream. -n sets an exact line count. head -c selects an exact byte prefix, useful when inspecting file signatures or fixed headers.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "head"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-head-4",
      "section": "03-reading-content",
      "command": "head",
      "title": "Exclude trailing lines",
      "about": "head prints the beginning of a file or stream. -n sets an exact line count. A negative -n count prints everything except the final number of lines.",
      "example": "head -n -2 list.txt",
      "example_output": "one\ntwo",
      "breakdown": [
        "head is the featured command in the worked pattern.",
        "head prints the beginning of a file or stream. -n sets an exact line count. A negative -n count prints everything except the final number of lines.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print tasks.txt except for its final two archived tasks.",
      "solution": "head -n -2 tasks.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: head -n -2 <value>",
        "Use man head or head --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "tasks.txt": "build\ntest\ndeploy\nold-one\nold-two\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Exclude trailing lines”.",
          "expected": "build\ntest\ndeploy",
          "normalize": "trim"
        }
      ],
      "completion": "A negative -n count prints everything except the final number of lines. You can now use this behavior as part of a larger command-line workflow.",
      "order": 114,
      "command_order": 23,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "head prints the beginning of a file or stream. -n sets an exact line count. A negative -n count prints everything except the final number of lines.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "head"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-head-5",
      "section": "03-reading-content",
      "command": "head",
      "title": "Preview several files with labels",
      "about": "head prints the beginning of a file or stream. -n sets an exact line count. head -v prints headers even for a single file and naturally labels previews from several files.",
      "example": "head -n 1 -v api.log web.log",
      "example_output": "==> api.log <==\napi start\n\n==> web.log <==\nweb start",
      "breakdown": [
        "head is the featured command in the worked pattern.",
        "head prints the beginning of a file or stream. -n sets an exact line count. head -v prints headers even for a single file and naturally labels previews from several files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the first line of api.log and db.log with filename headers.",
      "solution": "head -n 1 -v api.log db.log",
      "hints": [
        "Use the checker as a specification: The api preview was labeled. The database preview was labeled.",
        "Try this command shape, replacing placeholders with fixture values: head -n <value> -v <values>",
        "Use man head or head --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "api.log": "api ready\napi next\n",
        "db.log": "db ready\ndb next\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The api preview was labeled.",
          "expected": "==> api.log <=="
        },
        {
          "type": "stdout-contains",
          "description": "The database preview was labeled.",
          "expected": "==> db.log <=="
        }
      ],
      "completion": "head -v prints headers even for a single file and naturally labels previews from several files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 115,
      "command_order": 23,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "head prints the beginning of a file or stream. -n sets an exact line count. head -v prints headers even for a single file and naturally labels previews from several files.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "head"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-tail",
      "order": 116,
      "section": "03-reading-content",
      "command": "tail",
      "title": "Read the end",
      "about": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively.",
      "example": "tail -n 2 queue.txt",
      "example_output": "fourth\nfifth\n",
      "breakdown": [
        "-n 2 selects the final two lines.",
        "No file content is changed."
      ],
      "task": "Print only the final two lines of events.txt.",
      "solution": "tail -n 2 events.txt",
      "hints": [
        "Use tail with -n 2.",
        "Use the checker as a specification: The final two events were printed.",
        "Try this command shape, replacing placeholders with fixture values: tail -n <values>"
      ],
      "files": {
        "events.txt": "login\nquery\nexport\nlogout\narchive\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The final two events were printed.",
          "expected": "logout\narchive",
          "normalize": "trim"
        }
      ],
      "completion": "tail -f is useful for live logs, but always stop the follower cleanly with Ctrl-C.",
      "command_order": 24,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "tail"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tail-2",
      "section": "03-reading-content",
      "command": "tail",
      "title": "Choose a trailing line count",
      "about": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -n prints an exact number of lines from the end of a file.",
      "example": "tail -n 3 events.log",
      "example_output": "eight\nnine\nten",
      "breakdown": [
        "tail is the featured command in the worked pattern.",
        "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -n prints an exact number of lines from the end of a file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the final four entries in history.log.",
      "solution": "tail -n 4 history.log",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tail -n <values>",
        "Use man tail or tail --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "history.log": "one\ntwo\nthree\nfour\nfive\nsix\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Choose a trailing line count”.",
          "expected": "three\nfour\nfive\nsix",
          "normalize": "trim"
        }
      ],
      "completion": "tail -n prints an exact number of lines from the end of a file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 117,
      "command_order": 24,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -n prints an exact number of lines from the end of a file.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "tail"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tail-3",
      "section": "03-reading-content",
      "command": "tail",
      "title": "Start at a specific line",
      "about": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -n +N prints from line N through the end instead of counting backward.",
      "example": "tail -n +3 table.txt",
      "example_output": "row3\nrow4",
      "breakdown": [
        "tail is the featured command in the worked pattern.",
        "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -n +N prints from line N through the end instead of counting backward.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Skip the two header lines in export.txt and print every data row.",
      "solution": "tail -n +3 export.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tail -n <values>",
        "Use man tail or tail --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "export.txt": "Generated report\nname,status\napi,up\ndb,down\nweb,up\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Start at a specific line”.",
          "expected": "api,up\ndb,down\nweb,up",
          "normalize": "trim"
        }
      ],
      "completion": "tail -n +N prints from line N through the end instead of counting backward. You can now use this behavior as part of a larger command-line workflow.",
      "order": 118,
      "command_order": 24,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -n +N prints from line N through the end instead of counting backward.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "tail"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tail-4",
      "section": "03-reading-content",
      "command": "tail",
      "title": "Read a byte suffix",
      "about": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -c selects a fixed number of bytes from the end of a file.",
      "example": "tail -c 4 digest.txt",
      "example_output": "cdef",
      "breakdown": [
        "tail is the featured command in the worked pattern.",
        "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -c selects a fixed number of bytes from the end of a file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the final six bytes of token.txt.",
      "solution": "tail -c 6 token.txt",
      "hints": [
        "Use the checker as a specification: The six-byte suffix was printed.",
        "Try this command shape, replacing placeholders with fixture values: tail -c <values>",
        "Use man tail or tail --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "token.txt": "prefix-ABC123"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The six-byte suffix was printed.",
          "expected": "ABC123",
          "normalize": "exact"
        }
      ],
      "completion": "tail -c selects a fixed number of bytes from the end of a file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 119,
      "command_order": 24,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail -c selects a fixed number of bytes from the end of a file.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "tail"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tail-5",
      "section": "03-reading-content",
      "command": "tail",
      "title": "Extract a recent log window",
      "about": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail composes with grep to search only a recent, bounded portion of a large log.",
      "example": "tail -n 100 app.log | grep ERROR",
      "example_output": "ERROR timeout",
      "breakdown": [
        "tail is the featured command in the worked pattern.",
        "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail composes with grep to search only a recent, bounded portion of a large log.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Search only the final five lines of app.log and print ERROR rows.",
      "solution": "tail -n 5 app.log | grep ERROR",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tail -n <values> | grep <value>",
        "Use man tail or tail --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.log": "ERROR old\nINFO 1\nINFO 2\nINFO 3\nERROR recent-one\nWARN 4\nERROR recent-two\nINFO 5\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Extract a recent log window”.",
          "expected": "ERROR recent-one\nERROR recent-two",
          "normalize": "trim"
        }
      ],
      "completion": "tail composes with grep to search only a recent, bounded portion of a large log. You can now use this behavior as part of a larger command-line workflow.",
      "order": 120,
      "command_order": 24,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "tail prints the end of a file or stream. -n chooses the count; -f follows a growing file interactively. tail composes with grep to search only a recent, bounded portion of a large log.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "tail",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nl",
      "order": 121,
      "section": "03-reading-content",
      "command": "nl",
      "title": "Number text lines",
      "about": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab.",
      "example": "nl -ba note.txt",
      "example_output": "     1\talpha\n     2\t\n     3\tbeta\n",
      "breakdown": [
        "-b a numbers all lines.",
        "The original file is unchanged."
      ],
      "task": "Number every line in note.txt, including the blank line.",
      "solution": "nl -ba note.txt",
      "hints": [
        "Use body-numbering style a to number all lines.",
        "Use the checker as a specification: The blank line received line number 2. The third line contains beta.",
        "Try this command shape, replacing placeholders with fixture values: nl -ba <value>"
      ],
      "files": {
        "note.txt": "alpha\n\nbeta\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The blank line received line number 2.",
          "expected": "2"
        },
        {
          "type": "stdout-contains",
          "description": "The third line contains beta.",
          "expected": "beta"
        }
      ],
      "completion": "nl has its own rules for logical pages and blank lines. Use cat -n when its simpler numbering behavior is what you need.",
      "command_order": 25,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "nl"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-nl-2",
      "section": "03-reading-content",
      "command": "nl",
      "title": "Number all lines",
      "about": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. nl -ba numbers every line, including blank separators.",
      "example": "nl -ba notes.txt",
      "example_output": "     1\tone\n     2\t\n     3\ttwo",
      "breakdown": [
        "nl is the featured command in the worked pattern.",
        "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. nl -ba numbers every line, including blank separators.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Number every line in plan.txt, including its blank line.",
      "solution": "nl -ba plan.txt",
      "hints": [
        "Use the checker as a specification: The blank line consumed number 2.",
        "Try this command shape, replacing placeholders with fixture values: nl -ba <value>",
        "Use man nl or nl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "plan.txt": "build\n\ntest\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The blank line consumed number 2.",
          "expected": "3\ttest"
        }
      ],
      "completion": "nl -ba numbers every line, including blank separators. You can now use this behavior as part of a larger command-line workflow.",
      "order": 122,
      "command_order": 25,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. nl -ba numbers every line, including blank separators.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "nl"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-nl-3",
      "section": "03-reading-content",
      "command": "nl",
      "title": "Choose number width and separator",
      "about": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. The -w and -s options make line numbers compact and machine-friendly.",
      "example": "nl -ba -w2 -s': ' notes.txt",
      "example_output": " 1: one",
      "breakdown": [
        "nl is the featured command in the worked pattern.",
        "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. The -w and -s options make line numbers compact and machine-friendly.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Number tasks.txt with width 2 and the separator \" | \".",
      "solution": "nl -ba -w2 -s' | ' tasks.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: nl -ba -w2 <value> | <values>",
        "Use man nl or nl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "tasks.txt": "build\ntest\ndeploy\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Choose number width and separator”.",
          "expected": " 1 | build\n 2 | test\n 3 | deploy",
          "normalize": "trim"
        }
      ],
      "completion": "The -w and -s options make line numbers compact and machine-friendly. You can now use this behavior as part of a larger command-line workflow.",
      "order": 123,
      "command_order": 25,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. The -w and -s options make line numbers compact and machine-friendly.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "nl",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nl-4",
      "section": "03-reading-content",
      "command": "nl",
      "title": "Start numbering at a chosen value",
      "about": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. The -v option sets the starting number for a numbered stream.",
      "example": "nl -v 101 records.txt",
      "example_output": "   101\tfirst",
      "breakdown": [
        "nl is the featured command in the worked pattern.",
        "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. The -v option sets the starting number for a numbered stream.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Number records.txt starting at 501.",
      "solution": "nl -v 501 records.txt",
      "hints": [
        "Use the checker as a specification: The sequence began at 501. The sequence advanced to 503.",
        "Try this command shape, replacing placeholders with fixture values: nl -v <values>",
        "Use man nl or nl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "records.txt": "alpha\nbeta\ngamma\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The sequence began at 501.",
          "expected": "501\talpha"
        },
        {
          "type": "stdout-contains",
          "description": "The sequence advanced to 503.",
          "expected": "503\tgamma"
        }
      ],
      "completion": "The -v option sets the starting number for a numbered stream. You can now use this behavior as part of a larger command-line workflow.",
      "order": 124,
      "command_order": 25,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. The -v option sets the starting number for a numbered stream.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "nl"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-nl-5",
      "section": "03-reading-content",
      "command": "nl",
      "title": "Number filtered records",
      "about": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. A pipeline can filter a dataset first and then assign stable display numbers with nl.",
      "example": "grep '^ERROR' app.log | nl -w2 -s'. '",
      "example_output": " 1. ERROR timeout",
      "breakdown": [
        "nl is the featured command in the worked pattern.",
        "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. A pipeline can filter a dataset first and then assign stable display numbers with nl.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Filter failed rows from jobs.csv and number them with width 2 and \") \" as the separator.",
      "solution": "grep ',failed$' jobs.csv | nl -w2 -s') '",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: grep <values> | nl -w2 <values>",
        "Use man nl or nl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "jobs.csv": "api,ok\ndb,failed\nweb,ok\nbackup,failed\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Number filtered records”.",
          "expected": " 1) db,failed\n 2) backup,failed",
          "normalize": "trim"
        }
      ],
      "completion": "A pipeline can filter a dataset first and then assign stable display numbers with nl. You can now use this behavior as part of a larger command-line workflow.",
      "order": 125,
      "command_order": 25,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "nl numbers lines with configurable treatment of blank lines. Its default output includes padded line numbers and a tab. A pipeline can filter a dataset first and then assign stable display numbers with nl.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "nl",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tac",
      "order": 126,
      "section": "03-reading-content",
      "command": "tac",
      "title": "Reverse records by line",
      "about": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters.",
      "example": "tac steps.txt",
      "example_output": "third\nsecond\nfirst\n",
      "breakdown": [
        "The final input line is printed first.",
        "Characters inside each line keep their order."
      ],
      "task": "Print priorities.txt from its final line to its first.",
      "solution": "tac priorities.txt",
      "hints": [
        "Pass the filename directly to tac.",
        "Use the checker as a specification: The line order was reversed.",
        "Try this command shape, replacing placeholders with fixture values: tac <value>"
      ],
      "files": {
        "priorities.txt": "low\nmedium\nhigh\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The line order was reversed.",
          "expected": "high\nmedium\nlow",
          "normalize": "trim"
        }
      ],
      "completion": "tac is a GNU/Linux utility; it reverses records, not bytes or words.",
      "command_order": 26,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "tac"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tac-2",
      "section": "03-reading-content",
      "command": "tac",
      "title": "Reverse several files as one stream",
      "about": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac processes each supplied file and reverses the records within each file.",
      "example": "tac first.txt second.txt",
      "example_output": "two\none\nfour\nthree",
      "breakdown": [
        "tac is the featured command in the worked pattern.",
        "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac processes each supplied file and reverses the records within each file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Reverse a.txt followed by b.txt.",
      "solution": "tac a.txt b.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tac <values>",
        "Use man tac or tac --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "a.txt": "a1\na2\n",
        "b.txt": "b1\nb2\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Reverse several files as one stream”.",
          "expected": "a2\na1\nb2\nb1",
          "normalize": "trim"
        }
      ],
      "completion": "tac processes each supplied file and reverses the records within each file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 127,
      "command_order": 26,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac processes each supplied file and reverses the records within each file.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "tac"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tac-3",
      "section": "03-reading-content",
      "command": "tac",
      "title": "Use a custom record separator",
      "about": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac -s treats a chosen string as the record separator instead of newline.",
      "example": "tac -s, values.txt",
      "example_output": "three,two,one,",
      "breakdown": [
        "tac is the featured command in the worked pattern.",
        "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac -s treats a chosen string as the record separator instead of newline.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Reverse the pipe-separated records in values.txt.",
      "solution": "tac -s'|' values.txt",
      "hints": [
        "Use the checker as a specification: The pipe-delimited records were reversed.",
        "Try this command shape, replacing placeholders with fixture values: tac <value> | <values>",
        "Use man tac or tac --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "values.txt": "alpha|beta|gamma|"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The pipe-delimited records were reversed.",
          "expected": "gamma|beta|alpha|",
          "normalize": "exact"
        }
      ],
      "completion": "tac -s treats a chosen string as the record separator instead of newline. You can now use this behavior as part of a larger command-line workflow.",
      "order": 128,
      "command_order": 26,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac -s treats a chosen string as the record separator instead of newline.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "tac",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tac-4",
      "section": "03-reading-content",
      "command": "tac",
      "title": "Attach separators before records",
      "about": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. The -b option attaches a separator before each record, useful for formats where delimiters lead fields.",
      "example": "tac -b -s: values.txt",
      "example_output": ":three:two:one",
      "breakdown": [
        "tac is the featured command in the worked pattern.",
        "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. The -b option attaches a separator before each record, useful for formats where delimiters lead fields.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Reverse the colon-led records in values.txt while keeping each colon before its record.",
      "solution": "tac -b -s: values.txt",
      "hints": [
        "Use the checker as a specification: Leading separators were preserved.",
        "Try this command shape, replacing placeholders with fixture values: tac -b -s: <value>",
        "Use man tac or tac --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "values.txt": ":one:two:three"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Leading separators were preserved.",
          "expected": ":three:two:one",
          "normalize": "exact"
        }
      ],
      "completion": "The -b option attaches a separator before each record, useful for formats where delimiters lead fields. You can now use this behavior as part of a larger command-line workflow.",
      "order": 129,
      "command_order": 26,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. The -b option attaches a separator before each record, useful for formats where delimiters lead fields.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "tac"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tac-5",
      "section": "03-reading-content",
      "command": "tac",
      "title": "Inspect newest records first",
      "about": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac can reverse an append-only log before head selects only the newest few records.",
      "example": "tac events.log | head -n 3",
      "example_output": "newest\nrecent\nolder",
      "breakdown": [
        "tac is the featured command in the worked pattern.",
        "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac can reverse an append-only log before head selects only the newest few records.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the three newest audit records from an oldest-first audit.log.",
      "solution": "tac audit.log | head -n 3",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tac <value> | head -n <value>",
        "Use man tac or tac --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "audit.log": "09:00 login\n09:10 read\n09:20 write\n09:30 logout\n09:40 login\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Inspect newest records first”.",
          "expected": "09:40 login\n09:30 logout\n09:20 write",
          "normalize": "trim"
        }
      ],
      "completion": "tac can reverse an append-only log before head selects only the newest few records. You can now use this behavior as part of a larger command-line workflow.",
      "order": 130,
      "command_order": 26,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "tac prints file lines from last to first. Its name is cat reversed, but it reverses record order rather than characters. tac can reverse an append-only log before head selects only the newest few records.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "tac",
        "pipelines"
      ],
      "prerequisites": [
        "head"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-strings",
      "order": 131,
      "section": "03-reading-content",
      "command": "strings",
      "title": "Extract printable sequences",
      "about": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser.",
      "example": "strings blob.bin",
      "example_output": "HEADER\nmode=lab\n",
      "breakdown": [
        "Nonprintable bytes are skipped.",
        "Printable runs meeting the minimum length are emitted."
      ],
      "task": "Extract printable strings from blob.bin.",
      "solution": "strings blob.bin",
      "hints": [
        "No option is required for the supplied blob.",
        "Use the checker as a specification: HEADER was recovered. The synthetic secret string was recovered.",
        "Try this command shape, replacing placeholders with fixture values: strings <value>"
      ],
      "files": {
        "blob.bin": "\u0000\u0001HEADER\u0000\u0002secret=synthetic\u0000"
      },
      "packages": [
        "binutils"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "HEADER was recovered.",
          "expected": "HEADER"
        },
        {
          "type": "stdout-contains",
          "description": "The synthetic secret string was recovered.",
          "expected": "secret=synthetic"
        }
      ],
      "completion": "strings reveals printable runs, not necessarily meaningful fields. Validate findings with format-aware tools.",
      "command_order": 27,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "strings"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-strings-2",
      "section": "03-reading-content",
      "command": "strings",
      "title": "Require a minimum string length",
      "about": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -n suppresses short runs and prints only text at least the chosen length.",
      "example": "strings -n 8 artifact.bin",
      "example_output": "LONGVALUE",
      "breakdown": [
        "strings is the featured command in the worked pattern.",
        "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -n suppresses short runs and prints only text at least the chosen length.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only strings at least eight characters long from artifact.bin.",
      "solution": "strings -n 8 artifact.bin",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: strings -n <values>",
        "Use man strings or strings --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact.bin": "\u0000short\u0000ACCESS_TOKEN\u0000tiny\u0000CONFIG_PATH\u0000"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Require a minimum string length”.",
          "expected": "ACCESS_TOKEN\nCONFIG_PATH",
          "normalize": "trim"
        }
      ],
      "completion": "strings -n suppresses short runs and prints only text at least the chosen length. You can now use this behavior as part of a larger command-line workflow.",
      "order": 132,
      "command_order": 27,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -n suppresses short runs and prints only text at least the chosen length.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "strings"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-strings-3",
      "section": "03-reading-content",
      "command": "strings",
      "title": "Show decimal offsets",
      "about": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -t d prefixes each discovered string with its decimal byte offset.",
      "example": "strings -t d sample.bin",
      "example_output": "      4 hello",
      "breakdown": [
        "strings is the featured command in the worked pattern.",
        "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -t d prefixes each discovered string with its decimal byte offset.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print strings from sample.bin with decimal offsets.",
      "solution": "strings -t d sample.bin",
      "hints": [
        "Use the checker as a specification: The first embedded string was printed. The second embedded string was printed.",
        "Try this command shape, replacing placeholders with fixture values: strings -t <values>",
        "Use man strings or strings --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sample.bin": "\u0000\u0000\u0000\u0000HELLO_WORLD\u0000SECOND_VALUE\u0000"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The first embedded string was printed.",
          "expected": "HELLO_WORLD"
        },
        {
          "type": "stdout-contains",
          "description": "The second embedded string was printed.",
          "expected": "SECOND_VALUE"
        }
      ],
      "completion": "strings -t d prefixes each discovered string with its decimal byte offset. You can now use this behavior as part of a larger command-line workflow.",
      "order": 133,
      "command_order": 27,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -t d prefixes each discovered string with its decimal byte offset.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "strings"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-strings-4",
      "section": "03-reading-content",
      "command": "strings",
      "title": "Show hexadecimal offsets",
      "about": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -t x uses hexadecimal offsets, matching the notation common in binary analysis.",
      "example": "strings -t x sample.bin",
      "example_output": "      10 embedded",
      "breakdown": [
        "strings is the featured command in the worked pattern.",
        "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -t x uses hexadecimal offsets, matching the notation common in binary analysis.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print embedded strings with hexadecimal offsets.",
      "solution": "strings -t x payload.bin",
      "hints": [
        "Use the checker as a specification: The configuration string was recovered.",
        "Try this command shape, replacing placeholders with fixture values: strings -t <values>",
        "Use man strings or strings --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "payload.bin": "\u0000\u0000\u0000\u0000\u0000\u0000\u0000\u0000CONFIGURATION\u0000RELEASE_2026\u0000"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The configuration string was recovered.",
          "expected": "CONFIGURATION"
        }
      ],
      "completion": "strings -t x uses hexadecimal offsets, matching the notation common in binary analysis. You can now use this behavior as part of a larger command-line workflow.",
      "order": 134,
      "command_order": 27,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. strings -t x uses hexadecimal offsets, matching the notation common in binary analysis.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "strings"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-strings-5",
      "section": "03-reading-content",
      "command": "strings",
      "title": "Search recovered strings",
      "about": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. Piping strings into grep narrows a binary inspection to meaningful markers without dumping every printable run.",
      "example": "strings image.bin | grep '^URL='",
      "example_output": "URL=https://internal",
      "breakdown": [
        "strings is the featured command in the worked pattern.",
        "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. Piping strings into grep narrows a binary inspection to meaningful markers without dumping every printable run.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Recover only KEY=value settings from image.bin.",
      "solution": "strings image.bin | grep -E '^[A-Z_]+='",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: strings <value> | grep -E <value>",
        "Use man strings or strings --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "image.bin": "\u0000noise\u0000API_URL=http://localhost\u0000version text\u0000RETRY_COUNT=3\u0000"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Search recovered strings”.",
          "expected": "API_URL=http://localhost\nRETRY_COUNT=3",
          "normalize": "trim"
        }
      ],
      "completion": "Piping strings into grep narrows a binary inspection to meaningful markers without dumping every printable run. You can now use this behavior as part of a larger command-line workflow.",
      "order": 135,
      "command_order": 27,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "strings scans data for printable character sequences. It is useful for triage, but extracted text lacks the structure and provenance of a parser. Piping strings into grep narrows a binary inspection to meaningful markers without dumping every printable run.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "strings",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-od",
      "order": 136,
      "section": "03-reading-content",
      "command": "od",
      "title": "Inspect exact byte values",
      "about": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values.",
      "example": "od -An -t x1 bytes.txt",
      "example_output": " 41 42 43 0a\n",
      "breakdown": [
        "-An suppresses address labels.",
        "x1 selects hexadecimal one byte at a time."
      ],
      "task": "Print bytes.txt as one-byte hexadecimal values without offsets.",
      "solution": "od -An -t x1 bytes.txt",
      "hints": [
        "Combine -An with -t x1.",
        "Use the checker as a specification: The exact four byte values were printed.",
        "Try this command shape, replacing placeholders with fixture values: od -An -t <values>"
      ],
      "files": {
        "bytes.txt": "ABC\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The exact four byte values were printed.",
          "expected": "41 42 43 0a",
          "normalize": "whitespace"
        }
      ],
      "completion": "Byte views expose line endings, encodings, and hidden delimiters that rendered text can conceal.",
      "command_order": 28,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "od"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-od-2",
      "section": "03-reading-content",
      "command": "od",
      "title": "Display hexadecimal bytes",
      "about": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. od -An -tx1 prints raw bytes as two-digit hexadecimal values without address labels.",
      "example": "printf 'ABC' | od -An -tx1",
      "example_output": " 41 42 43",
      "breakdown": [
        "od is the featured command in the worked pattern.",
        "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. od -An -tx1 prints raw bytes as two-digit hexadecimal values without address labels.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the hexadecimal bytes of magic.bin without offsets.",
      "solution": "od -An -tx1 magic.bin",
      "hints": [
        "Use the checker as a specification: The four magic bytes were printed.",
        "Try this command shape, replacing placeholders with fixture values: od -An -tx1 <value>",
        "Use man od or od --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "magic.bin": "GIF8"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The four magic bytes were printed.",
          "expected": "47 49 46 38",
          "normalize": "whitespace"
        }
      ],
      "completion": "od -An -tx1 prints raw bytes as two-digit hexadecimal values without address labels. You can now use this behavior as part of a larger command-line workflow.",
      "order": 137,
      "command_order": 28,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. od -An -tx1 prints raw bytes as two-digit hexadecimal values without address labels.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "od"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-od-3",
      "section": "03-reading-content",
      "command": "od",
      "title": "Display unsigned decimal bytes",
      "about": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. The -tu1 type renders each byte as an unsigned decimal number.",
      "example": "printf 'AZ' | od -An -tu1",
      "example_output": "65 90",
      "breakdown": [
        "od is the featured command in the worked pattern.",
        "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. The -tu1 type renders each byte as an unsigned decimal number.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the unsigned decimal byte values of letters.txt.",
      "solution": "od -An -tu1 letters.txt",
      "hints": [
        "Use the checker as a specification: The decimal byte values were printed.",
        "Try this command shape, replacing placeholders with fixture values: od -An -tu1 <value>",
        "Use man od or od --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "letters.txt": "ABC"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The decimal byte values were printed.",
          "expected": "65 66 67",
          "normalize": "whitespace"
        }
      ],
      "completion": "The -tu1 type renders each byte as an unsigned decimal number. You can now use this behavior as part of a larger command-line workflow.",
      "order": 138,
      "command_order": 28,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. The -tu1 type renders each byte as an unsigned decimal number.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "od"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-od-4",
      "section": "03-reading-content",
      "command": "od",
      "title": "Skip a binary header",
      "about": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. The -j option skips leading bytes and -N limits how many subsequent bytes are displayed.",
      "example": "od -An -tx1 -j4 -N3 packet.bin",
      "example_output": " 41 42 43",
      "breakdown": [
        "od is the featured command in the worked pattern.",
        "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. The -j option skips leading bytes and -N limits how many subsequent bytes are displayed.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Skip the four-byte HEAD prefix and print the next four bytes as hex.",
      "solution": "od -An -tx1 -j4 -N4 packet.bin",
      "hints": [
        "Use the checker as a specification: Only the DATA bytes were rendered.",
        "Try this command shape, replacing placeholders with fixture values: od -An -tx1 -j4 -N4 <value>",
        "Use man od or od --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "packet.bin": "HEADDATAtrailer"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only the DATA bytes were rendered.",
          "expected": "44 41 54 41",
          "normalize": "whitespace"
        }
      ],
      "completion": "The -j option skips leading bytes and -N limits how many subsequent bytes are displayed. You can now use this behavior as part of a larger command-line workflow.",
      "order": 139,
      "command_order": 28,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. The -j option skips leading bytes and -N limits how many subsequent bytes are displayed.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "od"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-od-5",
      "section": "03-reading-content",
      "command": "od",
      "title": "Verify a file signature",
      "about": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. A small od/head pipeline can turn a binary prefix into a compact signature suitable for a validation check.",
      "example": "od -An -tx1 -N4 image.bin | tr -d ' \\n'",
      "example_output": "89504e47",
      "breakdown": [
        "od is the featured command in the worked pattern.",
        "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. A small od/head pipeline can turn a binary prefix into a compact signature suitable for a validation check.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the first four bytes of artifact.bin as one lowercase hexadecimal signature.",
      "solution": "od -An -tx1 -N4 artifact.bin | tr -d ' \\n'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: od -An -tx1 -N4 <value> | tr -d <values>",
        "Use man od or od --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact.bin": "PK12payload"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Verify a file signature”.",
          "expected": "504b3132",
          "normalize": "trim"
        }
      ],
      "completion": "A small od/head pipeline can turn a binary prefix into a compact signature suitable for a validation check. You can now use this behavior as part of a larger command-line workflow.",
      "order": 140,
      "command_order": 28,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "od displays file bytes in selected numeric forms. -An removes offsets and -t x1 requests one-byte hexadecimal values. A small od/head pipeline can turn a binary prefix into a compact signature suitable for a validation check.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "od",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "tr"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xxd",
      "order": 141,
      "section": "03-reading-content",
      "command": "xxd",
      "title": "Create a plain hexadecimal dump",
      "about": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form.",
      "example": "xxd -p bytes.txt",
      "example_output": "4142430a\n",
      "breakdown": [
        "-p omits addresses and ASCII columns.",
        "Each byte becomes two hex digits."
      ],
      "task": "Print bytes.txt as plain hexadecimal.",
      "solution": "xxd -p bytes.txt",
      "hints": [
        "Use xxd plain mode.",
        "Use the checker as a specification: The plain hex bytes were printed.",
        "Try this command shape, replacing placeholders with fixture values: xxd -p <value>"
      ],
      "files": {
        "bytes.txt": "ABC\n"
      },
      "packages": [
        "xxd"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The plain hex bytes were printed.",
          "expected": "4142430a",
          "normalize": "trim"
        }
      ],
      "completion": "xxd -r reverses a compatible dump. A hex representation is evidence of bytes, not an interpretation of their format.",
      "command_order": 29,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "xxd"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xxd-2",
      "section": "03-reading-content",
      "command": "xxd",
      "title": "Create a compact hex dump",
      "about": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -g1 groups output by individual bytes, making exact byte boundaries easy to see.",
      "example": "printf 'ABC' | xxd -g1",
      "example_output": "00000000: 41 42 43  ABC",
      "breakdown": [
        "xxd is the featured command in the worked pattern.",
        "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -g1 groups output by individual bytes, making exact byte boundaries easy to see.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Hex-dump code.bin with one-byte groups.",
      "solution": "xxd -g1 code.bin",
      "hints": [
        "Use the checker as a specification: The byte sequence was displayed.",
        "Try this command shape, replacing placeholders with fixture values: xxd -g1 <value>",
        "Use man xxd or xxd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "code.bin": "ABCD"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The byte sequence was displayed.",
          "expected": "41 42 43 44"
        }
      ],
      "completion": "xxd -g1 groups output by individual bytes, making exact byte boundaries easy to see. You can now use this behavior as part of a larger command-line workflow.",
      "order": 142,
      "command_order": 29,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -g1 groups output by individual bytes, making exact byte boundaries easy to see.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "xxd"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-xxd-3",
      "section": "03-reading-content",
      "command": "xxd",
      "title": "Print plain hexadecimal data",
      "about": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -p omits addresses and text columns, producing a continuous hexadecimal representation.",
      "example": "printf 'Hi' | xxd -p",
      "example_output": "4869",
      "breakdown": [
        "xxd is the featured command in the worked pattern.",
        "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -p omits addresses and text columns, producing a continuous hexadecimal representation.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print payload.txt as plain hexadecimal.",
      "solution": "xxd -p payload.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: xxd -p <value>",
        "Use man xxd or xxd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "payload.txt": "CLI"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print plain hexadecimal data”.",
          "expected": "434c49",
          "normalize": "trim"
        }
      ],
      "completion": "xxd -p omits addresses and text columns, producing a continuous hexadecimal representation. You can now use this behavior as part of a larger command-line workflow.",
      "order": 143,
      "command_order": 29,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -p omits addresses and text columns, producing a continuous hexadecimal representation.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "xxd"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xxd-4",
      "section": "03-reading-content",
      "command": "xxd",
      "title": "Reverse a plain hex file",
      "about": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -r -p converts plain hexadecimal text back into its original bytes.",
      "example": "printf '4869' | xxd -r -p",
      "example_output": "Hi",
      "breakdown": [
        "xxd is the featured command in the worked pattern.",
        "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -r -p converts plain hexadecimal text back into its original bytes.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Decode encoded.hex and print the recovered text.",
      "solution": "xxd -r -p encoded.hex",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: xxd -r -p <value>",
        "Use man xxd or xxd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "encoded.hex": "4861636b657220434c492047796d0a"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Reverse a plain hex file”.",
          "expected": "Hacker CLI Gym",
          "normalize": "trim"
        }
      ],
      "completion": "xxd -r -p converts plain hexadecimal text back into its original bytes. You can now use this behavior as part of a larger command-line workflow.",
      "order": 144,
      "command_order": 29,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. xxd -r -p converts plain hexadecimal text back into its original bytes.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "xxd"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xxd-5",
      "section": "03-reading-content",
      "command": "xxd",
      "title": "Patch bytes at a known offset",
      "about": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. Plain hex plus dd can update a reviewed byte range while leaving the rest of a file intact.",
      "example": "printf '4f4b' | xxd -r -p | dd of=status.bin bs=1 seek=4 conv=notrunc",
      "example_output": "",
      "breakdown": [
        "xxd is the featured command in the worked pattern.",
        "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. Plain hex plus dd can update a reviewed byte range while leaving the rest of a file intact.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Replace bytes 4-5 of status.bin with OK by decoding 4f4b through xxd, then print the final file.",
      "solution": "printf '4f4b' | xxd -r -p | dd of=status.bin bs=1 seek=4 conv=notrunc status=none && cat status.bin",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: printf <value> | xxd -r -p | dd <values> && cat <value>",
        "Use man xxd or xxd --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "status.bin": "HEADNOEND"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Patch bytes at a known offset”.",
          "expected": "HEADOKEND",
          "normalize": "trim"
        }
      ],
      "completion": "Plain hex plus dd can update a reviewed byte range while leaving the rest of a file intact. You can now use this behavior as part of a larger command-line workflow.",
      "order": 145,
      "command_order": 29,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "xxd converts bytes to hexadecimal and can reverse a compatible dump. -p selects a plain continuous hex form. Plain hex plus dd can update a reviewed byte range while leaving the rest of a file intact.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "xxd",
        "pipelines",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [
        "printf"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-wc",
      "order": 146,
      "section": "03-reading-content",
      "command": "wc",
      "title": "Count lines",
      "about": "wc reports lines, words, and byte or character counts. -l asks specifically for lines.",
      "example": "wc -l events.txt",
      "example_output": "5 events.txt\n",
      "breakdown": [
        "-l chooses line count.",
        "Naming a file includes its name in output."
      ],
      "task": "Count the lines in requests.txt and retain the filename label.",
      "solution": "wc -l requests.txt",
      "hints": [
        "Use the line-count option.",
        "Use the checker as a specification: wc reported five lines for requests.txt.",
        "Try this command shape, replacing placeholders with fixture values: wc -l <value>"
      ],
      "files": {
        "requests.txt": "one\ntwo\nthree\nfour\nfive\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "wc reported five lines for requests.txt.",
          "expected": "5 requests.txt",
          "normalize": "whitespace"
        }
      ],
      "completion": "In a pipeline, wc -l counts records that survive the preceding filters.",
      "command_order": 30,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "wc reports lines, words, and byte or character counts. -l asks specifically for lines.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "wc"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-wc-2",
      "section": "03-reading-content",
      "command": "wc",
      "title": "Count words",
      "about": "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc -w counts whitespace-delimited words in a text stream.",
      "example": "wc -w < note.txt",
      "example_output": "4",
      "breakdown": [
        "wc is the featured command in the worked pattern.",
        "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc -w counts whitespace-delimited words in a text stream.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the number of words in briefing.txt.",
      "solution": "wc -w < briefing.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: wc -w < <value>",
        "Use man wc or wc --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "briefing.txt": "alpha beta\ngamma delta epsilon\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Count words”.",
          "expected": "5",
          "normalize": "trim"
        }
      ],
      "completion": "wc -w counts whitespace-delimited words in a text stream. You can now use this behavior as part of a larger command-line workflow.",
      "order": 147,
      "command_order": 30,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc -w counts whitespace-delimited words in a text stream.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "wc",
        "redirection"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-wc-3",
      "section": "03-reading-content",
      "command": "wc",
      "title": "Count bytes",
      "about": "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc -c reports bytes, which differs from characters for multibyte text.",
      "example": "wc -c < payload.bin",
      "example_output": "12",
      "breakdown": [
        "wc is the featured command in the worked pattern.",
        "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc -c reports bytes, which differs from characters for multibyte text.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the byte count of payload.txt.",
      "solution": "wc -c < payload.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: wc -c < <value>",
        "Use man wc or wc --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "payload.txt": "1234567890"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Count bytes”.",
          "expected": "10",
          "normalize": "trim"
        }
      ],
      "completion": "wc -c reports bytes, which differs from characters for multibyte text. You can now use this behavior as part of a larger command-line workflow.",
      "order": 148,
      "command_order": 30,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc -c reports bytes, which differs from characters for multibyte text.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "wc",
        "redirection"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-wc-4",
      "section": "03-reading-content",
      "command": "wc",
      "title": "Report counts for several files",
      "about": "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc labels per-file counts and adds a total when multiple files are supplied.",
      "example": "wc -l one.txt two.txt",
      "example_output": "2 one.txt\n3 two.txt\n5 total",
      "breakdown": [
        "wc is the featured command in the worked pattern.",
        "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc labels per-file counts and adds a total when multiple files are supplied.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Count lines in api.log and web.log and include the total.",
      "solution": "wc -l api.log web.log",
      "hints": [
        "Use the checker as a specification: The API count was shown. The web count was shown.",
        "Try this command shape, replacing placeholders with fixture values: wc -l <values>",
        "Use man wc or wc --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "api.log": "a\nb\n",
        "web.log": "c\nd\ne\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The API count was shown.",
          "expected": "2 api.log"
        },
        {
          "type": "stdout-contains",
          "description": "The web count was shown.",
          "expected": "3 web.log"
        },
        {
          "type": "stdout-contains",
          "description": "The total was shown.",
          "expected": "5 total"
        }
      ],
      "completion": "wc labels per-file counts and adds a total when multiple files are supplied. You can now use this behavior as part of a larger command-line workflow.",
      "order": 149,
      "command_order": 30,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc labels per-file counts and adds a total when multiple files are supplied.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "wc"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-wc-5",
      "section": "03-reading-content",
      "command": "wc",
      "title": "Count filtered records",
      "about": "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc at the end of a pipeline measures only records selected by earlier stages.",
      "example": "grep ',failed$' jobs.csv | wc -l",
      "example_output": "2",
      "breakdown": [
        "wc is the featured command in the worked pattern.",
        "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc at the end of a pipeline measures only records selected by earlier stages.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the number of ERROR records in app.log. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "grep '^ERROR' app.log | wc -l && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: grep <values> | wc -l",
        "Use man wc or wc --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.log": "INFO start\nERROR timeout\nWARN retry\nERROR unavailable\nERROR aborted\n",
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Count filtered records”.",
          "expected": "3",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "wc at the end of a pipeline measures only records selected by earlier stages. You can now use this behavior as part of a larger command-line workflow.",
      "order": 150,
      "command_order": 30,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "wc reports lines, words, and byte or character counts. -l asks specifically for lines. wc at the end of a pipeline measures only records selected by earlier stages.",
      "stage_kind": "workflow",
      "mode": "capstone",
      "concepts": [
        "wc",
        "pipelines",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "cp"
      ],
      "introduced_inline": [
        "grep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cut",
      "order": 151,
      "section": "04-text-processing",
      "command": "cut",
      "title": "Select delimited fields",
      "about": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields.",
      "example": "cut -d',' -f1 crew.csv",
      "example_output": "neo\ntrinity\nmorpheus\n",
      "breakdown": [
        "-d',' sets comma as delimiter.",
        "-f1 selects the first field."
      ],
      "task": "accounts.txt uses colons. Print only the username in field 2.",
      "solution": "cut -d':' -f2 accounts.txt",
      "hints": [
        "Set colon as delimiter and select field 2.",
        "Use the checker as a specification: Only the usernames were printed.",
        "Try this command shape, replacing placeholders with fixture values: cut <value> -f2 <value>"
      ],
      "files": {
        "accounts.txt": "1001:alice:analyst\n1002:bob:developer\n1003:carol:operator\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only the usernames were printed.",
          "expected": "alice\nbob\ncarol",
          "normalize": "trim"
        }
      ],
      "completion": "cut does not understand quoted CSV. Use a format-aware parser when delimiters can appear inside fields.",
      "command_order": 31,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "cut",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cut-2",
      "section": "04-text-processing",
      "command": "cut",
      "title": "Select character positions",
      "about": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. cut -c selects fixed character positions when records have a dependable layout.",
      "example": "cut -c1-4 codes.txt",
      "example_output": "ABCD\nEFGH",
      "breakdown": [
        "cut is the featured command in the worked pattern.",
        "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. cut -c selects fixed character positions when records have a dependable layout.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the first three characters from every line in codes.txt.",
      "solution": "cut -c1-3 codes.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cut -c1-3 <value>",
        "Use man cut or cut --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "codes.txt": "API-ready\nWEB-ready\nDBA-ready\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Select character positions”.",
          "expected": "API\nWEB\nDBA",
          "normalize": "trim"
        }
      ],
      "completion": "cut -c selects fixed character positions when records have a dependable layout. You can now use this behavior as part of a larger command-line workflow.",
      "order": 152,
      "command_order": 31,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. cut -c selects fixed character positions when records have a dependable layout.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "cut"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cut-3",
      "section": "04-text-processing",
      "command": "cut",
      "title": "Select several delimited fields",
      "about": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. A comma-separated field list selects multiple columns while preserving their input order.",
      "example": "cut -d, -f1,3 inventory.csv",
      "example_output": "router,up\nswitch,down",
      "breakdown": [
        "cut is the featured command in the worked pattern.",
        "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. A comma-separated field list selects multiple columns while preserving their input order.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the name and role fields, columns 1 and 3, from people.csv.",
      "solution": "cut -d, -f1,3 people.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cut <values>",
        "Use man cut or cut --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "people.csv": "alice,42,admin\nbob,35,analyst\ncarol,29,developer\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Select several delimited fields”.",
          "expected": "alice,admin\nbob,analyst\ncarol,developer",
          "normalize": "trim"
        }
      ],
      "completion": "A comma-separated field list selects multiple columns while preserving their input order. You can now use this behavior as part of a larger command-line workflow.",
      "order": 153,
      "command_order": 31,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. A comma-separated field list selects multiple columns while preserving their input order.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "cut"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cut-4",
      "section": "04-text-processing",
      "command": "cut",
      "title": "Complement selected fields",
      "about": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. The --complement option prints every field except those named by -f.",
      "example": "cut -d: -f2 --complement records.txt",
      "example_output": "id:status",
      "breakdown": [
        "cut is the featured command in the worked pattern.",
        "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. The --complement option prints every field except those named by -f.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove the secret second field from accounts.txt and print the remaining fields.",
      "solution": "cut -d: -f2 --complement accounts.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cut -d: -f2 --complement <value>",
        "Use man cut or cut --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "accounts.txt": "alice:token-a:active\nbob:token-b:locked\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Complement selected fields”.",
          "expected": "alice:active\nbob:locked",
          "normalize": "trim"
        }
      ],
      "completion": "The --complement option prints every field except those named by -f. You can now use this behavior as part of a larger command-line workflow.",
      "order": 154,
      "command_order": 31,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. The --complement option prints every field except those named by -f.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "cut"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cut-5",
      "section": "04-text-processing",
      "command": "cut",
      "title": "Extract fields from filtered records",
      "about": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. grep can select relevant records before cut projects only the columns needed downstream.",
      "example": "grep ',failed,' jobs.csv | cut -d, -f1,4",
      "example_output": "backup,nightly",
      "breakdown": [
        "cut is the featured command in the worked pattern.",
        "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. grep can select relevant records before cut projects only the columns needed downstream.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "From services.csv, select enabled rows and print service=port pairs using fields 1 and 3.",
      "solution": "grep ',enabled,' services.csv | cut -d, -f1,3 | sed 's/,/=/'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: grep <values> | cut <values> | sed <value>",
        "Use man cut or cut --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services.csv": "api,enabled,8080\ndb,disabled,5432\nweb,enabled,443\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Extract fields from filtered records”.",
          "expected": "api=8080\nweb=443",
          "normalize": "trim"
        }
      ],
      "completion": "grep can select relevant records before cut projects only the columns needed downstream. You can now use this behavior as part of a larger command-line workflow.",
      "order": 155,
      "command_order": 31,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "cut selects fields when input has a dependable simple delimiter. -d names the delimiter and -f chooses fields. grep can select relevant records before cut projects only the columns needed downstream.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "cut",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "grep",
        "sed"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-paste",
      "order": 156,
      "section": "04-text-processing",
      "command": "paste",
      "title": "Merge files line by line",
      "about": "paste combines corresponding lines from input files. -d selects the output delimiter.",
      "example": "paste -d, names.txt roles.txt",
      "example_output": "alice,analyst\nbob,developer\n",
      "breakdown": [
        "Line 1 from each file is joined.",
        "-d, places a comma between values."
      ],
      "task": "Combine hosts.txt and states.txt line by line with a comma.",
      "solution": "paste -d, hosts.txt states.txt",
      "hints": [
        "Use comma as paste's delimiter.",
        "Use the checker as a specification: Each host was paired with its state.",
        "Try this command shape, replacing placeholders with fixture values: paste <values>"
      ],
      "files": {
        "hosts.txt": "web-01\ndb-01\napi-02\n",
        "states.txt": "online\nmaintenance\noffline\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Each host was paired with its state.",
          "expected": "web-01,online\ndb-01,maintenance\napi-02,offline",
          "normalize": "trim"
        }
      ],
      "completion": "paste pairs by line position. Validate row counts first when accidental misalignment would matter.",
      "command_order": 32,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "paste combines corresponding lines from input files. -d selects the output delimiter.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "paste"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-paste-2",
      "section": "04-text-processing",
      "command": "paste",
      "title": "Join columns with a delimiter",
      "about": "paste combines corresponding lines from input files. -d selects the output delimiter. paste -d chooses the delimiter inserted between corresponding input lines.",
      "example": "paste -d, names.txt scores.txt",
      "example_output": "alice,90\nbob,85",
      "breakdown": [
        "paste is the featured command in the worked pattern.",
        "paste combines corresponding lines from input files. -d selects the output delimiter. paste -d chooses the delimiter inserted between corresponding input lines.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Join hosts.txt and ports.txt with a colon.",
      "solution": "paste -d: hosts.txt ports.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: paste -d: <values>",
        "Use man paste or paste --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "hosts.txt": "api\ndb\nweb\n",
        "ports.txt": "8080\n5432\n443\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Join columns with a delimiter”.",
          "expected": "api:8080\ndb:5432\nweb:443",
          "normalize": "trim"
        }
      ],
      "completion": "paste -d chooses the delimiter inserted between corresponding input lines. You can now use this behavior as part of a larger command-line workflow.",
      "order": 157,
      "command_order": 32,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "paste combines corresponding lines from input files. -d selects the output delimiter. paste -d chooses the delimiter inserted between corresponding input lines.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "paste"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-paste-3",
      "section": "04-text-processing",
      "command": "paste",
      "title": "Combine three aligned files",
      "about": "paste combines corresponding lines from input files. -d selects the output delimiter. paste aligns corresponding lines from any number of files into wider records.",
      "example": "paste names.txt roles.txt teams.txt",
      "example_output": "alice\tadmin\tblue",
      "breakdown": [
        "paste is the featured command in the worked pattern.",
        "paste combines corresponding lines from input files. -d selects the output delimiter. paste aligns corresponding lines from any number of files into wider records.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create tab-separated name, status, and owner rows from three files.",
      "solution": "paste names.txt status.txt owners.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: paste <values>",
        "Use man paste or paste --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "names.txt": "api\ndb\n",
        "status.txt": "up\ndown\n",
        "owners.txt": "platform\ndata\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Combine three aligned files”.",
          "expected": "api\tup\tplatform\ndb\tdown\tdata",
          "normalize": "trim"
        }
      ],
      "completion": "paste aligns corresponding lines from any number of files into wider records. You can now use this behavior as part of a larger command-line workflow.",
      "order": 158,
      "command_order": 32,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "paste combines corresponding lines from input files. -d selects the output delimiter. paste aligns corresponding lines from any number of files into wider records.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "paste"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-paste-4",
      "section": "04-text-processing",
      "command": "paste",
      "title": "Serialize lines from one file",
      "about": "paste combines corresponding lines from input files. -d selects the output delimiter. paste -s combines successive lines from each input file onto one output row.",
      "example": "paste -sd, values.txt",
      "example_output": "one,two,three",
      "breakdown": [
        "paste is the featured command in the worked pattern.",
        "paste combines corresponding lines from input files. -d selects the output delimiter. paste -s combines successive lines from each input file onto one output row.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Turn tags.txt into one comma-separated line.",
      "solution": "paste -sd, tags.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: paste <values>",
        "Use man paste or paste --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "tags.txt": "linux\ncli\npractice\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Serialize lines from one file”.",
          "expected": "linux,cli,practice",
          "normalize": "trim"
        }
      ],
      "completion": "paste -s combines successive lines from each input file onto one output row. You can now use this behavior as part of a larger command-line workflow.",
      "order": 159,
      "command_order": 32,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "paste combines corresponding lines from input files. -d selects the output delimiter. paste -s combines successive lines from each input file onto one output row.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "paste"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-paste-5",
      "section": "04-text-processing",
      "command": "paste",
      "title": "Build records from generated columns",
      "about": "paste combines corresponding lines from input files. -d selects the output delimiter. Process substitution lets paste align columns produced by separate transformations.",
      "example": "paste -d= <(cut -d, -f1 data.csv) <(cut -d, -f2 data.csv)",
      "example_output": "api=up\ndb=down",
      "breakdown": [
        "paste is the featured command in the worked pattern.",
        "paste combines corresponding lines from input files. -d selects the output delimiter. Process substitution lets paste align columns produced by separate transformations.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use paste and process substitution to print service=port from services.csv.",
      "solution": "paste -d= <(cut -d, -f1 services.csv) <(cut -d, -f3 services.csv)",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: paste -d= < <values> -f1 <value> < <values> -f3 <value>",
        "Use man paste or paste --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services.csv": "api,tcp,8080\ndb,tcp,5432\nweb,tcp,443\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Build records from generated columns”.",
          "expected": "api=8080\ndb=5432\nweb=443",
          "normalize": "trim"
        }
      ],
      "completion": "Process substitution lets paste align columns produced by separate transformations. You can now use this behavior as part of a larger command-line workflow.",
      "order": 160,
      "command_order": 32,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "paste combines corresponding lines from input files. -d selects the output delimiter. Process substitution lets paste align columns produced by separate transformations.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "paste",
        "redirection"
      ],
      "prerequisites": [
        "cut"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-grep",
      "order": 161,
      "section": "04-text-processing",
      "command": "grep",
      "title": "Keep matching records",
      "about": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally.",
      "example": "grep ERROR app.log",
      "example_output": "12:02 ERROR database unavailable\n",
      "breakdown": [
        "ERROR is the search pattern.",
        "Matching complete lines are emitted."
      ],
      "task": "Print every service.log line containing DENIED.",
      "solution": "grep DENIED service.log",
      "hints": [
        "Give grep the pattern and filename.",
        "Use the checker as a specification: Only the DENIED records were printed.",
        "Try this command shape, replacing placeholders with fixture values: grep <values>"
      ],
      "files": {
        "service.log": "08:00 ALLOWED alice\n08:02 DENIED bob\n08:04 ALLOWED carol\n08:05 DENIED dave\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only the DENIED records were printed.",
          "expected": "08:02 DENIED bob\n08:05 DENIED dave",
          "normalize": "trim"
        }
      ],
      "completion": "Start literal, then add regular-expression power only when the task needs it.",
      "command_order": 33,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "grep"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-grep-2",
      "section": "04-text-processing",
      "command": "grep",
      "title": "Match case-insensitively",
      "about": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -i matches text without treating letter case as significant.",
      "example": "grep -i 'error' app.log",
      "example_output": "ERROR timeout\nError retry",
      "breakdown": [
        "grep is the featured command in the worked pattern.",
        "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -i matches text without treating letter case as significant.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print every warning line from app.log regardless of case.",
      "solution": "grep -i 'warning' app.log",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: grep -i <values>",
        "Use man grep or grep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.log": "INFO start\nWARNING disk\nWarning memory\nwarning network\nINFO end\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Match case-insensitively”.",
          "expected": "WARNING disk\nWarning memory\nwarning network",
          "normalize": "trim"
        }
      ],
      "completion": "grep -i matches text without treating letter case as significant. You can now use this behavior as part of a larger command-line workflow.",
      "order": 162,
      "command_order": 33,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -i matches text without treating letter case as significant.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "grep",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-grep-3",
      "section": "04-text-processing",
      "command": "grep",
      "title": "Show matching line numbers",
      "about": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -n prefixes each match with its source line number.",
      "example": "grep -n '^ERROR' app.log",
      "example_output": "3:ERROR timeout",
      "breakdown": [
        "grep is the featured command in the worked pattern.",
        "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -n prefixes each match with its source line number.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print TODO lines from plan.txt with their line numbers.",
      "solution": "grep -n 'TODO' plan.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: grep -n <values>",
        "Use man grep or grep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "plan.txt": "done setup\nTODO test\nnotes\nTODO deploy\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Show matching line numbers”.",
          "expected": "2:TODO test\n4:TODO deploy",
          "normalize": "trim"
        }
      ],
      "completion": "grep -n prefixes each match with its source line number. You can now use this behavior as part of a larger command-line workflow.",
      "order": 163,
      "command_order": 33,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -n prefixes each match with its source line number.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "grep",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-grep-4",
      "section": "04-text-processing",
      "command": "grep",
      "title": "Search selected files recursively",
      "about": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -R descends a tree while --include restricts the filename pattern being searched.",
      "example": "grep -R --include='*.conf' 'debug=true' config",
      "example_output": "config/app.conf:debug=true",
      "breakdown": [
        "grep is the featured command in the worked pattern.",
        "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -R descends a tree while --include restricts the filename pattern being searched.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Recursively find timeout=30 only in .ini files below services.",
      "solution": "grep -R --include='*.ini' 'timeout=30' services",
      "hints": [
        "Use the checker as a specification: The matching INI file was reported.",
        "Try this command shape, replacing placeholders with fixture values: grep -R <values>",
        "Use man grep or grep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services/api/app.ini": "timeout=30\n",
        "services/web/app.ini": "timeout=10\n",
        "services/db/notes.txt": "timeout=30\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The matching INI file was reported.",
          "expected": "services/api/app.ini:timeout=30"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 1 nonblank output line was printed.",
          "expected": 1
        }
      ],
      "completion": "grep -R descends a tree while --include restricts the filename pattern being searched. You can now use this behavior as part of a larger command-line workflow.",
      "order": 164,
      "command_order": 33,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -R descends a tree while --include restricts the filename pattern being searched.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "grep",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "timeout"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-grep-5",
      "section": "04-text-processing",
      "command": "grep",
      "title": "Summarize matches by file",
      "about": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -c reports a per-file match count, which can feed a compact operational summary.",
      "example": "grep -c '^ERROR' *.log",
      "example_output": "api.log:2\nweb.log:1",
      "breakdown": [
        "grep is the featured command in the worked pattern.",
        "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -c reports a per-file match count, which can feed a compact operational summary.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Count ERROR lines in api.log, db.log, and web.log.",
      "solution": "grep -c '^ERROR' api.log db.log web.log",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: grep -c <values>",
        "Use man grep or grep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "api.log": "ERROR one\nINFO ok\nERROR two\n",
        "db.log": "INFO ok\n",
        "web.log": "ERROR three\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Summarize matches by file”.",
          "expected": "api.log:2\ndb.log:0\nweb.log:1",
          "normalize": "trim"
        }
      ],
      "completion": "grep -c reports a per-file match count, which can feed a compact operational summary. You can now use this behavior as part of a larger command-line workflow.",
      "order": 165,
      "command_order": 33,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "grep searches text and prints matching lines. -F is a useful choice when the pattern must be treated literally. grep -c reports a per-file match count, which can feed a compact operational summary.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "grep",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sed",
      "order": 166,
      "section": "04-text-processing",
      "command": "sed",
      "title": "Transform a text stream",
      "about": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged.",
      "example": "sed 's/old/new/g' status.txt",
      "example_output": "new value, new owner\n",
      "breakdown": [
        "s begins substitution.",
        "g replaces all matches on each line."
      ],
      "task": "Print config.txt with every DEBUG changed to INFO, without editing the file.",
      "solution": "sed 's/DEBUG/INFO/g' config.txt",
      "hints": [
        "Use a global substitution and omit -i.",
        "Use the checker as a specification: Every DEBUG value was transformed. The source remained unchanged.",
        "Try this command shape, replacing placeholders with fixture values: sed <values>"
      ],
      "files": {
        "config.txt": "level=DEBUG\nlabel=DEBUG-build\nkeep=true\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Every DEBUG value was transformed.",
          "expected": "level=INFO\nlabel=INFO-build\nkeep=true",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "The source remained unchanged.",
          "path": "config.txt",
          "expected": "level=DEBUG\nlabel=DEBUG-build\nkeep=true",
          "normalize": "trim"
        }
      ],
      "completion": "Preview stream output before an in-place edit. That small habit prevents many accidental file changes.",
      "command_order": 34,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "sed",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sed-2",
      "section": "04-text-processing",
      "command": "sed",
      "title": "Replace every occurrence",
      "about": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. The g flag applies a substitution to every match on each line instead of only the first.",
      "example": "sed 's/dev/prod/g' config.txt",
      "example_output": "prod prod",
      "breakdown": [
        "sed is the featured command in the worked pattern.",
        "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. The g flag applies a substitution to every match on each line instead of only the first.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Replace every occurrence of localhost with api.internal in endpoints.txt.",
      "solution": "sed 's/localhost/api.internal/g' endpoints.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sed <values>",
        "Use man sed or sed --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "endpoints.txt": "http://localhost:8080 localhost\nbackup=localhost\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Replace every occurrence”.",
          "expected": "http://api.internal:8080 api.internal\nbackup=api.internal",
          "normalize": "trim"
        }
      ],
      "completion": "The g flag applies a substitution to every match on each line instead of only the first. You can now use this behavior as part of a larger command-line workflow.",
      "order": 167,
      "command_order": 34,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. The g flag applies a substitution to every match on each line instead of only the first.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "sed",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sed-3",
      "section": "04-text-processing",
      "command": "sed",
      "title": "Print a selected line range",
      "about": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. sed -n suppresses normal output while an address range plus p prints only chosen lines.",
      "example": "sed -n '3,5p' notes.txt",
      "example_output": "three\nfour\nfive",
      "breakdown": [
        "sed is the featured command in the worked pattern.",
        "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. sed -n suppresses normal output while an address range plus p prints only chosen lines.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print lines 2 through 4 of runbook.txt.",
      "solution": "sed -n '2,4p' runbook.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sed -n <values>",
        "Use man sed or sed --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "runbook.txt": "prepare\nbuild\ntest\ndeploy\nverify\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print a selected line range”.",
          "expected": "build\ntest\ndeploy",
          "normalize": "trim"
        }
      ],
      "completion": "sed -n suppresses normal output while an address range plus p prints only chosen lines. You can now use this behavior as part of a larger command-line workflow.",
      "order": 168,
      "command_order": 34,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. sed -n suppresses normal output while an address range plus p prints only chosen lines.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "sed",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sed-4",
      "section": "04-text-processing",
      "command": "sed",
      "title": "Delete comments and blank lines",
      "about": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. Multiple sed expressions can remove noise and leave only active configuration records.",
      "example": "sed -e '/^#/d' -e '/^$/d' app.conf",
      "example_output": "mode=prod\nport=8080",
      "breakdown": [
        "sed is the featured command in the worked pattern.",
        "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. Multiple sed expressions can remove noise and leave only active configuration records.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print services.conf without comments or blank lines.",
      "solution": "sed -e '/^[[:space:]]*#/d' -e '/^[[:space:]]*$/d' services.conf",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sed -e <value> -e <values>",
        "Use man sed or sed --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services.conf": "# services\n\napi=enabled\n  # old db\nweb=enabled\n\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Delete comments and blank lines”.",
          "expected": "api=enabled\nweb=enabled",
          "normalize": "trim"
        }
      ],
      "completion": "Multiple sed expressions can remove noise and leave only active configuration records. You can now use this behavior as part of a larger command-line workflow.",
      "order": 169,
      "command_order": 34,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. Multiple sed expressions can remove noise and leave only active configuration records.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "sed",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sed-5",
      "section": "04-text-processing",
      "command": "sed",
      "title": "Edit a configuration file in place",
      "about": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. sed -i writes the transformation back to the file; a suffix can retain a reviewable backup.",
      "example": "sed -i.bak 's/^mode=.*/mode=prod/' app.conf",
      "example_output": "",
      "breakdown": [
        "sed is the featured command in the worked pattern.",
        "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. sed -i writes the transformation back to the file; a suffix can retain a reviewable backup.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Change mode=dev to mode=production in app.conf and retain app.conf.bak.",
      "solution": "sed -i.bak 's/^mode=.*/mode=production/' app.conf",
      "hints": [
        "Use the checker as a specification: The active configuration was updated. The original configuration was backed up.",
        "Try this command shape, replacing placeholders with fixture values: sed <values>",
        "Use man sed or sed --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.conf": "mode=dev\nport=8080\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "The active configuration was updated.",
          "path": "app.conf",
          "expected": "mode=production\nport=8080\n"
        },
        {
          "type": "file-content",
          "description": "The original configuration was backed up.",
          "path": "app.conf.bak",
          "expected": "mode=dev\nport=8080\n"
        }
      ],
      "completion": "sed -i writes the transformation back to the file; a suffix can retain a reviewable backup. You can now use this behavior as part of a larger command-line workflow.",
      "order": 170,
      "command_order": 34,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "sed applies editing expressions to a stream. Without -i, transformed output goes to stdout and the source remains unchanged. sed -i writes the transformation back to the file; a suffix can retain a reviewable backup.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "sed",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-awk",
      "order": 171,
      "section": "04-text-processing",
      "command": "awk",
      "title": "Process records and fields",
      "about": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation.",
      "example": "awk -F, '{print $1 \": \" $3}' inventory.csv",
      "example_output": "router: online\nswitch: offline\n",
      "breakdown": [
        "-F, sets comma as field separator.",
        "$1 and $3 select fields.",
        "Adjacent expressions concatenate output."
      ],
      "task": "Print asset name, colon-space, and status from inventory.csv fields 1 and 3.",
      "solution": "awk -F, '{print $1 \": \" $3}' inventory.csv",
      "hints": [
        "Set comma as -F and concatenate fields 1 and 3.",
        "Use the checker as a specification: Each asset and status was formatted correctly.",
        "Try this command shape, replacing placeholders with fixture values: awk <values>"
      ],
      "files": {
        "inventory.csv": "web-01,alice,online\ndb-01,bob,maintenance\napi-02,carol,offline\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Each asset and status was formatted correctly.",
          "expected": "web-01: online\ndb-01: maintenance\napi-02: offline",
          "normalize": "trim"
        }
      ],
      "completion": "awk is a record-processing language. Build and inspect small stages before compressing complex logic.",
      "command_order": 35,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "awk",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-awk-2",
      "section": "04-text-processing",
      "command": "awk",
      "title": "Filter records by a field",
      "about": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. An awk pattern can compare a named field and run an action only for matching records.",
      "example": "awk -F, '$3 == \"down\" {print $1}' inventory.csv",
      "example_output": "db",
      "breakdown": [
        "awk is the featured command in the worked pattern.",
        "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. An awk pattern can compare a named field and run an action only for matching records.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the names in field 1 where field 3 is failed.",
      "solution": "awk -F, '$3 == \"failed\" {print $1}' jobs.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: awk <values>",
        "Use man awk or awk --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "jobs.csv": "api,12,ok\ndb,7,failed\nweb,19,ok\nbackup,4,failed\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Filter records by a field”.",
          "expected": "db\nbackup",
          "normalize": "trim"
        }
      ],
      "completion": "An awk pattern can compare a named field and run an action only for matching records. You can now use this behavior as part of a larger command-line workflow.",
      "order": 172,
      "command_order": 35,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. An awk pattern can compare a named field and run an action only for matching records.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "awk",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-awk-3",
      "section": "04-text-processing",
      "command": "awk",
      "title": "Calculate a derived field",
      "about": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. awk performs arithmetic directly on numeric fields while formatting the output record.",
      "example": "awk -F, '{print $1, $2 * 2}' values.csv",
      "example_output": "alpha 20",
      "breakdown": [
        "awk is the featured command in the worked pattern.",
        "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. awk performs arithmetic directly on numeric fields while formatting the output record.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print item=total where total is quantity field 2 times price field 3.",
      "solution": "awk -F, '{printf \"%s=%.2f\\n\", $1, $2 * $3}' orders.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: awk <values>",
        "Use man awk or awk --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "orders.csv": "cable,3,4.50\nadapter,2,8.25\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Calculate a derived field”.",
          "expected": "cable=13.50\nadapter=16.50",
          "normalize": "trim"
        }
      ],
      "completion": "awk performs arithmetic directly on numeric fields while formatting the output record. You can now use this behavior as part of a larger command-line workflow.",
      "order": 173,
      "command_order": 35,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. awk performs arithmetic directly on numeric fields while formatting the output record.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "awk",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "printf"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-awk-4",
      "section": "04-text-processing",
      "command": "awk",
      "title": "Aggregate across all records",
      "about": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. Variables accumulate values per record and an END block prints the final summary once.",
      "example": "awk -F, '{sum += $2} END {print sum}' values.csv",
      "example_output": "42",
      "breakdown": [
        "awk is the featured command in the worked pattern.",
        "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. Variables accumulate values per record and an END block prints the final summary once.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print total_requests=<sum of field 2> from traffic.csv.",
      "solution": "awk -F, '{sum += $2} END {print \"total_requests=\" sum}' traffic.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: awk <values>",
        "Use man awk or awk --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "traffic.csv": "api,120\nweb,80\ndb,35\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Aggregate across all records”.",
          "expected": "total_requests=235",
          "normalize": "trim"
        }
      ],
      "completion": "Variables accumulate values per record and an END block prints the final summary once. You can now use this behavior as part of a larger command-line workflow.",
      "order": 174,
      "command_order": 35,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. Variables accumulate values per record and an END block prints the final summary once.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "awk",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-awk-5",
      "section": "04-text-processing",
      "command": "awk",
      "title": "Summarize records by category",
      "about": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. Associative arrays let awk accumulate one value per category before an END block emits a grouped report.",
      "example": "awk -F, '{sum[$1]+=$2} END {for (k in sum) print k,sum[k]}' data.csv",
      "example_output": "blue 12\ngreen 8",
      "breakdown": [
        "awk is the featured command in the worked pattern.",
        "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. Associative arrays let awk accumulate one value per category before an END block emits a grouped report.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sum field 3 by team field 2 and print sorted team=total rows.",
      "solution": "awk -F, '{sum[$2]+=$3} END {for (team in sum) print team \"=\" sum[team]}' usage.csv | sort",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: awk <values> | sort",
        "Use man awk or awk --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "usage.csv": "api,blue,10\nweb,green,7\ndb,blue,4\ncache,green,3\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Summarize records by category”.",
          "expected": "blue=14\ngreen=10",
          "normalize": "trim"
        }
      ],
      "completion": "Associative arrays let awk accumulate one value per category before an END block emits a grouped report. You can now use this behavior as part of a larger command-line workflow.",
      "order": 175,
      "command_order": 35,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "awk splits input into records and fields, then applies a small program. It handles conditional selection, formatting, and aggregation. Associative arrays let awk accumulate one value per category before an END block emits a grouped report.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "awk",
        "pipelines",
        "quoting",
        "iteration",
        "ordering and grouping"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "sort"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tr",
      "order": 176,
      "section": "04-text-processing",
      "command": "tr",
      "title": "Translate characters",
      "about": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields.",
      "example": "tr '[:lower:]' '[:upper:]' < note.txt",
      "example_output": "READY\n",
      "breakdown": [
        "Character classes describe lower and upper case.",
        "< supplies the file through stdin."
      ],
      "task": "Print message.txt with lowercase letters converted to uppercase.",
      "solution": "tr '[:lower:]' '[:upper:]' < message.txt",
      "hints": [
        "Redirect the file into tr and use character classes.",
        "Use the checker as a specification: The message was converted to uppercase.",
        "Try this command shape, replacing placeholders with fixture values: tr <values> < <value>"
      ],
      "files": {
        "message.txt": "verify the result\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The message was converted to uppercase.",
          "expected": "VERIFY THE RESULT",
          "normalize": "trim"
        }
      ],
      "completion": "tr is ideal for character-level mappings. Use sed or awk when context and fields matter.",
      "command_order": 36,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "tr",
        "redirection",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tr-2",
      "section": "04-text-processing",
      "command": "tr",
      "title": "Normalize letter case",
      "about": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. Character classes let tr convert all lowercase letters to uppercase without listing an alphabet.",
      "example": "tr '[:lower:]' '[:upper:]' < names.txt",
      "example_output": "ALICE\nBOB",
      "breakdown": [
        "tr is the featured command in the worked pattern.",
        "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. Character classes let tr convert all lowercase letters to uppercase without listing an alphabet.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print tags.txt with every letter converted to lowercase.",
      "solution": "tr '[:upper:]' '[:lower:]' < tags.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tr <values> < <value>",
        "Use man tr or tr --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "tags.txt": "LINUX\nCli\nPRACTICE\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Normalize letter case”.",
          "expected": "linux\ncli\npractice",
          "normalize": "trim"
        }
      ],
      "completion": "Character classes let tr convert all lowercase letters to uppercase without listing an alphabet. You can now use this behavior as part of a larger command-line workflow.",
      "order": 177,
      "command_order": 36,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. Character classes let tr convert all lowercase letters to uppercase without listing an alphabet.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "tr",
        "redirection",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tr-3",
      "section": "04-text-processing",
      "command": "tr",
      "title": "Delete unwanted characters",
      "about": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. tr -d removes every character belonging to the specified set.",
      "example": "tr -d ' -' < phone.txt",
      "example_output": "5551234567",
      "breakdown": [
        "tr is the featured command in the worked pattern.",
        "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. tr -d removes every character belonging to the specified set.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove parentheses, spaces, and hyphens from phone.txt.",
      "solution": "tr -d '() -' < phone.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tr -d <values> < <value>",
        "Use man tr or tr --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "phone.txt": "(555) 123-4567\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Delete unwanted characters”.",
          "expected": "5551234567",
          "normalize": "trim"
        }
      ],
      "completion": "tr -d removes every character belonging to the specified set. You can now use this behavior as part of a larger command-line workflow.",
      "order": 178,
      "command_order": 36,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. tr -d removes every character belonging to the specified set.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "tr",
        "redirection",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tr-4",
      "section": "04-text-processing",
      "command": "tr",
      "title": "Squeeze repeated whitespace",
      "about": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. tr -s collapses each run of characters in a set to one occurrence.",
      "example": "tr -s ' ' < rough.txt",
      "example_output": "one two three",
      "breakdown": [
        "tr is the featured command in the worked pattern.",
        "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. tr -s collapses each run of characters in a set to one occurrence.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Collapse every run of spaces in rough.txt to one space.",
      "solution": "tr -s ' ' < rough.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tr -s <values> < <value>",
        "Use man tr or tr --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "rough.txt": "alpha     beta  gamma\ndelta   epsilon\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Squeeze repeated whitespace”.",
          "expected": "alpha beta gamma\ndelta epsilon",
          "normalize": "trim"
        }
      ],
      "completion": "tr -s collapses each run of characters in a set to one occurrence. You can now use this behavior as part of a larger command-line workflow.",
      "order": 179,
      "command_order": 36,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. tr -s collapses each run of characters in a set to one occurrence.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "tr",
        "redirection",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tr-5",
      "section": "04-text-processing",
      "command": "tr",
      "title": "Create safe identifier slugs",
      "about": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. Case conversion, character translation, and squeezing combine into a small text-normalization workflow.",
      "example": "printf 'API Service' | tr '[:upper:] ' '[:lower:]-'",
      "example_output": "api-service",
      "breakdown": [
        "tr is the featured command in the worked pattern.",
        "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. Case conversion, character translation, and squeezing combine into a small text-normalization workflow.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Convert each title in titles.txt to a lowercase hyphenated slug with repeated hyphens squeezed.",
      "solution": "tr '[:upper:] ' '[:lower:]-' < titles.txt | tr -s '-'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: tr <values> < <value> | tr -s <value>",
        "Use man tr or tr --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "titles.txt": "API  Gateway\nDatabase Backup\nCLI   Practice\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Create safe identifier slugs”.",
          "expected": "api-gateway\ndatabase-backup\ncli-practice",
          "normalize": "trim"
        }
      ],
      "completion": "Case conversion, character translation, and squeezing combine into a small text-normalization workflow. You can now use this behavior as part of a larger command-line workflow.",
      "order": 180,
      "command_order": 36,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "tr translates or deletes characters from standard input. It operates character by character rather than on words or fields. Case conversion, character translation, and squeezing combine into a small text-normalization workflow.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "tr",
        "pipelines",
        "redirection",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sort",
      "order": 181,
      "section": "04-text-processing",
      "command": "sort",
      "title": "Order records",
      "about": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended.",
      "example": "sort names.txt",
      "example_output": "alice\nbob\ncarol\n",
      "breakdown": [
        "Every line becomes a sort record.",
        "The source file is not edited."
      ],
      "task": "Print hosts.txt in ascending lexical order.",
      "solution": "sort hosts.txt",
      "hints": [
        "Pass the filename to sort.",
        "Use the checker as a specification: The hosts were sorted ascending.",
        "Try this command shape, replacing placeholders with fixture values: sort <value>"
      ],
      "files": {
        "hosts.txt": "web-03\ndb-01\nweb-01\napi-02\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The hosts were sorted ascending.",
          "expected": "api-02\ndb-01\nweb-01\nweb-03",
          "normalize": "trim"
        }
      ],
      "completion": "Sorting output does not sort the source file. Redirect to a new file and verify before replacing data.",
      "command_order": 37,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "sort",
        "ordering and grouping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sort-2",
      "section": "04-text-processing",
      "command": "sort",
      "title": "Sort numbers numerically",
      "about": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. sort -n compares numeric value rather than lexicographic character order.",
      "example": "sort -n values.txt",
      "example_output": "2\n10\n100",
      "breakdown": [
        "sort is the featured command in the worked pattern.",
        "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. sort -n compares numeric value rather than lexicographic character order.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print scores.txt in ascending numeric order.",
      "solution": "sort -n scores.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sort -n <value>",
        "Use man sort or sort --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "scores.txt": "42\n7\n100\n15\n3\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Sort numbers numerically”.",
          "expected": "3\n7\n15\n42\n100",
          "normalize": "trim"
        }
      ],
      "completion": "sort -n compares numeric value rather than lexicographic character order. You can now use this behavior as part of a larger command-line workflow.",
      "order": 182,
      "command_order": 37,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. sort -n compares numeric value rather than lexicographic character order.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "sort",
        "ordering and grouping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sort-3",
      "section": "04-text-processing",
      "command": "sort",
      "title": "Sort by a delimited field",
      "about": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. The -t and -k options select a field to use as the sort key.",
      "example": "sort -t, -k2,2 inventory.csv",
      "example_output": "db,down\napi,up",
      "breakdown": [
        "sort is the featured command in the worked pattern.",
        "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. The -t and -k options select a field to use as the sort key.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sort people.csv alphabetically by role in field 3.",
      "solution": "sort -t, -k3,3 people.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sort <values>",
        "Use man sort or sort --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "people.csv": "alice,42,developer\nbob,35,admin\ncarol,29,analyst\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Sort by a delimited field”.",
          "expected": "bob,35,admin\ncarol,29,analyst\nalice,42,developer",
          "normalize": "trim"
        }
      ],
      "completion": "The -t and -k options select a field to use as the sort key. You can now use this behavior as part of a larger command-line workflow.",
      "order": 183,
      "command_order": 37,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. The -t and -k options select a field to use as the sort key.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "sort",
        "ordering and grouping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sort-4",
      "section": "04-text-processing",
      "command": "sort",
      "title": "Sort a numeric field descending",
      "about": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. A key can carry numeric and reverse modifiers so only that field controls descending order.",
      "example": "sort -t, -k2,2nr scores.csv",
      "example_output": "alice,98\nbob,85",
      "breakdown": [
        "sort is the featured command in the worked pattern.",
        "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. A key can carry numeric and reverse modifiers so only that field controls descending order.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sort usage.csv from highest to lowest value in field 2.",
      "solution": "sort -s -t, -k2,2nr usage.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sort -s <values>",
        "Use man sort or sort --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "usage.csv": "api,120\ndb,35\nweb,80\ncache,80\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Sort a numeric field descending”.",
          "expected": "api,120\nweb,80\ncache,80\ndb,35",
          "normalize": "trim"
        }
      ],
      "completion": "A key can carry numeric and reverse modifiers so only that field controls descending order. You can now use this behavior as part of a larger command-line workflow.",
      "order": 184,
      "command_order": 37,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. A key can carry numeric and reverse modifiers so only that field controls descending order.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "sort",
        "ordering and grouping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sort-5",
      "section": "04-text-processing",
      "command": "sort",
      "title": "Apply primary and secondary keys",
      "about": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. Multiple -k keys provide deterministic ordering when primary values tie.",
      "example": "sort -t, -k2,2 -k3,3nr data.csv",
      "example_output": "a,blue,9\nb,blue,4\nc,green,7",
      "breakdown": [
        "sort is the featured command in the worked pattern.",
        "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. Multiple -k keys provide deterministic ordering when primary values tie.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sort jobs.csv by status field 2 alphabetically, then duration field 3 numerically descending.",
      "solution": "sort -t, -k2,2 -k3,3nr jobs.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sort <values>",
        "Use man sort or sort --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "jobs.csv": "api,ok,12\nbackup,failed,4\ndb,failed,9\nweb,ok,20\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Apply primary and secondary keys”.",
          "expected": "db,failed,9\nbackup,failed,4\nweb,ok,20\napi,ok,12",
          "normalize": "trim"
        }
      ],
      "completion": "Multiple -k keys provide deterministic ordering when primary values tie. You can now use this behavior as part of a larger command-line workflow.",
      "order": 185,
      "command_order": 37,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "sort orders lines. Its default is lexical; use -n when numeric value rather than string order is intended. Multiple -k keys provide deterministic ordering when primary values tie.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "sort",
        "ordering and grouping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uniq",
      "order": 186,
      "section": "04-text-processing",
      "command": "uniq",
      "title": "Collapse adjacent duplicates",
      "about": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input.",
      "example": "sort colors.txt | uniq -c",
      "example_output": "2 blue\n1 green\n",
      "breakdown": [
        "sort groups equal values.",
        "uniq -c counts adjacent groups."
      ],
      "task": "Print each distinct color once in ascending order.",
      "solution": "sort colors.txt | uniq",
      "hints": [
        "Pipe sorted input into uniq.",
        "Use the checker as a specification: Each color was printed once in order.",
        "Try this command shape, replacing placeholders with fixture values: sort <value> | uniq"
      ],
      "files": {
        "colors.txt": "red\nblue\nred\ngreen\nblue\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Each color was printed once in order.",
          "expected": "blue\ngreen\nred",
          "normalize": "trim"
        }
      ],
      "completion": "uniq is adjacency-based. sort | uniq is the standard pattern for unordered text values.",
      "command_order": 38,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "uniq",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uniq-2",
      "section": "04-text-processing",
      "command": "uniq",
      "title": "Count adjacent duplicates",
      "about": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -c prefixes each run with its occurrence count; sorted input groups matching values first.",
      "example": "sort names.txt | uniq -c",
      "example_output": "      2 alice\n      1 bob",
      "breakdown": [
        "uniq is the featured command in the worked pattern.",
        "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -c prefixes each run with its occurrence count; sorted input groups matching values first.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sort status.txt and print counts for each status.",
      "solution": "sort status.txt | uniq -c",
      "hints": [
        "Use the checker as a specification: The down count is two. The up count is three.",
        "Try this command shape, replacing placeholders with fixture values: sort <value> | uniq -c",
        "Use man uniq or uniq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "status.txt": "up\ndown\nup\nunknown\ndown\nup\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The down count is two.",
          "expected": "2 down"
        },
        {
          "type": "stdout-contains",
          "description": "The up count is three.",
          "expected": "3 up"
        }
      ],
      "completion": "uniq -c prefixes each run with its occurrence count; sorted input groups matching values first. You can now use this behavior as part of a larger command-line workflow.",
      "order": 187,
      "command_order": 38,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -c prefixes each run with its occurrence count; sorted input groups matching values first.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "uniq",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-uniq-3",
      "section": "04-text-processing",
      "command": "uniq",
      "title": "Print only duplicated values",
      "about": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -d emits one representative only for values that occur more than once in adjacent input.",
      "example": "sort names.txt | uniq -d",
      "example_output": "alice",
      "breakdown": [
        "uniq is the featured command in the worked pattern.",
        "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -d emits one representative only for values that occur more than once in adjacent input.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only IDs that appear more than once in ids.txt.",
      "solution": "sort ids.txt | uniq -d",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sort <value> | uniq -d",
        "Use man uniq or uniq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "ids.txt": "A12\nB07\nA12\nC33\nB07\nD99\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print only duplicated values”.",
          "expected": "A12\nB07",
          "normalize": "trim"
        }
      ],
      "completion": "uniq -d emits one representative only for values that occur more than once in adjacent input. You can now use this behavior as part of a larger command-line workflow.",
      "order": 188,
      "command_order": 38,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -d emits one representative only for values that occur more than once in adjacent input.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "uniq",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uniq-4",
      "section": "04-text-processing",
      "command": "uniq",
      "title": "Print values that occur once",
      "about": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -u emits only runs with a single record, useful for finding unmatched values.",
      "example": "sort names.txt | uniq -u",
      "example_output": "bob",
      "breakdown": [
        "uniq is the featured command in the worked pattern.",
        "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -u emits only runs with a single record, useful for finding unmatched values.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only tags that occur exactly once.",
      "solution": "sort tags.txt | uniq -u",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sort <value> | uniq -u",
        "Use man uniq or uniq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "tags.txt": "linux\ncli\nlinux\nshell\ndocker\ncli\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print values that occur once”.",
          "expected": "docker\nshell",
          "normalize": "trim"
        }
      ],
      "completion": "uniq -u emits only runs with a single record, useful for finding unmatched values. You can now use this behavior as part of a larger command-line workflow.",
      "order": 189,
      "command_order": 38,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -u emits only runs with a single record, useful for finding unmatched values.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "uniq",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uniq-5",
      "section": "04-text-processing",
      "command": "uniq",
      "title": "Deduplicate by a later field",
      "about": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -f skips leading fields during comparison while preserving the first full record in each run.",
      "example": "sort -k2 records.txt | uniq -f1",
      "example_output": "1 blue\n3 green",
      "breakdown": [
        "uniq is the featured command in the worked pattern.",
        "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -f skips leading fields during comparison while preserving the first full record in each run.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sort sessions.txt by username field 2 and retain the first session ID for each username.",
      "solution": "sort -k2,2 sessions.txt | uniq -f1",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sort <values> | uniq -f1",
        "Use man uniq or uniq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.txt": "101 alice\n102 bob\n103 alice\n104 carol\n105 bob\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Deduplicate by a later field”.",
          "expected": "101 alice\n102 bob\n104 carol",
          "normalize": "trim"
        }
      ],
      "completion": "uniq -f skips leading fields during comparison while preserving the first full record in each run. You can now use this behavior as part of a larger command-line workflow.",
      "order": 190,
      "command_order": 38,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "uniq compares neighboring lines. Sort first when equal values may be separated in the original input. uniq -f skips leading fields during comparison while preserving the first full record in each run.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "uniq",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-column",
      "order": 191,
      "section": "04-text-processing",
      "command": "column",
      "title": "Align delimited text for people",
      "about": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format.",
      "example": "column -t -s, inventory.csv",
      "example_output": "web-01  alice  online\n",
      "breakdown": [
        "-s, treats comma as separator.",
        "-t creates a table."
      ],
      "task": "Format people.csv as an aligned table using comma as the separator.",
      "solution": "column -t -s, people.csv",
      "hints": [
        "Combine table mode with comma separator.",
        "Use the checker as a specification: The table includes alice's row. The table includes carol's row.",
        "Try this command shape, replacing placeholders with fixture values: column -t <values>"
      ],
      "files": {
        "people.csv": "alice,analyst,blue\nbob,developer,green\ncarol,operator,blue\n"
      },
      "packages": [
        "util-linux"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The table includes alice's row.",
          "expected": "alice"
        },
        {
          "type": "stdout-contains",
          "description": "The table includes carol's row.",
          "expected": "carol"
        }
      ],
      "completion": "column improves human readability. Preserve CSV, TSV, or JSON for machine consumption.",
      "command_order": 39,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "column"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-column-2",
      "section": "04-text-processing",
      "command": "column",
      "title": "Align a delimited table",
      "about": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. column -t with -s parses a delimiter and aligns values into readable columns.",
      "example": "column -t -s, data.csv",
      "example_output": "api  up\ndb   down",
      "breakdown": [
        "column is the featured command in the worked pattern.",
        "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. column -t with -s parses a delimiter and aligns values into readable columns.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Format inventory.csv as aligned columns using comma as the separator.",
      "solution": "column -t -s, inventory.csv",
      "hints": [
        "Use the checker as a specification: The long database name was preserved. The header was preserved.",
        "Try this command shape, replacing placeholders with fixture values: column -t <values>",
        "Use man column or column --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "inventory.csv": "service,status,port\napi,up,8080\ndatabase,down,5432\nweb,up,443\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The long database name was preserved.",
          "expected": "database"
        },
        {
          "type": "stdout-contains",
          "description": "The header was preserved.",
          "expected": "service"
        }
      ],
      "completion": "column -t with -s parses a delimiter and aligns values into readable columns. You can now use this behavior as part of a larger command-line workflow.",
      "order": 192,
      "command_order": 39,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. column -t with -s parses a delimiter and aligns values into readable columns.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "column"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-column-3",
      "section": "04-text-processing",
      "command": "column",
      "title": "Choose an output separator",
      "about": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. The -o option controls the text inserted between aligned output columns.",
      "example": "column -t -s, -o ' | ' data.csv",
      "example_output": "api | up",
      "breakdown": [
        "column is the featured command in the worked pattern.",
        "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. The -o option controls the text inserted between aligned output columns.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Format services.csv with \" | \" between aligned columns.",
      "solution": "column -t -s, -o ' | ' services.csv",
      "hints": [
        "Use the checker as a specification: Pipe separators were added. The database row was included.",
        "Try this command shape, replacing placeholders with fixture values: column -t <value> -o <value> | <values>",
        "Use man column or column --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services.csv": "api,enabled,8080\ndatabase,disabled,5432\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "Pipe separators were added.",
          "expected": " | "
        },
        {
          "type": "stdout-contains",
          "description": "The database row was included.",
          "expected": "database"
        }
      ],
      "completion": "The -o option controls the text inserted between aligned output columns. You can now use this behavior as part of a larger command-line workflow.",
      "order": 193,
      "command_order": 39,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. The -o option controls the text inserted between aligned output columns.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "column",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-column-4",
      "section": "04-text-processing",
      "command": "column",
      "title": "Format whitespace records",
      "about": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. Without -s, column -t treats whitespace as field separation and aligns a simple report.",
      "example": "printf 'NAME STATUS\\napi up\\ndatabase down\\n' | column -t",
      "example_output": "NAME      STATUS\napi       up\ndatabase  down",
      "breakdown": [
        "column is the featured command in the worked pattern.",
        "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. Without -s, column -t treats whitespace as field separation and aligns a simple report.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Align the whitespace-separated rows in processes.txt.",
      "solution": "column -t processes.txt",
      "hints": [
        "Use the checker as a specification: The process header was shown. The database row was shown.",
        "Try this command shape, replacing placeholders with fixture values: column -t <value>",
        "Use man column or column --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "processes.txt": "PID NAME STATE\n7 api running\n42 database stopped\n105 worker running\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The process header was shown.",
          "expected": "PID"
        },
        {
          "type": "stdout-contains",
          "description": "The database row was shown.",
          "expected": "database"
        }
      ],
      "completion": "Without -s, column -t treats whitespace as field separation and aligns a simple report. You can now use this behavior as part of a larger command-line workflow.",
      "order": 194,
      "command_order": 39,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. Without -s, column -t treats whitespace as field separation and aligns a simple report.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "column"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-column-5",
      "section": "04-text-processing",
      "command": "column",
      "title": "Create a readable filtered report",
      "about": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. A selection pipeline can produce delimited rows that column turns into a human-readable operational report.",
      "example": "awk -F, '$2==\"down\" {print $1,$2,$3}' data.csv | column -t",
      "example_output": "db  down  5432",
      "breakdown": [
        "column is the featured command in the worked pattern.",
        "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. A selection pipeline can produce delimited rows that column turns into a human-readable operational report.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Select enabled services, format service, port, and owner as tab-separated fields, and align them with column -t.",
      "solution": "awk -F, '$2==\"enabled\" {print $1 \"\\t\" $3 \"\\t\" $4}' services.csv | column -t",
      "hints": [
        "Use the checker as a specification: The API service was included. The web owner was included.",
        "Try this command shape, replacing placeholders with fixture values: awk <values> | column -t",
        "Use man column or column --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services.csv": "api,enabled,8080,platform\ndb,disabled,5432,data\nweb,enabled,443,frontend\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The API service was included.",
          "expected": "api"
        },
        {
          "type": "stdout-contains",
          "description": "The web owner was included.",
          "expected": "frontend"
        }
      ],
      "completion": "A selection pipeline can produce delimited rows that column turns into a human-readable operational report. You can now use this behavior as part of a larger command-line workflow.",
      "order": 195,
      "command_order": 39,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "column formats input into aligned columns. It is a display tool; do not parse its padded output as a stable data format. A selection pipeline can produce delimited rows that column turns into a human-readable operational report.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "column",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-fmt",
      "order": 196,
      "section": "04-text-processing",
      "command": "fmt",
      "title": "Reflow paragraph text",
      "about": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning.",
      "example": "fmt -w 20 paragraph.txt",
      "example_output": "This is a short line\nthat wraps cleanly.\n",
      "breakdown": [
        "-w 20 selects a twenty-column goal.",
        "Words are reflowed without editing the file."
      ],
      "task": "Reflow paragraph.txt to a width of 24 columns.",
      "solution": "fmt -w 24 paragraph.txt",
      "hints": [
        "Use -w to choose the target width.",
        "Use the checker as a specification: Reflowed paragraph text was printed. The source paragraph remained unchanged.",
        "Try this command shape, replacing placeholders with fixture values: fmt -w <values>"
      ],
      "files": {
        "paragraph.txt": "Linux commands become easier when each pipeline stage has one clear responsibility.\n"
      },
      "checks": [
        {
          "type": "stdout-nonempty",
          "description": "Reflowed paragraph text was printed."
        },
        {
          "type": "file-content",
          "description": "The source paragraph remained unchanged.",
          "path": "paragraph.txt",
          "expected": "Linux commands become easier when each pipeline stage has one clear responsibility.",
          "normalize": "trim"
        }
      ],
      "completion": "fmt changes presentation line breaks. Never use it on logs or formats where a line is a record.",
      "command_order": 40,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "fmt"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-fmt-2",
      "section": "04-text-processing",
      "command": "fmt",
      "title": "Wrap text to a chosen width",
      "about": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -w sets a target output width for reflowing prose.",
      "example": "fmt -w 30 paragraph.txt",
      "example_output": "A paragraph wrapped near thirty\ncolumns.",
      "breakdown": [
        "fmt is the featured command in the worked pattern.",
        "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -w sets a target output width for reflowing prose.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Reflow paragraph.txt to a width of 32 columns.",
      "solution": "fmt -w 32 paragraph.txt",
      "hints": [
        "Use the checker as a specification: The paragraph text remained intact.",
        "Try this command shape, replacing placeholders with fixture values: fmt -w <values>",
        "Use man fmt or fmt --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "paragraph.txt": "Command line practice becomes easier when each exercise has a clear goal and immediate feedback.\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The paragraph text remained intact.",
          "expected": "Command line practice"
        }
      ],
      "completion": "fmt -w sets a target output width for reflowing prose. You can now use this behavior as part of a larger command-line workflow.",
      "order": 197,
      "command_order": 40,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -w sets a target output width for reflowing prose.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "fmt"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-fmt-3",
      "section": "04-text-processing",
      "command": "fmt",
      "title": "Preserve a uniform prefix",
      "about": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -p reflows only lines carrying a chosen prefix and preserves that prefix on output lines.",
      "example": "fmt -w 30 -p '# ' comments.txt",
      "example_output": "# A wrapped comment that\n# keeps its prefix.",
      "breakdown": [
        "fmt is the featured command in the worked pattern.",
        "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -p reflows only lines carrying a chosen prefix and preserves that prefix on output lines.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Wrap the # comment in comments.txt to width 36 while preserving the prefix.",
      "solution": "fmt -w 36 -p '# ' comments.txt",
      "hints": [
        "Use the checker as a specification: The comment prefix was preserved.",
        "Try this command shape, replacing placeholders with fixture values: fmt -w <value> -p <values>",
        "Use man fmt or fmt --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "comments.txt": "# This operational note should wrap cleanly while every output line remains a shell comment.\nmode=practice\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The comment prefix was preserved.",
          "expected": "# This operational"
        }
      ],
      "completion": "fmt -p reflows only lines carrying a chosen prefix and preserves that prefix on output lines. You can now use this behavior as part of a larger command-line workflow.",
      "order": 198,
      "command_order": 40,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -p reflows only lines carrying a chosen prefix and preserves that prefix on output lines.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "fmt",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-fmt-4",
      "section": "04-text-processing",
      "command": "fmt",
      "title": "Format tagged paragraphs",
      "about": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -t treats the first line indentation as a tag and aligns later lines as paragraph text.",
      "example": "fmt -w 40 -t options.txt",
      "example_output": "--check  Validate the deployment before\n         publishing anything.",
      "breakdown": [
        "fmt is the featured command in the worked pattern.",
        "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -t treats the first line indentation as a tag and aligns later lines as paragraph text.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Format options.txt as tagged paragraphs at width 42.",
      "solution": "fmt -w 42 -t options.txt",
      "hints": [
        "Use the checker as a specification: The check tag was preserved. The force tag was preserved.",
        "Try this command shape, replacing placeholders with fixture values: fmt -w <value> -t <value>",
        "Use man fmt or fmt --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "options.txt": "--check  Validate all configuration and dependencies without publishing any files.\n--force  Publish without the usual confirmation prompt when automation has already reviewed the plan.\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The check tag was preserved.",
          "expected": "--check"
        },
        {
          "type": "stdout-contains",
          "description": "The force tag was preserved.",
          "expected": "--force"
        }
      ],
      "completion": "fmt -t treats the first line indentation as a tag and aligns later lines as paragraph text. You can now use this behavior as part of a larger command-line workflow.",
      "order": 199,
      "command_order": 40,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. fmt -t treats the first line indentation as a tag and aligns later lines as paragraph text.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "fmt"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-fmt-5",
      "section": "04-text-processing",
      "command": "fmt",
      "title": "Normalize prose from a pipeline",
      "about": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. tr can first collapse irregular whitespace before fmt reflows clean paragraphs to a standard width.",
      "example": "tr -s ' ' < rough.txt | fmt -w 40",
      "example_output": "Clean consistently wrapped prose...",
      "breakdown": [
        "fmt is the featured command in the worked pattern.",
        "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. tr can first collapse irregular whitespace before fmt reflows clean paragraphs to a standard width.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Collapse repeated spaces in rough.txt and reflow the result to width 38. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "tr -s ' ' < rough.txt | fmt -w 38 && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: The normalized runbook text was emitted.",
        "Try this command shape, replacing placeholders with fixture values: tr -s <values> < <value> | fmt -w <value>",
        "Use man fmt or fmt --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "rough.txt": "A   concise   runbook   is easier to review, update, and use during an incident when every paragraph follows a predictable width.\n",
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The normalized runbook text was emitted.",
          "expected": "A concise runbook"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "tr can first collapse irregular whitespace before fmt reflows clean paragraphs to a standard width. You can now use this behavior as part of a larger command-line workflow.",
      "order": 200,
      "command_order": 40,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "fmt reflows paragraphs to a target width. It is useful for prose, not structured records where line boundaries carry meaning. tr can first collapse irregular whitespace before fmt reflows clean paragraphs to a standard width.",
      "stage_kind": "workflow",
      "mode": "capstone",
      "concepts": [
        "fmt",
        "pipelines",
        "redirection",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "cp",
        "tr"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-echo",
      "order": 201,
      "section": "05-composition-and-comparison",
      "command": "echo",
      "title": "Print a simple string",
      "about": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting.",
      "example": "echo 'hello linux'",
      "example_output": "hello linux\n",
      "breakdown": [
        "Quotes keep the words as one shell argument.",
        "echo appends a newline."
      ],
      "task": "Print the exact text practical linux.",
      "solution": "echo 'practical linux'",
      "hints": [
        "Quote the two-word string.",
        "Use the checker as a specification: The exact phrase was printed.",
        "Try this command shape, replacing placeholders with fixture values: echo <values>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The exact phrase was printed.",
          "expected": "practical linux",
          "normalize": "trim"
        }
      ],
      "completion": "echo is convenient for simple messages. Prefer printf when escapes, leading dashes, or exact formatting matter.",
      "command_order": 41,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "echo",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-echo-2",
      "section": "05-composition-and-comparison",
      "command": "echo",
      "title": "Suppress the trailing newline",
      "about": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. echo -n omits its usual final newline when output must continue on the same line.",
      "example": "echo -n 'prefix='",
      "example_output": "prefix=",
      "breakdown": [
        "echo is the featured command in the worked pattern.",
        "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. echo -n omits its usual final newline when output must continue on the same line.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print token=abc123 without a trailing newline.",
      "solution": "echo -n 'token=abc123'",
      "hints": [
        "Use the checker as a specification: The text was printed without an added newline.",
        "Try this command shape, replacing placeholders with fixture values: echo -n <value>",
        "Use man echo or echo --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The text was printed without an added newline.",
          "expected": "token=abc123",
          "normalize": "exact"
        }
      ],
      "adversarial_commands": [
        "echo 'token=abc123'"
      ],
      "completion": "echo -n omits its usual final newline when output must continue on the same line. You can now use this behavior as part of a larger command-line workflow.",
      "order": 202,
      "command_order": 41,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. echo -n omits its usual final newline when output must continue on the same line.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "echo",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-echo-3",
      "section": "05-composition-and-comparison",
      "command": "echo",
      "title": "Interpret escape sequences",
      "about": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. Bash echo -e interprets escapes such as newline and tab in a controlled string.",
      "example": "echo -e 'name\\tstatus'",
      "example_output": "name\tstatus",
      "breakdown": [
        "echo is the featured command in the worked pattern.",
        "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. Bash echo -e interprets escapes such as newline and tab in a controlled string.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print alpha, beta, and gamma on separate lines from one echo command.",
      "solution": "echo -e 'alpha\\nbeta\\ngamma'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: echo -e <value>",
        "Use man echo or echo --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Interpret escape sequences”.",
          "expected": "alpha\nbeta\ngamma",
          "normalize": "trim"
        }
      ],
      "completion": "Bash echo -e interprets escapes such as newline and tab in a controlled string. You can now use this behavior as part of a larger command-line workflow.",
      "order": 203,
      "command_order": 41,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. Bash echo -e interprets escapes such as newline and tab in a controlled string.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "echo",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-echo-4",
      "section": "05-composition-and-comparison",
      "command": "echo",
      "title": "Compose output from variables",
      "about": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. Quoted parameter expansion lets echo combine labels and shell values without accidental word splitting.",
      "example": "name=api; echo \"service=$name\"",
      "example_output": "service=api",
      "breakdown": [
        "echo is the featured command in the worked pattern.",
        "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. Quoted parameter expansion lets echo combine labels and shell values without accidental word splitting.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set service to web and port to 443, then print service=web port=443.",
      "solution": "service=web; port=443; echo \"service=$service port=$port\"",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <value> ; <value> ; echo <values>",
        "Use man echo or echo --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Compose output from variables”.",
          "expected": "service=web port=443",
          "normalize": "trim"
        }
      ],
      "completion": "Quoted parameter expansion lets echo combine labels and shell values without accidental word splitting. You can now use this behavior as part of a larger command-line workflow.",
      "order": 204,
      "command_order": 41,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. Quoted parameter expansion lets echo combine labels and shell values without accidental word splitting.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "echo",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-echo-5",
      "section": "05-composition-and-comparison",
      "command": "echo",
      "title": "Generate a simple environment file",
      "about": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. A grouped set of echo commands can emit a short reviewed configuration atomically through one redirection.",
      "example": "{ echo 'MODE=prod'; echo 'PORT=8080'; } > app.env",
      "example_output": "",
      "breakdown": [
        "echo is the featured command in the worked pattern.",
        "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. A grouped set of echo commands can emit a short reviewed configuration atomically through one redirection.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create service.env containing NAME=api, PORT=8080, and ENABLED=true on separate lines.",
      "solution": "{ echo 'NAME=api'; echo 'PORT=8080'; echo 'ENABLED=true'; } > service.env",
      "hints": [
        "Use the checker as a specification: service.env has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: { echo <value> ; echo <value> ; echo <value> ; } > <value>",
        "Use man echo or echo --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "file-content",
          "description": "service.env has the expected content.",
          "path": "service.env",
          "expected": "NAME=api\nPORT=8080\nENABLED=true\n"
        }
      ],
      "completion": "A grouped set of echo commands can emit a short reviewed configuration atomically through one redirection. You can now use this behavior as part of a larger command-line workflow.",
      "order": 205,
      "command_order": 41,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "echo prints arguments separated by spaces. Option and escape behavior can vary, so use printf for strict portable formatting. A grouped set of echo commands can emit a short reviewed configuration atomically through one redirection.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "echo",
        "redirection",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "env"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printf",
      "order": 206,
      "section": "05-composition-and-comparison",
      "command": "printf",
      "title": "Format output predictably",
      "about": "printf applies a format string to values and does not add a newline unless the format requests one.",
      "example": "printf '%s\\n' alpha beta",
      "example_output": "alpha\nbeta\n",
      "breakdown": [
        "%s formats one string at a time.",
        "\\n requests a newline.",
        "The format repeats for remaining values."
      ],
      "task": "Print red, green, and blue on separate lines with one printf command.",
      "solution": "printf '%s\\n' red green blue",
      "hints": [
        "Use %s followed by a newline escape.",
        "Use the checker as a specification: Each color was printed on its own line.",
        "Try this command shape, replacing placeholders with fixture values: printf <values>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "Each color was printed on its own line.",
          "expected": "red\ngreen\nblue",
          "normalize": "trim"
        }
      ],
      "completion": "Treat the format string as data you control. Do not use untrusted text as the printf format.",
      "command_order": 42,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "printf applies a format string to values and does not add a newline unless the format requests one.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "printf",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printf-2",
      "section": "05-composition-and-comparison",
      "command": "printf",
      "title": "Control field width",
      "about": "printf applies a format string to values and does not add a newline unless the format requests one. Width specifiers align values into predictable columns without a separate formatting tool.",
      "example": "printf '%-10s %5d\\n' api 42",
      "example_output": "api           42",
      "breakdown": [
        "printf is the featured command in the worked pattern.",
        "printf applies a format string to values and does not add a newline unless the format requests one. Width specifiers align values into predictable columns without a separate formatting tool.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print api left-aligned in width 10 and 8080 right-aligned in width 6.",
      "solution": "printf '%-10s %6d\\n' api 8080",
      "hints": [
        "Use the checker as a specification: The fields were aligned.",
        "Try this command shape, replacing placeholders with fixture values: printf <values>",
        "Use man printf or printf --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The fields were aligned.",
          "expected": "api          8080\n",
          "normalize": "exact"
        }
      ],
      "completion": "Width specifiers align values into predictable columns without a separate formatting tool. You can now use this behavior as part of a larger command-line workflow.",
      "order": 207,
      "command_order": 42,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "printf applies a format string to values and does not add a newline unless the format requests one. Width specifiers align values into predictable columns without a separate formatting tool.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "printf",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printf-3",
      "section": "05-composition-and-comparison",
      "command": "printf",
      "title": "Choose numeric precision",
      "about": "printf applies a format string to values and does not add a newline unless the format requests one. A precision on %f controls how many digits appear after the decimal point.",
      "example": "printf '%.2f\\n' 3.14159",
      "example_output": "3.14",
      "breakdown": [
        "printf is the featured command in the worked pattern.",
        "printf applies a format string to values and does not add a newline unless the format requests one. A precision on %f controls how many digits appear after the decimal point.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print 98.7654 rounded to two decimal places followed by a percent sign.",
      "solution": "printf '%.2f%%\\n' 98.7654",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: printf <values>",
        "Use man printf or printf --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Choose numeric precision”.",
          "expected": "98.77%",
          "normalize": "trim"
        }
      ],
      "completion": "A precision on %f controls how many digits appear after the decimal point. You can now use this behavior as part of a larger command-line workflow.",
      "order": 208,
      "command_order": 42,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "printf applies a format string to values and does not add a newline unless the format requests one. A precision on %f controls how many digits appear after the decimal point.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "printf",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printf-4",
      "section": "05-composition-and-comparison",
      "command": "printf",
      "title": "Reuse a format for many values",
      "about": "printf applies a format string to values and does not add a newline unless the format requests one. printf reuses its format string until all remaining arguments have been consumed.",
      "example": "printf '%s\\n' alpha beta gamma",
      "example_output": "alpha\nbeta\ngamma",
      "breakdown": [
        "printf is the featured command in the worked pattern.",
        "printf applies a format string to values and does not add a newline unless the format requests one. printf reuses its format string until all remaining arguments have been consumed.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print api=8080, db=5432, and web=443 using one reusable format string.",
      "solution": "printf '%s=%s\\n' api 8080 db 5432 web 443",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: printf <values>",
        "Use man printf or printf --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Reuse a format for many values”.",
          "expected": "api=8080\ndb=5432\nweb=443",
          "normalize": "trim"
        }
      ],
      "completion": "printf reuses its format string until all remaining arguments have been consumed. You can now use this behavior as part of a larger command-line workflow.",
      "order": 209,
      "command_order": 42,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "printf applies a format string to values and does not add a newline unless the format requests one. printf reuses its format string until all remaining arguments have been consumed.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "printf",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printf-5",
      "section": "05-composition-and-comparison",
      "command": "printf",
      "title": "Format a report from parsed records",
      "about": "printf applies a format string to values and does not add a newline unless the format requests one. read and printf combine parsing with consistent types, widths, and alignment in a shell workflow.",
      "example": "while IFS=, read -r name value; do printf '%-8s %4d\\n' \"$name\" \"$value\"; done < data.csv",
      "example_output": "api        42",
      "breakdown": [
        "printf is the featured command in the worked pattern.",
        "printf applies a format string to values and does not add a newline unless the format requests one. read and printf combine parsing with consistent types, widths, and alignment in a shell workflow.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read usage.csv and print each name left-aligned width 8, a space, and its value right-aligned width 4.",
      "solution": "while IFS=, read -r name value; do printf '%-8s %4d\\n' \"$name\" \"$value\"; done < usage.csv",
      "hints": [
        "Use the checker as a specification: The usage report was formatted.",
        "Try this command shape, replacing placeholders with fixture values: while <values> -r <values> ; do <values> ; done < <value>",
        "Use man printf or printf --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "usage.csv": "api,12\ndatabase,7\nweb,105\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The usage report was formatted.",
          "expected": "api        12\ndatabase    7\nweb       105\n",
          "normalize": "exact"
        }
      ],
      "completion": "read and printf combine parsing with consistent types, widths, and alignment in a shell workflow. You can now use this behavior as part of a larger command-line workflow.",
      "order": 210,
      "command_order": 42,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "printf applies a format string to values and does not add a newline unless the format requests one. read and printf combine parsing with consistent types, widths, and alignment in a shell workflow.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "printf",
        "redirection",
        "command chaining",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tee",
      "order": 211,
      "section": "05-composition-and-comparison",
      "command": "tee",
      "title": "See and save one stream",
      "about": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved.",
      "example": "printf 'verified\\n' | tee result.txt",
      "example_output": "verified\n",
      "breakdown": [
        "printf produces the stream.",
        "tee writes result.txt and passes the same text onward."
      ],
      "task": "Print ready and save the same line in status.txt.",
      "solution": "printf 'ready\\n' | tee status.txt",
      "hints": [
        "Pipe printf into tee and name the output file.",
        "Use the checker as a specification: ready remained visible on stdout. status.txt contains ready.",
        "Try this command shape, replacing placeholders with fixture values: printf <value> | tee <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "ready remained visible on stdout.",
          "expected": "ready",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "status.txt contains ready.",
          "path": "status.txt",
          "expected": "ready",
          "normalize": "trim"
        }
      ],
      "completion": "tee is not an audit guarantee; later stages can still fail. Preserve exit status and verification when the result matters.",
      "command_order": 43,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "tee",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tee-2",
      "section": "05-composition-and-comparison",
      "command": "tee",
      "title": "Append while displaying output",
      "about": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee -a appends to a file instead of replacing it while still forwarding the new input.",
      "example": "echo beta | tee -a log.txt",
      "example_output": "beta",
      "breakdown": [
        "tee is the featured command in the worked pattern.",
        "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee -a appends to a file instead of replacing it while still forwarding the new input.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Append deployed to events.log and also print deployed.",
      "solution": "echo deployed | tee -a events.log",
      "hints": [
        "Use the checker as a specification: The requested result was printed. events.log has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: echo <value> | tee -a <value>",
        "Use man tee or tee --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "events.log": "started\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Append while displaying output”.",
          "expected": "deployed",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "events.log has the expected content.",
          "path": "events.log",
          "expected": "started\ndeployed\n"
        }
      ],
      "completion": "tee -a appends to a file instead of replacing it while still forwarding the new input. You can now use this behavior as part of a larger command-line workflow.",
      "order": 212,
      "command_order": 43,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee -a appends to a file instead of replacing it while still forwarding the new input.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "tee",
        "pipelines"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tee-3",
      "section": "05-composition-and-comparison",
      "command": "tee",
      "title": "Write the same stream to several files",
      "about": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee accepts multiple file operands and writes an identical copy to each.",
      "example": "printf 'ready\\n' | tee one.txt two.txt",
      "example_output": "ready",
      "breakdown": [
        "tee is the featured command in the worked pattern.",
        "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee accepts multiple file operands and writes an identical copy to each.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Write release-2026 to primary.txt, backup.txt, and standard output.",
      "solution": "echo release-2026 | tee primary.txt backup.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed. primary.txt has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: echo <value> | tee <values>",
        "Use man tee or tee --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Write the same stream to several files”.",
          "expected": "release-2026",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "primary.txt has the expected content.",
          "path": "primary.txt",
          "expected": "release-2026\n"
        },
        {
          "type": "file-content",
          "description": "backup.txt has the expected content.",
          "path": "backup.txt",
          "expected": "release-2026\n"
        }
      ],
      "completion": "tee accepts multiple file operands and writes an identical copy to each. You can now use this behavior as part of a larger command-line workflow.",
      "order": 213,
      "command_order": 43,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee accepts multiple file operands and writes an identical copy to each.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "tee",
        "pipelines"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tee-4",
      "section": "05-composition-and-comparison",
      "command": "tee",
      "title": "Capture an intermediate pipeline stage",
      "about": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee can preserve intermediate data for debugging while allowing the pipeline to continue transforming it.",
      "example": "sort values.txt | tee sorted.txt | uniq",
      "example_output": "a\nb",
      "breakdown": [
        "tee is the featured command in the worked pattern.",
        "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee can preserve intermediate data for debugging while allowing the pipeline to continue transforming it.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sort names.txt, save the full sorted stream to sorted.txt, then print unique names.",
      "solution": "sort names.txt | tee sorted.txt | uniq",
      "hints": [
        "Use the checker as a specification: The requested result was printed. sorted.txt has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: sort <value> | tee <value> | uniq",
        "Use man tee or tee --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "names.txt": "bob\nalice\nbob\ncarol\nalice\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Capture an intermediate pipeline stage”.",
          "expected": "alice\nbob\ncarol",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "sorted.txt has the expected content.",
          "path": "sorted.txt",
          "expected": "alice\nalice\nbob\nbob\ncarol\n"
        }
      ],
      "completion": "tee can preserve intermediate data for debugging while allowing the pipeline to continue transforming it. You can now use this behavior as part of a larger command-line workflow.",
      "order": 214,
      "command_order": 43,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. tee can preserve intermediate data for debugging while allowing the pipeline to continue transforming it.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "tee",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort",
        "uniq"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tee-5",
      "section": "05-composition-and-comparison",
      "command": "tee",
      "title": "Publish a report and retain its source",
      "about": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. A pipeline ending in tee can make a generated report visible and persist the exact reviewed output.",
      "example": "awk -F, '{print $1 \"=\" $2}' data.csv | tee report.txt",
      "example_output": "api=up",
      "breakdown": [
        "tee is the featured command in the worked pattern.",
        "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. A pipeline ending in tee can make a generated report visible and persist the exact reviewed output.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Select failed jobs as name=owner rows, print them, and save the same rows to failed.txt.",
      "solution": "awk -F, '$2==\"failed\" {print $1 \"=\" $3}' jobs.csv | tee failed.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed. failed.txt has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: awk <values> | tee <value>",
        "Use man tee or tee --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "jobs.csv": "api,ok,platform\ndb,failed,data\nweb,ok,frontend\nbackup,failed,ops\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Publish a report and retain its source”.",
          "expected": "db=data\nbackup=ops",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "failed.txt has the expected content.",
          "path": "failed.txt",
          "expected": "db=data\nbackup=ops\n"
        }
      ],
      "completion": "A pipeline ending in tee can make a generated report visible and persist the exact reviewed output. You can now use this behavior as part of a larger command-line workflow.",
      "order": 215,
      "command_order": 43,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "tee copies standard input to standard output and one or more files. It is useful when a pipeline result must remain visible and be saved. A pipeline ending in tee can make a generated report visible and persist the exact reviewed output.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "tee",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xargs",
      "order": 216,
      "section": "05-composition-and-comparison",
      "command": "xargs",
      "title": "Build argument lists from input",
      "about": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input.",
      "example": "printf 'alpha\\nbeta\\n' | xargs -n 1 echo",
      "example_output": "alpha\nbeta\n",
      "breakdown": [
        "printf supplies two items.",
        "-n 1 invokes echo with one item at a time."
      ],
      "task": "Pass red, green, and blue to echo one item per invocation.",
      "solution": "printf 'red\\ngreen\\nblue\\n' | xargs -n 1 echo",
      "hints": [
        "Use -n 1 so each command receives one value.",
        "Use the checker as a specification: Each color was echoed on its own line.",
        "Try this command shape, replacing placeholders with fixture values: printf <value> | xargs -n <values>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "Each color was echoed on its own line.",
          "expected": "red\ngreen\nblue",
          "normalize": "trim"
        }
      ],
      "completion": "Use find -print0 with xargs -0 for arbitrary filenames. Whitespace-delimited input is not filename-safe.",
      "command_order": 44,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "xargs",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "echo",
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xargs-2",
      "section": "05-composition-and-comparison",
      "command": "xargs",
      "title": "Group a fixed number of arguments",
      "about": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. xargs -n limits how many input items are supplied to each command invocation.",
      "example": "printf '%s\\n' a b c d | xargs -n2 echo",
      "example_output": "a b\nc d",
      "breakdown": [
        "xargs is the featured command in the worked pattern.",
        "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. xargs -n limits how many input items are supplied to each command invocation.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read words.txt and print them in groups of three per line.",
      "solution": "xargs -n3 echo < words.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: xargs -n3 <value> < <value>",
        "Use man xargs or xargs --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "words.txt": "one\ntwo\nthree\nfour\nfive\nsix\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Group a fixed number of arguments”.",
          "expected": "one two three\nfour five six",
          "normalize": "trim"
        }
      ],
      "completion": "xargs -n limits how many input items are supplied to each command invocation. You can now use this behavior as part of a larger command-line workflow.",
      "order": 217,
      "command_order": 44,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. xargs -n limits how many input items are supplied to each command invocation.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "xargs",
        "redirection"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xargs-3",
      "section": "05-composition-and-comparison",
      "command": "xargs",
      "title": "Insert each item into a template",
      "about": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. xargs -I replaces a placeholder anywhere in a command template for each input record.",
      "example": "printf '%s\\n' api web | xargs -I{} echo service={}",
      "example_output": "service=api\nservice=web",
      "breakdown": [
        "xargs is the featured command in the worked pattern.",
        "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. xargs -I replaces a placeholder anywhere in a command template for each input record.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read names.txt and print user=<name> for each line.",
      "solution": "xargs -I{} echo 'user={}' < names.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: xargs <values> < <value>",
        "Use man xargs or xargs --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "names.txt": "alice\nbob\ncarol\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Insert each item into a template”.",
          "expected": "user=alice\nuser=bob\nuser=carol",
          "normalize": "trim"
        }
      ],
      "completion": "xargs -I replaces a placeholder anywhere in a command template for each input record. You can now use this behavior as part of a larger command-line workflow.",
      "order": 218,
      "command_order": 44,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. xargs -I replaces a placeholder anywhere in a command template for each input record.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "xargs",
        "redirection",
        "quoting"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xargs-4",
      "section": "05-composition-and-comparison",
      "command": "xargs",
      "title": "Handle filenames containing spaces",
      "about": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. Null-delimited input with xargs -0 preserves filenames exactly, including embedded spaces.",
      "example": "find docs -type f -print0 | xargs -0 wc -l",
      "example_output": "2 docs/one file.txt",
      "breakdown": [
        "xargs is the featured command in the worked pattern.",
        "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. Null-delimited input with xargs -0 preserves filenames exactly, including embedded spaces.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Find .txt files under docs with -print0 and pass them safely to wc -l.",
      "solution": "find docs -type f -name '*.txt' -print0 | sort -z | xargs -0 wc -l",
      "hints": [
        "Use the checker as a specification: The spaced first filename was processed. The spaced second filename was processed.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -type <value> -name <value> -print0 | sort -z | xargs -0 <value> -l",
        "Use man xargs or xargs --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "docs/one file.txt": "a\nb\n",
        "docs/two file.txt": "c\nd\ne\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The spaced first filename was processed.",
          "expected": "docs/one file.txt"
        },
        {
          "type": "stdout-contains",
          "description": "The spaced second filename was processed.",
          "expected": "docs/two file.txt"
        }
      ],
      "completion": "Null-delimited input with xargs -0 preserves filenames exactly, including embedded spaces. You can now use this behavior as part of a larger command-line workflow.",
      "order": 219,
      "command_order": 44,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. Null-delimited input with xargs -0 preserves filenames exactly, including embedded spaces.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "xargs",
        "pipelines",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "wc",
        "sort"
      ],
      "introduced_inline": [
        "find"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-xargs-5",
      "section": "05-composition-and-comparison",
      "command": "xargs",
      "title": "Build artifacts from a manifest",
      "about": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. A reviewed manifest plus xargs can apply the same creation command consistently to many items.",
      "example": "xargs -I{} sh -c 'mkdir -p output/{}' < names.txt",
      "example_output": "",
      "breakdown": [
        "xargs is the featured command in the worked pattern.",
        "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. A reviewed manifest plus xargs can apply the same creation command consistently to many items.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read directories.txt and use xargs with mkdir -p to create each path.",
      "solution": "xargs mkdir -p < directories.txt",
      "hints": [
        "Use the checker as a specification: build/api exists. build/web exists.",
        "Try this command shape, replacing placeholders with fixture values: xargs <value> -p < <value>",
        "Use man xargs or xargs --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "directories.txt": "build/api\nbuild/web\nbuild/database\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "build/api exists.",
          "path": "build/api",
          "kind": "directory"
        },
        {
          "type": "path-exists",
          "description": "build/web exists.",
          "path": "build/web",
          "kind": "directory"
        },
        {
          "type": "path-exists",
          "description": "build/database exists.",
          "path": "build/database",
          "kind": "directory"
        }
      ],
      "completion": "A reviewed manifest plus xargs can apply the same creation command consistently to many items. You can now use this behavior as part of a larger command-line workflow.",
      "order": 220,
      "command_order": 44,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "xargs converts input items into command arguments. Delimiters and filenames with spaces require deliberate handling such as NUL-delimited input. A reviewed manifest plus xargs can apply the same creation command consistently to many items.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "xargs",
        "redirection"
      ],
      "prerequisites": [
        "mkdir"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-diff",
      "order": 221,
      "section": "05-composition-and-comparison",
      "command": "diff",
      "title": "Show line-oriented differences",
      "about": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches.",
      "example": "diff -u old.txt new.txt",
      "example_output": "-alpha\n+beta\n",
      "breakdown": [
        "-u selects unified format.",
        "Exit status 1 means files differ, not that diff crashed."
      ],
      "task": "Print a unified comparison of old.txt and new.txt.",
      "solution": "diff -u old.txt new.txt",
      "hints": [
        "Use unified output with -u.",
        "Use the checker as a specification: The removed alpha line was shown. The added beta line was shown.",
        "Try this command shape, replacing placeholders with fixture values: diff -u <values>"
      ],
      "files": {
        "old.txt": "alpha\nshared\n",
        "new.txt": "beta\nshared\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The removed alpha line was shown.",
          "expected": "-alpha"
        },
        {
          "type": "stdout-contains",
          "description": "The added beta line was shown.",
          "expected": "+beta"
        },
        {
          "type": "exit-code",
          "description": "diff reported that files differ.",
          "expected": 1
        }
      ],
      "completion": "diff exit 1 means a difference was found. Always interpret a command's documented exit-status contract.",
      "command_order": 45,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "diff"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": false,
      "check_strength": "permissive"
    },
    {
      "id": "linux-diff-2",
      "section": "05-composition-and-comparison",
      "command": "diff",
      "title": "Produce a unified diff",
      "about": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -u adds file headers and contextual hunks in the format commonly reviewed as a patch.",
      "example": "diff -u old.conf new.conf",
      "example_output": "--- old.conf\n+++ new.conf\n@@ ...",
      "breakdown": [
        "diff is the featured command in the worked pattern.",
        "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -u adds file headers and contextual hunks in the format commonly reviewed as a patch.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print a unified diff between old.conf and new.conf.",
      "solution": "diff -u old.conf new.conf || true",
      "hints": [
        "Use the checker as a specification: The removed setting was shown. The added setting was shown.",
        "Try this command shape, replacing placeholders with fixture values: diff -u <values> | | true",
        "Use man diff or diff --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "old.conf": "mode=dev\nport=8080\n",
        "new.conf": "mode=prod\nport=8080\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The removed setting was shown.",
          "expected": "-mode=dev"
        },
        {
          "type": "stdout-contains",
          "description": "The added setting was shown.",
          "expected": "+mode=prod"
        }
      ],
      "completion": "diff -u adds file headers and contextual hunks in the format commonly reviewed as a patch. You can now use this behavior as part of a larger command-line workflow.",
      "order": 222,
      "command_order": 45,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -u adds file headers and contextual hunks in the format commonly reviewed as a patch.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "diff",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-diff-3",
      "section": "05-composition-and-comparison",
      "command": "diff",
      "title": "Compare files side by side",
      "about": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -y presents old and new lines in adjacent columns for a visual review.",
      "example": "diff -y old.txt new.txt",
      "example_output": "same  same\nold | new",
      "breakdown": [
        "diff is the featured command in the worked pattern.",
        "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -y presents old and new lines in adjacent columns for a visual review.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compare before.txt and after.txt side by side.",
      "solution": "diff -y before.txt after.txt || true",
      "hints": [
        "Use the checker as a specification: The changed uppercase line was shown.",
        "Try this command shape, replacing placeholders with fixture values: diff -y <values> | | true",
        "Use man diff or diff --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "before.txt": "alpha\nbeta\ngamma\n",
        "after.txt": "alpha\nBETA\ngamma\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The changed uppercase line was shown.",
          "expected": "BETA"
        }
      ],
      "completion": "diff -y presents old and new lines in adjacent columns for a visual review. You can now use this behavior as part of a larger command-line workflow.",
      "order": 223,
      "command_order": 45,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -y presents old and new lines in adjacent columns for a visual review.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "diff",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-diff-4",
      "section": "05-composition-and-comparison",
      "command": "diff",
      "title": "Report only whether files differ",
      "about": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -q suppresses line details and reports only whether two files differ.",
      "example": "diff -q a.txt b.txt",
      "example_output": "Files a.txt and b.txt differ",
      "breakdown": [
        "diff is the featured command in the worked pattern.",
        "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -q suppresses line details and reports only whether two files differ.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use diff -q to report whether expected.txt and actual.txt differ.",
      "solution": "diff -q expected.txt actual.txt || true",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: diff -q <values> | | true",
        "Use man diff or diff --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "expected.txt": "ready\n",
        "actual.txt": "pending\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Report only whether files differ”.",
          "expected": "Files expected.txt and actual.txt differ",
          "normalize": "trim"
        }
      ],
      "completion": "diff -q suppresses line details and reports only whether two files differ. You can now use this behavior as part of a larger command-line workflow.",
      "order": 224,
      "command_order": 45,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -q suppresses line details and reports only whether two files differ.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "diff",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-diff-5",
      "section": "05-composition-and-comparison",
      "command": "diff",
      "title": "Compare directory trees recursively",
      "about": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -r descends corresponding directory trees and reports changed or missing files.",
      "example": "diff -r baseline candidate",
      "example_output": "diff -r baseline/app.conf candidate/app.conf ...",
      "breakdown": [
        "diff is the featured command in the worked pattern.",
        "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -r descends corresponding directory trees and reports changed or missing files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Recursively compare baseline and candidate and show every difference.",
      "solution": "diff -r baseline candidate || true",
      "hints": [
        "Use the checker as a specification: The changed configuration was reported. The candidate-only file was reported.",
        "Try this command shape, replacing placeholders with fixture values: diff -r <values> | | true",
        "Use man diff or diff --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "baseline/app.conf": "mode=prod\n",
        "baseline/keep.txt": "same\n",
        "candidate/app.conf": "mode=stage\n",
        "candidate/keep.txt": "same\n",
        "candidate/new.txt": "new\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The changed configuration was reported.",
          "expected": "app.conf"
        },
        {
          "type": "stdout-contains",
          "description": "The candidate-only file was reported.",
          "expected": "new.txt"
        }
      ],
      "completion": "diff -r descends corresponding directory trees and reports changed or missing files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 225,
      "command_order": 45,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "diff compares files line by line. Unified format (-u) provides context and is widely used in reviews and patches. diff -r descends corresponding directory trees and reports changed or missing files.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "diff",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-cmp",
      "order": 226,
      "section": "05-composition-and-comparison",
      "command": "cmp",
      "title": "Compare files byte for byte",
      "about": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality.",
      "example": "cmp -s first.bin second.bin",
      "example_output": "",
      "breakdown": [
        "-s selects silent mode.",
        "Exit 0 means the byte sequences match."
      ],
      "task": "Silently verify that first.txt and second.txt contain identical bytes, then print identical.",
      "solution": "cmp -s first.txt second.txt && echo identical",
      "hints": [
        "Use cmp silent mode.",
        "Use the checker as a specification: Equality was confirmed after the silent comparison. cmp confirmed byte equality.",
        "Try this command shape, replacing placeholders with fixture values: cmp -s <values> && echo <value>"
      ],
      "files": {
        "first.txt": "identical\n",
        "second.txt": "identical\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Equality was confirmed after the silent comparison.",
          "expected": "identical",
          "normalize": "trim"
        },
        {
          "type": "exit-code",
          "description": "cmp confirmed byte equality.",
          "expected": 0
        }
      ],
      "completion": "cmp proves equality of the observed bytes, not that the files are safe, trusted, or correctly named.",
      "command_order": 46,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "cmp",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-cmp-2",
      "section": "05-composition-and-comparison",
      "command": "cmp",
      "title": "Silently test equality",
      "about": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. cmp -s emits nothing and communicates equality through its exit status.",
      "example": "cmp -s expected.bin actual.bin && echo match",
      "example_output": "match",
      "breakdown": [
        "cmp is the featured command in the worked pattern.",
        "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. cmp -s emits nothing and communicates equality through its exit status.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use cmp -s and print identical when copy.bin matches original.bin.",
      "solution": "cmp -s original.bin copy.bin && echo identical",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cmp -s <values> && echo <value>",
        "Use man cmp or cmp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "original.bin": "same bytes\n",
        "copy.bin": "same bytes\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Silently test equality”.",
          "expected": "identical",
          "normalize": "trim"
        }
      ],
      "completion": "cmp -s emits nothing and communicates equality through its exit status. You can now use this behavior as part of a larger command-line workflow.",
      "order": 227,
      "command_order": 46,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. cmp -s emits nothing and communicates equality through its exit status.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "cmp",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cmp-3",
      "section": "05-composition-and-comparison",
      "command": "cmp",
      "title": "Report the first differing byte",
      "about": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. Default cmp output identifies the first byte and line where two files differ.",
      "example": "cmp old.bin new.bin",
      "example_output": "old.bin new.bin differ: byte 5, line 1",
      "breakdown": [
        "cmp is the featured command in the worked pattern.",
        "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. Default cmp output identifies the first byte and line where two files differ.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compare expected.bin and actual.bin and print the first-difference report.",
      "solution": "cmp expected.bin actual.bin || true",
      "hints": [
        "Use the checker as a specification: The first differing byte was reported.",
        "Try this command shape, replacing placeholders with fixture values: cmp <values> | | true",
        "Use man cmp or cmp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "expected.bin": "HEAD-ONE\n",
        "actual.bin": "HEAD-TWO\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The first differing byte was reported.",
          "expected": "differ: byte"
        }
      ],
      "completion": "Default cmp output identifies the first byte and line where two files differ. You can now use this behavior as part of a larger command-line workflow.",
      "order": 228,
      "command_order": 46,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. Default cmp output identifies the first byte and line where two files differ.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "cmp",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-cmp-4",
      "section": "05-composition-and-comparison",
      "command": "cmp",
      "title": "List every differing byte",
      "about": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. cmp -l prints the position and byte values for every difference.",
      "example": "cmp -l a.bin b.bin",
      "example_output": "2 102 130",
      "breakdown": [
        "cmp is the featured command in the worked pattern.",
        "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. cmp -l prints the position and byte values for every difference.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use cmp -l to list all byte differences between a.bin and b.bin.",
      "solution": "cmp -l a.bin b.bin || true",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cmp -l <values> | | true",
        "Use man cmp or cmp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "a.bin": "ABCDEF",
        "b.bin": "AXCYEZ"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “List every differing byte”.",
          "expected": "2 102 130\n4 104 131\n6 106 132",
          "normalize": "trim"
        }
      ],
      "completion": "cmp -l prints the position and byte values for every difference. You can now use this behavior as part of a larger command-line workflow.",
      "order": 229,
      "command_order": 46,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. cmp -l prints the position and byte values for every difference.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "cmp",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cmp-5",
      "section": "05-composition-and-comparison",
      "command": "cmp",
      "title": "Validate only a fixed header",
      "about": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. The -n limit compares only an initial byte range, useful when validating a header independently of payload data.",
      "example": "cmp -s -n 4 one.bin two.bin && echo header-ok",
      "example_output": "header-ok",
      "breakdown": [
        "cmp is the featured command in the worked pattern.",
        "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. The -n limit compares only an initial byte range, useful when validating a header independently of payload data.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compare only the first six bytes of expected.bin and actual.bin and print header-ok when they match.",
      "solution": "cmp -s -n 6 expected.bin actual.bin && echo header-ok",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: cmp -s -n <values> && echo header-ok",
        "Use man cmp or cmp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "expected.bin": "HEADERpayload-one",
        "actual.bin": "HEADERdifferent-payload"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Validate only a fixed header”.",
          "expected": "header-ok",
          "normalize": "trim"
        }
      ],
      "completion": "The -n limit compares only an initial byte range, useful when validating a header independently of payload data. You can now use this behavior as part of a larger command-line workflow.",
      "order": 230,
      "command_order": 46,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "cmp compares two files byte by byte. -s suppresses output so the exit status alone reports equality. The -n limit compares only an initial byte range, useful when validating a header independently of payload data.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "cmp",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-comm",
      "order": 231,
      "section": "05-composition-and-comparison",
      "command": "comm",
      "title": "Compare sorted sets of lines",
      "about": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both.",
      "example": "comm -12 left.txt right.txt",
      "example_output": "beta\n",
      "breakdown": [
        "-1 suppresses first-only lines.",
        "-2 suppresses second-only lines.",
        "Only common lines remain."
      ],
      "task": "Print only lines common to allowed.txt and observed.txt.",
      "solution": "comm -12 allowed.txt observed.txt",
      "hints": [
        "Both inputs are already sorted; suppress columns 1 and 2.",
        "Use the checker as a specification: Only common values were printed.",
        "Try this command shape, replacing placeholders with fixture values: comm -12 <values>"
      ],
      "files": {
        "allowed.txt": "alpha\nbeta\ndelta\n",
        "observed.txt": "beta\ndelta\ngamma\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only common values were printed.",
          "expected": "beta\ndelta",
          "normalize": "trim"
        }
      ],
      "completion": "comm assumes sorted inputs under the same locale. Violating that precondition can silently mislead.",
      "command_order": 47,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "comm"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-comm-2",
      "section": "05-composition-and-comparison",
      "command": "comm",
      "title": "Print values common to both files",
      "about": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. comm -12 suppresses the two unique columns and leaves only shared sorted lines.",
      "example": "comm -12 old.txt new.txt",
      "example_output": "shared",
      "breakdown": [
        "comm is the featured command in the worked pattern.",
        "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. comm -12 suppresses the two unique columns and leaves only shared sorted lines.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print package names present in both required.txt and installed.txt.",
      "solution": "comm -12 required.txt installed.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: comm -12 <values>",
        "Use man comm or comm --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "required.txt": "bash\ncurl\njq\ntar\n",
        "installed.txt": "bash\ngrep\njq\nsed\ntar\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print values common to both files”.",
          "expected": "bash\njq\ntar",
          "normalize": "trim"
        }
      ],
      "completion": "comm -12 suppresses the two unique columns and leaves only shared sorted lines. You can now use this behavior as part of a larger command-line workflow.",
      "order": 232,
      "command_order": 47,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. comm -12 suppresses the two unique columns and leaves only shared sorted lines.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "comm"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-comm-3",
      "section": "05-composition-and-comparison",
      "command": "comm",
      "title": "Print values only in the first file",
      "about": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. comm -23 suppresses the second-file-only and common columns.",
      "example": "comm -23 desired.txt actual.txt",
      "example_output": "missing",
      "breakdown": [
        "comm is the featured command in the worked pattern.",
        "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. comm -23 suppresses the second-file-only and common columns.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print users listed in desired.txt but absent from actual.txt.",
      "solution": "comm -23 desired.txt actual.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: comm -23 <values>",
        "Use man comm or comm --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "desired.txt": "alice\nbob\ncarol\ndave\n",
        "actual.txt": "alice\ncarol\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print values only in the first file”.",
          "expected": "bob\ndave",
          "normalize": "trim"
        }
      ],
      "completion": "comm -23 suppresses the second-file-only and common columns. You can now use this behavior as part of a larger command-line workflow.",
      "order": 233,
      "command_order": 47,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. comm -23 suppresses the second-file-only and common columns.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "comm"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-comm-4",
      "section": "05-composition-and-comparison",
      "command": "comm",
      "title": "Compare unsorted inputs safely",
      "about": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. Process substitution can sort each source before comm performs its required ordered comparison.",
      "example": "comm -3 <(sort one.txt) <(sort two.txt)",
      "example_output": "only-one\n\tonly-two",
      "breakdown": [
        "comm is the featured command in the worked pattern.",
        "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. Process substitution can sort each source before comm performs its required ordered comparison.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the differences between unsorted blue.txt and green.txt.",
      "solution": "comm -3 <(sort blue.txt) <(sort green.txt)",
      "hints": [
        "Use the checker as a specification: The blue-only value was shown. The green-only value was shown.",
        "Try this command shape, replacing placeholders with fixture values: comm -3 < <values> < <values>",
        "Use man comm or comm --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "blue.txt": "api\ndb\ncache\n",
        "green.txt": "web\napi\ncache\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The blue-only value was shown.",
          "expected": "db"
        },
        {
          "type": "stdout-contains",
          "description": "The green-only value was shown.",
          "expected": "web"
        }
      ],
      "completion": "Process substitution can sort each source before comm performs its required ordered comparison. You can now use this behavior as part of a larger command-line workflow.",
      "order": 234,
      "command_order": 47,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. Process substitution can sort each source before comm performs its required ordered comparison.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "comm",
        "redirection",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-comm-5",
      "section": "05-composition-and-comparison",
      "command": "comm",
      "title": "Create a missing-and-extra audit",
      "about": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. Two comm projections can label missing desired values and extra actual values in one reconciliation report.",
      "example": "comm -23 desired actual | sed 's/^/missing: /'; comm -13 desired actual | sed 's/^/extra: /'",
      "example_output": "missing: db\nextra: cache",
      "breakdown": [
        "comm is the featured command in the worked pattern.",
        "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. Two comm projections can label missing desired values and extra actual values in one reconciliation report.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compare sorted desired.txt and actual.txt, printing missing: and extra: labels.",
      "solution": "comm -23 desired.txt actual.txt | sed 's/^/missing: /'; comm -13 desired.txt actual.txt | sed 's/^/extra: /'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: comm -23 <values> | sed <values> ; comm -13 <values> | sed <values>",
        "Use man comm or comm --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "desired.txt": "api\ndb\nweb\n",
        "actual.txt": "api\ncache\nweb\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Create a missing-and-extra audit”.",
          "expected": "missing: db\nextra: cache",
          "normalize": "trim"
        }
      ],
      "completion": "Two comm projections can label missing desired values and extra actual values in one reconciliation report. You can now use this behavior as part of a larger command-line workflow.",
      "order": 235,
      "command_order": 47,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "comm compares two sorted files in three columns: unique to first, unique to second, and common to both. Two comm projections can label missing desired values and extra actual values in one reconciliation report.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "comm",
        "pipelines",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "sed"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-join",
      "order": 236,
      "section": "05-composition-and-comparison",
      "command": "join",
      "title": "Join records by a key",
      "about": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join.",
      "example": "join -t, users.csv roles.csv",
      "example_output": "1,alice,analyst\n",
      "breakdown": [
        "-t, selects comma delimiter.",
        "The first field is the default join key."
      ],
      "task": "Join users.csv and roles.csv by their first comma-delimited field.",
      "solution": "join -t, users.csv roles.csv",
      "hints": [
        "Both files are sorted by field 1, so the default key works.",
        "Use the checker as a specification: Every user was joined to a role.",
        "Try this command shape, replacing placeholders with fixture values: join <values>"
      ],
      "files": {
        "users.csv": "1,alice\n2,bob\n3,carol\n",
        "roles.csv": "1,analyst\n2,developer\n3,operator\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Every user was joined to a role.",
          "expected": "1,alice,analyst\n2,bob,developer\n3,carol,operator",
          "normalize": "trim"
        }
      ],
      "completion": "join expects sorted keys. Use a format-aware data tool when quoting, escaping, or complex joins matter.",
      "command_order": 48,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "join"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "users"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-join-2",
      "section": "05-composition-and-comparison",
      "command": "join",
      "title": "Join on a shared key",
      "about": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. join combines sorted records whose first fields match.",
      "example": "join users.txt roles.txt",
      "example_output": "1 alice admin",
      "breakdown": [
        "join is the featured command in the worked pattern.",
        "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. join combines sorted records whose first fields match.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Join services.txt and ports.txt on their first field.",
      "solution": "join services.txt ports.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: join <values>",
        "Use man join or join --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services.txt": "api enabled\ndb disabled\nweb enabled\n",
        "ports.txt": "api 8080\ndb 5432\nweb 443\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Join on a shared key”.",
          "expected": "api enabled 8080\ndb disabled 5432\nweb enabled 443",
          "normalize": "trim"
        }
      ],
      "completion": "join combines sorted records whose first fields match. You can now use this behavior as part of a larger command-line workflow.",
      "order": 237,
      "command_order": 48,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. join combines sorted records whose first fields match.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "join"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-join-3",
      "section": "05-composition-and-comparison",
      "command": "join",
      "title": "Join comma-delimited records",
      "about": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. join -t sets both the input and output field delimiter.",
      "example": "join -t, users.csv roles.csv",
      "example_output": "1,alice,admin",
      "breakdown": [
        "join is the featured command in the worked pattern.",
        "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. join -t sets both the input and output field delimiter.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Join inventory.csv and owners.csv using comma as the delimiter.",
      "solution": "join -t, inventory.csv owners.csv",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: join <values>",
        "Use man join or join --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "inventory.csv": "api,up\ndb,down\nweb,up\n",
        "owners.csv": "api,platform\ndb,data\nweb,frontend\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Join comma-delimited records”.",
          "expected": "api,up,platform\ndb,down,data\nweb,up,frontend",
          "normalize": "trim"
        }
      ],
      "completion": "join -t sets both the input and output field delimiter. You can now use this behavior as part of a larger command-line workflow.",
      "order": 238,
      "command_order": 48,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. join -t sets both the input and output field delimiter.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "join"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-join-4",
      "section": "05-composition-and-comparison",
      "command": "join",
      "title": "Include unmatched records",
      "about": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. The -a option includes unpairable lines from one or both inputs instead of dropping them.",
      "example": "join -a1 -a2 one.txt two.txt",
      "example_output": "shared ...\nonly-one ...\nonly-two ...",
      "breakdown": [
        "join is the featured command in the worked pattern.",
        "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. The -a option includes unpairable lines from one or both inputs instead of dropping them.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Join desired.txt and actual.txt while including unmatched keys from both files.",
      "solution": "join -a1 -a2 desired.txt actual.txt",
      "hints": [
        "Use the checker as a specification: The desired-only database row remained. The actual-only cache row remained.",
        "Try this command shape, replacing placeholders with fixture values: join -a1 -a2 <values>",
        "Use man join or join --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "desired.txt": "api required\ndb required\nweb required\n",
        "actual.txt": "api running\ncache running\nweb stopped\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The desired-only database row remained.",
          "expected": "db required"
        },
        {
          "type": "stdout-contains",
          "description": "The actual-only cache row remained.",
          "expected": "cache running"
        }
      ],
      "completion": "The -a option includes unpairable lines from one or both inputs instead of dropping them. You can now use this behavior as part of a larger command-line workflow.",
      "order": 239,
      "command_order": 48,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. The -a option includes unpairable lines from one or both inputs instead of dropping them.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "join"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-join-5",
      "section": "05-composition-and-comparison",
      "command": "join",
      "title": "Join unsorted data and select output fields",
      "about": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. Sorted process substitutions satisfy join ordering, while -o chooses and reorders fields in the result.",
      "example": "join -t, -o 1.1,1.2,2.2 <(sort data.csv) <(sort owners.csv)",
      "example_output": "api,up,platform",
      "breakdown": [
        "join is the featured command in the worked pattern.",
        "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. Sorted process substitutions satisfy join ordering, while -o chooses and reorders fields in the result.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sort and join services.csv with owners.csv, printing name, owner, then status.",
      "solution": "join -t, -o 1.1,2.2,1.2 <(sort services.csv) <(sort owners.csv)",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: join <value> -o <value> < <values> < <values>",
        "Use man join or join --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services.csv": "web,up\napi,up\ndb,down\n",
        "owners.csv": "db,data\nweb,frontend\napi,platform\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Join unsorted data and select output fields”.",
          "expected": "api,platform,up\ndb,data,down\nweb,frontend,up",
          "normalize": "trim"
        }
      ],
      "completion": "Sorted process substitutions satisfy join ordering, while -o chooses and reorders fields in the result. You can now use this behavior as part of a larger command-line workflow.",
      "order": 240,
      "command_order": 48,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "join combines lines from sorted files when a selected key matches. It is a text-file analogue of a relational join. Sorted process substitutions satisfy join ordering, while -o chooses and reorders fields in the result.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "join",
        "redirection",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-split",
      "order": 241,
      "section": "05-composition-and-comparison",
      "command": "split",
      "title": "Split a file into fixed-size chunks",
      "about": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes.",
      "example": "split -l 2 events.txt chunk-",
      "example_output": "",
      "breakdown": [
        "-l 2 places two lines in each part.",
        "chunk- is the output prefix."
      ],
      "task": "Split events.txt into two-line files with prefix part-.",
      "solution": "split -l 2 events.txt part-",
      "hints": [
        "Use -l 2 and provide an output prefix.",
        "Use the checker as a specification: part-aa contains the first two events. part-ac contains the final event.",
        "Try this command shape, replacing placeholders with fixture values: split -l <values>"
      ],
      "files": {
        "events.txt": "one\ntwo\nthree\nfour\nfive\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "part-aa contains the first two events.",
          "path": "part-aa",
          "expected": "one\ntwo",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "part-ac contains the final event.",
          "path": "part-ac",
          "expected": "five",
          "normalize": "trim"
        }
      ],
      "completion": "Record the split rule and naming pattern so the parts can be reassembled and verified correctly.",
      "command_order": 49,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "split"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-split-2",
      "section": "05-composition-and-comparison",
      "command": "split",
      "title": "Split by line count",
      "about": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. split -l creates chunks containing a fixed maximum number of lines.",
      "example": "split -l 2 records.txt chunk-",
      "example_output": "",
      "breakdown": [
        "split is the featured command in the worked pattern.",
        "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. split -l creates chunks containing a fixed maximum number of lines.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Split records.txt into two-line files with prefix part-.",
      "solution": "split -l 2 records.txt part-",
      "hints": [
        "Use the checker as a specification: part-aa has the expected content. part-ab has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: split -l <values>",
        "Use man split or split --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "records.txt": "one\ntwo\nthree\nfour\nfive\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "part-aa has the expected content.",
          "path": "part-aa",
          "expected": "one\ntwo\n"
        },
        {
          "type": "file-content",
          "description": "part-ab has the expected content.",
          "path": "part-ab",
          "expected": "three\nfour\n"
        },
        {
          "type": "file-content",
          "description": "part-ac has the expected content.",
          "path": "part-ac",
          "expected": "five\n"
        }
      ],
      "completion": "split -l creates chunks containing a fixed maximum number of lines. You can now use this behavior as part of a larger command-line workflow.",
      "order": 242,
      "command_order": 49,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. split -l creates chunks containing a fixed maximum number of lines.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "split"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-split-3",
      "section": "05-composition-and-comparison",
      "command": "split",
      "title": "Use numeric suffixes",
      "about": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. The -d option names output chunks with numeric suffixes that sort naturally in many workflows.",
      "example": "split -d -l 2 data.txt chunk-",
      "example_output": "",
      "breakdown": [
        "split is the featured command in the worked pattern.",
        "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. The -d option names output chunks with numeric suffixes that sort naturally in many workflows.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Split data.txt into three-line chunks named chunk-00, chunk-01, and so on.",
      "solution": "split -d -l 3 data.txt chunk-",
      "hints": [
        "Use the checker as a specification: chunk-00 has the expected content. chunk-01 has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: split -d -l <values>",
        "Use man split or split --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "data.txt": "1\n2\n3\n4\n5\n6\n7\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "chunk-00 has the expected content.",
          "path": "chunk-00",
          "expected": "1\n2\n3\n"
        },
        {
          "type": "file-content",
          "description": "chunk-01 has the expected content.",
          "path": "chunk-01",
          "expected": "4\n5\n6\n"
        },
        {
          "type": "file-content",
          "description": "chunk-02 has the expected content.",
          "path": "chunk-02",
          "expected": "7\n"
        }
      ],
      "completion": "The -d option names output chunks with numeric suffixes that sort naturally in many workflows. You can now use this behavior as part of a larger command-line workflow.",
      "order": 243,
      "command_order": 49,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. The -d option names output chunks with numeric suffixes that sort naturally in many workflows.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "split"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-split-4",
      "section": "05-composition-and-comparison",
      "command": "split",
      "title": "Split by byte size",
      "about": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. split -b caps each output chunk by bytes rather than line boundaries.",
      "example": "split -b 4 payload.bin piece-",
      "example_output": "",
      "breakdown": [
        "split is the featured command in the worked pattern.",
        "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. split -b caps each output chunk by bytes rather than line boundaries.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Split payload.bin into four-byte chunks with prefix piece-.",
      "solution": "split -b 4 payload.bin piece-",
      "hints": [
        "Use the checker as a specification: piece-aa has the expected content. piece-ab has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: split -b <values>",
        "Use man split or split --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "payload.bin": "ABCDEFGHIJ"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "piece-aa has the expected content.",
          "path": "piece-aa",
          "expected": "ABCD"
        },
        {
          "type": "file-content",
          "description": "piece-ab has the expected content.",
          "path": "piece-ab",
          "expected": "EFGH"
        },
        {
          "type": "file-content",
          "description": "piece-ac has the expected content.",
          "path": "piece-ac",
          "expected": "IJ"
        }
      ],
      "completion": "split -b caps each output chunk by bytes rather than line boundaries. You can now use this behavior as part of a larger command-line workflow.",
      "order": 244,
      "command_order": 49,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. split -b caps each output chunk by bytes rather than line boundaries.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "split"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-split-5",
      "section": "05-composition-and-comparison",
      "command": "split",
      "title": "Create named CSV batches",
      "about": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. --additional-suffix gives every chunk a meaningful extension while numeric suffixes preserve batch order.",
      "example": "split -d -l 100 --additional-suffix=.csv export.csv batch-",
      "example_output": "",
      "breakdown": [
        "split is the featured command in the worked pattern.",
        "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. --additional-suffix gives every chunk a meaningful extension while numeric suffixes preserve batch order.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Split export.csv into two-line batches named batch-00.csv, batch-01.csv, and batch-02.csv.",
      "solution": "split -d -l 2 --additional-suffix=.csv export.csv batch-",
      "hints": [
        "Use the checker as a specification: batch-00.csv has the expected content. batch-01.csv has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: split -d -l <values>",
        "Use man split or split --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "export.csv": "a,1\nb,2\nc,3\nd,4\ne,5\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "batch-00.csv has the expected content.",
          "path": "batch-00.csv",
          "expected": "a,1\nb,2\n"
        },
        {
          "type": "file-content",
          "description": "batch-01.csv has the expected content.",
          "path": "batch-01.csv",
          "expected": "c,3\nd,4\n"
        },
        {
          "type": "file-content",
          "description": "batch-02.csv has the expected content.",
          "path": "batch-02.csv",
          "expected": "e,5\n"
        }
      ],
      "completion": "--additional-suffix gives every chunk a meaningful extension while numeric suffixes preserve batch order. You can now use this behavior as part of a larger command-line workflow.",
      "order": 245,
      "command_order": 49,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "split divides input into output files by line count or byte size. Output names use a supplied prefix plus generated suffixes. --additional-suffix gives every chunk a meaningful extension while numeric suffixes preserve batch order.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "split"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-csplit",
      "order": 246,
      "section": "05-composition-and-comparison",
      "command": "csplit",
      "title": "Split at a content position",
      "about": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure.",
      "example": "csplit -s sections.txt 3",
      "example_output": "",
      "breakdown": [
        "-s suppresses byte counts.",
        "3 starts the second output at line 3."
      ],
      "task": "Split sections.txt before line 3 without printing byte counts.",
      "solution": "csplit -s sections.txt 3",
      "hints": [
        "Use silent mode and the line number 3.",
        "Use the checker as a specification: xx00 contains the first section. xx01 contains the second section.",
        "Try this command shape, replacing placeholders with fixture values: csplit -s <values>"
      ],
      "files": {
        "sections.txt": "alpha-1\nalpha-2\nbeta-1\nbeta-2\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "xx00 contains the first section.",
          "path": "xx00",
          "expected": "alpha-1\nalpha-2",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "xx01 contains the second section.",
          "path": "xx01",
          "expected": "beta-1\nbeta-2",
          "normalize": "trim"
        }
      ],
      "completion": "csplit boundaries are structural assumptions. Inspect the resulting parts before treating them as complete records.",
      "command_order": 50,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "csplit"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-csplit-2",
      "section": "05-composition-and-comparison",
      "command": "csplit",
      "title": "Split at a matching line",
      "about": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. A csplit /pattern/ operand starts a new output file immediately before the matching line.",
      "example": "csplit document.txt '/^SECTION 2$/'",
      "example_output": "...byte counts...",
      "breakdown": [
        "csplit is the featured command in the worked pattern.",
        "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. A csplit /pattern/ operand starts a new output file immediately before the matching line.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Split document.txt before the SECTION B line.",
      "solution": "csplit -s document.txt '/^SECTION B$/'",
      "hints": [
        "Use the checker as a specification: xx00 has the expected content. xx01 has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: csplit -s <values>",
        "Use man csplit or csplit --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "document.txt": "SECTION A\none\ntwo\nSECTION B\nthree\nfour\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "xx00 has the expected content.",
          "path": "xx00",
          "expected": "SECTION A\none\ntwo\n"
        },
        {
          "type": "file-content",
          "description": "xx01 has the expected content.",
          "path": "xx01",
          "expected": "SECTION B\nthree\nfour\n"
        }
      ],
      "completion": "A csplit /pattern/ operand starts a new output file immediately before the matching line. You can now use this behavior as part of a larger command-line workflow.",
      "order": 247,
      "command_order": 50,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. A csplit /pattern/ operand starts a new output file immediately before the matching line.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "csplit",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-csplit-3",
      "section": "05-composition-and-comparison",
      "command": "csplit",
      "title": "Repeat a split pattern",
      "about": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. The {*} repeat operand applies the preceding pattern until no further matches remain.",
      "example": "csplit log.txt '/^---$/' '{*}'",
      "example_output": "...byte counts...",
      "breakdown": [
        "csplit is the featured command in the worked pattern.",
        "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. The {*} repeat operand applies the preceding pattern until no further matches remain.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Split chapters.txt before every CHAPTER heading.",
      "solution": "csplit -s chapters.txt '/^CHAPTER/' '{*}'",
      "hints": [
        "Use the checker as a specification: xx00 has the expected content. xx01 has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: csplit -s <values>",
        "Use man csplit or csplit --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "chapters.txt": "PREFACE\nintro\nCHAPTER ONE\nalpha\nCHAPTER TWO\nbeta\nCHAPTER THREE\ngamma\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "xx00 has the expected content.",
          "path": "xx00",
          "expected": "PREFACE\nintro\n"
        },
        {
          "type": "file-content",
          "description": "xx01 has the expected content.",
          "path": "xx01",
          "expected": "CHAPTER ONE\nalpha\n"
        },
        {
          "type": "file-content",
          "description": "xx02 has the expected content.",
          "path": "xx02",
          "expected": "CHAPTER TWO\nbeta\n"
        },
        {
          "type": "file-content",
          "description": "xx03 has the expected content.",
          "path": "xx03",
          "expected": "CHAPTER THREE\ngamma\n"
        }
      ],
      "completion": "The {*} repeat operand applies the preceding pattern until no further matches remain. You can now use this behavior as part of a larger command-line workflow.",
      "order": 248,
      "command_order": 50,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. The {*} repeat operand applies the preceding pattern until no further matches remain.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "csplit",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-csplit-4",
      "section": "05-composition-and-comparison",
      "command": "csplit",
      "title": "Choose output names and suffix width",
      "about": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. The -f prefix and -n digit count create predictable chunk names for downstream processing.",
      "example": "csplit -s -f part- -n 3 data.txt '/^MARK$/'",
      "example_output": "",
      "breakdown": [
        "csplit is the featured command in the worked pattern.",
        "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. The -f prefix and -n digit count create predictable chunk names for downstream processing.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Split data.txt at MARK and name outputs segment-000 and segment-001.",
      "solution": "csplit -s -f segment- -n 3 data.txt '/^MARK$/'",
      "hints": [
        "Use the checker as a specification: segment-000 has the expected content. segment-001 has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: csplit -s -f <value> -n <values>",
        "Use man csplit or csplit --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "data.txt": "before\nMARK\nafter\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "segment-000 has the expected content.",
          "path": "segment-000",
          "expected": "before\n"
        },
        {
          "type": "file-content",
          "description": "segment-001 has the expected content.",
          "path": "segment-001",
          "expected": "MARK\nafter\n"
        }
      ],
      "completion": "The -f prefix and -n digit count create predictable chunk names for downstream processing. You can now use this behavior as part of a larger command-line workflow.",
      "order": 249,
      "command_order": 50,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. The -f prefix and -n digit count create predictable chunk names for downstream processing.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "csplit",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-csplit-5",
      "section": "05-composition-and-comparison",
      "command": "csplit",
      "title": "Create one file per timestamped block",
      "about": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. A repeated regular-expression split can turn a combined stream into independently processable records.",
      "example": "csplit -s -f event- log.txt '/^BEGIN /' '{*}'",
      "example_output": "",
      "breakdown": [
        "csplit is the featured command in the worked pattern.",
        "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. A repeated regular-expression split can turn a combined stream into independently processable records.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Split events.log before every BEGIN line, using event- as the output prefix and two-digit suffixes. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "csplit -s -f event- events.log '/^BEGIN /' '{*}' && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: event-00 has the expected content. event-01 has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: csplit -s -f <values>",
        "Use man csplit or csplit --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "events.log": "metadata\nBEGIN 09:00\napi up\nBEGIN 10:00\ndb down\nBEGIN 11:00\nweb up\n",
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "event-00 has the expected content.",
          "path": "event-00",
          "expected": "metadata\n"
        },
        {
          "type": "file-content",
          "description": "event-01 has the expected content.",
          "path": "event-01",
          "expected": "BEGIN 09:00\napi up\n"
        },
        {
          "type": "file-content",
          "description": "event-02 has the expected content.",
          "path": "event-02",
          "expected": "BEGIN 10:00\ndb down\n"
        },
        {
          "type": "file-content",
          "description": "event-03 has the expected content.",
          "path": "event-03",
          "expected": "BEGIN 11:00\nweb up\n"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "A repeated regular-expression split can turn a combined stream into independently processable records. You can now use this behavior as part of a larger command-line workflow.",
      "order": 250,
      "command_order": 50,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "csplit divides a file at line numbers or matching contexts. Unlike split, boundaries can follow content structure. A repeated regular-expression split can turn a combined stream into independently processable records.",
      "stage_kind": "transfer",
      "mode": "capstone",
      "concepts": [
        "csplit",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "cp"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chmod",
      "order": 251,
      "section": "06-identity-and-permissions",
      "command": "chmod",
      "title": "Set private permission bits",
      "about": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions.",
      "example": "chmod 600 private.txt",
      "example_output": "",
      "breakdown": [
        "6 is owner read plus write.",
        "Both zeros remove group and other mode permissions."
      ],
      "task": "Set secret.txt to mode 600.",
      "solution": "chmod 600 secret.txt",
      "hints": [
        "Pass numeric mode 600 before the filename.",
        "Use the checker as a specification: secret.txt has mode 600.",
        "Try this command shape, replacing placeholders with fixture values: chmod <values>"
      ],
      "files": {
        "secret.txt": "synthetic secret\n"
      },
      "checks": [
        {
          "type": "file-mode",
          "description": "secret.txt has mode 600.",
          "path": "secret.txt",
          "expected": "600"
        }
      ],
      "completion": "Mode bits are one layer. Ownership, ACLs, elevated users, and directory permissions also affect access.",
      "command_order": 51,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "chmod"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chmod-2",
      "section": "06-identity-and-permissions",
      "command": "chmod",
      "title": "Add execute permission symbolically",
      "about": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. Symbolic modes such as u+x modify selected permission bits without replacing the others.",
      "example": "chmod u+x script.sh",
      "example_output": "",
      "breakdown": [
        "chmod is the featured command in the worked pattern.",
        "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. Symbolic modes such as u+x modify selected permission bits without replacing the others.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Add execute permission for the owner of deploy.sh while keeping its other bits.",
      "solution": "chmod u+x deploy.sh",
      "hints": [
        "Use the checker as a specification: deploy.sh has mode 744.",
        "Try this command shape, replacing placeholders with fixture values: chmod <values>",
        "Use man chmod or chmod --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "deploy.sh": "#!/bin/sh\n"
      },
      "checks": [
        {
          "type": "file-mode",
          "description": "deploy.sh has mode 744.",
          "path": "deploy.sh",
          "expected": "744"
        }
      ],
      "completion": "Symbolic modes such as u+x modify selected permission bits without replacing the others. You can now use this behavior as part of a larger command-line workflow.",
      "order": 252,
      "command_order": 51,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. Symbolic modes such as u+x modify selected permission bits without replacing the others.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "chmod"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chmod-3",
      "section": "06-identity-and-permissions",
      "command": "chmod",
      "title": "Assign different permissions by class",
      "about": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. A comma-separated symbolic mode can set owner, group, and other permissions in one operation.",
      "example": "chmod u=rw,g=r,o= config.ini",
      "example_output": "",
      "breakdown": [
        "chmod is the featured command in the worked pattern.",
        "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. A comma-separated symbolic mode can set owner, group, and other permissions in one operation.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set app.conf so the owner can read/write, the group can read, and others have no access.",
      "solution": "chmod u=rw,g=r,o= app.conf",
      "hints": [
        "Use the checker as a specification: app.conf has mode 640.",
        "Try this command shape, replacing placeholders with fixture values: chmod <values>",
        "Use man chmod or chmod --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.conf": "secret=true\n"
      },
      "checks": [
        {
          "type": "file-mode",
          "description": "app.conf has mode 640.",
          "path": "app.conf",
          "expected": "640"
        }
      ],
      "completion": "A comma-separated symbolic mode can set owner, group, and other permissions in one operation. You can now use this behavior as part of a larger command-line workflow.",
      "order": 253,
      "command_order": 51,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. A comma-separated symbolic mode can set owner, group, and other permissions in one operation.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "chmod"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chmod-4",
      "section": "06-identity-and-permissions",
      "command": "chmod",
      "title": "Change a tree recursively",
      "about": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. chmod -R applies a mode through a directory tree; use it only on a precisely scoped path.",
      "example": "chmod -R go-rwx private",
      "example_output": "",
      "breakdown": [
        "chmod is the featured command in the worked pattern.",
        "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. chmod -R applies a mode through a directory tree; use it only on a precisely scoped path.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Recursively remove all group and other permissions from private.",
      "solution": "chmod -R go-rwx private",
      "hints": [
        "Use the checker as a specification: private/one.txt has mode 600. private/nested/two.txt has mode 600.",
        "Try this command shape, replacing placeholders with fixture values: chmod -R go-rwx <value>",
        "Use man chmod or chmod --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "private/one.txt": "one\n",
        "private/nested/two.txt": "two\n"
      },
      "checks": [
        {
          "type": "file-mode",
          "description": "private/one.txt has mode 600.",
          "path": "private/one.txt",
          "expected": "600"
        },
        {
          "type": "file-mode",
          "description": "private/nested/two.txt has mode 600.",
          "path": "private/nested/two.txt",
          "expected": "600"
        }
      ],
      "completion": "chmod -R applies a mode through a directory tree; use it only on a precisely scoped path. You can now use this behavior as part of a larger command-line workflow.",
      "order": 254,
      "command_order": 51,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. chmod -R applies a mode through a directory tree; use it only on a precisely scoped path.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "chmod"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chmod-5",
      "section": "06-identity-and-permissions",
      "command": "chmod",
      "title": "Apply a reference permission model",
      "about": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. chmod --reference copies exact mode bits from a reviewed template file.",
      "example": "chmod --reference=template.sh deploy.sh",
      "example_output": "",
      "breakdown": [
        "chmod is the featured command in the worked pattern.",
        "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. chmod --reference copies exact mode bits from a reviewed template file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set template.sh to 750, then apply its permissions to api.sh, web.sh, and worker.sh.",
      "solution": "chmod 750 template.sh && chmod --reference=template.sh api.sh web.sh worker.sh",
      "hints": [
        "Use the checker as a specification: api.sh has mode 750. web.sh has mode 750.",
        "Try this command shape, replacing placeholders with fixture values: chmod <values> && chmod <values>",
        "Use man chmod or chmod --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "template.sh": "",
        "api.sh": "",
        "web.sh": "",
        "worker.sh": ""
      },
      "checks": [
        {
          "type": "file-mode",
          "description": "api.sh has mode 750.",
          "path": "api.sh",
          "expected": "750"
        },
        {
          "type": "file-mode",
          "description": "web.sh has mode 750.",
          "path": "web.sh",
          "expected": "750"
        },
        {
          "type": "file-mode",
          "description": "worker.sh has mode 750.",
          "path": "worker.sh",
          "expected": "750"
        }
      ],
      "completion": "chmod --reference copies exact mode bits from a reviewed template file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 255,
      "command_order": 51,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "chmod changes POSIX mode bits. Numeric 600 grants owner read/write and no group or other permissions. chmod --reference copies exact mode bits from a reviewed template file.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "chmod",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chown",
      "order": 256,
      "section": "06-identity-and-permissions",
      "command": "chown",
      "title": "Apply owner metadata by reference",
      "about": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name.",
      "example": "chown --reference=reference.txt target.txt",
      "example_output": "",
      "breakdown": [
        "reference.txt supplies ownership metadata.",
        "target.txt receives it."
      ],
      "task": "Apply reference.txt ownership to target.txt.",
      "solution": "chown --reference=reference.txt target.txt",
      "hints": [
        "Use GNU chown's --reference option.",
        "Use the checker as a specification: target.txt has the reference owner. target.txt has the reference group.",
        "Try this command shape, replacing placeholders with fixture values: chown <values>"
      ],
      "files": {
        "reference.txt": "owner source\n",
        "target.txt": "target\n"
      },
      "setup": [
        "chown nobody:nogroup reference.txt"
      ],
      "checks": [
        {
          "type": "same-owner",
          "description": "target.txt has the reference owner.",
          "path": "target.txt",
          "other_path": "reference.txt"
        },
        {
          "type": "same-group",
          "description": "target.txt has the reference group.",
          "path": "target.txt",
          "other_path": "reference.txt"
        }
      ],
      "completion": "Changing ownership can require privilege when the requested owner differs. This rep remains within the current synthetic owner.",
      "command_order": 52,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "chown"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chown-2",
      "section": "06-identity-and-permissions",
      "command": "chown",
      "title": "Change a file owner by name",
      "about": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown accepts an account name and resolves it through the local user database.",
      "example": "chown nobody artifact.txt",
      "example_output": "",
      "breakdown": [
        "chown is the featured command in the worked pattern.",
        "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown accepts an account name and resolves it through the local user database.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Change artifact.txt owner to nobody and print its owner name.",
      "solution": "chown nobody artifact.txt && stat -c '%U' artifact.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: chown <values> && stat -c <values>",
        "Use man chown or chown --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact.txt": "data\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Change a file owner by name”.",
          "expected": "nobody",
          "normalize": "trim"
        }
      ],
      "completion": "chown accepts an account name and resolves it through the local user database. You can now use this behavior as part of a larger command-line workflow.",
      "order": 257,
      "command_order": 52,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown accepts an account name and resolves it through the local user database.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "chown",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "stat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chown-3",
      "section": "06-identity-and-permissions",
      "command": "chown",
      "title": "Change owner and group together",
      "about": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. The owner:group form updates both ownership fields in one chown operation.",
      "example": "chown nobody:nogroup cache.dat",
      "example_output": "",
      "breakdown": [
        "chown is the featured command in the worked pattern.",
        "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. The owner:group form updates both ownership fields in one chown operation.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Change cache.dat to owner nobody and group nogroup, then print owner:group.",
      "solution": "chown nobody:nogroup cache.dat && stat -c '%U:%G' cache.dat",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: chown <values> && stat -c <values>",
        "Use man chown or chown --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "cache.dat": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Change owner and group together”.",
          "expected": "nobody:nogroup",
          "normalize": "trim"
        }
      ],
      "completion": "The owner:group form updates both ownership fields in one chown operation. You can now use this behavior as part of a larger command-line workflow.",
      "order": 258,
      "command_order": 52,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. The owner:group form updates both ownership fields in one chown operation.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "chown",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "stat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chown-4",
      "section": "06-identity-and-permissions",
      "command": "chown",
      "title": "Apply ownership recursively",
      "about": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown -R updates descendants throughout a precisely named directory tree.",
      "example": "chown -R nobody:nogroup shared",
      "example_output": "",
      "breakdown": [
        "chown is the featured command in the worked pattern.",
        "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown -R updates descendants throughout a precisely named directory tree.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Recursively assign nobody:nogroup to every file under shared.",
      "solution": "chown -R nobody:nogroup shared",
      "hints": [
        "Use the checker as a specification: shared/a.txt has the reference owner. shared/a.txt has the reference group.",
        "Try this command shape, replacing placeholders with fixture values: chown -R <values>",
        "Use man chown or chown --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "shared/a.txt": "",
        "shared/nested/b.txt": "",
        ".expected-owner": ""
      },
      "setup": [
        "chown nobody:nogroup .expected-owner"
      ],
      "checks": [
        {
          "type": "same-owner",
          "description": "shared/a.txt has the reference owner.",
          "path": "shared/a.txt",
          "other_path": ".expected-owner"
        },
        {
          "type": "same-group",
          "description": "shared/a.txt has the reference group.",
          "path": "shared/a.txt",
          "other_path": ".expected-owner"
        },
        {
          "type": "same-owner",
          "description": "shared/nested/b.txt has the reference owner.",
          "path": "shared/nested/b.txt",
          "other_path": ".expected-owner"
        },
        {
          "type": "same-group",
          "description": "shared/nested/b.txt has the reference group.",
          "path": "shared/nested/b.txt",
          "other_path": ".expected-owner"
        }
      ],
      "completion": "chown -R updates descendants throughout a precisely named directory tree. You can now use this behavior as part of a larger command-line workflow.",
      "order": 259,
      "command_order": 52,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown -R updates descendants throughout a precisely named directory tree.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "chown"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chown-5",
      "section": "06-identity-and-permissions",
      "command": "chown",
      "title": "Restrict changes to an expected owner",
      "about": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown --from changes an item only when its current owner and group match an expected state.",
      "example": "chown --from=root:root nobody:nogroup file.txt",
      "example_output": "",
      "breakdown": [
        "chown is the featured command in the worked pattern.",
        "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown --from changes an item only when its current owner and group match an expected state.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use --from=root:root to change approved.txt to nobody:nogroup while leaving already-nobody.txt unchanged, then print both owners.",
      "solution": "chown nobody:nogroup already-nobody.txt && chown --from=root:root nobody:nogroup approved.txt already-nobody.txt && stat -c '%n=%U:%G' approved.txt already-nobody.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: chown <values> && chown --from=root:root <values> && stat -c <values>",
        "Use man chown or chown --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "approved.txt": "",
        "already-nobody.txt": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Restrict changes to an expected owner”.",
          "expected": "approved.txt=nobody:nogroup\nalready-nobody.txt=nobody:nogroup",
          "normalize": "trim"
        }
      ],
      "completion": "chown --from changes an item only when its current owner and group match an expected state. You can now use this behavior as part of a larger command-line workflow.",
      "order": 260,
      "command_order": 52,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "chown changes file ownership. --reference copies owner and group metadata from a known file without hard-coding an account name. chown --from changes an item only when its current owner and group match an expected state.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "chown",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "stat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chgrp",
      "order": 261,
      "section": "06-identity-and-permissions",
      "command": "chgrp",
      "title": "Apply group metadata by reference",
      "about": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name.",
      "example": "chgrp --reference=reference.txt target.txt",
      "example_output": "",
      "breakdown": [
        "The reference supplies group metadata.",
        "The target group is updated."
      ],
      "task": "Apply reference.txt group ownership to target.txt.",
      "solution": "chgrp --reference=reference.txt target.txt",
      "hints": [
        "Use the --reference option.",
        "Use the checker as a specification: target.txt has the reference group.",
        "Try this command shape, replacing placeholders with fixture values: chgrp <values>"
      ],
      "files": {
        "reference.txt": "group source\n",
        "target.txt": "target\n"
      },
      "setup": [
        "chgrp nogroup reference.txt"
      ],
      "checks": [
        {
          "type": "same-group",
          "description": "target.txt has the reference group.",
          "path": "target.txt",
          "other_path": "reference.txt"
        }
      ],
      "completion": "Group membership and directory inheritance determine whether a group assignment produces the intended effective access.",
      "command_order": 53,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "chgrp"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chgrp-2",
      "section": "06-identity-and-permissions",
      "command": "chgrp",
      "title": "Change a file group by name",
      "about": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp resolves a group name and assigns that group without changing the owner.",
      "example": "chgrp nogroup report.txt",
      "example_output": "",
      "breakdown": [
        "chgrp is the featured command in the worked pattern.",
        "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp resolves a group name and assigns that group without changing the owner.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Change report.txt group to nogroup and print its group name.",
      "solution": "chgrp nogroup report.txt && stat -c '%G' report.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: chgrp <values> && stat -c <values>",
        "Use man chgrp or chgrp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "report.txt": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Change a file group by name”.",
          "expected": "nogroup",
          "normalize": "trim"
        }
      ],
      "completion": "chgrp resolves a group name and assigns that group without changing the owner. You can now use this behavior as part of a larger command-line workflow.",
      "order": 262,
      "command_order": 53,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp resolves a group name and assigns that group without changing the owner.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "chgrp",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "stat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chgrp-3",
      "section": "06-identity-and-permissions",
      "command": "chgrp",
      "title": "Change several files together",
      "about": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp accepts multiple paths so a reviewed batch can receive one group consistently.",
      "example": "chgrp nogroup one.txt two.txt",
      "example_output": "",
      "breakdown": [
        "chgrp is the featured command in the worked pattern.",
        "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp accepts multiple paths so a reviewed batch can receive one group consistently.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Assign nogroup to api.conf, db.conf, and web.conf, then print each name:group.",
      "solution": "chgrp nogroup api.conf db.conf web.conf && stat -c '%n:%G' api.conf db.conf web.conf",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: chgrp <values> && stat -c <values>",
        "Use man chgrp or chgrp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "api.conf": "",
        "db.conf": "",
        "web.conf": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Change several files together”.",
          "expected": "api.conf:nogroup\ndb.conf:nogroup\nweb.conf:nogroup",
          "normalize": "trim"
        }
      ],
      "completion": "chgrp accepts multiple paths so a reviewed batch can receive one group consistently. You can now use this behavior as part of a larger command-line workflow.",
      "order": 263,
      "command_order": 53,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp accepts multiple paths so a reviewed batch can receive one group consistently.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "chgrp",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "stat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chgrp-4",
      "section": "06-identity-and-permissions",
      "command": "chgrp",
      "title": "Apply group ownership recursively",
      "about": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp -R updates every descendant under one carefully scoped directory.",
      "example": "chgrp -R nogroup shared",
      "example_output": "",
      "breakdown": [
        "chgrp is the featured command in the worked pattern.",
        "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp -R updates every descendant under one carefully scoped directory.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Recursively assign group nogroup to files below shared.",
      "solution": "chgrp -R nogroup shared",
      "hints": [
        "Use the checker as a specification: shared/one.txt has the reference group. shared/nested/two.txt has the reference group.",
        "Try this command shape, replacing placeholders with fixture values: chgrp -R <values>",
        "Use man chgrp or chgrp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "shared/one.txt": "",
        "shared/nested/two.txt": "",
        ".expected-group": ""
      },
      "setup": [
        "chgrp nogroup .expected-group"
      ],
      "checks": [
        {
          "type": "same-group",
          "description": "shared/one.txt has the reference group.",
          "path": "shared/one.txt",
          "other_path": ".expected-group"
        },
        {
          "type": "same-group",
          "description": "shared/nested/two.txt has the reference group.",
          "path": "shared/nested/two.txt",
          "other_path": ".expected-group"
        }
      ],
      "completion": "chgrp -R updates every descendant under one carefully scoped directory. You can now use this behavior as part of a larger command-line workflow.",
      "order": 264,
      "command_order": 53,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp -R updates every descendant under one carefully scoped directory.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "chgrp"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-chgrp-5",
      "section": "06-identity-and-permissions",
      "command": "chgrp",
      "title": "Mirror a reference group across artifacts",
      "about": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp --reference copies the group from a reviewed reference to one or more targets.",
      "example": "chgrp --reference=template one two",
      "example_output": "",
      "breakdown": [
        "chgrp is the featured command in the worked pattern.",
        "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp --reference copies the group from a reviewed reference to one or more targets.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Copy template's group to release.tar, checksums.txt, and manifest.json.",
      "solution": "chgrp --reference=template release.tar checksums.txt manifest.json",
      "hints": [
        "Use the checker as a specification: release.tar has the reference group. checksums.txt has the reference group.",
        "Try this command shape, replacing placeholders with fixture values: chgrp --reference=template <values>",
        "Use man chgrp or chgrp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "template": "",
        "release.tar": "",
        "checksums.txt": "",
        "manifest.json": ""
      },
      "setup": [
        "chgrp nogroup template"
      ],
      "checks": [
        {
          "type": "same-group",
          "description": "release.tar has the reference group.",
          "path": "release.tar",
          "other_path": "template"
        },
        {
          "type": "same-group",
          "description": "checksums.txt has the reference group.",
          "path": "checksums.txt",
          "other_path": "template"
        },
        {
          "type": "same-group",
          "description": "manifest.json has the reference group.",
          "path": "manifest.json",
          "other_path": "template"
        }
      ],
      "completion": "chgrp --reference copies the group from a reviewed reference to one or more targets. You can now use this behavior as part of a larger command-line workflow.",
      "order": 265,
      "command_order": 53,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "chgrp changes a file's group. --reference copies the group from another file and avoids embedding a local group name. chgrp --reference copies the group from a reviewed reference to one or more targets.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "chgrp"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "tar"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-umask",
      "order": 266,
      "section": "06-identity-and-permissions",
      "command": "umask",
      "title": "Control default creation permissions",
      "about": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell.",
      "example": "umask 077 && touch private.txt",
      "example_output": "",
      "breakdown": [
        "077 masks all group and other bits.",
        "The following file is created with mode 600 under normal base permissions."
      ],
      "task": "Set umask 077 and create private.txt in the same shell command.",
      "solution": "umask 077 && touch private.txt",
      "hints": [
        "Set the mask, then use && before touch.",
        "Use the checker as a specification: private.txt was created with mode 600.",
        "Try this command shape, replacing placeholders with fixture values: umask <value> && touch <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "file-mode",
          "description": "private.txt was created with mode 600.",
          "path": "private.txt",
          "expected": "600"
        }
      ],
      "completion": "umask subtracts permissions from creation defaults; it does not add bits or retroactively change existing paths.",
      "command_order": 54,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "umask",
        "command chaining"
      ],
      "prerequisites": [
        "touch"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-umask-2",
      "section": "06-identity-and-permissions",
      "command": "umask",
      "title": "Read the symbolic mask",
      "about": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. umask -S expresses the effective creation permissions symbolically instead of as removed octal bits.",
      "example": "umask 027; umask -S",
      "example_output": "u=rwx,g=rx,o=",
      "breakdown": [
        "umask is the featured command in the worked pattern.",
        "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. umask -S expresses the effective creation permissions symbolically instead of as removed octal bits.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set umask 027 and print it in symbolic form.",
      "solution": "umask 027 && umask -S",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: umask <value> && umask -S",
        "Use man umask or umask --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Read the symbolic mask”.",
          "expected": "u=rwx,g=rx,o=",
          "normalize": "trim"
        }
      ],
      "completion": "umask -S expresses the effective creation permissions symbolically instead of as removed octal bits. You can now use this behavior as part of a larger command-line workflow.",
      "order": 267,
      "command_order": 54,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. umask -S expresses the effective creation permissions symbolically instead of as removed octal bits.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "umask",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-umask-3",
      "section": "06-identity-and-permissions",
      "command": "umask",
      "title": "Create a group-readable file",
      "about": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. A 027 mask produces regular files with mode 640 from the normal 666 creation base.",
      "example": "umask 027; touch shared.txt",
      "example_output": "",
      "breakdown": [
        "umask is the featured command in the worked pattern.",
        "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. A 027 mask produces regular files with mode 640 from the normal 666 creation base.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set umask 027 and create shared.txt.",
      "solution": "umask 027 && touch shared.txt",
      "hints": [
        "Use the checker as a specification: shared.txt has mode 640.",
        "Try this command shape, replacing placeholders with fixture values: umask <value> && touch <value>",
        "Use man umask or umask --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "file-mode",
          "description": "shared.txt has mode 640.",
          "path": "shared.txt",
          "expected": "640"
        }
      ],
      "completion": "A 027 mask produces regular files with mode 640 from the normal 666 creation base. You can now use this behavior as part of a larger command-line workflow.",
      "order": 268,
      "command_order": 54,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. A 027 mask produces regular files with mode 640 from the normal 666 creation base.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "umask",
        "command chaining"
      ],
      "prerequisites": [
        "touch"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-umask-4",
      "section": "06-identity-and-permissions",
      "command": "umask",
      "title": "Compare file and directory defaults",
      "about": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. The same umask applies to different creation bases: files begin at 666 and directories at 777.",
      "example": "umask 027; touch file; mkdir dir",
      "example_output": "",
      "breakdown": [
        "umask is the featured command in the worked pattern.",
        "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. The same umask applies to different creation bases: files begin at 666 and directories at 777.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "With umask 027, create report.txt and reports, then print their numeric modes.",
      "solution": "umask 027 && touch report.txt && mkdir reports && stat -c '%a' report.txt reports",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: umask <value> && touch <value> && mkdir <value> && stat -c <values>",
        "Use man umask or umask --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Compare file and directory defaults”.",
          "expected": "640\n750",
          "normalize": "trim"
        }
      ],
      "completion": "The same umask applies to different creation bases: files begin at 666 and directories at 777. You can now use this behavior as part of a larger command-line workflow.",
      "order": 269,
      "command_order": 54,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. The same umask applies to different creation bases: files begin at 666 and directories at 777.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "umask",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "mkdir",
        "touch",
        "stat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-umask-5",
      "section": "06-identity-and-permissions",
      "command": "umask",
      "title": "Limit a restrictive mask to a subshell",
      "about": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. A subshell contains a temporary umask so later commands in the parent shell retain their normal default.",
      "example": "(umask 077; touch private); touch public",
      "example_output": "",
      "breakdown": [
        "umask is the featured command in the worked pattern.",
        "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. A subshell contains a temporary umask so later commands in the parent shell retain their normal default.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create private.txt with mask 077 in a subshell, then create normal.txt with the gym default, and print both modes.",
      "solution": "(umask 077; touch private.txt); touch normal.txt; stat -c '%a' private.txt normal.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <values> ; touch <value> ; touch <value> ; stat -c <values>",
        "Use man umask or umask --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Limit a restrictive mask to a subshell”.",
          "expected": "600\n644",
          "normalize": "trim"
        }
      ],
      "completion": "A subshell contains a temporary umask so later commands in the parent shell retain their normal default. You can now use this behavior as part of a larger command-line workflow.",
      "order": 270,
      "command_order": 54,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "umask removes permission bits from newly created files and directories. It affects future creation in the current shell. A subshell contains a temporary umask so later commands in the parent shell retain their normal default.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "umask",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "touch",
        "stat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-id",
      "order": 271,
      "section": "06-identity-and-permissions",
      "command": "id",
      "title": "Inspect numeric identity context",
      "about": "id reports user and group identity. -u emits only the effective numeric user ID.",
      "example": "id -u",
      "example_output": "1000\n",
      "breakdown": [
        "-u selects the user ID.",
        "The result is numeric and environment-specific."
      ],
      "task": "Print only your effective numeric user ID.",
      "solution": "id -u",
      "hints": [
        "Use id's user-only option.",
        "Use the checker as a specification: A numeric user ID was printed.",
        "Try this command shape, replacing placeholders with fixture values: id -u"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "A numeric user ID was printed.",
          "expected": "[0-9]+"
        }
      ],
      "completion": "Names are labels resolved through identity databases. Authorization decisions ultimately use numeric IDs and groups.",
      "command_order": 55,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "id reports user and group identity. -u emits only the effective numeric user ID.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "id"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-id-2",
      "section": "06-identity-and-permissions",
      "command": "id",
      "title": "Print the effective username",
      "about": "id reports user and group identity. -u emits only the effective numeric user ID. id -un combines the user-only and name-output options.",
      "example": "id -un",
      "example_output": "root",
      "breakdown": [
        "id is the featured command in the worked pattern.",
        "id reports user and group identity. -u emits only the effective numeric user ID. id -un combines the user-only and name-output options.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only your effective user name with id.",
      "solution": "id -un",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: id -un",
        "Use man id or id --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print the effective username”.",
          "expected": "root",
          "normalize": "trim"
        }
      ],
      "completion": "id -un combines the user-only and name-output options. You can now use this behavior as part of a larger command-line workflow.",
      "order": 272,
      "command_order": 55,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "id reports user and group identity. -u emits only the effective numeric user ID. id -un combines the user-only and name-output options.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "id"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-id-3",
      "section": "06-identity-and-permissions",
      "command": "id",
      "title": "Inspect a named account",
      "about": "id reports user and group identity. -u emits only the effective numeric user ID. Passing a username queries that account rather than the current process identity.",
      "example": "id nobody",
      "example_output": "uid=65534(nobody) gid=65534(nogroup) groups=65534(nogroup)",
      "breakdown": [
        "id is the featured command in the worked pattern.",
        "id reports user and group identity. -u emits only the effective numeric user ID. Passing a username queries that account rather than the current process identity.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the identity record for nobody.",
      "solution": "id nobody",
      "hints": [
        "Use the checker as a specification: The nobody account was identified. Its primary group was included.",
        "Try this command shape, replacing placeholders with fixture values: id <value>",
        "Use man id or id --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The nobody account was identified.",
          "expected": "nobody"
        },
        {
          "type": "stdout-contains",
          "description": "Its primary group was included.",
          "expected": "nogroup"
        }
      ],
      "completion": "Passing a username queries that account rather than the current process identity. You can now use this behavior as part of a larger command-line workflow.",
      "order": 273,
      "command_order": 55,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "id reports user and group identity. -u emits only the effective numeric user ID. Passing a username queries that account rather than the current process identity.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "id"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-id-4",
      "section": "06-identity-and-permissions",
      "command": "id",
      "title": "Print group names only",
      "about": "id reports user and group identity. -u emits only the effective numeric user ID. id -Gn lists supplementary and primary group names without numeric IDs.",
      "example": "id -Gn nobody",
      "example_output": "nogroup",
      "breakdown": [
        "id is the featured command in the worked pattern.",
        "id reports user and group identity. -u emits only the effective numeric user ID. id -Gn lists supplementary and primary group names without numeric IDs.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the group names for nobody.",
      "solution": "id -Gn nobody",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: id -Gn <value>",
        "Use man id or id --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print group names only”.",
          "expected": "nogroup",
          "normalize": "trim"
        }
      ],
      "completion": "id -Gn lists supplementary and primary group names without numeric IDs. You can now use this behavior as part of a larger command-line workflow.",
      "order": 274,
      "command_order": 55,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "id reports user and group identity. -u emits only the effective numeric user ID. id -Gn lists supplementary and primary group names without numeric IDs.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "id"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-id-5",
      "section": "06-identity-and-permissions",
      "command": "id",
      "title": "Build a compact identity record",
      "about": "id reports user and group identity. -u emits only the effective numeric user ID. Several focused id queries can be composed into a stable user, UID, group, and GID report.",
      "example": "printf 'user=%s uid=%s\\n' \"$(id -un)\" \"$(id -u)\"",
      "example_output": "user=root uid=0",
      "breakdown": [
        "id is the featured command in the worked pattern.",
        "id reports user and group identity. -u emits only the effective numeric user ID. Several focused id queries can be composed into a stable user, UID, group, and GID report.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print user=root uid=0 group=root gid=0 using id substitutions.",
      "solution": "printf 'user=%s uid=%s group=%s gid=%s\\n' \"$(id -un)\" \"$(id -u)\" \"$(id -gn)\" \"$(id -g)\"",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: printf <values>",
        "Use man id or id --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Build a compact identity record”.",
          "expected": "user=root uid=0 group=root gid=0",
          "normalize": "trim"
        }
      ],
      "completion": "Several focused id queries can be composed into a stable user, UID, group, and GID report. You can now use this behavior as part of a larger command-line workflow.",
      "order": 275,
      "command_order": 55,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "id reports user and group identity. -u emits only the effective numeric user ID. Several focused id queries can be composed into a stable user, UID, group, and GID report.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "id",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-whoami",
      "order": 276,
      "section": "06-identity-and-permissions",
      "command": "whoami",
      "title": "Print the effective username",
      "about": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems.",
      "example": "whoami",
      "example_output": "learner\n",
      "breakdown": [
        "The effective identity is queried.",
        "One username is printed."
      ],
      "task": "Print your effective username.",
      "solution": "whoami",
      "hints": [
        "The topic command takes no argument.",
        "Use the checker as a specification: A nonempty username was printed.",
        "Try this command shape, replacing placeholders with fixture values: whoami"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "A nonempty username was printed.",
          "expected": ".+"
        }
      ],
      "completion": "whoami is a quick identity check. Use id for the complete user and group context.",
      "command_order": 56,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "whoami"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-whoami-2",
      "section": "06-identity-and-permissions",
      "command": "whoami",
      "title": "Verify the expected account",
      "about": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. whoami output can be compared in a conditional before a privileged or account-specific workflow continues.",
      "example": "if [ \"$(whoami)\" = root ]; then echo ready; fi",
      "example_output": "ready",
      "breakdown": [
        "whoami is the featured command in the worked pattern.",
        "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. whoami output can be compared in a conditional before a privileged or account-specific workflow continues.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use whoami in a conditional and print root-session when running as root.",
      "solution": "if [ \"$(whoami)\" = root ]; then echo root-session; fi",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: if <values> ; then <value> root-session ; fi",
        "Use man whoami or whoami --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Verify the expected account”.",
          "expected": "root-session",
          "normalize": "trim"
        }
      ],
      "completion": "whoami output can be compared in a conditional before a privileged or account-specific workflow continues. You can now use this behavior as part of a larger command-line workflow.",
      "order": 277,
      "command_order": 56,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. whoami output can be compared in a conditional before a privileged or account-specific workflow continues.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "whoami",
        "command chaining",
        "command substitution",
        "quoting",
        "filtering and conditions"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-whoami-3",
      "section": "06-identity-and-permissions",
      "command": "whoami",
      "title": "Compare two identity commands",
      "about": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. whoami and id -un should agree on the effective account, providing a simple sanity check.",
      "example": "[ \"$(whoami)\" = \"$(id -un)\" ] && echo match",
      "example_output": "match",
      "breakdown": [
        "whoami is the featured command in the worked pattern.",
        "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. whoami and id -un should agree on the effective account, providing a simple sanity check.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compare whoami with id -un and print identities-match when equal.",
      "solution": "[ \"$(whoami)\" = \"$(id -un)\" ] && echo identities-match",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <values> && echo identities-match",
        "Use man whoami or whoami --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Compare two identity commands”.",
          "expected": "identities-match",
          "normalize": "trim"
        }
      ],
      "completion": "whoami and id -un should agree on the effective account, providing a simple sanity check. You can now use this behavior as part of a larger command-line workflow.",
      "order": 278,
      "command_order": 56,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. whoami and id -un should agree on the effective account, providing a simple sanity check.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "whoami",
        "command chaining",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [
        "echo",
        "id"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-whoami-4",
      "section": "06-identity-and-permissions",
      "command": "whoami",
      "title": "Observe identity under another account",
      "about": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. runuser can execute whoami under a selected local account in this disposable root container.",
      "example": "runuser -u nobody -- whoami",
      "example_output": "nobody",
      "breakdown": [
        "whoami is the featured command in the worked pattern.",
        "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. runuser can execute whoami under a selected local account in this disposable root container.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run whoami as nobody and print the resulting effective username.",
      "solution": "runuser -u nobody -- whoami",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: runuser -u <value> -- <value>",
        "Use man whoami or whoami --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Observe identity under another account”.",
          "expected": "nobody",
          "normalize": "trim"
        }
      ],
      "completion": "runuser can execute whoami under a selected local account in this disposable root container. You can now use this behavior as part of a larger command-line workflow.",
      "order": 279,
      "command_order": 56,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. runuser can execute whoami under a selected local account in this disposable root container.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "whoami"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-whoami-5",
      "section": "06-identity-and-permissions",
      "command": "whoami",
      "title": "Record the operator in an audit entry",
      "about": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. Command substitution inserts the effective username into a timestamp-independent audit record.",
      "example": "printf 'operator=%s action=test\\n' \"$(whoami)\"",
      "example_output": "operator=root action=test",
      "breakdown": [
        "whoami is the featured command in the worked pattern.",
        "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. Command substitution inserts the effective username into a timestamp-independent audit record.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Write operator=<whoami> action=deploy to audit.log and print the same line.",
      "solution": "printf 'operator=%s action=deploy\\n' \"$(whoami)\" | tee audit.log",
      "hints": [
        "Use the checker as a specification: The requested result was printed. The audit entry was saved.",
        "Try this command shape, replacing placeholders with fixture values: printf <values> | tee <value>",
        "Use man whoami or whoami --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Record the operator in an audit entry”.",
          "expected": "operator=root action=deploy",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "The audit entry was saved.",
          "path": "audit.log",
          "expected": "operator=root action=deploy\n"
        }
      ],
      "completion": "Command substitution inserts the effective username into a timestamp-independent audit record. You can now use this behavior as part of a larger command-line workflow.",
      "order": 280,
      "command_order": 56,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "whoami prints the name associated with the effective user ID. It is equivalent to id -un on common Linux systems. Command substitution inserts the effective username into a timestamp-independent audit record.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "whoami",
        "pipelines",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [
        "printf",
        "tee"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-groups",
      "order": 281,
      "section": "06-identity-and-permissions",
      "command": "groups",
      "title": "List group memberships",
      "about": "groups prints the group names associated with a user. With no argument it reports the current process identity context.",
      "example": "groups",
      "example_output": "learner sudo\n",
      "breakdown": [
        "No username means current identity.",
        "One or more group names are printed."
      ],
      "task": "Print the group memberships active for your current identity.",
      "solution": "groups",
      "hints": [
        "Run groups with no username.",
        "Use the checker as a specification: At least one group was printed.",
        "Try this command shape, replacing placeholders with fixture values: groups"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-nonempty",
          "description": "At least one group was printed."
        }
      ],
      "completion": "A group list is context, not proof of access. Directory traversal, ACLs, and mode bits still determine effective filesystem access.",
      "command_order": 57,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "groups prints the group names associated with a user. With no argument it reports the current process identity context.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "groups"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-groups-2",
      "section": "06-identity-and-permissions",
      "command": "groups",
      "title": "Inspect another account groups",
      "about": "groups prints the group names associated with a user. With no argument it reports the current process identity context. groups accepts a username and reports that account rather than only the current identity.",
      "example": "groups nobody",
      "example_output": "nobody : nogroup",
      "breakdown": [
        "groups is the featured command in the worked pattern.",
        "groups prints the group names associated with a user. With no argument it reports the current process identity context. groups accepts a username and reports that account rather than only the current identity.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print group membership for nobody.",
      "solution": "groups nobody",
      "hints": [
        "Use the checker as a specification: The nogroup membership was shown.",
        "Try this command shape, replacing placeholders with fixture values: groups <value>",
        "Use man groups or groups --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The nogroup membership was shown.",
          "expected": "nogroup"
        }
      ],
      "completion": "groups accepts a username and reports that account rather than only the current identity. You can now use this behavior as part of a larger command-line workflow.",
      "order": 282,
      "command_order": 57,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "groups prints the group names associated with a user. With no argument it reports the current process identity context. groups accepts a username and reports that account rather than only the current identity.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "groups"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-groups-3",
      "section": "06-identity-and-permissions",
      "command": "groups",
      "title": "Print group names one per line",
      "about": "groups prints the group names associated with a user. With no argument it reports the current process identity context. Shell word splitting can turn the compact groups output into a line-oriented stream for later tools.",
      "example": "groups | tr \" \" \"\\n\"",
      "example_output": "root",
      "breakdown": [
        "groups is the featured command in the worked pattern.",
        "groups prints the group names associated with a user. With no argument it reports the current process identity context. Shell word splitting can turn the compact groups output into a line-oriented stream for later tools.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the current account groups one per line.",
      "solution": "groups | tr ' ' '\\n'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: groups | tr <values>",
        "Use man groups or groups --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print group names one per line”.",
          "expected": "root",
          "normalize": "trim"
        }
      ],
      "completion": "Shell word splitting can turn the compact groups output into a line-oriented stream for later tools. You can now use this behavior as part of a larger command-line workflow.",
      "order": 283,
      "command_order": 57,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "groups prints the group names associated with a user. With no argument it reports the current process identity context. Shell word splitting can turn the compact groups output into a line-oriented stream for later tools.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "groups",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "tr"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-groups-4",
      "section": "06-identity-and-permissions",
      "command": "groups",
      "title": "Test membership in a required group",
      "about": "groups prints the group names associated with a user. With no argument it reports the current process identity context. A group list can be searched exactly before a workflow assumes group-based access.",
      "example": "groups | tr ' ' '\\n' | grep -qx root && echo member",
      "example_output": "member",
      "breakdown": [
        "groups is the featured command in the worked pattern.",
        "groups prints the group names associated with a user. With no argument it reports the current process identity context. A group list can be searched exactly before a workflow assumes group-based access.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use groups to verify membership in root and print root-member.",
      "solution": "groups | tr ' ' '\\n' | grep -qx root && echo root-member",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: groups | tr <values> | grep -qx <value> && echo root-member",
        "Use man groups or groups --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Test membership in a required group”.",
          "expected": "root-member",
          "normalize": "trim"
        }
      ],
      "completion": "A group list can be searched exactly before a workflow assumes group-based access. You can now use this behavior as part of a larger command-line workflow.",
      "order": 284,
      "command_order": 57,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "groups prints the group names associated with a user. With no argument it reports the current process identity context. A group list can be searched exactly before a workflow assumes group-based access.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "groups",
        "pipelines",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "grep",
        "tr",
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-groups-5",
      "section": "06-identity-and-permissions",
      "command": "groups",
      "title": "Build an account-to-groups report",
      "about": "groups prints the group names associated with a user. With no argument it reports the current process identity context. A loop around groups can inventory memberships for several named accounts.",
      "example": "for u in root nobody; do groups \"$u\"; done",
      "example_output": "root : root\nnobody : nogroup",
      "breakdown": [
        "groups is the featured command in the worked pattern.",
        "groups prints the group names associated with a user. With no argument it reports the current process identity context. A loop around groups can inventory memberships for several named accounts.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print groups reports for root and nobody in that order.",
      "solution": "for user in root nobody; do groups \"$user\"; done",
      "hints": [
        "Use the checker as a specification: The root report was included. The nobody report was included.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <values> ; done",
        "Use man groups or groups --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The root report was included.",
          "expected": "root : root"
        },
        {
          "type": "stdout-contains",
          "description": "The nobody report was included.",
          "expected": "nobody : nogroup"
        }
      ],
      "completion": "A loop around groups can inventory memberships for several named accounts. You can now use this behavior as part of a larger command-line workflow.",
      "order": 285,
      "command_order": 57,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "groups prints the group names associated with a user. With no argument it reports the current process identity context. A loop around groups can inventory memberships for several named accounts.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "groups",
        "command chaining",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-getent",
      "order": 286,
      "section": "06-identity-and-permissions",
      "command": "getent",
      "title": "Query configured identity databases",
      "about": "getent queries databases configured through the Name Service Switch, including local files and network identity sources.",
      "example": "getent passwd root",
      "example_output": "root:x:0:0:root:/root:/bin/bash\n",
      "breakdown": [
        "passwd names the database.",
        "root is the lookup key."
      ],
      "task": "Query the passwd database for root and print only its username field.",
      "solution": "getent passwd root | cut -d: -f1",
      "hints": [
        "Query passwd, then cut the first colon-delimited field.",
        "Use the checker as a specification: The resolved username root was printed.",
        "Try this command shape, replacing placeholders with fixture values: getent <values> | cut -d: -f1"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The resolved username root was printed.",
          "expected": "root",
          "normalize": "trim"
        }
      ],
      "completion": "getent follows configured resolution policy. Reading /etc/passwd alone can miss directory-backed identities.",
      "command_order": 58,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "getent queries databases configured through the Name Service Switch, including local files and network identity sources.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "getent",
        "pipelines"
      ],
      "prerequisites": [
        "cut"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-getent-2",
      "section": "06-identity-and-permissions",
      "command": "getent",
      "title": "Query a group database entry",
      "about": "getent queries databases configured through the Name Service Switch, including local files and network identity sources. The group database returns a colon-delimited record for a named group.",
      "example": "getent group root",
      "example_output": "root:x:0:",
      "breakdown": [
        "getent is the featured command in the worked pattern.",
        "getent queries databases configured through the Name Service Switch, including local files and network identity sources. The group database returns a colon-delimited record for a named group.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Query the group database for nogroup.",
      "solution": "getent group nogroup",
      "hints": [
        "Use the checker as a specification: The nogroup database row was returned.",
        "Try this command shape, replacing placeholders with fixture values: getent <values>",
        "Use man getent or getent --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The nogroup database row was returned.",
          "expected": "nogroup:"
        }
      ],
      "completion": "The group database returns a colon-delimited record for a named group. You can now use this behavior as part of a larger command-line workflow.",
      "order": 287,
      "command_order": 58,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "getent queries databases configured through the Name Service Switch, including local files and network identity sources. The group database returns a colon-delimited record for a named group.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "getent"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-getent-3",
      "section": "06-identity-and-permissions",
      "command": "getent",
      "title": "Resolve a service name and protocol",
      "about": "getent queries databases configured through the Name Service Switch, including local files and network identity sources. The services database maps well-known names and protocols to port numbers.",
      "example": "getent services ssh/tcp",
      "example_output": "ssh 22/tcp",
      "breakdown": [
        "getent is the featured command in the worked pattern.",
        "getent queries databases configured through the Name Service Switch, including local files and network identity sources. The services database maps well-known names and protocols to port numbers.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Look up HTTP in the services database and print the record.",
      "solution": "getent services http",
      "hints": [
        "Use the checker as a specification: The HTTP port mapping was returned.",
        "Try this command shape, replacing placeholders with fixture values: getent <values>",
        "Use man getent or getent --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The HTTP port mapping was returned.",
          "expected": "80/tcp"
        }
      ],
      "completion": "The services database maps well-known names and protocols to port numbers. You can now use this behavior as part of a larger command-line workflow.",
      "order": 288,
      "command_order": 58,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "getent queries databases configured through the Name Service Switch, including local files and network identity sources. The services database maps well-known names and protocols to port numbers.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "getent"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-getent-4",
      "section": "06-identity-and-permissions",
      "command": "getent",
      "title": "Resolve a host through NSS",
      "about": "getent queries databases configured through the Name Service Switch, including local files and network identity sources. getent hosts uses the system name-service switch rather than hard-coding a particular resolver source.",
      "example": "getent hosts localhost",
      "example_output": "::1 localhost ...",
      "breakdown": [
        "getent is the featured command in the worked pattern.",
        "getent queries databases configured through the Name Service Switch, including local files and network identity sources. getent hosts uses the system name-service switch rather than hard-coding a particular resolver source.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Resolve localhost with getent hosts.",
      "solution": "getent hosts localhost",
      "hints": [
        "Use the checker as a specification: The localhost record was returned.",
        "Try this command shape, replacing placeholders with fixture values: getent <values>",
        "Use man getent or getent --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The localhost record was returned.",
          "expected": "localhost"
        }
      ],
      "completion": "getent hosts uses the system name-service switch rather than hard-coding a particular resolver source. You can now use this behavior as part of a larger command-line workflow.",
      "order": 289,
      "command_order": 58,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "getent queries databases configured through the Name Service Switch, including local files and network identity sources. getent hosts uses the system name-service switch rather than hard-coding a particular resolver source.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "getent"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-getent-5",
      "section": "06-identity-and-permissions",
      "command": "getent",
      "title": "Create a local account summary",
      "about": "getent queries databases configured through the Name Service Switch, including local files and network identity sources. getent passwd provides structured account rows that awk can filter and format for an audit.",
      "example": "getent passwd | awk -F: '$3 < 10 {print $1 \"=\" $3}'",
      "example_output": "root=0",
      "breakdown": [
        "getent is the featured command in the worked pattern.",
        "getent queries databases configured through the Name Service Switch, including local files and network identity sources. getent passwd provides structured account rows that awk can filter and format for an audit.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Query root and nobody and print username=uid:shell rows in that order.",
      "solution": "getent passwd root nobody | awk -F: '{print $1 \"=\" $3 \":\" $7}'",
      "hints": [
        "Use the checker as a specification: The root UID and shell were summarized. The nobody account was summarized.",
        "Try this command shape, replacing placeholders with fixture values: getent <values> | awk -F: <values>",
        "Use man getent or getent --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The root UID and shell were summarized.",
          "expected": "root=0:/bin/bash"
        },
        {
          "type": "stdout-contains",
          "description": "The nobody account was summarized.",
          "expected": "nobody=65534:"
        }
      ],
      "completion": "getent passwd provides structured account rows that awk can filter and format for an audit. You can now use this behavior as part of a larger command-line workflow.",
      "order": 290,
      "command_order": 58,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "getent queries databases configured through the Name Service Switch, including local files and network identity sources. getent passwd provides structured account rows that awk can filter and format for an audit.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "getent",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-users",
      "order": 291,
      "section": "06-identity-and-permissions",
      "command": "users",
      "title": "Inspect logged-in usernames",
      "about": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate.",
      "example": "users",
      "example_output": "alice bob\n",
      "breakdown": [
        "Login records are read from the system session database.",
        "Repeated names can represent multiple sessions."
      ],
      "task": "Run users and print login-records-queried when the query succeeds.",
      "solution": "users >/dev/null && echo login-records-queried",
      "hints": [
        "No arguments are required; empty output is possible in a container.",
        "Use the checker as a specification: The successful login-record query was confirmed.",
        "Try this command shape, replacing placeholders with fixture values: users > <value> && echo login-records-queried"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The successful login-record query was confirmed.",
          "expected": "login-records-queried",
          "normalize": "trim"
        }
      ],
      "completion": "Empty users output does not prove nobody is active; session accounting depends on how the environment records logins.",
      "command_order": 59,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "users",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-users-2",
      "section": "06-identity-and-permissions",
      "command": "users",
      "title": "Read users from a login-record file",
      "about": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. users accepts an alternate utmp-format file, which makes historical or captured session data inspectable.",
      "example": "users sessions.utmp",
      "example_output": "alice bob",
      "breakdown": [
        "users is the featured command in the worked pattern.",
        "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. users accepts an alternate utmp-format file, which makes historical or captured session data inspectable.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Convert sessions.dump to sessions.utmp with utmpdump, then list its logged-in users.",
      "solution": "utmpdump -r sessions.dump > sessions.utmp 2>/dev/null && users sessions.utmp",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: utmpdump -r <value> > <values> > <value> && users <value>",
        "Use man users or users --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.dump": "[7] [01001] [pts/0       ] [alice   ] [pts/0       ] [10.0.0.10          ] [10.0.0.10     ] [2026-01-02T09:00:00,000000+00:00]\n[7] [01002] [pts/1       ] [bob     ] [pts/1       ] [10.0.0.11          ] [10.0.0.11     ] [2026-01-02T09:05:00,000000+00:00]\n[7] [01003] [pts/2       ] [alice   ] [pts/2       ] [10.0.0.12          ] [10.0.0.12     ] [2026-01-02T09:10:00,000000+00:00]\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Read users from a login-record file”.",
          "expected": "alice alice bob",
          "normalize": "trim"
        }
      ],
      "completion": "users accepts an alternate utmp-format file, which makes historical or captured session data inspectable. You can now use this behavior as part of a larger command-line workflow.",
      "order": 292,
      "command_order": 59,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. users accepts an alternate utmp-format file, which makes historical or captured session data inspectable.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "users",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-users-3",
      "section": "06-identity-and-permissions",
      "command": "users",
      "title": "Count active login records",
      "about": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. Because users emits one whitespace-delimited name per session, wc -w counts sessions directly.",
      "example": "users sessions.utmp | wc -w",
      "example_output": "3",
      "breakdown": [
        "users is the featured command in the worked pattern.",
        "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. Because users emits one whitespace-delimited name per session, wc -w counts sessions directly.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Build sessions.utmp and print its number of active login sessions.",
      "solution": "utmpdump -r sessions.dump > sessions.utmp 2>/dev/null && users sessions.utmp | wc -w",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: utmpdump -r <value> > <values> > <value> && users <value> | wc -w",
        "Use man users or users --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.dump": "[7] [01001] [pts/0       ] [alice   ] [pts/0       ] [10.0.0.10          ] [10.0.0.10     ] [2026-01-02T09:00:00,000000+00:00]\n[7] [01002] [pts/1       ] [bob     ] [pts/1       ] [10.0.0.11          ] [10.0.0.11     ] [2026-01-02T09:05:00,000000+00:00]\n[7] [01003] [pts/2       ] [alice   ] [pts/2       ] [10.0.0.12          ] [10.0.0.12     ] [2026-01-02T09:10:00,000000+00:00]\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Count active login records”.",
          "expected": "3",
          "normalize": "trim"
        }
      ],
      "completion": "Because users emits one whitespace-delimited name per session, wc -w counts sessions directly. You can now use this behavior as part of a larger command-line workflow.",
      "order": 293,
      "command_order": 59,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. Because users emits one whitespace-delimited name per session, wc -w counts sessions directly.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "users",
        "pipelines",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [
        "wc"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-users-4",
      "section": "06-identity-and-permissions",
      "command": "users",
      "title": "List unique logged-in names",
      "about": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. users sorts its output; translating spaces to lines and uniq removes repeated sessions for one account.",
      "example": "users sessions.utmp | tr ' ' '\\n' | uniq",
      "example_output": "alice\nbob",
      "breakdown": [
        "users is the featured command in the worked pattern.",
        "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. users sorts its output; translating spaces to lines and uniq removes repeated sessions for one account.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Build sessions.utmp and print each distinct logged-in username once.",
      "solution": "utmpdump -r sessions.dump > sessions.utmp 2>/dev/null && users sessions.utmp | tr ' ' '\\n' | uniq",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: utmpdump -r <value> > <values> > <value> && users <value> | tr <values> | uniq",
        "Use man users or users --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.dump": "[7] [01001] [pts/0       ] [alice   ] [pts/0       ] [10.0.0.10          ] [10.0.0.10     ] [2026-01-02T09:00:00,000000+00:00]\n[7] [01002] [pts/1       ] [bob     ] [pts/1       ] [10.0.0.11          ] [10.0.0.11     ] [2026-01-02T09:05:00,000000+00:00]\n[7] [01003] [pts/2       ] [alice   ] [pts/2       ] [10.0.0.12          ] [10.0.0.12     ] [2026-01-02T09:10:00,000000+00:00]\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “List unique logged-in names”.",
          "expected": "alice\nbob",
          "normalize": "trim"
        }
      ],
      "completion": "users sorts its output; translating spaces to lines and uniq removes repeated sessions for one account. You can now use this behavior as part of a larger command-line workflow.",
      "order": 294,
      "command_order": 59,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. users sorts its output; translating spaces to lines and uniq removes repeated sessions for one account.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "users",
        "pipelines",
        "redirection",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "tr",
        "uniq"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-users-5",
      "section": "06-identity-and-permissions",
      "command": "users",
      "title": "Create a session-count summary by user",
      "about": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. A users stream becomes a per-account session report after converting it to lines and counting adjacent names.",
      "example": "users sessions.utmp | tr ' ' '\\n' | uniq -c",
      "example_output": "2 alice\n1 bob",
      "breakdown": [
        "users is the featured command in the worked pattern.",
        "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. A users stream becomes a per-account session report after converting it to lines and counting adjacent names.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Build sessions.utmp and print username=session-count rows sorted by username.",
      "solution": "utmpdump -r sessions.dump > sessions.utmp 2>/dev/null && users sessions.utmp | tr ' ' '\\n' | uniq -c | awk '{print $2 \"=\" $1}'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: utmpdump -r <value> > <values> > <value> && users <value> | tr <values> | uniq -c | awk <values>",
        "Use man users or users --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.dump": "[7] [01001] [pts/0       ] [alice   ] [pts/0       ] [10.0.0.10          ] [10.0.0.10     ] [2026-01-02T09:00:00,000000+00:00]\n[7] [01002] [pts/1       ] [bob     ] [pts/1       ] [10.0.0.11          ] [10.0.0.11     ] [2026-01-02T09:05:00,000000+00:00]\n[7] [01003] [pts/2       ] [alice   ] [pts/2       ] [10.0.0.12          ] [10.0.0.12     ] [2026-01-02T09:10:00,000000+00:00]\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Create a session-count summary by user”.",
          "expected": "alice=2\nbob=1",
          "normalize": "trim"
        }
      ],
      "completion": "A users stream becomes a per-account session report after converting it to lines and counting adjacent names. You can now use this behavior as part of a larger command-line workflow.",
      "order": 295,
      "command_order": 59,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "users prints usernames recorded as currently logged in. Minimal containers and some VM sessions may have no utmp records, so empty output can be legitimate. A users stream becomes a per-account session report after converting it to lines and counting adjacent names.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "users",
        "pipelines",
        "redirection",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "awk",
        "tr",
        "uniq"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-who",
      "order": 296,
      "section": "06-identity-and-permissions",
      "command": "who",
      "title": "Inspect login session records",
      "about": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records.",
      "example": "who",
      "example_output": "alice pts/0 2026-08-03 10:00\n",
      "breakdown": [
        "The system session database is queried.",
        "Each record describes a logged-in session."
      ],
      "task": "Run who and print session-records-queried when the query succeeds.",
      "solution": "who >/dev/null && echo session-records-queried",
      "hints": [
        "Run the command without options; no records is a valid observation.",
        "Use the checker as a specification: The successful session-record query was confirmed.",
        "Try this command shape, replacing placeholders with fixture values: who > <value> && echo session-records-queried"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The successful session-record query was confirmed.",
          "expected": "session-records-queried",
          "normalize": "trim"
        }
      ],
      "completion": "Session accounting is one evidence source. Processes, remote access logs, and orchestration context may tell a different part of the story.",
      "command_order": 60,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "who",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-who-2",
      "section": "06-identity-and-permissions",
      "command": "who",
      "title": "Read detailed login records from a file",
      "about": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who accepts an alternate utmp-format file and prints a row per session.",
      "example": "who sessions.utmp",
      "example_output": "alice pts/0 2026-01-02 09:00 (10.0.0.10)",
      "breakdown": [
        "who is the featured command in the worked pattern.",
        "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who accepts an alternate utmp-format file and prints a row per session.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Build sessions.utmp from sessions.dump and display its session records with who.",
      "solution": "utmpdump -r sessions.dump > sessions.utmp 2>/dev/null && who sessions.utmp",
      "hints": [
        "Use the checker as a specification: Alice login records were shown. Bob login records were shown.",
        "Try this command shape, replacing placeholders with fixture values: utmpdump -r <value> > <values> > <value> && who <value>",
        "Use man who or who --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.dump": "[7] [01001] [pts/0       ] [alice   ] [pts/0       ] [10.0.0.10          ] [10.0.0.10     ] [2026-01-02T09:00:00,000000+00:00]\n[7] [01002] [pts/1       ] [bob     ] [pts/1       ] [10.0.0.11          ] [10.0.0.11     ] [2026-01-02T09:05:00,000000+00:00]\n[7] [01003] [pts/2       ] [alice   ] [pts/2       ] [10.0.0.12          ] [10.0.0.12     ] [2026-01-02T09:10:00,000000+00:00]\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "Alice login records were shown.",
          "expected": "alice"
        },
        {
          "type": "stdout-contains",
          "description": "Bob login records were shown.",
          "expected": "bob"
        }
      ],
      "completion": "who accepts an alternate utmp-format file and prints a row per session. You can now use this behavior as part of a larger command-line workflow.",
      "order": 297,
      "command_order": 60,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who accepts an alternate utmp-format file and prints a row per session.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "who",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-who-3",
      "section": "06-identity-and-permissions",
      "command": "who",
      "title": "Add a heading row",
      "about": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who -H labels the user, line, time, and comment columns for a readable report.",
      "example": "who -H sessions.utmp",
      "example_output": "NAME LINE TIME COMMENT\nalice pts/0 ...",
      "breakdown": [
        "who is the featured command in the worked pattern.",
        "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who -H labels the user, line, time, and comment columns for a readable report.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Build sessions.utmp and print who output with column headings.",
      "solution": "utmpdump -r sessions.dump > sessions.utmp 2>/dev/null && who -H sessions.utmp",
      "hints": [
        "Use the checker as a specification: The NAME heading was printed.",
        "Try this command shape, replacing placeholders with fixture values: utmpdump -r <value> > <values> > <value> && who -H <value>",
        "Use man who or who --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.dump": "[7] [01001] [pts/0       ] [alice   ] [pts/0       ] [10.0.0.10          ] [10.0.0.10     ] [2026-01-02T09:00:00,000000+00:00]\n[7] [01002] [pts/1       ] [bob     ] [pts/1       ] [10.0.0.11          ] [10.0.0.11     ] [2026-01-02T09:05:00,000000+00:00]\n[7] [01003] [pts/2       ] [alice   ] [pts/2       ] [10.0.0.12          ] [10.0.0.12     ] [2026-01-02T09:10:00,000000+00:00]\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The NAME heading was printed.",
          "expected": "NAME"
        }
      ],
      "completion": "who -H labels the user, line, time, and comment columns for a readable report. You can now use this behavior as part of a larger command-line workflow.",
      "order": 298,
      "command_order": 60,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who -H labels the user, line, time, and comment columns for a readable report.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "who",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-who-4",
      "section": "06-identity-and-permissions",
      "command": "who",
      "title": "Print a quick user count",
      "about": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who -q emits a compact username list and a final count.",
      "example": "who -q sessions.utmp",
      "example_output": "alice bob alice\n# users=3",
      "breakdown": [
        "who is the featured command in the worked pattern.",
        "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who -q emits a compact username list and a final count.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Build sessions.utmp and use who -q to print its names and total session count.",
      "solution": "utmpdump -r sessions.dump > sessions.utmp 2>/dev/null && who -q sessions.utmp",
      "hints": [
        "Use the checker as a specification: The session total was reported.",
        "Try this command shape, replacing placeholders with fixture values: utmpdump -r <value> > <values> > <value> && who -q <value>",
        "Use man who or who --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.dump": "[7] [01001] [pts/0       ] [alice   ] [pts/0       ] [10.0.0.10          ] [10.0.0.10     ] [2026-01-02T09:00:00,000000+00:00]\n[7] [01002] [pts/1       ] [bob     ] [pts/1       ] [10.0.0.11          ] [10.0.0.11     ] [2026-01-02T09:05:00,000000+00:00]\n[7] [01003] [pts/2       ] [alice   ] [pts/2       ] [10.0.0.12          ] [10.0.0.12     ] [2026-01-02T09:10:00,000000+00:00]\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The session total was reported.",
          "expected": "# users=3"
        }
      ],
      "completion": "who -q emits a compact username list and a final count. You can now use this behavior as part of a larger command-line workflow.",
      "order": 299,
      "command_order": 60,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who -q emits a compact username list and a final count.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "who",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-who-5",
      "section": "06-identity-and-permissions",
      "command": "who",
      "title": "Summarize sessions by remote host",
      "about": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who provides structured session rows that awk can reshape into a concise user=terminal@host audit.",
      "example": "who sessions.utmp | awk '{print $1 \"=\" $2 \"@\" $5}'",
      "example_output": "alice=pts/0@(10.0.0.10)",
      "breakdown": [
        "who is the featured command in the worked pattern.",
        "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who provides structured session rows that awk can reshape into a concise user=terminal@host audit.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Build sessions.utmp and print user=terminal@host for every session. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "utmpdump -r sessions.dump > sessions.utmp 2>/dev/null && who sessions.utmp | awk '{print $1 \"=\" $2 \"@\" $5}' && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: Alice terminal data was summarized. Bob terminal data was summarized.",
        "Try this command shape, replacing placeholders with fixture values: utmpdump -r <value> > <values> > <value> && who <value> | awk <values>",
        "Use man who or who --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "sessions.dump": "[7] [01001] [pts/0       ] [alice   ] [pts/0       ] [10.0.0.10          ] [10.0.0.10     ] [2026-01-02T09:00:00,000000+00:00]\n[7] [01002] [pts/1       ] [bob     ] [pts/1       ] [10.0.0.11          ] [10.0.0.11     ] [2026-01-02T09:05:00,000000+00:00]\n[7] [01003] [pts/2       ] [alice   ] [pts/2       ] [10.0.0.12          ] [10.0.0.12     ] [2026-01-02T09:10:00,000000+00:00]\n",
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "Alice terminal data was summarized.",
          "expected": "alice=pts/0@"
        },
        {
          "type": "stdout-contains",
          "description": "Bob terminal data was summarized.",
          "expected": "bob=pts/1@"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "who provides structured session rows that awk can reshape into a concise user=terminal@host audit. You can now use this behavior as part of a larger command-line workflow.",
      "order": 300,
      "command_order": 60,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "who prints detailed login records such as user, terminal, and login time. Container environments often have no such records. who provides structured session rows that awk can reshape into a concise user=terminal@host audit.",
      "stage_kind": "workflow",
      "mode": "capstone",
      "concepts": [
        "who",
        "pipelines",
        "redirection",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "cp",
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-ps",
      "order": 301,
      "section": "07-processes-and-execution",
      "command": "ps",
      "title": "Take a process snapshot",
      "about": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults.",
      "example": "ps -o pid=,comm= -p 1",
      "example_output": "1 systemd\n",
      "breakdown": [
        "-p 1 selects PID 1.",
        "-o chooses PID and command-name columns.",
        "= removes headers."
      ],
      "task": "Print PID and command name for process 1 without headers.",
      "solution": "ps -o pid=,comm= -p 1",
      "hints": [
        "Use -p 1 and an explicit -o format.",
        "Use the checker as a specification: Process 1 information was printed. ps completed successfully.",
        "Try this command shape, replacing placeholders with fixture values: ps -o <value> -p <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-nonempty",
          "description": "Process 1 information was printed."
        },
        {
          "type": "exit-code",
          "description": "ps completed successfully.",
          "expected": 0
        }
      ],
      "completion": "ps is a snapshot. Processes can exit, fork, or change state immediately after observation.",
      "command_order": 61,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "ps"
      ],
      "prerequisites": [
        "comm"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ps-2",
      "section": "07-processes-and-execution",
      "command": "ps",
      "title": "Choose process columns",
      "about": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. ps -o selects and orders only the fields needed for a process report.",
      "example": "ps -o pid,ppid,comm -p 1",
      "example_output": "PID PPID COMMAND\n1 0 gym",
      "breakdown": [
        "ps is the featured command in the worked pattern.",
        "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. ps -o selects and orders only the fields needed for a process report.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print PID, PPID, and command for process 1 with headers.",
      "solution": "ps -o pid,ppid,comm -p 1",
      "hints": [
        "Use the checker as a specification: The PID heading was shown. Process 1 was included.",
        "Try this command shape, replacing placeholders with fixture values: ps -o <value> -p <value>",
        "Use man ps or ps --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The PID heading was shown.",
          "expected": "PID"
        },
        {
          "type": "stdout-contains",
          "description": "Process 1 was included.",
          "expected": "1"
        }
      ],
      "completion": "ps -o selects and orders only the fields needed for a process report. You can now use this behavior as part of a larger command-line workflow.",
      "order": 302,
      "command_order": 61,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. ps -o selects and orders only the fields needed for a process report.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "ps"
      ],
      "prerequisites": [
        "comm"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ps-3",
      "section": "07-processes-and-execution",
      "command": "ps",
      "title": "Inspect a process you started",
      "about": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. A saved $! PID lets ps inspect one exact background process without broad matching.",
      "example": "sleep 30 & pid=$!; until [ \"$(ps -o comm= -p \"$pid\")\" = sleep ]; do :; done; ps -o pid=,comm= -p \"$pid\"; kill \"$pid\"",
      "example_output": "123 sleep",
      "breakdown": [
        "ps is the featured command in the worked pattern.",
        "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. A saved $! PID lets ps inspect one exact background process without broad matching.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start sleep 30 in the background, print its PID and command with ps without headers, then stop it.",
      "solution": "sleep 30 & pid=$!; until [ \"$(ps -o comm= -p \"$pid\")\" = sleep ]; do :; done; ps -o pid=,comm= -p \"$pid\"; kill \"$pid\"",
      "hints": [
        "Use the checker as a specification: The sleep command was identified.",
        "Try this command shape, replacing placeholders with fixture values: sleep <values> ; until <values> -o comm= -p <values> ; do <value> ; done ; ps -o <value> -p <value> ; kill <value>",
        "Use man ps or ps --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The sleep command was identified.",
          "expected": "sleep"
        }
      ],
      "completion": "A saved $! PID lets ps inspect one exact background process without broad matching. You can now use this behavior as part of a larger command-line workflow.",
      "order": 303,
      "command_order": 61,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. A saved $! PID lets ps inspect one exact background process without broad matching.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "ps",
        "command chaining",
        "command substitution",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [
        "comm"
      ],
      "introduced_inline": [
        "kill",
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ps-4",
      "section": "07-processes-and-execution",
      "command": "ps",
      "title": "Sort processes by memory use",
      "about": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. ps --sort orders a custom process table by a selected field such as resident memory.",
      "example": "ps -eo pid,comm,rss --sort=-rss | head",
      "example_output": "PID COMMAND RSS\n...",
      "breakdown": [
        "ps is the featured command in the worked pattern.",
        "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. ps --sort orders a custom process table by a selected field such as resident memory.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the header and five process rows sorted by descending RSS.",
      "solution": "ps -eo pid,comm,rss --sort=-rss | head -n 6",
      "hints": [
        "Use the checker as a specification: The RSS column was included.",
        "Try this command shape, replacing placeholders with fixture values: ps -eo <value> --sort=-rss | head -n <value>",
        "Use man ps or ps --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The RSS column was included.",
          "expected": "RSS"
        }
      ],
      "completion": "ps --sort orders a custom process table by a selected field such as resident memory. You can now use this behavior as part of a larger command-line workflow.",
      "order": 304,
      "command_order": 61,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. ps --sort orders a custom process table by a selected field such as resident memory.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "ps",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "head",
        "comm"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ps-5",
      "section": "07-processes-and-execution",
      "command": "ps",
      "title": "Create a filtered process inventory",
      "about": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. A custom ps table can feed awk so a workflow retains only rows matching a command name.",
      "example": "ps -eo pid=,comm= | awk '$2==\"sleep\"'",
      "example_output": "123 sleep",
      "breakdown": [
        "ps is the featured command in the worked pattern.",
        "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. A custom ps table can feed awk so a workflow retains only rows matching a command name.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start a background sleep, use ps and awk to print only PID=sleep for that PID, then stop it.",
      "solution": "sleep 30 & pid=$!; until [ \"$(ps -o comm= -p \"$pid\")\" = sleep ]; do :; done; ps -o pid=,comm= -p \"$pid\" | awk '{print $1 \"=\" $2}'; kill \"$pid\"",
      "hints": [
        "Use the checker as a specification: The process inventory row was formatted.",
        "Try this command shape, replacing placeholders with fixture values: sleep <values> ; until <values> -o comm= -p <values> ; do <value> ; done ; ps -o <value> -p <value> | awk <values> ; kill <value>",
        "Use man ps or ps --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "The process inventory row was formatted.",
          "expected": "[0-9]+=sleep"
        }
      ],
      "completion": "A custom ps table can feed awk so a workflow retains only rows matching a command name. You can now use this behavior as part of a larger command-line workflow.",
      "order": 305,
      "command_order": 61,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "ps reports a point-in-time process snapshot. Explicit columns make the output easier to reason about than relying on defaults. A custom ps table can feed awk so a workflow retains only rows matching a command name.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "ps",
        "pipelines",
        "command chaining",
        "command substitution",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [
        "awk",
        "comm"
      ],
      "introduced_inline": [
        "kill",
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-top",
      "order": 306,
      "section": "07-processes-and-execution",
      "command": "top",
      "title": "Capture a batch process overview",
      "about": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline.",
      "example": "top -b -n 1 | head -n 5",
      "example_output": "top - 10:00:00 up 1 day\nTasks: 100 total\n",
      "breakdown": [
        "-b disables interactive control.",
        "-n 1 limits collection to one sample.",
        "head bounds this rep."
      ],
      "task": "Capture the first five lines of one batch-mode top sample.",
      "solution": "top -b -n 1 | head -n 5",
      "hints": [
        "Use batch mode, one iteration, and head.",
        "Use the checker as a specification: The top summary was captured.",
        "Try this command shape, replacing placeholders with fixture values: top -b -n <value> | head -n <value>"
      ],
      "files": {},
      "packages": [
        "procps"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The top summary was captured.",
          "expected": "top -"
        }
      ],
      "completion": "One top sample is not a performance conclusion. Compare multiple observations and the workload context.",
      "command_order": 62,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "top",
        "pipelines"
      ],
      "prerequisites": [
        "head"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-top-2",
      "section": "07-processes-and-execution",
      "command": "top",
      "title": "Capture one noninteractive sample",
      "about": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. Batch mode -b and one iteration -n 1 make top usable in scripts and logs.",
      "example": "top -b -n 1 | head",
      "example_output": "top - ...\nTasks: ...",
      "breakdown": [
        "top is the featured command in the worked pattern.",
        "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. Batch mode -b and one iteration -n 1 make top usable in scripts and logs.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Capture the first ten lines from one batch-mode top sample.",
      "solution": "top -b -n 1 | head -n 10",
      "hints": [
        "Use the checker as a specification: The task summary was captured.",
        "Try this command shape, replacing placeholders with fixture values: top -b -n <value> | head -n <value>",
        "Use man top or top --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The task summary was captured.",
          "expected": "Tasks:"
        }
      ],
      "completion": "Batch mode -b and one iteration -n 1 make top usable in scripts and logs. You can now use this behavior as part of a larger command-line workflow.",
      "order": 307,
      "command_order": 62,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. Batch mode -b and one iteration -n 1 make top usable in scripts and logs.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "top",
        "pipelines"
      ],
      "prerequisites": [
        "head"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-top-3",
      "section": "07-processes-and-execution",
      "command": "top",
      "title": "Monitor one selected PID",
      "about": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. top -p restricts the display to one process ID instead of the whole system.",
      "example": "top -b -n 1 -p 1",
      "example_output": "... PID USER ...",
      "breakdown": [
        "top is the featured command in the worked pattern.",
        "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. top -p restricts the display to one process ID instead of the whole system.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Capture one batch-mode top sample restricted to process 1.",
      "solution": "top -b -n 1 -p 1",
      "hints": [
        "Use the checker as a specification: The process table header was printed.",
        "Try this command shape, replacing placeholders with fixture values: top -b -n <value> -p <value>",
        "Use man top or top --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The process table header was printed.",
          "expected": "PID"
        }
      ],
      "completion": "top -p restricts the display to one process ID instead of the whole system. You can now use this behavior as part of a larger command-line workflow.",
      "order": 308,
      "command_order": 62,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. top -p restricts the display to one process ID instead of the whole system.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "top"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-top-4",
      "section": "07-processes-and-execution",
      "command": "top",
      "title": "Sort a process snapshot by CPU",
      "about": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. The -o field option changes how top orders its process rows in batch output.",
      "example": "top -b -n 1 -o %CPU | head -n 12",
      "example_output": "... PID USER ... %CPU ...",
      "breakdown": [
        "top is the featured command in the worked pattern.",
        "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. The -o field option changes how top orders its process rows in batch output.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Capture the first twelve lines of one top sample sorted by descending CPU use.",
      "solution": "top -b -n 1 -o %CPU | head -n 12",
      "hints": [
        "Use the checker as a specification: The process table PID heading was included. The CPU column was included.",
        "Try this command shape, replacing placeholders with fixture values: top -b -n <value> -o <value> | head -n <value>",
        "Use man top or top --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The process table PID heading was included.",
          "expected": "PID"
        },
        {
          "type": "stdout-contains",
          "description": "The CPU column was included.",
          "expected": "%CPU"
        }
      ],
      "completion": "The -o field option changes how top orders its process rows in batch output. You can now use this behavior as part of a larger command-line workflow.",
      "order": 309,
      "command_order": 62,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. The -o field option changes how top orders its process rows in batch output.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "top",
        "pipelines"
      ],
      "prerequisites": [
        "head"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-top-5",
      "section": "07-processes-and-execution",
      "command": "top",
      "title": "Extract selected system metrics",
      "about": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. A top batch snapshot can be filtered into just the task, CPU, and memory summaries needed for a health record.",
      "example": "top -b -n 1 | grep -E '^(Tasks|%Cpu|MiB Mem)'",
      "example_output": "Tasks: ...\n%Cpu(s): ...\nMiB Mem : ...",
      "breakdown": [
        "top is the featured command in the worked pattern.",
        "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. A top batch snapshot can be filtered into just the task, CPU, and memory summaries needed for a health record.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Capture one top sample and print only its Tasks, CPU, and memory summary lines.",
      "solution": "top -b -n 1 | grep -E '^(Tasks|%Cpu|MiB Mem)'",
      "hints": [
        "Use the checker as a specification: The task summary was included. The memory summary was included.",
        "Try this command shape, replacing placeholders with fixture values: top -b -n <value> | grep -E <value> | <value> | MiB <value>",
        "Use man top or top --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The task summary was included.",
          "expected": "Tasks:"
        },
        {
          "type": "stdout-contains",
          "description": "The memory summary was included.",
          "expected": "MiB Mem"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 3 nonblank output lines were printed.",
          "expected": 3
        }
      ],
      "completion": "A top batch snapshot can be filtered into just the task, CPU, and memory summaries needed for a health record. You can now use this behavior as part of a larger command-line workflow.",
      "order": 310,
      "command_order": 62,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "top is normally interactive. -b enables batch output and -n 1 captures one refresh suitable for a file or pipeline. A top batch snapshot can be filtered into just the task, CPU, and memory summaries needed for a health record.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "top",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "grep"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-pgrep",
      "order": 311,
      "section": "07-processes-and-execution",
      "command": "pgrep",
      "title": "Find processes by name or command line",
      "about": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line.",
      "example": "pgrep -a -f ssh",
      "example_output": "1234 ssh-agent -s\n",
      "breakdown": [
        "-f broadens matching to full command lines.",
        "-a preserves visible context for each PID."
      ],
      "task": "Find the running Hacker CLI Gym process by matching its full command line and print its PID plus command line.",
      "solution": "pgrep -a -f hacker-cli-gym",
      "hints": [
        "Combine -a and -f with the pattern hacker-cli-gym.",
        "Use the checker as a specification: At least one Hacker CLI Gym process context was printed.",
        "Try this command shape, replacing placeholders with fixture values: pgrep -a -f hacker-cli-gym"
      ],
      "files": {},
      "packages": [
        "procps"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "At least one Hacker CLI Gym process context was printed.",
          "expected": "hacker-cli-gym"
        }
      ],
      "completion": "A name match is a candidate set, not authorization to signal every result. Verify PID, owner, arguments, and role first.",
      "command_order": 63,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "pgrep"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-pgrep-2",
      "section": "07-processes-and-execution",
      "command": "pgrep",
      "title": "Match an exact process name",
      "about": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -x requires the entire process name to match instead of accepting a substring.",
      "example": "pgrep -x bash",
      "example_output": "123",
      "breakdown": [
        "pgrep is the featured command in the worked pattern.",
        "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -x requires the entire process name to match instead of accepting a substring.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start sleep 30, use pgrep -x sleep to print a matching PID, then stop the process.",
      "solution": "sleep 30 & pid=$!; pgrep -x sleep; kill \"$pid\"",
      "hints": [
        "Use the checker as a specification: A numeric sleep PID was printed.",
        "Try this command shape, replacing placeholders with fixture values: sleep <values> ; pgrep -x <value> ; kill <value>",
        "Use man pgrep or pgrep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "A numeric sleep PID was printed.",
          "expected": "[0-9]+"
        }
      ],
      "completion": "pgrep -x requires the entire process name to match instead of accepting a substring. You can now use this behavior as part of a larger command-line workflow.",
      "order": 312,
      "command_order": 63,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -x requires the entire process name to match instead of accepting a substring.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "pgrep",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "kill",
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-pgrep-3",
      "section": "07-processes-and-execution",
      "command": "pgrep",
      "title": "Print PID and command line",
      "about": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -a includes each matching PID and its command line for review.",
      "example": "pgrep -a -x sleep",
      "example_output": "123 sleep 30",
      "breakdown": [
        "pgrep is the featured command in the worked pattern.",
        "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -a includes each matching PID and its command line for review.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start sleep 30, print its PID and command line with pgrep, then stop it.",
      "solution": "sleep 30 & pid=$!; pgrep -a -x sleep; kill \"$pid\"",
      "hints": [
        "Use the checker as a specification: The sleep duration appeared in the command line.",
        "Try this command shape, replacing placeholders with fixture values: sleep <values> ; pgrep -a -x <value> ; kill <value>",
        "Use man pgrep or pgrep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The sleep duration appeared in the command line.",
          "expected": "sleep 30"
        }
      ],
      "completion": "pgrep -a includes each matching PID and its command line for review. You can now use this behavior as part of a larger command-line workflow.",
      "order": 313,
      "command_order": 63,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -a includes each matching PID and its command line for review.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "pgrep",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "kill",
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-pgrep-4",
      "section": "07-processes-and-execution",
      "command": "pgrep",
      "title": "Select the newest matching process",
      "about": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -n returns only the newest process among all matches.",
      "example": "pgrep -n sleep",
      "example_output": "456",
      "breakdown": [
        "pgrep is the featured command in the worked pattern.",
        "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -n returns only the newest process among all matches.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start two sleep processes, print only the newest sleep PID with pgrep -n, then stop both.",
      "solution": "sleep 30 & first=$!; sleep 30 & second=$!; pgrep -n -x sleep; kill \"$first\" \"$second\"",
      "hints": [
        "Use the checker as a specification: One newest PID was printed.",
        "Try this command shape, replacing placeholders with fixture values: sleep <values> ; sleep <values> ; pgrep -n -x <value> ; kill <values>",
        "Use man pgrep or pgrep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "One newest PID was printed.",
          "expected": "[0-9]+"
        }
      ],
      "completion": "pgrep -n returns only the newest process among all matches. You can now use this behavior as part of a larger command-line workflow.",
      "order": 314,
      "command_order": 63,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -n returns only the newest process among all matches.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "pgrep",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "kill",
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-pgrep-5",
      "section": "07-processes-and-execution",
      "command": "pgrep",
      "title": "Match a full command line",
      "about": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -f searches complete argument lists, allowing a distinctive workflow label to identify a process.",
      "example": "bash -c 'exec -a worker sleep 30' & pgrep -af worker",
      "example_output": "123 worker 30",
      "breakdown": [
        "pgrep is the featured command in the worked pattern.",
        "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -f searches complete argument lists, allowing a distinctive workflow label to identify a process.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start sleep 30 with argv[0] gym-worker, locate it with pgrep -af gym-worker, then stop it.",
      "solution": "bash -c 'exec -a gym-worker sleep 30' & pid=$!; pgrep -af gym-worker; kill \"$pid\"",
      "hints": [
        "Use the checker as a specification: The labeled worker was found.",
        "Try this command shape, replacing placeholders with fixture values: bash -c <value> -a gym-worker <values> ; pgrep -af gym-worker ; kill <value>",
        "Use man pgrep or pgrep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The labeled worker was found.",
          "expected": "gym-worker"
        }
      ],
      "completion": "pgrep -f searches complete argument lists, allowing a distinctive workflow label to identify a process. You can now use this behavior as part of a larger command-line workflow.",
      "order": 315,
      "command_order": 63,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "pgrep searches the process table. -a prints matching PIDs with command lines and -f matches the full command line. pgrep -f searches complete argument lists, allowing a distinctive workflow label to identify a process.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "pgrep",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "kill",
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-kill",
      "order": 316,
      "section": "07-processes-and-execution",
      "command": "kill",
      "title": "Resolve a signal name safely",
      "about": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form.",
      "example": "kill -l TERM",
      "example_output": "15\n",
      "breakdown": [
        "-l requests signal translation.",
        "TERM maps to the standard termination signal number."
      ],
      "task": "Translate the signal name TERM to its number without signaling a process.",
      "solution": "kill -l TERM",
      "hints": [
        "Use kill's list/translate option.",
        "Use the checker as a specification: TERM was translated to signal 15.",
        "Try this command shape, replacing placeholders with fixture values: kill -l <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "TERM was translated to signal 15.",
          "expected": "15",
          "normalize": "trim"
        }
      ],
      "completion": "Prefer TERM before KILL so a process can clean up. Confirm the exact PID and ownership before signaling.",
      "command_order": 64,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "kill"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-kill-2",
      "section": "07-processes-and-execution",
      "command": "kill",
      "title": "List the available signals",
      "about": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. kill -l lists signal names and numbers without sending anything to a process.",
      "example": "kill -l | head -n 1",
      "example_output": "1) SIGHUP 2) SIGINT ...",
      "breakdown": [
        "kill is the featured command in the worked pattern.",
        "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. kill -l lists signal names and numbers without sending anything to a process.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the signal list and prove it includes SIGTERM.",
      "solution": "kill -l",
      "hints": [
        "Use the checker as a specification: SIGTERM was listed.",
        "Try this command shape, replacing placeholders with fixture values: kill -l",
        "Use man kill or kill --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "SIGTERM was listed.",
          "expected": "SIGTERM"
        }
      ],
      "completion": "kill -l lists signal names and numbers without sending anything to a process. You can now use this behavior as part of a larger command-line workflow.",
      "order": 317,
      "command_order": 64,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. kill -l lists signal names and numbers without sending anything to a process.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "kill"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-kill-3",
      "section": "07-processes-and-execution",
      "command": "kill",
      "title": "Probe whether a process exists",
      "about": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. Signal 0 performs permission and existence checks without delivering a real signal.",
      "example": "kill -0 \"$pid\" && echo alive",
      "example_output": "alive",
      "breakdown": [
        "kill is the featured command in the worked pattern.",
        "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. Signal 0 performs permission and existence checks without delivering a real signal.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start sleep 30, use kill -0 to print alive, then terminate it.",
      "solution": "sleep 30 & pid=$!; kill -0 \"$pid\" && echo alive; kill \"$pid\"",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sleep <values> ; kill -0 <value> && echo <value> ; kill <value>",
        "Use man kill or kill --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Probe whether a process exists”.",
          "expected": "alive",
          "normalize": "trim"
        }
      ],
      "completion": "Signal 0 performs permission and existence checks without delivering a real signal. You can now use this behavior as part of a larger command-line workflow.",
      "order": 318,
      "command_order": 64,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. Signal 0 performs permission and existence checks without delivering a real signal.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "kill",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-kill-4",
      "section": "07-processes-and-execution",
      "command": "kill",
      "title": "Send a named termination signal",
      "about": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. A named signal such as -TERM documents intent better than a raw number.",
      "example": "kill -TERM \"$pid\"",
      "example_output": "",
      "breakdown": [
        "kill is the featured command in the worked pattern.",
        "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. A named signal such as -TERM documents intent better than a raw number.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start sleep 30, send it TERM with kill, wait for it, and print stopped.",
      "solution": "sleep 30 & pid=$!; kill -TERM \"$pid\"; wait \"$pid\" 2>/dev/null || true; echo stopped",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sleep <values> ; kill -TERM <value> ; wait <values> > <value> | | true ; echo <value>",
        "Use man kill or kill --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Send a named termination signal”.",
          "expected": "stopped",
          "normalize": "trim"
        }
      ],
      "completion": "A named signal such as -TERM documents intent better than a raw number. You can now use this behavior as part of a larger command-line workflow.",
      "order": 319,
      "command_order": 64,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. A named signal such as -TERM documents intent better than a raw number.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "kill",
        "pipelines",
        "redirection",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-kill-5",
      "section": "07-processes-and-execution",
      "command": "kill",
      "title": "Trigger a signal-aware cleanup",
      "about": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. Programs can trap a signal and perform cleanup before exiting; kill provides the explicit trigger.",
      "example": "bash -c 'trap \"echo clean > done\" TERM; while :; do sleep 1; done' & kill -TERM $!",
      "example_output": "",
      "breakdown": [
        "kill is the featured command in the worked pattern.",
        "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. Programs can trap a signal and perform cleanup before exiting; kill provides the explicit trigger.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start the provided signal-aware worker, send TERM, wait, and print the cleanup file.",
      "solution": "bash worker.sh & pid=$!; sleep 0.1; kill -TERM \"$pid\"; wait \"$pid\" 2>/dev/null || true; cat cleanup.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: bash <values> ; sleep <value> ; kill -TERM <value> ; wait <values> > <value> | | true ; cat <value>",
        "Use man kill or kill --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "worker.sh": "#!/bin/bash\ntrap 'echo cleanup-complete > cleanup.txt; exit 0' TERM\nwhile :; do sleep 0.1; done\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Trigger a signal-aware cleanup”.",
          "expected": "cleanup-complete",
          "normalize": "trim"
        }
      ],
      "completion": "Programs can trap a signal and perform cleanup before exiting; kill provides the explicit trigger. You can now use this behavior as part of a larger command-line workflow.",
      "order": 320,
      "command_order": 64,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "kill sends signals to processes, while kill -l can list or translate signal names without signaling anything. This rep uses the safe translation form. Programs can trap a signal and perform cleanup before exiting; kill provides the explicit trigger.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "kill",
        "pipelines",
        "redirection",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nohup",
      "order": 321,
      "section": "07-processes-and-execution",
      "command": "nohup",
      "title": "Run independently of terminal hangup",
      "about": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable.",
      "example": "nohup printf 'done\\n' > result.txt",
      "example_output": "",
      "breakdown": [
        "printf runs with hangup ignored.",
        "> sends stdout to a declared file."
      ],
      "task": "Use nohup to run printf and write ready to nohup-result.txt.",
      "solution": "nohup printf 'ready\\n' > nohup-result.txt",
      "hints": [
        "Put printf after nohup and redirect stdout.",
        "Use the checker as a specification: nohup-result.txt contains ready.",
        "Try this command shape, replacing placeholders with fixture values: nohup <values> > <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "file-content",
          "description": "nohup-result.txt contains ready.",
          "path": "nohup-result.txt",
          "expected": "ready",
          "normalize": "trim"
        }
      ],
      "completion": "nohup is not a service manager. It provides limited detachment, not supervision, restart policy, or structured logs.",
      "command_order": 65,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "nohup",
        "redirection",
        "quoting"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nohup-2",
      "section": "07-processes-and-execution",
      "command": "nohup",
      "title": "Use the default output file",
      "about": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. When standard output is a terminal, nohup redirects it to nohup.out so it survives disconnection.",
      "example": "nohup sh -c 'echo ready'",
      "example_output": "nohup: ignoring input and appending output to nohup.out",
      "breakdown": [
        "nohup is the featured command in the worked pattern.",
        "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. When standard output is a terminal, nohup redirects it to nohup.out so it survives disconnection.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run echo background-ready through nohup with output explicitly saved to nohup.out.",
      "solution": "nohup sh -c 'echo background-ready' > nohup.out",
      "hints": [
        "Use the checker as a specification: The detached output was saved.",
        "Try this command shape, replacing placeholders with fixture values: nohup <value> -c <values> > <value>",
        "Use man nohup or nohup --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "file-content",
          "description": "The detached output was saved.",
          "path": "nohup.out",
          "expected": "background-ready\n"
        }
      ],
      "completion": "When standard output is a terminal, nohup redirects it to nohup.out so it survives disconnection. You can now use this behavior as part of a larger command-line workflow.",
      "order": 322,
      "command_order": 65,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. When standard output is a terminal, nohup redirects it to nohup.out so it survives disconnection.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "nohup",
        "redirection",
        "quoting"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nohup-3",
      "section": "07-processes-and-execution",
      "command": "nohup",
      "title": "Separate standard output and errors",
      "about": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Shell redirections around nohup can keep normal output and diagnostics in separate files.",
      "example": "nohup sh -c 'echo ok; echo warn >&2' >out.log 2>err.log",
      "example_output": "",
      "breakdown": [
        "nohup is the featured command in the worked pattern.",
        "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Shell redirections around nohup can keep normal output and diagnostics in separate files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run job.sh with nohup, saving stdout to job.out and stderr to job.err.",
      "solution": "nohup bash job.sh > job.out 2> job.err",
      "hints": [
        "Use the checker as a specification: Normal output was saved. Diagnostic output was saved.",
        "Try this command shape, replacing placeholders with fixture values: nohup <values> > <values> > <value>",
        "Use man nohup or nohup --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "job.sh": "#!/bin/bash\necho completed\necho diagnostic >&2\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "Normal output was saved.",
          "path": "job.out",
          "expected": "completed\n"
        },
        {
          "type": "file-content",
          "description": "Diagnostic output was saved.",
          "path": "job.err",
          "expected": "diagnostic\n"
        }
      ],
      "completion": "Shell redirections around nohup can keep normal output and diagnostics in separate files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 323,
      "command_order": 65,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Shell redirections around nohup can keep normal output and diagnostics in separate files.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "nohup",
        "redirection"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nohup-4",
      "section": "07-processes-and-execution",
      "command": "nohup",
      "title": "Launch a detached background job",
      "about": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Appending & returns control immediately while nohup gives the child hangup-resistant signal handling.",
      "example": "nohup sh -c 'sleep 1; echo done' > result.txt &",
      "example_output": "",
      "breakdown": [
        "nohup is the featured command in the worked pattern.",
        "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Appending & returns control immediately while nohup gives the child hangup-resistant signal handling.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start job.sh under nohup in the background, wait for its PID, then print result.txt.",
      "solution": "nohup bash job.sh > result.txt 2>/dev/null & pid=$!; wait \"$pid\"; cat result.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: nohup <values> > <values> > <values> ; wait <value> ; cat <value>",
        "Use man nohup or nohup --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "job.sh": "#!/bin/bash\nsleep 0.1\necho detached-complete\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Launch a detached background job”.",
          "expected": "detached-complete",
          "normalize": "trim"
        }
      ],
      "completion": "Appending & returns control immediately while nohup gives the child hangup-resistant signal handling. You can now use this behavior as part of a larger command-line workflow.",
      "order": 324,
      "command_order": 65,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Appending & returns control immediately while nohup gives the child hangup-resistant signal handling.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "nohup",
        "redirection",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nohup-5",
      "section": "07-processes-and-execution",
      "command": "nohup",
      "title": "Record a detached job PID",
      "about": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Saving $! after a nohup launch creates a PID file that later monitoring or cleanup can consume.",
      "example": "nohup sleep 30 >/dev/null 2>&1 & echo $! > job.pid",
      "example_output": "",
      "breakdown": [
        "nohup is the featured command in the worked pattern.",
        "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Saving $! after a nohup launch creates a PID file that later monitoring or cleanup can consume.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Launch sleep 30 with nohup, save its PID to worker.pid, verify it with kill -0, print running, then stop it.",
      "solution": "nohup sleep 30 >/dev/null 2>&1 & echo $! > worker.pid; pid=$(cat worker.pid); kill -0 \"$pid\" && echo running; kill \"$pid\"",
      "hints": [
        "Use the checker as a specification: The requested result was printed. The worker PID file was created.",
        "Try this command shape, replacing placeholders with fixture values: nohup <values> > <values> > <values> > <value> ; <values> ; kill -0 <value> && echo <value> ; kill <value>",
        "Use man nohup or nohup --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Record a detached job PID”.",
          "expected": "running",
          "normalize": "trim"
        },
        {
          "type": "path-exists",
          "description": "The worker PID file was created.",
          "path": "worker.pid",
          "kind": "file"
        }
      ],
      "completion": "Saving $! after a nohup launch creates a PID file that later monitoring or cleanup can consume. You can now use this behavior as part of a larger command-line workflow.",
      "order": 325,
      "command_order": 65,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "nohup changes hangup-signal handling for a command. Redirect output explicitly so its destination is predictable. Saving $! after a nohup launch creates a PID file that later monitoring or cleanup can consume.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "nohup",
        "redirection",
        "command chaining",
        "command substitution",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "cat",
        "echo",
        "kill"
      ],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nice",
      "order": 326,
      "section": "07-processes-and-execution",
      "command": "nice",
      "title": "Start with adjusted scheduling priority",
      "about": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority.",
      "example": "nice -n 5 true",
      "example_output": "",
      "breakdown": [
        "-n 5 requests niceness 5.",
        "true exits successfully without work."
      ],
      "task": "Run a shell with niceness 5 and print its NI value.",
      "solution": "nice -n 5 sh -c 'ps -o ni= -p $$'",
      "hints": [
        "Use nice -n 5 followed by true.",
        "Use the checker as a specification: The child shell reported niceness 5.",
        "Try this command shape, replacing placeholders with fixture values: nice -n <values> -c <value> -o ni= -p <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The child shell reported niceness 5.",
          "expected": "5",
          "normalize": "trim"
        }
      ],
      "completion": "Niceness influences CPU scheduling; it is not a hard resource limit or a guarantee of runtime share.",
      "command_order": 66,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "nice",
        "quoting"
      ],
      "prerequisites": [
        "ps"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nice-2",
      "section": "07-processes-and-execution",
      "command": "nice",
      "title": "Run with a chosen niceness",
      "about": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. nice -n supplies an explicit niceness adjustment for the child command.",
      "example": "nice -n 10 sh -c 'ps -o ni= -p $$'",
      "example_output": "10",
      "breakdown": [
        "nice is the featured command in the worked pattern.",
        "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. nice -n supplies an explicit niceness adjustment for the child command.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run a shell at niceness 7 and print its NI value.",
      "solution": "nice -n 7 sh -c 'ps -o ni= -p $$'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: nice -n <values> -c <value> -o ni= -p <value>",
        "Use man nice or nice --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Run with a chosen niceness”.",
          "expected": "7",
          "normalize": "trim"
        }
      ],
      "completion": "nice -n supplies an explicit niceness adjustment for the child command. You can now use this behavior as part of a larger command-line workflow.",
      "order": 327,
      "command_order": 66,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. nice -n supplies an explicit niceness adjustment for the child command.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "nice",
        "quoting"
      ],
      "prerequisites": [
        "ps"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nice-3",
      "section": "07-processes-and-execution",
      "command": "nice",
      "title": "Use the default adjustment",
      "about": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. Without -n, nice applies its default adjustment of 10 to the child process.",
      "example": "nice sh -c 'ps -o ni= -p $$'",
      "example_output": "10",
      "breakdown": [
        "nice is the featured command in the worked pattern.",
        "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. Without -n, nice applies its default adjustment of 10 to the child process.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run a shell with default nice behavior and print its NI value.",
      "solution": "nice sh -c 'ps -o ni= -p $$'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: nice <value> -c <value> -o ni= -p <value>",
        "Use man nice or nice --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Use the default adjustment”.",
          "expected": "10",
          "normalize": "trim"
        }
      ],
      "completion": "Without -n, nice applies its default adjustment of 10 to the child process. You can now use this behavior as part of a larger command-line workflow.",
      "order": 328,
      "command_order": 66,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. Without -n, nice applies its default adjustment of 10 to the child process.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "nice",
        "quoting"
      ],
      "prerequisites": [
        "ps"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nice-4",
      "section": "07-processes-and-execution",
      "command": "nice",
      "title": "Inspect a niced background process",
      "about": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. A longer-running nice child can be inspected with ps using its captured PID.",
      "example": "nice -n 12 sleep 30 & ps -o ni= -p $!",
      "example_output": "12",
      "breakdown": [
        "nice is the featured command in the worked pattern.",
        "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. A longer-running nice child can be inspected with ps using its captured PID.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start sleep 30 at niceness 12, print its niceness with ps, then stop it.",
      "solution": "nice -n 12 sleep 30 & pid=$!; ps -o ni= -p \"$pid\"; kill \"$pid\"",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: nice -n <values> ; ps -o ni= -p <value> ; kill <value>",
        "Use man nice or nice --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Inspect a niced background process”.",
          "expected": "12",
          "normalize": "trim"
        }
      ],
      "completion": "A longer-running nice child can be inspected with ps using its captured PID. You can now use this behavior as part of a larger command-line workflow.",
      "order": 329,
      "command_order": 66,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. A longer-running nice child can be inspected with ps using its captured PID.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "nice",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "ps",
        "kill"
      ],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-nice-5",
      "section": "07-processes-and-execution",
      "command": "nice",
      "title": "Run a low-priority batch workflow",
      "about": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. nice can wrap an entire shell pipeline so CPU-intensive batch work starts with a lower scheduling priority.",
      "example": "nice -n 15 sh -c 'sort big.txt > sorted.txt'",
      "example_output": "",
      "breakdown": [
        "nice is the featured command in the worked pattern.",
        "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. nice can wrap an entire shell pipeline so CPU-intensive batch work starts with a lower scheduling priority.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run a shell at niceness 15 that sorts values.txt into sorted.txt and records its niceness in priority.txt.",
      "solution": "nice -n 15 sh -c 'ps -o ni= -p $$ > priority.txt; sort -n values.txt > sorted.txt'",
      "hints": [
        "Use the checker as a specification: The batch output was sorted. The batch priority was recorded.",
        "Try this command shape, replacing placeholders with fixture values: nice -n <values> -c <value> -o ni= -p <value> > <value> ; sort -n <value> > <value>",
        "Use man nice or nice --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "values.txt": "9\n2\n7\n1\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "The batch output was sorted.",
          "path": "sorted.txt",
          "expected": "1\n2\n7\n9\n"
        },
        {
          "type": "file-content",
          "description": "The batch priority was recorded.",
          "path": "priority.txt",
          "expected": "15",
          "normalize": "trim"
        }
      ],
      "completion": "nice can wrap an entire shell pipeline so CPU-intensive batch work starts with a lower scheduling priority. You can now use this behavior as part of a larger command-line workflow.",
      "order": 330,
      "command_order": 66,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "nice starts a command with an adjusted niceness value. Higher niceness generally means lower CPU scheduling priority. nice can wrap an entire shell pipeline so CPU-intensive batch work starts with a lower scheduling priority.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "nice",
        "redirection",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort",
        "ps"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-timeout",
      "order": 331,
      "section": "07-processes-and-execution",
      "command": "timeout",
      "title": "Bound command runtime",
      "about": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success.",
      "example": "timeout 1 sleep 5",
      "example_output": "",
      "breakdown": [
        "1 is a one-second duration.",
        "sleep requests five seconds.",
        "Exit 124 reports timeout expiration."
      ],
      "task": "Limit sleep 5 to one second.",
      "solution": "timeout 1 sleep 5",
      "hints": [
        "Put the one-second limit before the sleep command.",
        "Use the checker as a specification: timeout reported deadline expiration.",
        "Try this command shape, replacing placeholders with fixture values: timeout <values>"
      ],
      "files": {},
      "checks": [
        {
          "type": "exit-code",
          "description": "timeout reported deadline expiration.",
          "expected": 124
        }
      ],
      "completion": "A timeout bounds waiting; it does not prove the child and every descendant cleaned up all external state.",
      "command_order": 67,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "timeout"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": false,
      "check_strength": "permissive"
    },
    {
      "id": "linux-timeout-2",
      "section": "07-processes-and-execution",
      "command": "timeout",
      "title": "Allow a command that finishes in time",
      "about": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. timeout returns the child status normally when the deadline is not reached.",
      "example": "timeout 2 sh -c 'echo ready'",
      "example_output": "ready",
      "breakdown": [
        "timeout is the featured command in the worked pattern.",
        "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. timeout returns the child status normally when the deadline is not reached.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Give echo completed one second to finish.",
      "solution": "timeout 1 sh -c 'echo completed'",
      "hints": [
        "Use the checker as a specification: The requested result was printed. The command finished before its deadline.",
        "Try this command shape, replacing placeholders with fixture values: timeout <values> -c <values>",
        "Use man timeout or timeout --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Allow a command that finishes in time”.",
          "expected": "completed",
          "normalize": "trim"
        },
        {
          "type": "exit-code",
          "description": "The command finished before its deadline.",
          "expected": 0
        }
      ],
      "completion": "timeout returns the child status normally when the deadline is not reached. You can now use this behavior as part of a larger command-line workflow.",
      "order": 332,
      "command_order": 67,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. timeout returns the child status normally when the deadline is not reached.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "timeout",
        "quoting"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-timeout-3",
      "section": "07-processes-and-execution",
      "command": "timeout",
      "title": "Choose the expiration signal",
      "about": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. The -s option selects which signal timeout sends when the duration expires.",
      "example": "timeout -s INT 0.1 sleep 5",
      "example_output": "",
      "breakdown": [
        "timeout is the featured command in the worked pattern.",
        "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. The -s option selects which signal timeout sends when the duration expires.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Limit sleep 5 to 0.1 seconds using the INT signal.",
      "solution": "timeout -s INT 0.1 sleep 5",
      "hints": [
        "Use the checker as a specification: timeout reported deadline expiration.",
        "Try this command shape, replacing placeholders with fixture values: timeout -s <values>",
        "Use man timeout or timeout --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "exit-code",
          "description": "timeout reported deadline expiration.",
          "expected": 124
        }
      ],
      "completion": "The -s option selects which signal timeout sends when the duration expires. You can now use this behavior as part of a larger command-line workflow.",
      "order": 333,
      "command_order": 67,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. The -s option selects which signal timeout sends when the duration expires.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "timeout"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": false,
      "check_strength": "permissive"
    },
    {
      "id": "linux-timeout-4",
      "section": "07-processes-and-execution",
      "command": "timeout",
      "title": "Preserve a timed-out child status",
      "about": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. --preserve-status returns the child signal-derived status instead of timeout's usual 124.",
      "example": "timeout --preserve-status 0.1 sleep 5",
      "example_output": "",
      "breakdown": [
        "timeout is the featured command in the worked pattern.",
        "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. --preserve-status returns the child signal-derived status instead of timeout's usual 124.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run sleep 5 with a 0.1-second limit and preserve its TERM-derived status.",
      "solution": "timeout --preserve-status 0.1 sleep 5",
      "hints": [
        "Use the checker as a specification: The terminated child status was preserved.",
        "Try this command shape, replacing placeholders with fixture values: timeout --preserve-status <values>",
        "Use man timeout or timeout --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "exit-code",
          "description": "The terminated child status was preserved.",
          "expected": 143
        }
      ],
      "completion": "--preserve-status returns the child signal-derived status instead of timeout's usual 124. You can now use this behavior as part of a larger command-line workflow.",
      "order": 334,
      "command_order": 67,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. --preserve-status returns the child signal-derived status instead of timeout's usual 124.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "timeout"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": false,
      "check_strength": "permissive"
    },
    {
      "id": "linux-timeout-5",
      "section": "07-processes-and-execution",
      "command": "timeout",
      "title": "Handle a timeout in a workflow",
      "about": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. A shell conditional can translate timeout status 124 into a clear operational result.",
      "example": "timeout 1 slow-command || [ $? -eq 124 ] && echo timed-out",
      "example_output": "timed-out",
      "breakdown": [
        "timeout is the featured command in the worked pattern.",
        "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. A shell conditional can translate timeout status 124 into a clear operational result.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Limit sleep 5 to 0.1 seconds and print deadline-exceeded only when timeout returns 124.",
      "solution": "timeout 0.1 sleep 5; status=$?; if [ \"$status\" -eq 124 ]; then echo deadline-exceeded; fi",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: timeout <values> ; <value> ; if <values> -eq <values> ; then <value> deadline-exceeded ; fi",
        "Use man timeout or timeout --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Handle a timeout in a workflow”.",
          "expected": "deadline-exceeded",
          "normalize": "trim"
        }
      ],
      "completion": "A shell conditional can translate timeout status 124 into a clear operational result. You can now use this behavior as part of a larger command-line workflow.",
      "order": 335,
      "command_order": 67,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "timeout runs a command with a deadline and sends a signal if the duration is exceeded. Its exit status distinguishes a timeout from ordinary success. A shell conditional can translate timeout status 124 into a clear operational result.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "timeout",
        "command chaining",
        "variables",
        "quoting",
        "filtering and conditions"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [
        "sleep"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sleep",
      "order": 336,
      "section": "07-processes-and-execution",
      "command": "sleep",
      "title": "Pause for a bounded duration",
      "about": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep.",
      "example": "sleep 0.2",
      "example_output": "",
      "breakdown": [
        "0.2 requests a short fractional-second pause.",
        "Successful sleep produces no output."
      ],
      "task": "Pause for one tenth of a second, then print resumed.",
      "solution": "sleep 0.1 && echo resumed",
      "hints": [
        "Use a fractional duration of 0.1.",
        "Use the checker as a specification: Execution resumed after the short pause.",
        "Try this command shape, replacing placeholders with fixture values: sleep <value> && echo <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "Execution resumed after the short pause.",
          "expected": "resumed",
          "normalize": "trim"
        }
      ],
      "completion": "Sleep-based synchronization is fragile. Prefer an observable readiness condition when coordinating real work.",
      "command_order": 68,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "sleep",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sleep-2",
      "section": "07-processes-and-execution",
      "command": "sleep",
      "title": "Use a fractional duration",
      "about": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. GNU sleep accepts decimal seconds for short, controlled pauses.",
      "example": "sleep 0.25",
      "example_output": "",
      "breakdown": [
        "sleep is the featured command in the worked pattern.",
        "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. GNU sleep accepts decimal seconds for short, controlled pauses.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Pause for 0.05 seconds, then print resumed.",
      "solution": "sleep 0.05 && echo resumed",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sleep <value> && echo <value>",
        "Use man sleep or sleep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Use a fractional duration”.",
          "expected": "resumed",
          "normalize": "trim"
        }
      ],
      "completion": "GNU sleep accepts decimal seconds for short, controlled pauses. You can now use this behavior as part of a larger command-line workflow.",
      "order": 337,
      "command_order": 68,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. GNU sleep accepts decimal seconds for short, controlled pauses.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "sleep",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sleep-3",
      "section": "07-processes-and-execution",
      "command": "sleep",
      "title": "Use an explicit unit suffix",
      "about": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. Suffixes such as s, m, h, and d document the intended time unit.",
      "example": "sleep 0.2s",
      "example_output": "",
      "breakdown": [
        "sleep is the featured command in the worked pattern.",
        "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. Suffixes such as s, m, h, and d document the intended time unit.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Sleep for 0.05s using the seconds suffix, then print resumed.",
      "solution": "sleep 0.05s && echo resumed",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sleep <value> && echo <value>",
        "Use man sleep or sleep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Use an explicit unit suffix”.",
          "expected": "resumed",
          "normalize": "trim"
        }
      ],
      "completion": "Suffixes such as s, m, h, and d document the intended time unit. You can now use this behavior as part of a larger command-line workflow.",
      "order": 338,
      "command_order": 68,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. Suffixes such as s, m, h, and d document the intended time unit.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "sleep",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sleep-4",
      "section": "07-processes-and-execution",
      "command": "sleep",
      "title": "Pause between retry attempts",
      "about": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. sleep inside a loop spaces repeated attempts without duplicating control logic.",
      "example": "for n in 1 2; do echo attempt=$n; sleep 0.1; done",
      "example_output": "attempt=1\nattempt=2",
      "breakdown": [
        "sleep is the featured command in the worked pattern.",
        "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. sleep inside a loop spaces repeated attempts without duplicating control logic.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print attempt=1 through attempt=3 with a 0.02-second sleep after each.",
      "solution": "for n in 1 2 3; do echo attempt=$n; sleep 0.02; done",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <values> ; sleep <value> ; done",
        "Use man sleep or sleep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Pause between retry attempts”.",
          "expected": "attempt=1\nattempt=2\nattempt=3",
          "normalize": "trim"
        }
      ],
      "completion": "sleep inside a loop spaces repeated attempts without duplicating control logic. You can now use this behavior as part of a larger command-line workflow.",
      "order": 339,
      "command_order": 68,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. sleep inside a loop spaces repeated attempts without duplicating control logic.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "sleep",
        "command chaining",
        "variables",
        "iteration"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sleep-5",
      "section": "07-processes-and-execution",
      "command": "sleep",
      "title": "Coordinate a background result",
      "about": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. A background worker can sleep before producing a result while wait synchronizes the main workflow.",
      "example": "(sleep 0.1; echo ready > status) & wait; cat status",
      "example_output": "ready",
      "breakdown": [
        "sleep is the featured command in the worked pattern.",
        "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. A background worker can sleep before producing a result while wait synchronizes the main workflow.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start a background subshell that sleeps 0.05 seconds and writes complete to status.txt; wait and print it.",
      "solution": "(sleep 0.05; echo complete > status.txt) & wait; cat status.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <values> ; echo <value> > <values> ; cat <value>",
        "Use man sleep or sleep --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Coordinate a background result”.",
          "expected": "complete",
          "normalize": "trim"
        }
      ],
      "completion": "A background worker can sleep before producing a result while wait synchronizes the main workflow. You can now use this behavior as part of a larger command-line workflow.",
      "order": 340,
      "command_order": 68,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "sleep delays for a specified duration. Fractional seconds are supported by GNU sleep. A background worker can sleep before producing a result while wait synchronizes the main workflow.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "sleep",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [
        "cat",
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-time",
      "order": 341,
      "section": "07-processes-and-execution",
      "command": "time",
      "title": "Measure one command execution",
      "about": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape.",
      "example": "time -p sleep 0.1",
      "example_output": "real 0.10\nuser 0.00\nsys 0.00\n",
      "breakdown": [
        "-p selects a simple labeled format.",
        "Timing data is written to stderr by the shell."
      ],
      "task": "Measure sleep 0.1 with portable time output.",
      "solution": "time -p sleep 0.1",
      "hints": [
        "Use Bash time -p before sleep.",
        "Use the checker as a specification: Portable timing output includes real time. The measured command completed.",
        "Try this command shape, replacing placeholders with fixture values: time -p <values>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stderr-contains",
          "description": "Portable timing output includes real time.",
          "expected": "real"
        },
        {
          "type": "exit-code",
          "description": "The measured command completed.",
          "expected": 0
        }
      ],
      "completion": "One timing sample includes scheduler and system noise. Repeat controlled measurements before drawing performance conclusions.",
      "command_order": 69,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "time"
      ],
      "prerequisites": [
        "sleep"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-time-2",
      "section": "07-processes-and-execution",
      "command": "time",
      "title": "Use verbose resource statistics",
      "about": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -v reports elapsed time, CPU, memory, and context-switch details to standard error.",
      "example": "/usr/bin/time -v true",
      "example_output": "... Maximum resident set size ...",
      "breakdown": [
        "time is the featured command in the worked pattern.",
        "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -v reports elapsed time, CPU, memory, and context-switch details to standard error.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Measure true with /usr/bin/time -v.",
      "solution": "/usr/bin/time -v true",
      "hints": [
        "Use the checker as a specification: Maximum resident memory was reported.",
        "Try this command shape, replacing placeholders with fixture values: <value> -v true",
        "Use man time or time --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stderr-contains",
          "description": "Maximum resident memory was reported.",
          "expected": "Maximum resident set size"
        }
      ],
      "completion": "GNU time -v reports elapsed time, CPU, memory, and context-switch details to standard error. You can now use this behavior as part of a larger command-line workflow.",
      "order": 342,
      "command_order": 69,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -v reports elapsed time, CPU, memory, and context-switch details to standard error.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "time"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-time-3",
      "section": "07-processes-and-execution",
      "command": "time",
      "title": "Choose a custom timing format",
      "about": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -f emits only selected metrics using format placeholders.",
      "example": "/usr/bin/time -f 'elapsed=%e' sleep 0.1",
      "example_output": "elapsed=0.10",
      "breakdown": [
        "time is the featured command in the worked pattern.",
        "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -f emits only selected metrics using format placeholders.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Measure sleep 0.05 and print a custom elapsed=<seconds> line.",
      "solution": "/usr/bin/time -f 'elapsed=%e' sleep 0.05",
      "hints": [
        "Use the checker as a specification: The custom elapsed label was emitted.",
        "Try this command shape, replacing placeholders with fixture values: <value> -f <value> sleep <value>",
        "Use man time or time --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stderr-contains",
          "description": "The custom elapsed label was emitted.",
          "expected": "elapsed="
        }
      ],
      "completion": "GNU time -f emits only selected metrics using format placeholders. You can now use this behavior as part of a larger command-line workflow.",
      "order": 343,
      "command_order": 69,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -f emits only selected metrics using format placeholders.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "time",
        "quoting"
      ],
      "prerequisites": [
        "sleep"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-time-4",
      "section": "07-processes-and-execution",
      "command": "time",
      "title": "Write timing data to a file",
      "about": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -o keeps metrics separate from both the child's normal output and its diagnostics.",
      "example": "/usr/bin/time -o timing.txt -f '%e' command",
      "example_output": "",
      "breakdown": [
        "time is the featured command in the worked pattern.",
        "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -o keeps metrics separate from both the child's normal output and its diagnostics.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Measure sort -n values.txt, save elapsed=<seconds> to timing.txt, and save sorted output to sorted.txt.",
      "solution": "/usr/bin/time -o timing.txt -f 'elapsed=%e' sort -n values.txt > sorted.txt; cut -d= -f1 timing.txt",
      "hints": [
        "Use the checker as a specification: The command output was saved separately. The timing label was saved separately.",
        "Try this command shape, replacing placeholders with fixture values: <value> -o <value> -f <value> sort -n <value> > <value> ; cut -d= -f1 <value>",
        "Use man time or time --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "values.txt": "8\n2\n5\n1\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "The command output was saved separately.",
          "path": "sorted.txt",
          "expected": "1\n2\n5\n8\n"
        },
        {
          "type": "stdout",
          "description": "The timing label was saved separately.",
          "expected": "elapsed",
          "normalize": "trim"
        }
      ],
      "completion": "GNU time -o keeps metrics separate from both the child's normal output and its diagnostics. You can now use this behavior as part of a larger command-line workflow.",
      "order": 344,
      "command_order": 69,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. GNU time -o keeps metrics separate from both the child's normal output and its diagnostics.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "time",
        "redirection",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "cut",
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-time-5",
      "section": "07-processes-and-execution",
      "command": "time",
      "title": "Compare two implementations",
      "about": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. Separate time output files make it possible to compare alternative commands without mixing their result data.",
      "example": "/usr/bin/time -o first.time command1; /usr/bin/time -o second.time command2",
      "example_output": "",
      "breakdown": [
        "time is the featured command in the worked pattern.",
        "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. Separate time output files make it possible to compare alternative commands without mixing their result data.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Time sort and sort -n separately into lexical.time and numeric.time, then print the two metric labels.",
      "solution": "/usr/bin/time -o lexical.time -f 'lexical=%e' sort values.txt >/dev/null; /usr/bin/time -o numeric.time -f 'numeric=%e' sort -n values.txt >/dev/null; cut -d= -f1 lexical.time numeric.time",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <value> -o <value> -f <value> sort <value> > <value> ; <value> -o <value> -f <value> sort -n <value> > <value> ; cut -d= -f1 <values>",
        "Use man time or time --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "values.txt": "10\n2\n30\n4\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Compare two implementations”.",
          "expected": "lexical\nnumeric",
          "normalize": "trim"
        }
      ],
      "completion": "Separate time output files make it possible to compare alternative commands without mixing their result data. You can now use this behavior as part of a larger command-line workflow.",
      "order": 345,
      "command_order": 69,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "Bash time reports elapsed, user CPU, and system CPU time for a pipeline. -p uses a portable output shape. Separate time output files make it possible to compare alternative commands without mixing their result data.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "time",
        "redirection",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "cut",
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-seq",
      "order": 346,
      "section": "07-processes-and-execution",
      "command": "seq",
      "title": "Generate a numeric sequence",
      "about": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input.",
      "example": "seq 3",
      "example_output": "1\n2\n3\n",
      "breakdown": [
        "One argument is the final value.",
        "The default start and increment are both one."
      ],
      "task": "Print integers 3 through 7, inclusive.",
      "solution": "seq 3 7",
      "hints": [
        "Provide start 3 and end 7.",
        "Use the checker as a specification: The inclusive 3-through-7 sequence was printed.",
        "Try this command shape, replacing placeholders with fixture values: seq <values>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The inclusive 3-through-7 sequence was printed.",
          "expected": "3\n4\n5\n6\n7",
          "normalize": "trim"
        }
      ],
      "completion": "Keep generated ranges bounded. An accidental huge sequence can consume time, storage, or downstream resources.",
      "command_order": 70,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "seq"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-seq-2",
      "section": "07-processes-and-execution",
      "command": "seq",
      "title": "Use a custom increment",
      "about": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. A middle argument sets the step between the first and last values.",
      "example": "seq 2 2 10",
      "example_output": "2\n4\n6\n8\n10",
      "breakdown": [
        "seq is the featured command in the worked pattern.",
        "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. A middle argument sets the step between the first and last values.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print odd numbers from 1 through 11.",
      "solution": "seq 1 2 11",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: seq <values>",
        "Use man seq or seq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Use a custom increment”.",
          "expected": "1\n3\n5\n7\n9\n11",
          "normalize": "trim"
        }
      ],
      "completion": "A middle argument sets the step between the first and last values. You can now use this behavior as part of a larger command-line workflow.",
      "order": 347,
      "command_order": 70,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. A middle argument sets the step between the first and last values.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "seq"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-seq-3",
      "section": "07-processes-and-execution",
      "command": "seq",
      "title": "Count downward",
      "about": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. A negative increment produces a descending sequence when the first value is greater than the last.",
      "example": "seq 5 -1 1",
      "example_output": "5\n4\n3\n2\n1",
      "breakdown": [
        "seq is the featured command in the worked pattern.",
        "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. A negative increment produces a descending sequence when the first value is greater than the last.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print a countdown from 10 to 0 in steps of 2.",
      "solution": "seq 10 -2 0",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: seq <value> -2 <value>",
        "Use man seq or seq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Count downward”.",
          "expected": "10\n8\n6\n4\n2\n0",
          "normalize": "trim"
        }
      ],
      "completion": "A negative increment produces a descending sequence when the first value is greater than the last. You can now use this behavior as part of a larger command-line workflow.",
      "order": 348,
      "command_order": 70,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. A negative increment produces a descending sequence when the first value is greater than the last.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "seq"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-seq-4",
      "section": "07-processes-and-execution",
      "command": "seq",
      "title": "Format each generated value",
      "about": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. seq -f applies a printf-style floating-point format to every value.",
      "example": "seq -f 'item-%02g' 1 3",
      "example_output": "item-01\nitem-02\nitem-03",
      "breakdown": [
        "seq is the featured command in the worked pattern.",
        "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. seq -f applies a printf-style floating-point format to every value.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print batch-001 through batch-005.",
      "solution": "seq -f 'batch-%03g' 1 5",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: seq -f <values>",
        "Use man seq or seq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Format each generated value”.",
          "expected": "batch-001\nbatch-002\nbatch-003\nbatch-004\nbatch-005",
          "normalize": "trim"
        }
      ],
      "completion": "seq -f applies a printf-style floating-point format to every value. You can now use this behavior as part of a larger command-line workflow.",
      "order": 349,
      "command_order": 70,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. seq -f applies a printf-style floating-point format to every value.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "seq",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-seq-5",
      "section": "07-processes-and-execution",
      "command": "seq",
      "title": "Drive a repeatable batch loop",
      "about": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. Command substitution turns a seq into controlled loop inputs for creating or processing numbered artifacts.",
      "example": "for n in $(seq -w 1 3); do touch part-$n; done",
      "example_output": "",
      "breakdown": [
        "seq is the featured command in the worked pattern.",
        "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. Command substitution turns a seq into controlled loop inputs for creating or processing numbered artifacts.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use seq -w to create chunk-01.txt through chunk-05.txt, each containing its two-digit number, then print their names. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "for n in $(seq -w 01 05); do echo \"$n\" > \"chunk-$n.txt\"; done; printf \"%s\n\" chunk-*.txt && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> -w <values> ; do <values> > <value> ; done ; printf <values>",
        "Use man seq or seq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Drive a repeatable batch loop”.",
          "expected": "chunk-01.txt\nchunk-02.txt\nchunk-03.txt\nchunk-04.txt\nchunk-05.txt",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "Command substitution turns a seq into controlled loop inputs for creating or processing numbered artifacts. You can now use this behavior as part of a larger command-line workflow.",
      "order": 350,
      "command_order": 70,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "seq prints numeric sequences with optional start, increment, and end values. It is useful for bounded loops and generated test input. Command substitution turns a seq into controlled loop inputs for creating or processing numbered artifacts.",
      "stage_kind": "workflow",
      "mode": "capstone",
      "concepts": [
        "seq",
        "redirection",
        "command chaining",
        "command substitution",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [
        "cp",
        "echo",
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uname",
      "order": 351,
      "section": "08-system-and-storage",
      "command": "uname",
      "title": "Identify the kernel family",
      "about": "uname reports kernel and machine facts. -s selects only the kernel name.",
      "example": "uname -s",
      "example_output": "Linux\n",
      "breakdown": [
        "-s requests the kernel name.",
        "The result is Linux on the supported platform."
      ],
      "task": "Print only the kernel name.",
      "solution": "uname -s",
      "hints": [
        "Use the kernel-name option.",
        "Use the checker as a specification: The Linux kernel name was printed.",
        "Try this command shape, replacing placeholders with fixture values: uname -s"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The Linux kernel name was printed.",
          "expected": "Linux",
          "normalize": "trim"
        }
      ],
      "completion": "Kernel name, distribution, release, and architecture are different facts. Ask for each explicitly.",
      "command_order": 71,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "uname reports kernel and machine facts. -s selects only the kernel name.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "uname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uname-2",
      "section": "08-system-and-storage",
      "command": "uname",
      "title": "Print the kernel release",
      "about": "uname reports kernel and machine facts. -s selects only the kernel name. uname -r reports the running kernel release string.",
      "example": "uname -r",
      "example_output": "6.8.0-linux",
      "breakdown": [
        "uname is the featured command in the worked pattern.",
        "uname reports kernel and machine facts. -s selects only the kernel name. uname -r reports the running kernel release string.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the kernel release.",
      "solution": "uname -r",
      "hints": [
        "Use the checker as a specification: A kernel release was printed.",
        "Try this command shape, replacing placeholders with fixture values: uname -r",
        "Use man uname or uname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-nonempty",
          "description": "A kernel release was printed."
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 1 nonblank output line was printed.",
          "expected": 1
        }
      ],
      "completion": "uname -r reports the running kernel release string. You can now use this behavior as part of a larger command-line workflow.",
      "order": 352,
      "command_order": 71,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "uname reports kernel and machine facts. -s selects only the kernel name. uname -r reports the running kernel release string.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "uname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-uname-3",
      "section": "08-system-and-storage",
      "command": "uname",
      "title": "Print the machine architecture",
      "about": "uname reports kernel and machine facts. -s selects only the kernel name. uname -m identifies the hardware architecture exposed to the operating system.",
      "example": "uname -m",
      "example_output": "x86_64",
      "breakdown": [
        "uname is the featured command in the worked pattern.",
        "uname reports kernel and machine facts. -s selects only the kernel name. uname -m identifies the hardware architecture exposed to the operating system.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the machine architecture.",
      "solution": "uname -m",
      "hints": [
        "Use the checker as a specification: An architecture name was printed.",
        "Try this command shape, replacing placeholders with fixture values: uname -m",
        "Use man uname or uname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "An architecture name was printed.",
          "expected": "[A-Za-z0-9_.-]+"
        }
      ],
      "completion": "uname -m identifies the hardware architecture exposed to the operating system. You can now use this behavior as part of a larger command-line workflow.",
      "order": 353,
      "command_order": 71,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "uname reports kernel and machine facts. -s selects only the kernel name. uname -m identifies the hardware architecture exposed to the operating system.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "uname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uname-4",
      "section": "08-system-and-storage",
      "command": "uname",
      "title": "Print the GNU operating-system name",
      "about": "uname reports kernel and machine facts. -s selects only the kernel name. GNU uname -o identifies the operating-system layer separately from the kernel name.",
      "example": "uname -o",
      "example_output": "GNU/Linux",
      "breakdown": [
        "uname is the featured command in the worked pattern.",
        "uname reports kernel and machine facts. -s selects only the kernel name. GNU uname -o identifies the operating-system layer separately from the kernel name.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the operating-system name with uname -o.",
      "solution": "uname -o",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: uname -o",
        "Use man uname or uname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print the GNU operating-system name”.",
          "expected": "GNU/Linux",
          "normalize": "trim"
        }
      ],
      "completion": "GNU uname -o identifies the operating-system layer separately from the kernel name. You can now use this behavior as part of a larger command-line workflow.",
      "order": 354,
      "command_order": 71,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "uname reports kernel and machine facts. -s selects only the kernel name. GNU uname -o identifies the operating-system layer separately from the kernel name.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "uname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uname-5",
      "section": "08-system-and-storage",
      "command": "uname",
      "title": "Build a portable platform identifier",
      "about": "uname reports kernel and machine facts. -s selects only the kernel name. Focused uname fields can be combined into a concise kernel-release/architecture identifier.",
      "example": "printf '%s-%s\\n' \"$(uname -s)\" \"$(uname -m)\"",
      "example_output": "Linux-x86_64",
      "breakdown": [
        "uname is the featured command in the worked pattern.",
        "uname reports kernel and machine facts. -s selects only the kernel name. Focused uname fields can be combined into a concise kernel-release/architecture identifier.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print kernel=<name> release=<release> arch=<machine> using uname substitutions.",
      "solution": "printf 'kernel=%s release=%s arch=%s\\n' \"$(uname -s)\" \"$(uname -r)\" \"$(uname -m)\"",
      "hints": [
        "Use the checker as a specification: The Linux kernel name was labeled. The architecture was labeled.",
        "Try this command shape, replacing placeholders with fixture values: printf <values>",
        "Use man uname or uname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The Linux kernel name was labeled.",
          "expected": "kernel=Linux"
        },
        {
          "type": "stdout-contains",
          "description": "The architecture was labeled.",
          "expected": "arch="
        }
      ],
      "completion": "Focused uname fields can be combined into a concise kernel-release/architecture identifier. You can now use this behavior as part of a larger command-line workflow.",
      "order": 355,
      "command_order": 71,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "uname reports kernel and machine facts. -s selects only the kernel name. Focused uname fields can be combined into a concise kernel-release/architecture identifier.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "uname",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-hostname",
      "order": 356,
      "section": "08-system-and-storage",
      "command": "hostname",
      "title": "Read the current host name",
      "about": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only.",
      "example": "hostname",
      "example_output": "lab-vm\n",
      "breakdown": [
        "No option requests the current configured hostname.",
        "The value depends on the environment."
      ],
      "task": "Print the current hostname.",
      "solution": "hostname",
      "hints": [
        "Run hostname without options.",
        "Use the checker as a specification: A hostname was printed.",
        "Try this command shape, replacing placeholders with fixture values: hostname"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-nonempty",
          "description": "A hostname was printed."
        }
      ],
      "completion": "A hostname is an administrative label, not proof of identity, DNS registration, or network reachability.",
      "command_order": 72,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "hostname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-hostname-2",
      "section": "08-system-and-storage",
      "command": "hostname",
      "title": "Print the short host name",
      "about": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. hostname -s omits any DNS domain suffix and prints the local short name.",
      "example": "hostname -s",
      "example_output": "training-box",
      "breakdown": [
        "hostname is the featured command in the worked pattern.",
        "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. hostname -s omits any DNS domain suffix and prints the local short name.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the short hostname.",
      "solution": "hostname -s",
      "hints": [
        "Use the checker as a specification: A short hostname was printed.",
        "Try this command shape, replacing placeholders with fixture values: hostname -s",
        "Use man hostname or hostname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-nonempty",
          "description": "A short hostname was printed."
        }
      ],
      "completion": "hostname -s omits any DNS domain suffix and prints the local short name. You can now use this behavior as part of a larger command-line workflow.",
      "order": 357,
      "command_order": 72,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. hostname -s omits any DNS domain suffix and prints the local short name.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "hostname"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-hostname-3",
      "section": "08-system-and-storage",
      "command": "hostname",
      "title": "Resolve a host inventory name",
      "about": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. A short hostname can act as a lookup key in a reviewed host inventory without changing system resolver files.",
      "example": "short=$(hostname -s); awk -v host=\"$short\" '$3==host {print $2}' hosts.txt",
      "example_output": "training-box.example.test",
      "breakdown": [
        "hostname is the featured command in the worked pattern.",
        "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. A short hostname can act as a lookup key in a reviewed host inventory without changing system resolver files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use the current short hostname to print its fully qualified name from hosts.txt.",
      "solution": "short=$(hostname -s); awk -v host=\"$short\" '$3==host {print $2}' hosts.txt",
      "hints": [
        "Use the checker as a specification: The inventory fully qualified name was printed.",
        "Try this command shape, replacing placeholders with fixture values: <values> ; awk -v <values>",
        "Use man hostname or hostname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "setup": [
        "short=$(hostname -s); printf '127.0.1.1 %s.example.test %s\\n' \"$short\" \"$short\" > hosts.txt"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The inventory fully qualified name was printed.",
          "expected": ".example.test"
        }
      ],
      "completion": "A short hostname can act as a lookup key in a reviewed host inventory without changing system resolver files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 358,
      "command_order": 72,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. A short hostname can act as a lookup key in a reviewed host inventory without changing system resolver files.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "hostname",
        "command chaining",
        "command substitution",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-hostname-4",
      "section": "08-system-and-storage",
      "command": "hostname",
      "title": "Resolve a host inventory address",
      "about": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. Matching the current hostname against a controlled inventory makes local address reporting deterministic and auditable.",
      "example": "short=$(hostname -s); awk -v host=\"$short\" '$3==host {print $1}' hosts.txt",
      "example_output": "127.0.1.1",
      "breakdown": [
        "hostname is the featured command in the worked pattern.",
        "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. Matching the current hostname against a controlled inventory makes local address reporting deterministic and auditable.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use the current short hostname to print its address from hosts.txt.",
      "solution": "short=$(hostname -s); awk -v host=\"$short\" '$3==host {print $1}' hosts.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <values> ; awk -v <values>",
        "Use man hostname or hostname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "setup": [
        "short=$(hostname -s); printf '127.0.1.1 %s.example.test %s\\n' \"$short\" \"$short\" > hosts.txt"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Resolve a host inventory address”.",
          "expected": "127.0.1.1",
          "normalize": "trim"
        }
      ],
      "completion": "Matching the current hostname against a controlled inventory makes local address reporting deterministic and auditable. You can now use this behavior as part of a larger command-line workflow.",
      "order": 359,
      "command_order": 72,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. Matching the current hostname against a controlled inventory makes local address reporting deterministic and auditable.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "hostname",
        "command chaining",
        "command substitution",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-hostname-5",
      "section": "08-system-and-storage",
      "command": "hostname",
      "title": "Create a host identity record",
      "about": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. Combining hostname with a controlled inventory produces a small, reviewable identity record for logs and diagnostics.",
      "example": "short=$(hostname -s); awk -v host=\"$short\" '$3==host {print \"host=\" $3 \" fqdn=\" $2 \" address=\" $1}' hosts.txt",
      "example_output": "host=training-box fqdn=training-box.example.test address=127.0.1.1",
      "breakdown": [
        "hostname is the featured command in the worked pattern.",
        "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. Combining hostname with a controlled inventory produces a small, reviewable identity record for logs and diagnostics.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print host=<short-name> fqdn=<inventory-name> address=<inventory-address>.",
      "solution": "short=$(hostname -s); awk -v host=\"$short\" '$3==host {print \"host=\" $3 \" fqdn=\" $2 \" address=\" $1}' hosts.txt",
      "hints": [
        "Use the checker as a specification: The host label was included. The inventory fully qualified name was labeled.",
        "Try this command shape, replacing placeholders with fixture values: <values> ; awk -v <values>",
        "Use man hostname or hostname --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "setup": [
        "short=$(hostname -s); printf '127.0.1.1 %s.example.test %s\\n' \"$short\" \"$short\" > hosts.txt"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The host label was included.",
          "expected": "host="
        },
        {
          "type": "stdout-contains",
          "description": "The inventory fully qualified name was labeled.",
          "expected": " fqdn="
        },
        {
          "type": "stdout-contains",
          "description": "The inventory address was labeled.",
          "expected": " address=127.0.1.1"
        }
      ],
      "completion": "Combining hostname with a controlled inventory produces a small, reviewable identity record for logs and diagnostics. You can now use this behavior as part of a larger command-line workflow.",
      "order": 360,
      "command_order": 72,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "hostname prints or, with privilege and options, can change the system host name. This rep performs a read only. Combining hostname with a controlled inventory produces a small, reviewable identity record for logs and diagnostics.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "hostname",
        "command chaining",
        "command substitution",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-uptime",
      "order": 361,
      "section": "08-system-and-storage",
      "command": "uptime",
      "title": "Read uptime and load averages",
      "about": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage.",
      "example": "uptime",
      "example_output": "10:00:00 up 2 days, load average: 0.10, 0.20, 0.15\n",
      "breakdown": [
        "Uptime describes time since boot.",
        "The three loads cover different averaging windows."
      ],
      "task": "Print the current system uptime summary.",
      "solution": "uptime",
      "hints": [
        "No option is required.",
        "Use the checker as a specification: An uptime summary was printed.",
        "Try this command shape, replacing placeholders with fixture values: uptime"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-nonempty",
          "description": "An uptime summary was printed."
        }
      ],
      "completion": "Interpret load relative to runnable work, CPU count, and I/O conditions; it is not a standalone health score.",
      "command_order": 73,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "uptime"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-uptime-2",
      "section": "08-system-and-storage",
      "command": "uptime",
      "title": "Print a human-friendly duration",
      "about": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. uptime -p presents elapsed uptime as readable units rather than a full status line.",
      "example": "uptime -p",
      "example_output": "up 2 hours, 4 minutes",
      "breakdown": [
        "uptime is the featured command in the worked pattern.",
        "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. uptime -p presents elapsed uptime as readable units rather than a full status line.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the pretty uptime.",
      "solution": "uptime -p",
      "hints": [
        "Use the checker as a specification: The readable uptime begins with up.",
        "Try this command shape, replacing placeholders with fixture values: uptime -p",
        "Use man uptime or uptime --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The readable uptime begins with up.",
          "expected": "up"
        }
      ],
      "completion": "uptime -p presents elapsed uptime as readable units rather than a full status line. You can now use this behavior as part of a larger command-line workflow.",
      "order": 362,
      "command_order": 73,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. uptime -p presents elapsed uptime as readable units rather than a full status line.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "uptime"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-uptime-3",
      "section": "08-system-and-storage",
      "command": "uptime",
      "title": "Print the system start time",
      "about": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. uptime -s reports the timestamp when the current kernel session began.",
      "example": "uptime -s",
      "example_output": "2026-01-02 08:00:00",
      "breakdown": [
        "uptime is the featured command in the worked pattern.",
        "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. uptime -s reports the timestamp when the current kernel session began.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the system startup timestamp.",
      "solution": "uptime -s",
      "hints": [
        "Use the checker as a specification: A startup date and time were printed.",
        "Try this command shape, replacing placeholders with fixture values: uptime -s",
        "Use man uptime or uptime --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "A startup date and time were printed.",
          "expected": "[0-9]{4}-[0-9]{2}-[0-9]{2} [0-9]{2}:[0-9]{2}:[0-9]{2}"
        }
      ],
      "completion": "uptime -s reports the timestamp when the current kernel session began. You can now use this behavior as part of a larger command-line workflow.",
      "order": 363,
      "command_order": 73,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. uptime -s reports the timestamp when the current kernel session began.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "uptime"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uptime-4",
      "section": "08-system-and-storage",
      "command": "uptime",
      "title": "Extract the load averages",
      "about": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. The standard uptime line includes one-, five-, and fifteen-minute load averages after its final label.",
      "example": "uptime | sed 's/.*load average: //'",
      "example_output": "0.10, 0.15, 0.20",
      "breakdown": [
        "uptime is the featured command in the worked pattern.",
        "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. The standard uptime line includes one-, five-, and fifteen-minute load averages after its final label.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only the three load-average values from uptime.",
      "solution": "uptime | sed 's/.*load average: //'",
      "hints": [
        "Use the checker as a specification: Three load averages were printed.",
        "Try this command shape, replacing placeholders with fixture values: uptime | sed <values>",
        "Use man uptime or uptime --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "Three load averages were printed.",
          "expected": "[0-9., ]+"
        }
      ],
      "completion": "The standard uptime line includes one-, five-, and fifteen-minute load averages after its final label. You can now use this behavior as part of a larger command-line workflow.",
      "order": 364,
      "command_order": 73,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. The standard uptime line includes one-, five-, and fifteen-minute load averages after its final label.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "uptime",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "sed"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-uptime-5",
      "section": "08-system-and-storage",
      "command": "uptime",
      "title": "Build a boot-status record",
      "about": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. Pretty duration and startup time together provide both relative and absolute boot context.",
      "example": "printf 'started=%s\\nuptime=%s\\n' \"$(uptime -s)\" \"$(uptime -p)\"",
      "example_output": "started=...\nuptime=up ...",
      "breakdown": [
        "uptime is the featured command in the worked pattern.",
        "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. Pretty duration and startup time together provide both relative and absolute boot context.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print started=<uptime -s> followed by duration=<uptime -p>.",
      "solution": "printf 'started=%s\\nduration=%s\\n' \"$(uptime -s)\" \"$(uptime -p)\"",
      "hints": [
        "Use the checker as a specification: The startup field was included. The duration field was included.",
        "Try this command shape, replacing placeholders with fixture values: printf <values>",
        "Use man uptime or uptime --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The startup field was included.",
          "expected": "started="
        },
        {
          "type": "stdout-contains",
          "description": "The duration field was included.",
          "expected": "duration=up"
        }
      ],
      "completion": "Pretty duration and startup time together provide both relative and absolute boot context. You can now use this behavior as part of a larger command-line workflow.",
      "order": 365,
      "command_order": 73,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "uptime reports current time, time since boot, user count, and load averages. Load average is not CPU percentage. Pretty duration and startup time together provide both relative and absolute boot context.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "uptime",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-date",
      "order": 366,
      "section": "08-system-and-storage",
      "command": "date",
      "title": "Format a fixed timestamp",
      "about": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic.",
      "example": "date -u -d @0 +%F",
      "example_output": "1970-01-01\n",
      "breakdown": [
        "-u selects UTC.",
        "-d @0 uses Unix epoch zero.",
        "+%F formats YYYY-MM-DD."
      ],
      "task": "Print Unix epoch zero as a UTC YYYY-MM-DD date.",
      "solution": "date -u -d @0 +%F",
      "hints": [
        "Use UTC, input @0, and format %F.",
        "Use the checker as a specification: Epoch zero was formatted correctly.",
        "Try this command shape, replacing placeholders with fixture values: date -u -d <values>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "Epoch zero was formatted correctly.",
          "expected": "1970-01-01",
          "normalize": "trim"
        }
      ],
      "completion": "Always state timezone and input interpretation when timestamps become evidence or cross-system data.",
      "command_order": 74,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "date"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-date-2",
      "section": "08-system-and-storage",
      "command": "date",
      "title": "Choose a stable output format",
      "about": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. A leading + introduces a date format string with explicit fields.",
      "example": "date '+%Y-%m-%d'",
      "example_output": "2026-01-02",
      "breakdown": [
        "date is the featured command in the worked pattern.",
        "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. A leading + introduces a date format string with explicit fields.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the current UTC date as YYYY-MM-DD.",
      "solution": "date -u '+%Y-%m-%d'",
      "hints": [
        "Use the checker as a specification: An ISO calendar date was printed.",
        "Try this command shape, replacing placeholders with fixture values: date -u <value>",
        "Use man date or date --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-regex",
          "description": "An ISO calendar date was printed.",
          "expected": "[0-9]{4}-[0-9]{2}-[0-9]{2}"
        }
      ],
      "completion": "A leading + introduces a date format string with explicit fields. You can now use this behavior as part of a larger command-line workflow.",
      "order": 367,
      "command_order": 74,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. A leading + introduces a date format string with explicit fields.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "date",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-date-3",
      "section": "08-system-and-storage",
      "command": "date",
      "title": "Parse a fixed date expression",
      "about": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. date -d parses a supplied date or relative expression without changing the system clock.",
      "example": "date -d '2026-01-02 + 7 days' '+%F'",
      "example_output": "2026-01-09",
      "breakdown": [
        "date is the featured command in the worked pattern.",
        "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. date -d parses a supplied date or relative expression without changing the system clock.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the date 30 days after 2026-04-01 as YYYY-MM-DD.",
      "solution": "date -u -d '2026-04-01 + 30 days' '+%F'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: date -u -d <values>",
        "Use man date or date --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Parse a fixed date expression”.",
          "expected": "2026-05-01",
          "normalize": "trim"
        }
      ],
      "completion": "date -d parses a supplied date or relative expression without changing the system clock. You can now use this behavior as part of a larger command-line workflow.",
      "order": 368,
      "command_order": 74,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. date -d parses a supplied date or relative expression without changing the system clock.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "date",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-date-4",
      "section": "08-system-and-storage",
      "command": "date",
      "title": "Format a file timestamp",
      "about": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. date -r reads a file modification time and formats it like any other timestamp.",
      "example": "date -u -r artifact '+%FT%TZ'",
      "example_output": "2026-01-02T03:04:00Z",
      "breakdown": [
        "date is the featured command in the worked pattern.",
        "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. date -r reads a file modification time and formats it like any other timestamp.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set artifact.txt to 2025-06-15 12:30 UTC, then print that modification time as an ISO UTC timestamp.",
      "solution": "TZ=UTC touch -t 202506151230 artifact.txt && date -u -r artifact.txt '+%FT%TZ'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <value> touch -t <values> && date -u -r <values>",
        "Use man date or date --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact.txt": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Format a file timestamp”.",
          "expected": "2025-06-15T12:30:00Z",
          "normalize": "trim"
        }
      ],
      "completion": "date -r reads a file modification time and formats it like any other timestamp. You can now use this behavior as part of a larger command-line workflow.",
      "order": 369,
      "command_order": 74,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. date -r reads a file modification time and formats it like any other timestamp.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "date",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "touch"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-date-5",
      "section": "08-system-and-storage",
      "command": "date",
      "title": "Generate a deterministic range of dates",
      "about": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. A loop can pass relative offsets to date to build calendar-based filenames or schedules.",
      "example": "for n in 0 1 2; do date -d \"2026-01-01 +$n day\" +%F; done",
      "example_output": "2026-01-01\n2026-01-02\n2026-01-03",
      "breakdown": [
        "date is the featured command in the worked pattern.",
        "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. A loop can pass relative offsets to date to build calendar-based filenames or schedules.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print five dates beginning 2026-08-10, one day apart.",
      "solution": "for n in 0 1 2 3 4; do date -u -d \"2026-08-10 +$n day\" +%F; done",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <value> -u -d <values> ; done",
        "Use man date or date --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Generate a deterministic range of dates”.",
          "expected": "2026-08-10\n2026-08-11\n2026-08-12\n2026-08-13\n2026-08-14",
          "normalize": "trim"
        }
      ],
      "completion": "A loop can pass relative offsets to date to build calendar-based filenames or schedules. You can now use this behavior as part of a larger command-line workflow.",
      "order": 370,
      "command_order": 74,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "date displays and formats time. GNU date -d can parse an explicit input, making transformations deterministic. A loop can pass relative offsets to date to build calendar-based filenames or schedules.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "date",
        "command chaining",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-cal",
      "order": 371,
      "section": "08-system-and-storage",
      "command": "cal",
      "title": "Print a calendar",
      "about": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils.",
      "example": "cal 1 2000",
      "example_output": "January 2000\n",
      "breakdown": [
        "1 selects January.",
        "2000 is the year."
      ],
      "task": "Print the calendar for January 2000.",
      "solution": "cal 1 2000",
      "hints": [
        "Provide month number 1 and year 2000.",
        "Use the checker as a specification: The January 2000 calendar was printed.",
        "Try this command shape, replacing placeholders with fixture values: cal <values>"
      ],
      "files": {},
      "packages": [
        "ncal"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The January 2000 calendar was printed.",
          "expected": "2000"
        }
      ],
      "completion": "Calendar layout and week conventions can be locale-dependent. Do not parse the human display as a data format.",
      "command_order": 75,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "cal"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-cal-2",
      "section": "08-system-and-storage",
      "command": "cal",
      "title": "Display a specific month",
      "about": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal accepts a numeric month and year so output is deterministic rather than tied to today.",
      "example": "cal 1 2026",
      "example_output": "January 2026 ...",
      "breakdown": [
        "cal is the featured command in the worked pattern.",
        "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal accepts a numeric month and year so output is deterministic rather than tied to today.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Display the calendar for February 2028.",
      "solution": "cal 2 2028",
      "hints": [
        "Use the checker as a specification: The requested month heading was printed. Leap day 29 was present.",
        "Try this command shape, replacing placeholders with fixture values: cal <values>",
        "Use man cal or cal --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The requested month heading was printed.",
          "expected": "February 2028"
        },
        {
          "type": "stdout-contains",
          "description": "Leap day 29 was present.",
          "expected": "29"
        }
      ],
      "completion": "cal accepts a numeric month and year so output is deterministic rather than tied to today. You can now use this behavior as part of a larger command-line workflow.",
      "order": 372,
      "command_order": 75,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal accepts a numeric month and year so output is deterministic rather than tied to today.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "cal"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-cal-3",
      "section": "08-system-and-storage",
      "command": "cal",
      "title": "Show a whole year",
      "about": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. A single year operand prints all twelve monthly calendars.",
      "example": "cal 2026",
      "example_output": "2026 ... January ... December",
      "breakdown": [
        "cal is the featured command in the worked pattern.",
        "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. A single year operand prints all twelve monthly calendars.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Display the full calendar for 2027.",
      "solution": "cal 2027",
      "hints": [
        "Use the checker as a specification: The year heading was printed. December was included.",
        "Try this command shape, replacing placeholders with fixture values: cal <value>",
        "Use man cal or cal --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The year heading was printed.",
          "expected": "2027"
        },
        {
          "type": "stdout-contains",
          "description": "December was included.",
          "expected": "December"
        }
      ],
      "completion": "A single year operand prints all twelve monthly calendars. You can now use this behavior as part of a larger command-line workflow.",
      "order": 373,
      "command_order": 75,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. A single year operand prints all twelve monthly calendars.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "cal"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-cal-4",
      "section": "08-system-and-storage",
      "command": "cal",
      "title": "Display Julian day numbers",
      "about": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal -j replaces day-of-month values with their numbered day of the year.",
      "example": "cal -j 3 2026",
      "example_output": "March 2026\nSu Mo Tu ...\n60 61 62 ...",
      "breakdown": [
        "cal is the featured command in the worked pattern.",
        "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal -j replaces day-of-month values with their numbered day of the year.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Display March 2026 using Julian day-of-year numbers.",
      "solution": "cal -j 3 2026",
      "hints": [
        "Use the checker as a specification: The March heading was printed. March begins at day 60 in 2026.",
        "Try this command shape, replacing placeholders with fixture values: cal -j <values>",
        "Use man cal or cal --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The March heading was printed.",
          "expected": "March 2026"
        },
        {
          "type": "stdout-contains",
          "description": "March begins at day 60 in 2026.",
          "expected": "60"
        }
      ],
      "completion": "cal -j replaces day-of-month values with their numbered day of the year. You can now use this behavior as part of a larger command-line workflow.",
      "order": 374,
      "command_order": 75,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal -j replaces day-of-month values with their numbered day of the year.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "cal"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-cal-5",
      "section": "08-system-and-storage",
      "command": "cal",
      "title": "Show a three-month planning window",
      "about": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal -3 displays the previous, selected, and next months as one planning view.",
      "example": "cal -3 6 2026",
      "example_output": "May 2026 June 2026 July 2026",
      "breakdown": [
        "cal is the featured command in the worked pattern.",
        "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal -3 displays the previous, selected, and next months as one planning view.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Display May, June, and July 2026 using a three-month view centered on June.",
      "solution": "cal -3 6 2026",
      "hints": [
        "Use the checker as a specification: May was included. June was included.",
        "Try this command shape, replacing placeholders with fixture values: cal -3 <values>",
        "Use man cal or cal --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "May was included.",
          "expected": "May 2026"
        },
        {
          "type": "stdout-contains",
          "description": "June was included.",
          "expected": "June 2026"
        },
        {
          "type": "stdout-contains",
          "description": "July was included.",
          "expected": "July 2026"
        }
      ],
      "completion": "cal -3 displays the previous, selected, and next months as one planning view. You can now use this behavior as part of a larger command-line workflow.",
      "order": 375,
      "command_order": 75,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "cal prints calendars for a month or year. It is commonly provided by the ncal package rather than coreutils. cal -3 displays the previous, selected, and next months as one planning view.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "cal"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-env",
      "order": 376,
      "section": "08-system-and-storage",
      "command": "env",
      "title": "Run with a controlled environment",
      "about": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment.",
      "example": "env -i DEMO=ready printenv DEMO",
      "example_output": "ready\n",
      "breakdown": [
        "-i discards inherited variables.",
        "DEMO=ready adds one variable.",
        "printenv reads it in the child."
      ],
      "task": "Run printenv with an otherwise empty environment containing MODE=practice, and print MODE.",
      "solution": "env -i MODE=practice printenv MODE",
      "hints": [
        "Use env -i, add the assignment, then run printenv.",
        "Use the checker as a specification: The controlled MODE value was printed.",
        "Try this command shape, replacing placeholders with fixture values: env -i <values>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The controlled MODE value was printed.",
          "expected": "practice",
          "normalize": "trim"
        }
      ],
      "completion": "Environment variables influence child processes. Record relevant variables when reproducing context-dependent behavior.",
      "command_order": 76,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "env"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "printenv"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-env-2",
      "section": "08-system-and-storage",
      "command": "env",
      "title": "Set a variable for one command",
      "about": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env NAME=value applies a temporary environment assignment only to the child command.",
      "example": "env MODE=test printenv MODE",
      "example_output": "test",
      "breakdown": [
        "env is the featured command in the worked pattern.",
        "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env NAME=value applies a temporary environment assignment only to the child command.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run printenv APP_MODE with APP_MODE=practice supplied by env.",
      "solution": "env APP_MODE=practice printenv APP_MODE",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: env <values>",
        "Use man env or env --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Set a variable for one command”.",
          "expected": "practice",
          "normalize": "trim"
        }
      ],
      "completion": "env NAME=value applies a temporary environment assignment only to the child command. You can now use this behavior as part of a larger command-line workflow.",
      "order": 377,
      "command_order": 76,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env NAME=value applies a temporary environment assignment only to the child command.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "env"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "printenv"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-env-3",
      "section": "08-system-and-storage",
      "command": "env",
      "title": "Remove one inherited variable",
      "about": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env -u removes a selected name from the child environment without changing the parent shell.",
      "example": "TOKEN=x env -u TOKEN printenv TOKEN",
      "example_output": "",
      "breakdown": [
        "env is the featured command in the worked pattern.",
        "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env -u removes a selected name from the child environment without changing the parent shell.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set SECRET=temporary, then use env -u to run a shell that prints absent when SECRET is unset.",
      "solution": "SECRET=temporary env -u SECRET sh -c 'if [ -z \"${SECRET+x}\" ]; then echo absent; fi'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <value> env -u <values> -c <values> -z <values> ; then <values> ; <value>",
        "Use man env or env --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Remove one inherited variable”.",
          "expected": "absent",
          "normalize": "trim"
        }
      ],
      "completion": "env -u removes a selected name from the child environment without changing the parent shell. You can now use this behavior as part of a larger command-line workflow.",
      "order": 378,
      "command_order": 76,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env -u removes a selected name from the child environment without changing the parent shell.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "env",
        "command chaining",
        "quoting",
        "filtering and conditions"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-env-4",
      "section": "08-system-and-storage",
      "command": "env",
      "title": "Start with an empty environment",
      "about": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env -i clears inherited variables so only explicitly provided values reach the child.",
      "example": "env -i MODE=test printenv",
      "example_output": "MODE=test",
      "breakdown": [
        "env is the featured command in the worked pattern.",
        "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env -i clears inherited variables so only explicitly provided values reach the child.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Start an empty environment containing only MODE=clean and REGION=west, then print it sorted.",
      "solution": "env -i MODE=clean REGION=west printenv | sort",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: env -i <values> | sort",
        "Use man env or env --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Start with an empty environment”.",
          "expected": "MODE=clean\nREGION=west",
          "normalize": "trim"
        }
      ],
      "completion": "env -i clears inherited variables so only explicitly provided values reach the child. You can now use this behavior as part of a larger command-line workflow.",
      "order": 379,
      "command_order": 76,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env -i clears inherited variables so only explicitly provided values reach the child.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "env",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [
        "printenv"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-env-5",
      "section": "08-system-and-storage",
      "command": "env",
      "title": "Run a controlled build environment",
      "about": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env can document and isolate all per-command settings for a repeatable workflow invocation.",
      "example": "env MODE=prod REGION=west sh -c 'echo $MODE/$REGION'",
      "example_output": "prod/west",
      "breakdown": [
        "env is the featured command in the worked pattern.",
        "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env can document and isolate all per-command settings for a repeatable workflow invocation.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Run build.sh with MODE=release and TARGET=linux, saving its environment-derived output to build.txt.",
      "solution": "env MODE=release TARGET=linux sh build.sh > build.txt",
      "hints": [
        "Use the checker as a specification: The controlled build settings were used.",
        "Try this command shape, replacing placeholders with fixture values: env <values> > <value>",
        "Use man env or env --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "build.sh": "#!/bin/sh\nprintf \"mode=%s target=%s\\n\" \"$MODE\" \"$TARGET\"\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "The controlled build settings were used.",
          "path": "build.txt",
          "expected": "mode=release target=linux\n"
        }
      ],
      "completion": "env can document and isolate all per-command settings for a repeatable workflow invocation. You can now use this behavior as part of a larger command-line workflow.",
      "order": 380,
      "command_order": 76,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "env prints variables or runs a command with selected environment assignments. -i starts from an empty environment. env can document and isolate all per-command settings for a repeatable workflow invocation.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "env",
        "redirection"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printenv",
      "order": 381,
      "section": "08-system-and-storage",
      "command": "printenv",
      "title": "Read one environment variable",
      "about": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values.",
      "example": "printenv HOME",
      "example_output": "/home/learner\n",
      "breakdown": [
        "HOME is the requested variable name.",
        "Only its current value is printed."
      ],
      "task": "Print only the HOME value supplied to this disposable rep.",
      "solution": "printenv HOME",
      "hints": [
        "Name HOME explicitly instead of dumping the entire environment.",
        "Use the checker as a specification: The isolated HOME value was printed.",
        "Try this command shape, replacing placeholders with fixture values: printenv <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The isolated HOME value was printed.",
          "expected": "{{workspace}}",
          "normalize": "trim"
        }
      ],
      "completion": "Environment dumps can contain secrets. Query only the variable needed and handle output accordingly.",
      "command_order": 77,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "printenv"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printenv-2",
      "section": "08-system-and-storage",
      "command": "printenv",
      "title": "Inspect PATH one directory per line",
      "about": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv emits the raw PATH value, which tr can split into its ordered search directories.",
      "example": "printenv PATH | tr ':' '\\n'",
      "example_output": "/usr/local/sbin\n/usr/local/bin\n...",
      "breakdown": [
        "printenv is the featured command in the worked pattern.",
        "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv emits the raw PATH value, which tr can split into its ordered search directories.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the PATH search directories one per line.",
      "solution": "printenv PATH | tr ':' '\\n'",
      "hints": [
        "Use the checker as a specification: The standard binary directory was included.",
        "Try this command shape, replacing placeholders with fixture values: printenv <value> | tr <values>",
        "Use man printenv or printenv --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The standard binary directory was included.",
          "expected": "/usr/bin"
        }
      ],
      "completion": "printenv emits the raw PATH value, which tr can split into its ordered search directories. You can now use this behavior as part of a larger command-line workflow.",
      "order": 382,
      "command_order": 77,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv emits the raw PATH value, which tr can split into its ordered search directories.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "printenv",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "tr"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-printenv-3",
      "section": "08-system-and-storage",
      "command": "printenv",
      "title": "Print several named values",
      "about": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv accepts multiple names and prints each value in operand order.",
      "example": "printenv HOME LANG",
      "example_output": "/work\nC.UTF-8",
      "breakdown": [
        "printenv is the featured command in the worked pattern.",
        "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv accepts multiple names and prints each value in operand order.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print HOME, LANG, and TERM in that order.",
      "solution": "printenv HOME LANG TERM",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: printenv <values>",
        "Use man printenv or printenv --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print several named values”.",
          "expected": "{{workspace}}\nC.UTF-8\nxterm-256color",
          "normalize": "trim"
        }
      ],
      "completion": "printenv accepts multiple names and prints each value in operand order. You can now use this behavior as part of a larger command-line workflow.",
      "order": 383,
      "command_order": 77,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv accepts multiple names and prints each value in operand order.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "printenv"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printenv-4",
      "section": "08-system-and-storage",
      "command": "printenv",
      "title": "Test whether a variable is exported",
      "about": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv returns a nonzero status for a name absent from the process environment.",
      "example": "printenv MISSING || echo not-exported",
      "example_output": "not-exported",
      "breakdown": [
        "printenv is the featured command in the worked pattern.",
        "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv returns a nonzero status for a name absent from the process environment.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use printenv to check GYM_MISSING and print not-exported when it is absent.",
      "solution": "printenv GYM_MISSING >/dev/null || echo not-exported",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: printenv <value> > <value> | | echo not-exported",
        "Use man printenv or printenv --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Test whether a variable is exported”.",
          "expected": "not-exported",
          "normalize": "trim"
        }
      ],
      "completion": "printenv returns a nonzero status for a name absent from the process environment. You can now use this behavior as part of a larger command-line workflow.",
      "order": 384,
      "command_order": 77,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. printenv returns a nonzero status for a name absent from the process environment.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "printenv",
        "pipelines",
        "redirection"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-printenv-5",
      "section": "08-system-and-storage",
      "command": "printenv",
      "title": "Create a selected environment report",
      "about": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. A loop around printenv can label a reviewed subset instead of exposing an entire environment dump.",
      "example": "for n in HOME LANG; do printf '%s=%s\\n' \"$n\" \"$(printenv \"$n\")\"; done",
      "example_output": "HOME=/work\nLANG=C.UTF-8",
      "breakdown": [
        "printenv is the featured command in the worked pattern.",
        "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. A loop around printenv can label a reviewed subset instead of exposing an entire environment dump.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print labeled HOME, LANG, and TERM values in that order.",
      "solution": "for name in HOME LANG TERM; do printf '%s=%s\\n' \"$name\" \"$(printenv \"$name\")\"; done",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: for <values> ; do <values> ; done",
        "Use man printenv or printenv --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Create a selected environment report”.",
          "expected": "HOME={{workspace}}\nLANG=C.UTF-8\nTERM=xterm-256color",
          "normalize": "trim"
        }
      ],
      "completion": "A loop around printenv can label a reviewed subset instead of exposing an entire environment dump. You can now use this behavior as part of a larger command-line workflow.",
      "order": 385,
      "command_order": 77,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "printenv prints environment variables visible to the process. Naming one variable avoids dumping unrelated or sensitive values. A loop around printenv can label a reviewed subset instead of exposing an entire environment dump.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "printenv",
        "command chaining",
        "command substitution",
        "variables",
        "quoting",
        "iteration"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-df",
      "order": 386,
      "section": "08-system-and-storage",
      "command": "df",
      "title": "Inspect filesystem capacity",
      "about": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection.",
      "example": "df -P .",
      "example_output": "Filesystem 1024-blocks Used Available Capacity Mounted on\n",
      "breakdown": [
        ". selects the filesystem containing the current directory.",
        "-P uses one row per filesystem."
      ],
      "task": "Print the POSIX-style filesystem capacity row for the current workspace.",
      "solution": "df -P .",
      "hints": [
        "Use -P and select the current directory.",
        "Use the checker as a specification: A filesystem header was printed. df completed successfully.",
        "Try this command shape, replacing placeholders with fixture values: df -P <value>"
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "A filesystem header was printed.",
          "expected": "Filesystem"
        },
        {
          "type": "exit-code",
          "description": "df completed successfully.",
          "expected": 0
        }
      ],
      "completion": "df reports filesystem capacity; du reports space attributed to paths. They answer different questions.",
      "command_order": 78,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "df"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-df-2",
      "section": "08-system-and-storage",
      "command": "df",
      "title": "Show human-readable filesystem sizes",
      "about": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df -h scales block counts into readable units such as MiB and GiB.",
      "example": "df -h /work",
      "example_output": "Filesystem Size Used Avail Use% Mounted on ...",
      "breakdown": [
        "df is the featured command in the worked pattern.",
        "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df -h scales block counts into readable units such as MiB and GiB.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Show human-readable space information for the workspace filesystem.",
      "solution": "df -h /work",
      "hints": [
        "Use the checker as a specification: The size heading was printed. The available-space heading was printed.",
        "Try this command shape, replacing placeholders with fixture values: df -h <value>",
        "Use man df or df --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The size heading was printed.",
          "expected": "Size"
        },
        {
          "type": "stdout-contains",
          "description": "The available-space heading was printed.",
          "expected": "Avail"
        }
      ],
      "completion": "df -h scales block counts into readable units such as MiB and GiB. You can now use this behavior as part of a larger command-line workflow.",
      "order": 387,
      "command_order": 78,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df -h scales block counts into readable units such as MiB and GiB.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "df"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-df-3",
      "section": "08-system-and-storage",
      "command": "df",
      "title": "Inspect inode availability",
      "about": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df -i reports inode totals and use rather than storage blocks.",
      "example": "df -i /work",
      "example_output": "Filesystem Inodes IUsed IFree IUse% Mounted on ...",
      "breakdown": [
        "df is the featured command in the worked pattern.",
        "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df -i reports inode totals and use rather than storage blocks.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Show inode usage for /work.",
      "solution": "df -i /work",
      "hints": [
        "Use the checker as a specification: The inode heading was printed. Free inodes were reported.",
        "Try this command shape, replacing placeholders with fixture values: df -i <value>",
        "Use man df or df --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The inode heading was printed.",
          "expected": "Inodes"
        },
        {
          "type": "stdout-contains",
          "description": "Free inodes were reported.",
          "expected": "IFree"
        }
      ],
      "completion": "df -i reports inode totals and use rather than storage blocks. You can now use this behavior as part of a larger command-line workflow.",
      "order": 388,
      "command_order": 78,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df -i reports inode totals and use rather than storage blocks.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "df"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-df-4",
      "section": "08-system-and-storage",
      "command": "df",
      "title": "Select exact output columns",
      "about": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df --output creates a focused filesystem report with only named fields.",
      "example": "df --output=source,size,used,avail,pcent,target /work",
      "example_output": "Filesystem 1K-blocks Used Available Use% Mounted on",
      "breakdown": [
        "df is the featured command in the worked pattern.",
        "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df --output creates a focused filesystem report with only named fields.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print source, filesystem type, size, available space, use percent, and target for /work.",
      "solution": "df --output=source,fstype,size,avail,pcent,target /work",
      "hints": [
        "Use the checker as a specification: The filesystem type heading was included. The use percentage heading was included.",
        "Try this command shape, replacing placeholders with fixture values: df <values>",
        "Use man df or df --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The filesystem type heading was included.",
          "expected": "Type"
        },
        {
          "type": "stdout-contains",
          "description": "The use percentage heading was included.",
          "expected": "Use%"
        }
      ],
      "completion": "df --output creates a focused filesystem report with only named fields. You can now use this behavior as part of a larger command-line workflow.",
      "order": 389,
      "command_order": 78,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. df --output creates a focused filesystem report with only named fields.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "df"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "source"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-df-5",
      "section": "08-system-and-storage",
      "command": "df",
      "title": "Emit a compact capacity record",
      "about": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. The field-focused df output can feed awk to create a one-line capacity record for monitoring.",
      "example": "df -P /work | awk 'NR==2 {print \"used=\" $5}'",
      "example_output": "used=42%",
      "breakdown": [
        "df is the featured command in the worked pattern.",
        "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. The field-focused df output can feed awk to create a one-line capacity record for monitoring.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use POSIX df output and awk to print filesystem=<source> used=<percent> mount=<target> for /work.",
      "solution": "df -P /work | awk 'NR==2 {print \"filesystem=\" $1 \" used=\" $5 \" mount=\" $6}'",
      "hints": [
        "Use the checker as a specification: The filesystem was labeled. The used percentage was labeled.",
        "Try this command shape, replacing placeholders with fixture values: df -P <value> | awk <values>",
        "Use man df or df --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The filesystem was labeled.",
          "expected": "filesystem="
        },
        {
          "type": "stdout-contains",
          "description": "The used percentage was labeled.",
          "expected": " used="
        },
        {
          "type": "stdout-contains",
          "description": "The mount was labeled.",
          "expected": " mount="
        }
      ],
      "completion": "The field-focused df output can feed awk to create a one-line capacity record for monitoring. You can now use this behavior as part of a larger command-line workflow.",
      "order": 390,
      "command_order": 78,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "df reports filesystem-level space. -P requests a predictable POSIX-style row layout for simple inspection. The field-focused df output can feed awk to create a one-line capacity record for monitoring.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "df",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-du",
      "order": 391,
      "section": "08-system-and-storage",
      "command": "du",
      "title": "Measure path disk usage",
      "about": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units.",
      "example": "du -sk data",
      "example_output": "4 data\n",
      "breakdown": [
        "-s emits one summary.",
        "-k selects KiB units."
      ],
      "task": "Print one KiB summary for the data directory.",
      "solution": "du -sk data",
      "hints": [
        "Combine summary and KiB options.",
        "Use the checker as a specification: A numeric KiB count and data label were printed.",
        "Try this command shape, replacing placeholders with fixture values: du -sk <value>"
      ],
      "files": {
        "data/a.txt": "alpha\n",
        "data/b.txt": "beta\n"
      },
      "checks": [
        {
          "type": "stdout-regex",
          "description": "A numeric KiB count and data label were printed.",
          "expected": "[0-9]+\\s+data"
        }
      ],
      "completion": "Allocated size can differ from apparent byte size because of blocks, sparse files, compression, and deduplication.",
      "command_order": 79,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "du"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-du-2",
      "section": "08-system-and-storage",
      "command": "du",
      "title": "Summarize one directory",
      "about": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. du -s produces one total instead of a row for every descendant; -h scales it readably.",
      "example": "du -sh project",
      "example_output": "24K project",
      "breakdown": [
        "du is the featured command in the worked pattern.",
        "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. du -s produces one total instead of a row for every descendant; -h scales it readably.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print one human-readable size summary for project.",
      "solution": "du -sh project",
      "hints": [
        "Use the checker as a specification: The project path was labeled.",
        "Try this command shape, replacing placeholders with fixture values: du -sh <value>",
        "Use man du or du --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "project/a.txt": "alpha\n",
        "project/nested/b.txt": "beta\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The project path was labeled.",
          "expected": "project"
        }
      ],
      "completion": "du -s produces one total instead of a row for every descendant; -h scales it readably. You can now use this behavior as part of a larger command-line workflow.",
      "order": 392,
      "command_order": 79,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. du -s produces one total instead of a row for every descendant; -h scales it readably.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "du"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-du-3",
      "section": "08-system-and-storage",
      "command": "du",
      "title": "Show one directory depth",
      "about": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. du --max-depth=1 shows direct child totals plus the overall directory total.",
      "example": "du -h --max-depth=1 project",
      "example_output": "... project/src\n... project/tests\n... project",
      "breakdown": [
        "du is the featured command in the worked pattern.",
        "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. du --max-depth=1 shows direct child totals plus the overall directory total.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Show human-readable totals one level below workspace-data.",
      "solution": "du -h --max-depth=1 workspace-data",
      "hints": [
        "Use the checker as a specification: The logs child was included. The cache child was included.",
        "Try this command shape, replacing placeholders with fixture values: du -h --max-depth=1 workspace-data",
        "Use man du or du --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "workspace-data/logs/a.log": "a\n",
        "workspace-data/cache/b.dat": "b\n",
        "workspace-data/docs/c.txt": "c\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The logs child was included.",
          "expected": "workspace-data/logs"
        },
        {
          "type": "stdout-contains",
          "description": "The cache child was included.",
          "expected": "workspace-data/cache"
        }
      ],
      "completion": "du --max-depth=1 shows direct child totals plus the overall directory total. You can now use this behavior as part of a larger command-line workflow.",
      "order": 393,
      "command_order": 79,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. du --max-depth=1 shows direct child totals plus the overall directory total.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "du"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-du-4",
      "section": "08-system-and-storage",
      "command": "du",
      "title": "Measure apparent file size",
      "about": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. --apparent-size reports logical bytes rather than allocated disk blocks.",
      "example": "du -b file.txt",
      "example_output": "10 file.txt",
      "breakdown": [
        "du is the featured command in the worked pattern.",
        "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. --apparent-size reports logical bytes rather than allocated disk blocks.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the apparent byte size of payload.txt.",
      "solution": "du -b payload.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: du -b <value>",
        "Use man du or du --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "payload.txt": "1234567890"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Measure apparent file size”.",
          "expected": "10\tpayload.txt",
          "normalize": "trim"
        }
      ],
      "completion": "--apparent-size reports logical bytes rather than allocated disk blocks. You can now use this behavior as part of a larger command-line workflow.",
      "order": 394,
      "command_order": 79,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. --apparent-size reports logical bytes rather than allocated disk blocks.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "du"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-du-5",
      "section": "08-system-and-storage",
      "command": "du",
      "title": "Rank child directories by size",
      "about": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. Byte-based du output can be sorted numerically to identify the largest directory in a small tree.",
      "example": "du -sb data/* | sort -nr",
      "example_output": "100 data/large\n10 data/small",
      "breakdown": [
        "du is the featured command in the worked pattern.",
        "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. Byte-based du output can be sorted numerically to identify the largest directory in a small tree.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print apparent byte totals for each child of data, largest first.",
      "solution": "du -sb data/* | sort -nr",
      "hints": [
        "Use the checker as a specification: The large directory was included. The small directory was included.",
        "Try this command shape, replacing placeholders with fixture values: du -sb <value> | sort -nr",
        "Use man du or du --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "data/small/a.txt": "1",
        "data/medium/b.txt": "12345",
        "data/large/c.txt": "12345678901234567890"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The large directory was included.",
          "expected": "data/large"
        },
        {
          "type": "stdout-contains",
          "description": "The small directory was included.",
          "expected": "data/small"
        }
      ],
      "completion": "Byte-based du output can be sorted numerically to identify the largest directory in a small tree. You can now use this behavior as part of a larger command-line workflow.",
      "order": 395,
      "command_order": 79,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "du walks paths and reports allocated disk usage. -s summarizes and -k reports KiB units. Byte-based du output can be sorted numerically to identify the largest directory in a small tree.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "du",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-free",
      "order": 396,
      "section": "08-system-and-storage",
      "command": "free",
      "title": "Inspect memory accounting",
      "about": "free summarizes memory and swap from kernel accounting. -h selects human-readable units.",
      "example": "free -h",
      "example_output": "Mem: 15Gi 4Gi 3Gi 1Gi 8Gi 10Gi\n",
      "breakdown": [
        "-h scales units for people.",
        "available is usually more useful than treating cache as simply used."
      ],
      "task": "Print human-readable memory accounting.",
      "solution": "free -h",
      "hints": [
        "Use the human-readable option.",
        "Use the checker as a specification: The memory summary includes a Mem row.",
        "Try this command shape, replacing placeholders with fixture values: free -h"
      ],
      "files": {},
      "packages": [
        "procps"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The memory summary includes a Mem row.",
          "expected": "Mem:"
        }
      ],
      "completion": "Linux uses memory for cache and can reclaim it. Interpret available memory and workload behavior, not one used number alone.",
      "command_order": 80,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "free summarizes memory and swap from kernel accounting. -h selects human-readable units.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "free"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-free-2",
      "section": "08-system-and-storage",
      "command": "free",
      "title": "Show memory in mebibytes",
      "about": "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -m fixes every reported quantity to MiB for easy numeric comparison.",
      "example": "free -m",
      "example_output": "Mem: 7870 ...",
      "breakdown": [
        "free is the featured command in the worked pattern.",
        "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -m fixes every reported quantity to MiB for easy numeric comparison.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print memory statistics in mebibytes.",
      "solution": "free -m",
      "hints": [
        "Use the checker as a specification: The memory row was printed. The swap row was printed.",
        "Try this command shape, replacing placeholders with fixture values: free -m",
        "Use man free or free --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The memory row was printed.",
          "expected": "Mem:"
        },
        {
          "type": "stdout-contains",
          "description": "The swap row was printed.",
          "expected": "Swap:"
        }
      ],
      "completion": "free -m fixes every reported quantity to MiB for easy numeric comparison. You can now use this behavior as part of a larger command-line workflow.",
      "order": 397,
      "command_order": 80,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -m fixes every reported quantity to MiB for easy numeric comparison.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "free"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-free-3",
      "section": "08-system-and-storage",
      "command": "free",
      "title": "Include a total row",
      "about": "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -t adds an aggregate total row below memory and swap.",
      "example": "free -t",
      "example_output": "Total: ...",
      "breakdown": [
        "free is the featured command in the worked pattern.",
        "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -t adds an aggregate total row below memory and swap.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print memory statistics with a total row.",
      "solution": "free -t",
      "hints": [
        "Use the checker as a specification: The total row was included.",
        "Try this command shape, replacing placeholders with fixture values: free -t",
        "Use man free or free --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The total row was included.",
          "expected": "Total:"
        }
      ],
      "completion": "free -t adds an aggregate total row below memory and swap. You can now use this behavior as part of a larger command-line workflow.",
      "order": 398,
      "command_order": 80,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -t adds an aggregate total row below memory and swap.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "free"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-free-4",
      "section": "08-system-and-storage",
      "command": "free",
      "title": "Show wide memory fields",
      "about": "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -w separates buffers and cache into distinct columns for a more detailed view.",
      "example": "free -w",
      "example_output": "total used free shared buffers cache available",
      "breakdown": [
        "free is the featured command in the worked pattern.",
        "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -w separates buffers and cache into distinct columns for a more detailed view.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print wide-format memory statistics.",
      "solution": "free -w",
      "hints": [
        "Use the checker as a specification: The buffers column was shown. The cache column was shown.",
        "Try this command shape, replacing placeholders with fixture values: free -w",
        "Use man free or free --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The buffers column was shown.",
          "expected": "buffers"
        },
        {
          "type": "stdout-contains",
          "description": "The cache column was shown.",
          "expected": "cache"
        }
      ],
      "completion": "free -w separates buffers and cache into distinct columns for a more detailed view. You can now use this behavior as part of a larger command-line workflow.",
      "order": 399,
      "command_order": 80,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "free summarizes memory and swap from kernel accounting. -h selects human-readable units. free -w separates buffers and cache into distinct columns for a more detailed view.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "free"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-free-5",
      "section": "08-system-and-storage",
      "command": "free",
      "title": "Extract an available-memory metric",
      "about": "free summarizes memory and swap from kernel accounting. -h selects human-readable units. The stable free column layout can feed awk to isolate one metric for a health record.",
      "example": "free -b | awk '/^Mem:/ {print \"available_bytes=\" $7}'",
      "example_output": "available_bytes=123456789",
      "breakdown": [
        "free is the featured command in the worked pattern.",
        "free summarizes memory and swap from kernel accounting. -h selects human-readable units. The stable free column layout can feed awk to isolate one metric for a health record.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use free -b and awk to print available_bytes=<value>. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "free -b | awk '/^Mem:/ {print \"available_bytes=\" $7}' && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: Available bytes were emitted as a numeric metric.",
        "Try this command shape, replacing placeholders with fixture values: free -b | awk <values>",
        "Use man free or free --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "stdout-regex",
          "description": "Available bytes were emitted as a numeric metric.",
          "expected": "available_bytes=[0-9]+"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "The stable free column layout can feed awk to isolate one metric for a health record. You can now use this behavior as part of a larger command-line workflow.",
      "order": 400,
      "command_order": 80,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "free summarizes memory and swap from kernel accounting. -h selects human-readable units. The stable free column layout can feed awk to isolate one metric for a health record.",
      "stage_kind": "workflow",
      "mode": "capstone",
      "concepts": [
        "free",
        "pipelines",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "cp",
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tar",
      "order": 401,
      "section": "09-archives-data-and-automation",
      "command": "tar",
      "title": "Create an archive",
      "about": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression.",
      "example": "tar -czf bundle.tar.gz docs",
      "example_output": "",
      "breakdown": [
        "-c creates an archive.",
        "-z compresses with gzip.",
        "-f consumes the archive filename."
      ],
      "task": "Create evidence.tar.gz containing the evidence directory.",
      "solution": "tar -czf evidence.tar.gz evidence",
      "hints": [
        "Use create, gzip, and file options together.",
        "Use the checker as a specification: evidence.tar.gz was created.",
        "Try this command shape, replacing placeholders with fixture values: tar -czf <values>"
      ],
      "files": {
        "evidence/notes.txt": "verified\n",
        "evidence/hash.txt": "synthetic\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "evidence.tar.gz was created.",
          "path": "evidence.tar.gz",
          "kind": "file"
        }
      ],
      "completion": "An archive groups paths; compression reduces some data. List and test an archive before deleting sources.",
      "command_order": 81,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "tar"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tar-2",
      "section": "09-archives-data-and-automation",
      "command": "tar",
      "title": "List archive contents",
      "about": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. tar -t lists member paths without extracting or changing the workspace.",
      "example": "tar -tf backup.tar",
      "example_output": "project/\nproject/app.conf",
      "breakdown": [
        "tar is the featured command in the worked pattern.",
        "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. tar -t lists member paths without extracting or changing the workspace.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "List every member of backup.tar.",
      "solution": "tar -tf backup.tar",
      "hints": [
        "Use the checker as a specification: The configuration member was listed. The documentation member was listed.",
        "Try this command shape, replacing placeholders with fixture values: tar -tf <value>",
        "Use man tar or tar --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "source/app.conf": "mode=prod\n",
        "source/docs/readme.txt": "docs\n"
      },
      "setup": [
        "tar -cf backup.tar source && rm -rf source"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The configuration member was listed.",
          "expected": "source/app.conf"
        },
        {
          "type": "stdout-contains",
          "description": "The documentation member was listed.",
          "expected": "source/docs/readme.txt"
        }
      ],
      "completion": "tar -t lists member paths without extracting or changing the workspace. You can now use this behavior as part of a larger command-line workflow.",
      "order": 402,
      "command_order": 81,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. tar -t lists member paths without extracting or changing the workspace.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "tar"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-tar-3",
      "section": "09-archives-data-and-automation",
      "command": "tar",
      "title": "Create a gzip-compressed archive",
      "about": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. The -z option compresses the tar stream with gzip while -c creates the archive.",
      "example": "tar -czf project.tar.gz project",
      "example_output": "",
      "breakdown": [
        "tar is the featured command in the worked pattern.",
        "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. The -z option compresses the tar stream with gzip while -c creates the archive.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create release.tar.gz from release, then list its members.",
      "solution": "tar -czf release.tar.gz release && tar -tzf release.tar.gz",
      "hints": [
        "Use the checker as a specification: release.tar.gz exists. The application member was archived.",
        "Try this command shape, replacing placeholders with fixture values: tar -czf <values> && tar -tzf <value>",
        "Use man tar or tar --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "release/bin/app": "binary\n",
        "release/config/app.ini": "mode=prod\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "release.tar.gz exists.",
          "path": "release.tar.gz",
          "kind": "file"
        },
        {
          "type": "stdout-contains",
          "description": "The application member was archived.",
          "expected": "release/bin/app"
        },
        {
          "type": "stdout-contains",
          "description": "The configuration member was archived.",
          "expected": "release/config/app.ini"
        }
      ],
      "completion": "The -z option compresses the tar stream with gzip while -c creates the archive. You can now use this behavior as part of a larger command-line workflow.",
      "order": 403,
      "command_order": 81,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. The -z option compresses the tar stream with gzip while -c creates the archive.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "tar",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-tar-4",
      "section": "09-archives-data-and-automation",
      "command": "tar",
      "title": "Extract one selected member",
      "about": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. Naming a member after -x extracts only that path instead of the entire archive.",
      "example": "tar -xf backup.tar project/app.conf",
      "example_output": "",
      "breakdown": [
        "tar is the featured command in the worked pattern.",
        "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. Naming a member after -x extracts only that path instead of the entire archive.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Extract only bundle/config/app.conf from bundle.tar.",
      "solution": "tar -xf bundle.tar bundle/config/app.conf",
      "hints": [
        "Use the checker as a specification: bundle/config/app.conf has the expected content. bundle/data/large.dat is absent.",
        "Try this command shape, replacing placeholders with fixture values: tar -xf <values>",
        "Use man tar or tar --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "bundle/config/app.conf": "mode=practice\n",
        "bundle/data/large.dat": "do not extract\n"
      },
      "setup": [
        "tar -cf bundle.tar bundle && rm -rf bundle"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "bundle/config/app.conf has the expected content.",
          "path": "bundle/config/app.conf",
          "expected": "mode=practice\n"
        },
        {
          "type": "path-not-exists",
          "description": "bundle/data/large.dat is absent.",
          "path": "bundle/data/large.dat"
        }
      ],
      "completion": "Naming a member after -x extracts only that path instead of the entire archive. You can now use this behavior as part of a larger command-line workflow.",
      "order": 404,
      "command_order": 81,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. Naming a member after -x extracts only that path instead of the entire archive.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "tar"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-tar-5",
      "section": "09-archives-data-and-automation",
      "command": "tar",
      "title": "Archive a tree while excluding transient files",
      "about": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. --exclude prevents reviewed cache or temporary patterns from entering a release archive.",
      "example": "tar --exclude='*.tmp' -czf clean.tar.gz project",
      "example_output": "",
      "breakdown": [
        "tar is the featured command in the worked pattern.",
        "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. --exclude prevents reviewed cache or temporary patterns from entering a release archive.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create project.tar.gz from project while excluding every .tmp file, then list the archive.",
      "solution": "tar --exclude='*.tmp' -czf project.tar.gz project && tar -tzf project.tar.gz",
      "hints": [
        "Use the checker as a specification: The application was archived. The documentation was archived.",
        "Try this command shape, replacing placeholders with fixture values: tar <value> -czf <values> && tar -tzf <value>",
        "Use man tar or tar --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "project/src/app.sh": "echo ready\n",
        "project/cache/session.tmp": "discard\n",
        "project/docs/readme.md": "docs\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The application was archived.",
          "expected": "project/src/app.sh"
        },
        {
          "type": "stdout-contains",
          "description": "The documentation was archived.",
          "expected": "project/docs/readme.md"
        },
        {
          "type": "stdout-not-contains",
          "description": "No temporary member appears in the listing.",
          "expected": ".tmp"
        }
      ],
      "completion": "--exclude prevents reviewed cache or temporary patterns from entering a release archive. You can now use this behavior as part of a larger command-line workflow.",
      "order": 405,
      "command_order": 81,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "tar stores multiple paths and metadata in one archive. -c creates, -f names the archive, and -z adds gzip compression. --exclude prevents reviewed cache or temporary patterns from entering a release archive.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "tar",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-gzip",
      "order": 406,
      "section": "09-archives-data-and-automation",
      "command": "gzip",
      "title": "Compress one file with gzip",
      "about": "gzip compresses individual files and normally replaces the source with a .gz file.",
      "example": "gzip report.txt",
      "example_output": "",
      "breakdown": [
        "report.txt is compressed.",
        "The default result is report.txt.gz and the source name is removed."
      ],
      "task": "Compress report.txt with gzip.",
      "solution": "gzip report.txt",
      "hints": [
        "Pass the source filename directly to gzip.",
        "Use the checker as a specification: report.txt.gz exists. The default gzip operation removed report.txt.",
        "Try this command shape, replacing placeholders with fixture values: gzip <value>"
      ],
      "files": {
        "report.txt": "repeat repeat repeat repeat\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "report.txt.gz exists.",
          "path": "report.txt.gz",
          "kind": "file"
        },
        {
          "type": "path-not-exists",
          "description": "The default gzip operation removed report.txt.",
          "path": "report.txt"
        }
      ],
      "completion": "Use gzip -k when the original file must remain. Compression is not encryption or integrity authentication.",
      "command_order": 82,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "gzip compresses individual files and normally replaces the source with a .gz file.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "gzip"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-gzip-2",
      "section": "09-archives-data-and-automation",
      "command": "gzip",
      "title": "Keep the original file",
      "about": "gzip compresses individual files and normally replaces the source with a .gz file. gzip -k creates the .gz file while retaining the uncompressed source.",
      "example": "gzip -k report.txt",
      "example_output": "",
      "breakdown": [
        "gzip is the featured command in the worked pattern.",
        "gzip compresses individual files and normally replaces the source with a .gz file. gzip -k creates the .gz file while retaining the uncompressed source.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress report.txt while keeping report.txt, then verify the compressed stream by printing it.",
      "solution": "gzip -k report.txt && gzip -cd report.txt.gz",
      "hints": [
        "Use the checker as a specification: report.txt has the expected content. report.txt.gz exists.",
        "Try this command shape, replacing placeholders with fixture values: gzip -k <value> && gzip -cd <value>",
        "Use man gzip or gzip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "report.txt": "alpha\nbeta\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "report.txt has the expected content.",
          "path": "report.txt",
          "expected": "alpha\nbeta\n"
        },
        {
          "type": "path-exists",
          "description": "report.txt.gz exists.",
          "path": "report.txt.gz",
          "kind": "file"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Keep the original file”.",
          "expected": "alpha\nbeta",
          "normalize": "trim"
        }
      ],
      "completion": "gzip -k creates the .gz file while retaining the uncompressed source. You can now use this behavior as part of a larger command-line workflow.",
      "order": 407,
      "command_order": 82,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "gzip compresses individual files and normally replaces the source with a .gz file. gzip -k creates the .gz file while retaining the uncompressed source.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "gzip",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-gzip-3",
      "section": "09-archives-data-and-automation",
      "command": "gzip",
      "title": "Write compressed data to standard output",
      "about": "gzip compresses individual files and normally replaces the source with a .gz file. gzip -c leaves the source alone and sends compressed bytes to standard output for explicit redirection.",
      "example": "gzip -c app.log > app.log.gz",
      "example_output": "",
      "breakdown": [
        "gzip is the featured command in the worked pattern.",
        "gzip compresses individual files and normally replaces the source with a .gz file. gzip -c leaves the source alone and sends compressed bytes to standard output for explicit redirection.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress events.log to archive/events.gz with -c while preserving events.log, then print the decompressed archive.",
      "solution": "gzip -c events.log > archive/events.gz && gzip -cd archive/events.gz",
      "hints": [
        "Use the checker as a specification: events.log has the expected content. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: gzip -c <value> > <value> && gzip -cd <value>",
        "Use man gzip or gzip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "archive"
      ],
      "files": {
        "events.log": "start\nready\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "events.log has the expected content.",
          "path": "events.log",
          "expected": "start\nready\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Write compressed data to standard output”.",
          "expected": "start\nready",
          "normalize": "trim"
        }
      ],
      "completion": "gzip -c leaves the source alone and sends compressed bytes to standard output for explicit redirection. You can now use this behavior as part of a larger command-line workflow.",
      "order": 408,
      "command_order": 82,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "gzip compresses individual files and normally replaces the source with a .gz file. gzip -c leaves the source alone and sends compressed bytes to standard output for explicit redirection.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "gzip",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-gzip-4",
      "section": "09-archives-data-and-automation",
      "command": "gzip",
      "title": "Use maximum compression",
      "about": "gzip compresses individual files and normally replaces the source with a .gz file. gzip -9 spends more CPU to seek a smaller compressed representation.",
      "example": "gzip -9 dataset.txt",
      "example_output": "",
      "breakdown": [
        "gzip is the featured command in the worked pattern.",
        "gzip compresses individual files and normally replaces the source with a .gz file. gzip -9 spends more CPU to seek a smaller compressed representation.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress dataset.txt with level 9 and print its contents through gzip -cd.",
      "solution": "gzip -9 dataset.txt && gzip -cd dataset.txt.gz",
      "hints": [
        "Use the checker as a specification: dataset.txt is absent. dataset.txt.gz exists.",
        "Try this command shape, replacing placeholders with fixture values: gzip -9 <value> && gzip -cd <value>",
        "Use man gzip or gzip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "dataset.txt": "repeat repeat repeat repeat repeat\nrepeat repeat repeat repeat repeat\n"
      },
      "checks": [
        {
          "type": "path-not-exists",
          "description": "dataset.txt is absent.",
          "path": "dataset.txt"
        },
        {
          "type": "path-exists",
          "description": "dataset.txt.gz exists.",
          "path": "dataset.txt.gz",
          "kind": "file"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Use maximum compression”.",
          "expected": "repeat repeat repeat repeat repeat\nrepeat repeat repeat repeat repeat",
          "normalize": "trim"
        }
      ],
      "completion": "gzip -9 spends more CPU to seek a smaller compressed representation. You can now use this behavior as part of a larger command-line workflow.",
      "order": 409,
      "command_order": 82,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "gzip compresses individual files and normally replaces the source with a .gz file. gzip -9 spends more CPU to seek a smaller compressed representation.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "gzip",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-gzip-5",
      "section": "09-archives-data-and-automation",
      "command": "gzip",
      "title": "Compress a reviewed batch",
      "about": "gzip compresses individual files and normally replaces the source with a .gz file. find with null delimiters can pass exact .log paths to gzip as a safe batch operation.",
      "example": "find logs -name '*.log' -print0 | xargs -0 gzip",
      "example_output": "",
      "breakdown": [
        "gzip is the featured command in the worked pattern.",
        "gzip compresses individual files and normally replaces the source with a .gz file. find with null delimiters can pass exact .log paths to gzip as a safe batch operation.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress every .log file below logs while preserving notes.txt, then list the .gz paths.",
      "solution": "find logs -type f -name '*.log' -print0 | xargs -0 gzip; find logs -type f -name '*.gz' | sort",
      "hints": [
        "Use the checker as a specification: logs/api.log.gz exists. logs/old web.log.gz exists.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -type <value> -name <value> -print0 | xargs -0 <value> ; find <value> -type <value> -name <value> | sort",
        "Use man gzip or gzip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "logs/api.log": "api\n",
        "logs/old web.log": "web\n",
        "logs/notes.txt": "keep\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "logs/api.log.gz exists.",
          "path": "logs/api.log.gz",
          "kind": "file"
        },
        {
          "type": "path-exists",
          "description": "logs/old web.log.gz exists.",
          "path": "logs/old web.log.gz",
          "kind": "file"
        },
        {
          "type": "file-content",
          "description": "logs/notes.txt has the expected content.",
          "path": "logs/notes.txt",
          "expected": "keep\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Compress a reviewed batch”.",
          "expected": "logs/api.log.gz\nlogs/old web.log.gz",
          "normalize": "trim"
        }
      ],
      "completion": "find with null delimiters can pass exact .log paths to gzip as a safe batch operation. You can now use this behavior as part of a larger command-line workflow.",
      "order": 410,
      "command_order": 82,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "gzip compresses individual files and normally replaces the source with a .gz file. find with null delimiters can pass exact .log paths to gzip as a safe batch operation.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "gzip",
        "pipelines",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort",
        "xargs"
      ],
      "introduced_inline": [
        "find"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-source",
      "order": 411,
      "section": "09-archives-data-and-automation",
      "command": "source",
      "title": "Load shell settings into the current session",
      "about": "source reads a shell file in the current Bash process, so variables and functions remain available afterward.",
      "example": "source settings.sh && printf '%s\\n' \"$APP_MODE\"",
      "example_output": "practice",
      "breakdown": [
        "source settings.sh executes the file in the current shell.",
        "APP_MODE remains available to the following command."
      ],
      "task": "Source config/lab.env, then print the value of LAB_NAME.",
      "solution": "source config/lab.env && printf '%s\\n' \"$LAB_NAME\"",
      "hints": [
        "Use source with the file path; the variable is then available in this shell.",
        "Use the checker as a specification: The sourced LAB_NAME value was printed.",
        "Try this command shape, replacing placeholders with fixture values: source <value> && printf <values>"
      ],
      "files": {
        "config/lab.env": "LAB_NAME=cli-gym\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The sourced LAB_NAME value was printed.",
          "expected": "cli-gym",
          "normalize": "trim"
        }
      ],
      "completion": "Sourcing changes the current shell. Read unfamiliar files before sourcing them, because they execute with your shell's permissions.",
      "command_order": 83,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "source reads a shell file in the current Bash process, so variables and functions remain available afterward.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "source",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "printf",
        "env"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-gunzip"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-source-2",
      "section": "09-archives-data-and-automation",
      "command": "source",
      "title": "Load several exported settings",
      "about": "source reads a shell file in the current Bash process, so variables and functions remain available afterward. source applies every assignment in a reviewed environment file to the current shell.",
      "example": "source app.env && printf \"%s %s\\n\" \"$APP\" \"$PORT\"",
      "example_output": "api 8080",
      "breakdown": [
        "source is the featured command in the worked pattern.",
        "source reads a shell file in the current Bash process, so variables and functions remain available afterward. source applies every assignment in a reviewed environment file to the current shell.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Source service.env and print SERVICE followed by PORT.",
      "solution": "source service.env && printf \"%s %s\\n\" \"$SERVICE\" \"$PORT\"",
      "hints": [
        "Use the checker as a specification: Both sourced settings were available in the current shell.",
        "Try this command shape, replacing placeholders with fixture values: source <value> && printf <values>",
        "Use man source or source --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "service.env": "SERVICE=worker\nPORT=9000\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Both sourced settings were available in the current shell.",
          "expected": "worker 9000",
          "normalize": "trim"
        }
      ],
      "completion": "source applies every assignment in a reviewed environment file to the current shell. You can now use this behavior as part of a larger command-line workflow.",
      "order": 412,
      "command_order": 83,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "source reads a shell file in the current Bash process, so variables and functions remain available afterward. source applies every assignment in a reviewed environment file to the current shell.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "source",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "printf",
        "env"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-gunzip-2"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-source-3",
      "section": "09-archives-data-and-automation",
      "command": "source",
      "title": "Load and call a shell function",
      "about": "source reads a shell file in the current Bash process, so variables and functions remain available afterward. Functions declared by a sourced file remain callable because the file runs in the current shell.",
      "example": "source helpers.sh && greet Ada",
      "example_output": "hello Ada",
      "breakdown": [
        "source is the featured command in the worked pattern.",
        "source reads a shell file in the current Bash process, so variables and functions remain available afterward. Functions declared by a sourced file remain callable because the file runs in the current shell.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Source lib/format.sh and call label ready.",
      "solution": "source lib/format.sh && label ready",
      "hints": [
        "Use the checker as a specification: The sourced function produced the requested label.",
        "Try this command shape, replacing placeholders with fixture values: source <value> && label <value>",
        "Use man source or source --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "lib/format.sh": "label() { printf \"status=%s\\n\" \"$1\"; }\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The sourced function produced the requested label.",
          "expected": "status=ready",
          "normalize": "trim"
        }
      ],
      "completion": "Functions declared by a sourced file remain callable because the file runs in the current shell. You can now use this behavior as part of a larger command-line workflow.",
      "order": 413,
      "command_order": 83,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "source reads a shell file in the current Bash process, so variables and functions remain available afterward. Functions declared by a sourced file remain callable because the file runs in the current shell.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "source",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-gunzip-3"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-source-4",
      "section": "09-archives-data-and-automation",
      "command": "source",
      "title": "Select a configuration file dynamically",
      "about": "source reads a shell file in the current Bash process, so variables and functions remain available afterward. A variable can choose which reviewed file source loads, while quoting keeps its path intact.",
      "example": "profile=dev; source \"profiles/$profile.env\"; printf \"%s\\n\" \"$URL\"",
      "example_output": "http://dev.local",
      "breakdown": [
        "source is the featured command in the worked pattern.",
        "source reads a shell file in the current Bash process, so variables and functions remain available afterward. A variable can choose which reviewed file source loads, while quoting keeps its path intact.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set profile to test, source profiles/$profile.env, and print ENDPOINT.",
      "solution": "profile=test; source \"profiles/$profile.env\"; printf \"%s\\n\" \"$ENDPOINT\"",
      "hints": [
        "Use the checker as a specification: The dynamically selected test endpoint was printed.",
        "Try this command shape, replacing placeholders with fixture values: <value> ; source <value> ; printf <values>",
        "Use man source or source --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "profiles/test.env": "ENDPOINT=https://test.invalid\n",
        "profiles/prod.env": "ENDPOINT=https://prod.invalid\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The dynamically selected test endpoint was printed.",
          "expected": "https://test.invalid",
          "normalize": "trim"
        }
      ],
      "completion": "A variable can choose which reviewed file source loads, while quoting keeps its path intact. You can now use this behavior as part of a larger command-line workflow.",
      "order": 414,
      "command_order": 83,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "source reads a shell file in the current Bash process, so variables and functions remain available afterward. A variable can choose which reviewed file source loads, while quoting keeps its path intact.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "source",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "printf",
        "env"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-gunzip-4"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-source-5",
      "section": "09-archives-data-and-automation",
      "command": "source",
      "title": "Compose a reusable deployment check",
      "about": "source reads a shell file in the current Bash process, so variables and functions remain available afterward. A sourced library can provide variables and functions that a short workflow combines with exit status and redirection.",
      "example": "source deploy.sh && validate \"$TARGET\" > result.txt",
      "example_output": "",
      "breakdown": [
        "source is the featured command in the worked pattern.",
        "source reads a shell file in the current Bash process, so variables and functions remain available afterward. A sourced library can provide variables and functions that a short workflow combines with exit status and redirection.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Source lib/release.sh, run validate_release \"$VERSION\", save its output in result.txt, and print result.txt.",
      "solution": "source lib/release.sh && validate_release \"$VERSION\" > result.txt && cat result.txt",
      "hints": [
        "Use the checker as a specification: result.txt has the expected content. The sourced release workflow wrote and printed a valid result.",
        "Try this command shape, replacing placeholders with fixture values: source <value> && <values> > <value> && cat <value>",
        "Use man source or source --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "lib/release.sh": "VERSION=2.4.1\nvalidate_release() { [[ $1 =~ ^[0-9]+\\.[0-9]+\\.[0-9]+$ ]] && printf \"release=%s valid\\n\" \"$1\"; }\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "result.txt has the expected content.",
          "path": "result.txt",
          "expected": "release=2.4.1 valid\n"
        },
        {
          "type": "stdout",
          "description": "The sourced release workflow wrote and printed a valid result.",
          "expected": "release=2.4.1 valid",
          "normalize": "trim"
        }
      ],
      "completion": "A sourced library can provide variables and functions that a short workflow combines with exit status and redirection. You can now use this behavior as part of a larger command-line workflow.",
      "order": 415,
      "command_order": 83,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "source reads a shell file in the current Bash process, so variables and functions remain available afterward. A sourced library can provide variables and functions that a short workflow combines with exit status and redirection.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "source",
        "redirection",
        "command chaining",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-gunzip-5"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-bzip2",
      "order": 416,
      "section": "09-archives-data-and-automation",
      "command": "bzip2",
      "title": "Compress with bzip2",
      "about": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2.",
      "example": "bzip2 report.txt",
      "example_output": "",
      "breakdown": [
        "The source is compressed.",
        "report.txt.bz2 becomes the default output."
      ],
      "task": "Compress report.txt with bzip2.",
      "solution": "bzip2 report.txt",
      "hints": [
        "Pass the source directly to bzip2.",
        "Use the checker as a specification: report.txt.bz2 exists. The source name was replaced.",
        "Try this command shape, replacing placeholders with fixture values: bzip2 <value>"
      ],
      "files": {
        "report.txt": "repeat repeat repeat repeat\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "report.txt.bz2 exists.",
          "path": "report.txt.bz2",
          "kind": "file"
        },
        {
          "type": "path-not-exists",
          "description": "The source name was replaced.",
          "path": "report.txt"
        }
      ],
      "completion": "Compression formats trade speed, size, and compatibility. Choose based on the consumer and verify the result.",
      "command_order": 84,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "bzip2"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-bzip2-2",
      "section": "09-archives-data-and-automation",
      "command": "bzip2",
      "title": "Keep the source while compressing",
      "about": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -k retains the original file alongside its .bz2 archive.",
      "example": "bzip2 -k report.txt",
      "example_output": "",
      "breakdown": [
        "bzip2 is the featured command in the worked pattern.",
        "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -k retains the original file alongside its .bz2 archive.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress report.txt with bzip2 while retaining it, then print the archive through bzip2 -cd.",
      "solution": "bzip2 -k report.txt && bzip2 -cd report.txt.bz2",
      "hints": [
        "Use the checker as a specification: report.txt has the expected content. report.txt.bz2 exists.",
        "Try this command shape, replacing placeholders with fixture values: bzip2 -k <value> && bzip2 -cd <value>",
        "Use man bzip2 or bzip2 --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "report.txt": "bzip practice\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "report.txt has the expected content.",
          "path": "report.txt",
          "expected": "bzip practice\n"
        },
        {
          "type": "path-exists",
          "description": "report.txt.bz2 exists.",
          "path": "report.txt.bz2",
          "kind": "file"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Keep the source while compressing”.",
          "expected": "bzip practice",
          "normalize": "trim"
        }
      ],
      "completion": "bzip2 -k retains the original file alongside its .bz2 archive. You can now use this behavior as part of a larger command-line workflow.",
      "order": 417,
      "command_order": 84,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -k retains the original file alongside its .bz2 archive.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "bzip2",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-bzip2-3",
      "section": "09-archives-data-and-automation",
      "command": "bzip2",
      "title": "Write compressed bytes to stdout",
      "about": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -c streams compressed data so the destination name and directory remain explicit.",
      "example": "bzip2 -c data.txt > archive/data.bz2",
      "example_output": "",
      "breakdown": [
        "bzip2 is the featured command in the worked pattern.",
        "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -c streams compressed data so the destination name and directory remain explicit.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress data.txt to backups/data.bz2 while retaining data.txt, then print the decompressed stream.",
      "solution": "bzip2 -c data.txt > backups/data.bz2 && bzip2 -cd backups/data.bz2",
      "hints": [
        "Use the checker as a specification: data.txt has the expected content. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: bzip2 -c <value> > <value> && bzip2 -cd <value>",
        "Use man bzip2 or bzip2 --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "backups"
      ],
      "files": {
        "data.txt": "backup data\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "data.txt has the expected content.",
          "path": "data.txt",
          "expected": "backup data\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Write compressed bytes to stdout”.",
          "expected": "backup data",
          "normalize": "trim"
        }
      ],
      "completion": "bzip2 -c streams compressed data so the destination name and directory remain explicit. You can now use this behavior as part of a larger command-line workflow.",
      "order": 418,
      "command_order": 84,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -c streams compressed data so the destination name and directory remain explicit.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "bzip2",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-bzip2-4",
      "section": "09-archives-data-and-automation",
      "command": "bzip2",
      "title": "Test bzip2 integrity",
      "about": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -t validates compressed data without extracting it.",
      "example": "bzip2 -t archive.bz2 && echo valid",
      "example_output": "valid",
      "breakdown": [
        "bzip2 is the featured command in the worked pattern.",
        "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -t validates compressed data without extracting it.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Validate artifact.bz2 and print valid-bzip2.",
      "solution": "bzip2 -t artifact.bz2 && echo valid-bzip2",
      "hints": [
        "Use the checker as a specification: artifact.bz2 exists. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: bzip2 -t <value> && echo valid-bzip2",
        "Use man bzip2 or bzip2 --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact": "artifact data\n"
      },
      "setup": [
        "bzip2 artifact"
      ],
      "checks": [
        {
          "type": "path-exists",
          "description": "artifact.bz2 exists.",
          "path": "artifact.bz2",
          "kind": "file"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Test bzip2 integrity”.",
          "expected": "valid-bzip2",
          "normalize": "trim"
        }
      ],
      "completion": "bzip2 -t validates compressed data without extracting it. You can now use this behavior as part of a larger command-line workflow.",
      "order": 419,
      "command_order": 84,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -t validates compressed data without extracting it.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "bzip2",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-bzip2-5",
      "section": "09-archives-data-and-automation",
      "command": "bzip2",
      "title": "Compress a batch at maximum level",
      "about": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -9 applies its maximum block size to every named file in a reviewed batch.",
      "example": "bzip2 -9 one.log two.log",
      "example_output": "",
      "breakdown": [
        "bzip2 is the featured command in the worked pattern.",
        "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -9 applies its maximum block size to every named file in a reviewed batch.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress api.log, db.log, and web.log with -9, then list their archive names.",
      "solution": "bzip2 -9 api.log db.log web.log && printf \"%s\n\" *.bz2",
      "hints": [
        "Use the checker as a specification: api.log.bz2 exists. db.log.bz2 exists.",
        "Try this command shape, replacing placeholders with fixture values: bzip2 -9 <values> && printf <values>",
        "Use man bzip2 or bzip2 --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "api.log": "api\n",
        "db.log": "db\n",
        "web.log": "web\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "api.log.bz2 exists.",
          "path": "api.log.bz2",
          "kind": "file"
        },
        {
          "type": "path-exists",
          "description": "db.log.bz2 exists.",
          "path": "db.log.bz2",
          "kind": "file"
        },
        {
          "type": "path-exists",
          "description": "web.log.bz2 exists.",
          "path": "web.log.bz2",
          "kind": "file"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Compress a batch at maximum level”.",
          "expected": "api.log.bz2\ndb.log.bz2\nweb.log.bz2",
          "normalize": "trim"
        }
      ],
      "completion": "bzip2 -9 applies its maximum block size to every named file in a reviewed batch. You can now use this behavior as part of a larger command-line workflow.",
      "order": 420,
      "command_order": 84,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "bzip2 compresses files using the Burrows-Wheeler family of transforms and normally replaces the source with .bz2. bzip2 -9 applies its maximum block size to every named file in a reviewed batch.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "bzip2",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-find",
      "order": 421,
      "section": "09-archives-data-and-automation",
      "command": "find",
      "title": "Find files by name",
      "about": "find walks directory trees and applies predicates such as -type and -name to each path.",
      "example": "find logs -type f -name '*.log'",
      "example_output": "logs/app.log",
      "breakdown": [
        "logs is the search root.",
        "-type f limits results to files and -name matches the filename pattern."
      ],
      "task": "Find every .conf file below systems and print the paths in sorted order.",
      "solution": "find systems -type f -name '*.conf' | sort",
      "hints": [
        "Start at systems, restrict the result to files, then match the .conf suffix.",
        "Use the checker as a specification: Only the two configuration paths were printed in sorted order.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -type <value> -name <value> | sort"
      ],
      "files": {
        "systems/api/app.conf": "mode=api\n",
        "systems/db/db.conf": "mode=db\n",
        "systems/db/readme.txt": "notes\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only the two configuration paths were printed in sorted order.",
          "expected": "systems/api/app.conf\nsystems/db/db.conf",
          "normalize": "trim"
        }
      ],
      "completion": "find combines a search root with predicates. Quote wildcard patterns so Bash does not expand them before find sees them.",
      "command_order": 85,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "find walks directory trees and applies predicates such as -type and -name to each path.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "find",
        "pipelines",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-bunzip2"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-find-2",
      "section": "09-archives-data-and-automation",
      "command": "find",
      "title": "Limit a search by file type",
      "about": "find walks directory trees and applies predicates such as -type and -name to each path. -type distinguishes regular files, directories, and links before other predicates are applied.",
      "example": "find project -type d",
      "example_output": "project\nproject/src",
      "breakdown": [
        "find is the featured command in the worked pattern.",
        "find walks directory trees and applies predicates such as -type and -name to each path. -type distinguishes regular files, directories, and links before other predicates are applied.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only directories below workspace in sorted order.",
      "solution": "find workspace -type d | sort",
      "hints": [
        "Use the checker as a specification: Only directories were printed in deterministic order.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -type <value> | sort",
        "Use man find or find --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "workspace/app/lib",
        "workspace/docs"
      ],
      "files": {
        "workspace/app/main.sh": "",
        "workspace/docs/readme.md": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only directories were printed in deterministic order.",
          "expected": "workspace\nworkspace/app\nworkspace/app/lib\nworkspace/docs",
          "normalize": "trim"
        }
      ],
      "completion": "-type distinguishes regular files, directories, and links before other predicates are applied. You can now use this behavior as part of a larger command-line workflow.",
      "order": 422,
      "command_order": 85,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "find walks directory trees and applies predicates such as -type and -name to each path. -type distinguishes regular files, directories, and links before other predicates are applied.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "find",
        "pipelines",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-bunzip2-2"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-find-3",
      "section": "09-archives-data-and-automation",
      "command": "find",
      "title": "Combine predicates safely",
      "about": "find walks directory trees and applies predicates such as -type and -name to each path. find combines predicates with an implicit AND; quoting a wildcard leaves matching to find.",
      "example": "find logs -type f -name '*.log'",
      "example_output": "logs/api.log",
      "breakdown": [
        "find is the featured command in the worked pattern.",
        "find walks directory trees and applies predicates such as -type and -name to each path. find combines predicates with an implicit AND; quoting a wildcard leaves matching to find.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only .sh files below project, sorted.",
      "solution": "find project -type f -name '*.sh' | sort",
      "hints": [
        "Use the checker as a specification: Only regular shell-script files were selected.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -type <value> -name <value> | sort",
        "Use man find or find --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "project/bin/start.sh": "",
        "project/lib/check.sh": "",
        "project/readme.md": "",
        "project/fake.sh/notes.txt": ""
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only regular shell-script files were selected.",
          "expected": "project/bin/start.sh\nproject/lib/check.sh",
          "normalize": "trim"
        }
      ],
      "completion": "find combines predicates with an implicit AND; quoting a wildcard leaves matching to find. You can now use this behavior as part of a larger command-line workflow.",
      "order": 423,
      "command_order": 85,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "find walks directory trees and applies predicates such as -type and -name to each path. find combines predicates with an implicit AND; quoting a wildcard leaves matching to find.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "find",
        "pipelines",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-bunzip2-3"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-find-4",
      "section": "09-archives-data-and-automation",
      "command": "find",
      "title": "Filter by size and print portable names",
      "about": "find walks directory trees and applies predicates such as -type and -name to each path. -size selects by file size and -printf can emit only the fields a pipeline needs.",
      "example": "find data -type f -size +1k -printf \"%f\\n\"",
      "example_output": "large.bin",
      "breakdown": [
        "find is the featured command in the worked pattern.",
        "find walks directory trees and applies predicates such as -type and -name to each path. -size selects by file size and -printf can emit only the fields a pipeline needs.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the basenames of regular files larger than 20 bytes below data, sorted.",
      "solution": "find data -type f -size +20c -printf \"%f\\n\" | sort",
      "hints": [
        "Use the checker as a specification: Only basenames of files over the threshold were printed.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -type <value> -size <value> -printf <value> | sort",
        "Use man find or find --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "data/tiny.txt": "small\n",
        "data/archive/large.txt": "this file is definitely larger than twenty bytes\n",
        "data/medium.txt": "123456789012345678901\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only basenames of files over the threshold were printed.",
          "expected": "large.txt\nmedium.txt",
          "normalize": "trim"
        }
      ],
      "completion": "-size selects by file size and -printf can emit only the fields a pipeline needs. You can now use this behavior as part of a larger command-line workflow.",
      "order": 424,
      "command_order": 85,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "find walks directory trees and applies predicates such as -type and -name to each path. -size selects by file size and -printf can emit only the fields a pipeline needs.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "find",
        "pipelines",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-bunzip2-4"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-find-5",
      "section": "09-archives-data-and-automation",
      "command": "find",
      "title": "Act on exact null-delimited paths",
      "about": "find walks directory trees and applies predicates such as -type and -name to each path. -print0 and xargs -0 preserve spaces and other ordinary filename characters during batch workflows.",
      "example": "find inbox -type f -name '*.tmp' -print0 | xargs -0 rm",
      "example_output": "",
      "breakdown": [
        "find is the featured command in the worked pattern.",
        "find walks directory trees and applies predicates such as -type and -name to each path. -print0 and xargs -0 preserve spaces and other ordinary filename characters during batch workflows.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Remove every .tmp file below cache, including one with a space, then print each remaining path followed by its changing fixture content.",
      "solution": "find cache -type f -name '*.tmp' -print0 | xargs -0 rm --; find cache -type f -print0 | sort -z | xargs -0 awk '{print FILENAME \":\" $0}'",
      "hints": [
        "Use the checker as a specification: cache/old.tmp is absent. cache/old copy.tmp is absent.",
        "Try this command shape, replacing placeholders with fixture values: find <value> -type <value> -name <value> -print0 | xargs -0 <value> -- ; find <value> -type <value> -print0 | sort -z | xargs -0 <values>",
        "Use man find or find --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "cache/old.tmp": "",
        "cache/old copy.tmp": "",
        "cache/keep.txt": "keep={{nonce}}\n",
        "cache/nested/data.json": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "path-not-exists",
          "description": "cache/old.tmp is absent.",
          "path": "cache/old.tmp"
        },
        {
          "type": "path-not-exists",
          "description": "cache/old copy.tmp is absent.",
          "path": "cache/old copy.tmp"
        },
        {
          "type": "stdout",
          "description": "Temporary files were removed and the retained dynamic records were reported.",
          "expected": "cache/keep.txt:keep={{nonce}}\ncache/nested/data.json:token={{nonce}}",
          "normalize": "trim"
        }
      ],
      "completion": "-print0 and xargs -0 preserve spaces and other ordinary filename characters during batch workflows. You can now use this behavior as part of a larger command-line workflow.",
      "order": 425,
      "command_order": 85,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "find walks directory trees and applies predicates such as -type and -name to each path. -print0 and xargs -0 preserve spaces and other ordinary filename characters during batch workflows.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "find",
        "pipelines",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "rm",
        "awk",
        "sort",
        "xargs"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-bunzip2-5"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xz",
      "order": 426,
      "section": "09-archives-data-and-automation",
      "command": "xz",
      "title": "Compress with xz",
      "about": "xz compresses files using the LZMA2 format and normally replaces the source with .xz.",
      "example": "xz report.txt",
      "example_output": "",
      "breakdown": [
        "The file is compressed.",
        "The default result is report.txt.xz."
      ],
      "task": "Compress report.txt with xz.",
      "solution": "xz report.txt",
      "hints": [
        "Pass the source filename to xz.",
        "Use the checker as a specification: report.txt.xz exists. The source name was replaced.",
        "Try this command shape, replacing placeholders with fixture values: xz <value>"
      ],
      "files": {
        "report.txt": "repeat repeat repeat repeat\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "report.txt.xz exists.",
          "path": "report.txt.xz",
          "kind": "file"
        },
        {
          "type": "path-not-exists",
          "description": "The source name was replaced.",
          "path": "report.txt"
        }
      ],
      "completion": "xz often favors compression ratio over speed. The best choice depends on data, time, memory, and compatibility.",
      "command_order": 86,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "xz compresses files using the LZMA2 format and normally replaces the source with .xz.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "xz"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xz-2",
      "section": "09-archives-data-and-automation",
      "command": "xz",
      "title": "Keep the source while compressing",
      "about": "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -k retains the source file alongside the .xz stream.",
      "example": "xz -k report.txt",
      "example_output": "",
      "breakdown": [
        "xz is the featured command in the worked pattern.",
        "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -k retains the source file alongside the .xz stream.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress report.txt with xz while keeping it, then print the compressed stream through xz -cd.",
      "solution": "xz -k report.txt && xz -cd report.txt.xz",
      "hints": [
        "Use the checker as a specification: report.txt has the expected content. report.txt.xz exists.",
        "Try this command shape, replacing placeholders with fixture values: xz -k <value> && xz -cd <value>",
        "Use man xz or xz --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "report.txt": "xz report\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "report.txt has the expected content.",
          "path": "report.txt",
          "expected": "xz report\n"
        },
        {
          "type": "path-exists",
          "description": "report.txt.xz exists.",
          "path": "report.txt.xz",
          "kind": "file"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Keep the source while compressing”.",
          "expected": "xz report",
          "normalize": "trim"
        }
      ],
      "completion": "xz -k retains the source file alongside the .xz stream. You can now use this behavior as part of a larger command-line workflow.",
      "order": 427,
      "command_order": 86,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -k retains the source file alongside the .xz stream.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "xz",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xz-3",
      "section": "09-archives-data-and-automation",
      "command": "xz",
      "title": "Write an xz stream to stdout",
      "about": "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -c allows an explicit destination path and leaves the source unchanged.",
      "example": "xz -c data.txt > archive/data.xz",
      "example_output": "",
      "breakdown": [
        "xz is the featured command in the worked pattern.",
        "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -c allows an explicit destination path and leaves the source unchanged.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress data.txt to backup/data.xz with -c and print its restored content.",
      "solution": "xz -c data.txt > backup/data.xz && xz -cd backup/data.xz",
      "hints": [
        "Use the checker as a specification: data.txt has the expected content. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: xz -c <value> > <value> && xz -cd <value>",
        "Use man xz or xz --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "backup"
      ],
      "files": {
        "data.txt": "xz data\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "data.txt has the expected content.",
          "path": "data.txt",
          "expected": "xz data\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Write an xz stream to stdout”.",
          "expected": "xz data",
          "normalize": "trim"
        }
      ],
      "completion": "xz -c allows an explicit destination path and leaves the source unchanged. You can now use this behavior as part of a larger command-line workflow.",
      "order": 428,
      "command_order": 86,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -c allows an explicit destination path and leaves the source unchanged.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "xz",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xz-4",
      "section": "09-archives-data-and-automation",
      "command": "xz",
      "title": "Test xz integrity",
      "about": "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -t validates the stream without producing an output file.",
      "example": "xz -t artifact.xz && echo valid",
      "example_output": "valid",
      "breakdown": [
        "xz is the featured command in the worked pattern.",
        "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -t validates the stream without producing an output file.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Validate artifact.xz and print valid-xz.",
      "solution": "xz -t artifact.xz && echo valid-xz",
      "hints": [
        "Use the checker as a specification: The requested result was printed. artifact.xz exists.",
        "Try this command shape, replacing placeholders with fixture values: xz -t <value> && echo valid-xz",
        "Use man xz or xz --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact": "artifact\n"
      },
      "setup": [
        "xz artifact"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Test xz integrity”.",
          "expected": "valid-xz",
          "normalize": "trim"
        },
        {
          "type": "path-exists",
          "description": "artifact.xz exists.",
          "path": "artifact.xz",
          "kind": "file"
        }
      ],
      "completion": "xz -t validates the stream without producing an output file. You can now use this behavior as part of a larger command-line workflow.",
      "order": 429,
      "command_order": 86,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -t validates the stream without producing an output file.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "xz",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-xz-5",
      "section": "09-archives-data-and-automation",
      "command": "xz",
      "title": "Use multithreaded maximum compression",
      "about": "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -T0 can use available cores while -9 selects the strongest normal preset.",
      "example": "xz -T0 -9 dataset.txt",
      "example_output": "",
      "breakdown": [
        "xz is the featured command in the worked pattern.",
        "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -T0 can use available cores while -9 selects the strongest normal preset.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Compress dataset.txt with -T0 -9, then print the restored data.",
      "solution": "xz -T0 -9 dataset.txt && xz -cd dataset.txt.xz",
      "hints": [
        "Use the checker as a specification: dataset.txt.xz exists. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: xz -T0 -9 <value> && xz -cd <value>",
        "Use man xz or xz --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "dataset.txt": "repeat repeat repeat repeat\nrepeat repeat repeat repeat\n"
      },
      "checks": [
        {
          "type": "path-exists",
          "description": "dataset.txt.xz exists.",
          "path": "dataset.txt.xz",
          "kind": "file"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Use multithreaded maximum compression”.",
          "expected": "repeat repeat repeat repeat\nrepeat repeat repeat repeat",
          "normalize": "trim"
        }
      ],
      "completion": "xz -T0 can use available cores while -9 selects the strongest normal preset. You can now use this behavior as part of a larger command-line workflow.",
      "order": 430,
      "command_order": 86,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "xz compresses files using the LZMA2 format and normally replaces the source with .xz. xz -T0 can use available cores while -9 selects the strongest normal preset.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "xz",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-jq",
      "order": 431,
      "section": "09-archives-data-and-automation",
      "command": "jq",
      "title": "Read a field from JSON",
      "about": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text.",
      "example": "jq -r '.name' service.json",
      "example_output": "api",
      "breakdown": [
        ".name selects the name property.",
        "-r prints a string without JSON quotation marks."
      ],
      "task": "Print the owner field from project.json as plain text.",
      "solution": "jq -r '.owner' project.json",
      "hints": [
        "Use a property filter beginning with a dot and request raw string output.",
        "Use the checker as a specification: The owner value was printed without JSON quotes.",
        "Try this command shape, replacing placeholders with fixture values: jq -r <values>"
      ],
      "files": {
        "project.json": "{\"name\":\"gym\",\"owner\":\"student\",\"active\":true}\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The owner value was printed without JSON quotes.",
          "expected": "student",
          "normalize": "trim"
        }
      ],
      "completion": "jq filters JSON by structure. Prefer it to grep or sed when field boundaries and data types matter.",
      "command_order": 87,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "jq",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-unxz"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-jq-2",
      "section": "09-archives-data-and-automation",
      "command": "jq",
      "title": "Select nested JSON data",
      "about": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. Chained property filters navigate nested objects without relying on whitespace or key order.",
      "example": "jq -r '.server.host' config.json",
      "example_output": "localhost",
      "breakdown": [
        "jq is the featured command in the worked pattern.",
        "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. Chained property filters navigate nested objects without relying on whitespace or key order.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the nested database host from config.json.",
      "solution": "jq -r '.database.host' config.json",
      "hints": [
        "Use the checker as a specification: The nested host value was printed as plain text.",
        "Try this command shape, replacing placeholders with fixture values: jq -r <values>",
        "Use man jq or jq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "config.json": "{\"database\":{\"host\":\"db.internal\",\"port\":5432},\"debug\":false}\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The nested host value was printed as plain text.",
          "expected": "db.internal",
          "normalize": "trim"
        }
      ],
      "completion": "Chained property filters navigate nested objects without relying on whitespace or key order. You can now use this behavior as part of a larger command-line workflow.",
      "order": 432,
      "command_order": 87,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. Chained property filters navigate nested objects without relying on whitespace or key order.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "jq",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-unxz-2"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-jq-3",
      "section": "09-archives-data-and-automation",
      "command": "jq",
      "title": "Filter an array of objects",
      "about": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. .[] iterates array elements and select keeps objects whose fields satisfy a condition.",
      "example": "jq -r '.[] | select(.active) | .name' users.json",
      "example_output": "Ada",
      "breakdown": [
        "jq is the featured command in the worked pattern.",
        "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. .[] iterates array elements and select keeps objects whose fields satisfy a condition.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the names of enabled services from services.json, one per line.",
      "solution": "jq -r '.[] | select(.enabled) | .name' services.json",
      "hints": [
        "Use the checker as a specification: Only enabled service names were printed.",
        "Try this command shape, replacing placeholders with fixture values: jq -r <value> | <value> | <values>",
        "Use man jq or jq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "services.json": "[{\"name\":\"api\",\"enabled\":true},{\"name\":\"legacy\",\"enabled\":false},{\"name\":\"worker\",\"enabled\":true}]\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "Only enabled service names were printed.",
          "expected": "api\nworker",
          "normalize": "trim"
        }
      ],
      "completion": ".[] iterates array elements and select keeps objects whose fields satisfy a condition. You can now use this behavior as part of a larger command-line workflow.",
      "order": 433,
      "command_order": 87,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. .[] iterates array elements and select keeps objects whose fields satisfy a condition.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "jq",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-unxz-3"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-jq-4",
      "section": "09-archives-data-and-automation",
      "command": "jq",
      "title": "Build a new JSON shape",
      "about": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. Object construction creates predictable output containing only selected or calculated fields.",
      "example": "jq '{name: .service, healthy: (.status == \"ok\")}' status.json",
      "example_output": "{\n  \"name\": \"api\",\n  \"healthy\": true\n}",
      "breakdown": [
        "jq is the featured command in the worked pattern.",
        "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. Object construction creates predictable output containing only selected or calculated fields.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Transform app.json into a compact object with name and a ports count.",
      "solution": "jq -c '{name: .name, port_count: (.ports | length)}' app.json",
      "hints": [
        "Use the checker as a specification: The compact JSON object contains only the derived public fields.",
        "Try this command shape, replacing placeholders with fixture values: jq -c <values> | <values>",
        "Use man jq or jq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.json": "{\"name\":\"gateway\",\"ports\":[80,443],\"internal\":\"secret\"}\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The compact JSON object contains only the derived public fields.",
          "expected": "{\"name\":\"gateway\",\"port_count\":2}",
          "normalize": "trim"
        }
      ],
      "completion": "Object construction creates predictable output containing only selected or calculated fields. You can now use this behavior as part of a larger command-line workflow.",
      "order": 434,
      "command_order": 87,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. Object construction creates predictable output containing only selected or calculated fields.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "jq",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-unxz-4"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-jq-5",
      "section": "09-archives-data-and-automation",
      "command": "jq",
      "title": "Combine JSON documents in a workflow",
      "about": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. jq -s reads multiple documents into an array so a filter can aggregate their structured values.",
      "example": "jq -s 'map(.count) | add' reports/*.json",
      "example_output": "12",
      "breakdown": [
        "jq is the featured command in the worked pattern.",
        "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. jq -s reads multiple documents into an array so a filter can aggregate their structured values.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read every JSON file in reports and print total=<sum of requests>.",
      "solution": "printf 'total=%s\\n' \"$(jq -s 'map(.requests) | add' reports/*.json)\"",
      "hints": [
        "Use the checker as a specification: The request counts from all JSON reports were aggregated.",
        "Try this command shape, replacing placeholders with fixture values: printf <values> -s <value> | <values>",
        "Use man jq or jq --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "reports/api.json": "{\"requests\":12}\n",
        "reports/web.json": "{\"requests\":23}\n",
        "reports/worker.json": "{\"requests\":5}\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The request counts from all JSON reports were aggregated.",
          "expected": "total=40",
          "normalize": "trim"
        }
      ],
      "completion": "jq -s reads multiple documents into an array so a filter can aggregate their structured values. You can now use this behavior as part of a larger command-line workflow.",
      "order": 435,
      "command_order": 87,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "jq parses JSON and applies filters to structured values instead of treating the document as unstructured text. jq -s reads multiple documents into an array so a filter can aggregate their structured values.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "jq",
        "pipelines",
        "command substitution",
        "quoting"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [
        "linux-unxz-5"
      ],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-zip",
      "order": 436,
      "section": "09-archives-data-and-automation",
      "command": "zip",
      "title": "Create a ZIP archive",
      "about": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep.",
      "example": "zip -q bundle.zip one.txt two.txt",
      "example_output": "",
      "breakdown": [
        "bundle.zip is the archive path.",
        "The two source files remain in place."
      ],
      "task": "Create bundle.zip containing one.txt and two.txt.",
      "solution": "zip -q bundle.zip one.txt two.txt",
      "hints": [
        "Name the archive before the source files.",
        "Use the checker as a specification: bundle.zip was created. one.txt remains.",
        "Try this command shape, replacing placeholders with fixture values: zip -q <values>"
      ],
      "files": {
        "one.txt": "one\n",
        "two.txt": "two\n"
      },
      "packages": [
        "zip"
      ],
      "checks": [
        {
          "type": "path-exists",
          "description": "bundle.zip was created.",
          "path": "bundle.zip",
          "kind": "file"
        },
        {
          "type": "file-content",
          "description": "one.txt remains.",
          "path": "one.txt",
          "expected": "one",
          "normalize": "trim"
        }
      ],
      "completion": "ZIP paths can overwrite files when extracted. Inspect names and extract untrusted archives into a disposable directory.",
      "command_order": 88,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "zip"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-zip-2",
      "section": "09-archives-data-and-automation",
      "command": "zip",
      "title": "Archive a directory recursively",
      "about": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -r descends a directory tree and stores its nested files.",
      "example": "zip -r project.zip project",
      "example_output": "adding: project/...",
      "breakdown": [
        "zip is the featured command in the worked pattern.",
        "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -r descends a directory tree and stores its nested files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create release.zip containing the complete release directory and list its members.",
      "solution": "zip -q -r release.zip release && unzip -Z1 release.zip",
      "hints": [
        "Use the checker as a specification: The application member was archived. The configuration member was archived.",
        "Try this command shape, replacing placeholders with fixture values: zip -q -r <values> && unzip -Z1 <value>",
        "Use man zip or zip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "release/app": "app\n",
        "release/config/app.ini": "mode=prod\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The application member was archived.",
          "expected": "release/app"
        },
        {
          "type": "stdout-contains",
          "description": "The configuration member was archived.",
          "expected": "release/config/app.ini"
        }
      ],
      "completion": "zip -r descends a directory tree and stores its nested files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 437,
      "command_order": 88,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -r descends a directory tree and stores its nested files.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "zip",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "unzip"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-zip-3",
      "section": "09-archives-data-and-automation",
      "command": "zip",
      "title": "Exclude transient patterns",
      "about": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -x omits matching member paths from an otherwise recursive archive.",
      "example": "zip -r project.zip project -x '*.tmp'",
      "example_output": "adding: ...",
      "breakdown": [
        "zip is the featured command in the worked pattern.",
        "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -x omits matching member paths from an otherwise recursive archive.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create project.zip recursively while excluding every .tmp file, then list members.",
      "solution": "zip -q -r project.zip project -x '*.tmp' && unzip -Z1 project.zip",
      "hints": [
        "Use the checker as a specification: The application was archived. The documentation was archived.",
        "Try this command shape, replacing placeholders with fixture values: zip -q -r <values> -x <value> && unzip -Z1 <value>",
        "Use man zip or zip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "project/src/app.sh": "app\n",
        "project/cache/a.tmp": "",
        "project/docs/readme.md": "docs\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The application was archived.",
          "expected": "project/src/app.sh"
        },
        {
          "type": "stdout-contains",
          "description": "The documentation was archived.",
          "expected": "project/docs/readme.md"
        },
        {
          "type": "stdout-not-contains",
          "description": "No temporary file was archived.",
          "expected": ".tmp"
        }
      ],
      "completion": "zip -x omits matching member paths from an otherwise recursive archive. You can now use this behavior as part of a larger command-line workflow.",
      "order": 438,
      "command_order": 88,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -x omits matching member paths from an otherwise recursive archive.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "zip",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "unzip"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-zip-4",
      "section": "09-archives-data-and-automation",
      "command": "zip",
      "title": "Update changed archive members",
      "about": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -u adds new files and replaces members only when their source is newer.",
      "example": "zip -u bundle.zip app.conf",
      "example_output": "updating: app.conf",
      "breakdown": [
        "zip is the featured command in the worked pattern.",
        "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -u adds new files and replaces members only when their source is newer.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Update bundle.zip after changing app.conf from v1 to v2, then print the archived app.conf.",
      "solution": "echo v2 > app.conf && touch -d 'next hour' app.conf && zip -q -u bundle.zip app.conf && unzip -p bundle.zip app.conf",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: echo <value> > <value> && touch -d <values> && zip -q -u <values> && unzip -p <values>",
        "Use man zip or zip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "app.conf": "v1\n"
      },
      "setup": [
        "zip -q bundle.zip app.conf"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Update changed archive members”.",
          "expected": "v2",
          "normalize": "trim"
        }
      ],
      "completion": "zip -u adds new files and replaces members only when their source is newer. You can now use this behavior as part of a larger command-line workflow.",
      "order": 439,
      "command_order": 88,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -u adds new files and replaces members only when their source is newer.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "zip",
        "redirection",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "touch",
        "echo"
      ],
      "introduced_inline": [
        "unzip"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-zip-5",
      "section": "09-archives-data-and-automation",
      "command": "zip",
      "title": "Build a release archive from a manifest",
      "about": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -@ reads member names from standard input, allowing a reviewed manifest to define exact archive contents.",
      "example": "zip bundle.zip -@ < manifest.txt",
      "example_output": "adding: ...",
      "breakdown": [
        "zip is the featured command in the worked pattern.",
        "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -@ reads member names from standard input, allowing a reviewed manifest to define exact archive contents.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create bundle.zip using only paths in manifest.txt, then list its members.",
      "solution": "zip -q bundle.zip -@ < manifest.txt && unzip -Z1 bundle.zip",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: zip -q <values> < <value> && unzip -Z1 <value>",
        "Use man zip or zip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "manifest.txt": "app/bin/start.sh\napp/config/app.ini\n",
        "app/bin/start.sh": "start\n",
        "app/config/app.ini": "mode=prod\n",
        "app/cache/skip.tmp": "skip\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Build a release archive from a manifest”.",
          "expected": "app/bin/start.sh\napp/config/app.ini",
          "normalize": "trim"
        }
      ],
      "completion": "zip -@ reads member names from standard input, allowing a reviewed manifest to define exact archive contents. You can now use this behavior as part of a larger command-line workflow.",
      "order": 440,
      "command_order": 88,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "zip creates a broadly compatible archive that can contain multiple files. -q suppresses progress noise in this rep. zip -@ reads member names from standard input, allowing a reviewed manifest to define exact archive contents.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "zip",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [
        "unzip"
      ],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-unzip",
      "order": 441,
      "section": "09-archives-data-and-automation",
      "command": "unzip",
      "title": "Extract a ZIP archive",
      "about": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state.",
      "example": "zip -q bundle.zip note.txt && rm note.txt && unzip -q bundle.zip",
      "example_output": "",
      "breakdown": [
        "A local archive is created.",
        "The original is removed.",
        "unzip restores it after both earlier steps succeed."
      ],
      "task": "Restore note.txt from bundle.zip with unzip.",
      "solution": "unzip -q bundle.zip",
      "hints": [
        "Chain zip, rm, and unzip with &&.",
        "Use the checker as a specification: note.txt was restored from the archive.",
        "Try this command shape, replacing placeholders with fixture values: unzip -q <value>"
      ],
      "files": {
        "note.txt": "restored from zip\n"
      },
      "packages": [
        "zip",
        "unzip"
      ],
      "setup": [
        "zip -q bundle.zip note.txt && rm note.txt"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "note.txt was restored from the archive.",
          "path": "note.txt",
          "expected": "restored from zip",
          "normalize": "trim"
        }
      ],
      "completion": "Use unzip -l to inspect names first and extract unknown archives inside a controlled destination.",
      "command_order": 89,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "unzip"
      ],
      "prerequisites": [
        "zip"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-unzip-2",
      "section": "09-archives-data-and-automation",
      "command": "unzip",
      "title": "List archive members",
      "about": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -l inventories names, sizes, and timestamps without extracting.",
      "example": "unzip -l bundle.zip",
      "example_output": "Length Date Time Name ...",
      "breakdown": [
        "unzip is the featured command in the worked pattern.",
        "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -l inventories names, sizes, and timestamps without extracting.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "List the members of bundle.zip.",
      "solution": "unzip -l bundle.zip",
      "hints": [
        "Use the checker as a specification: The application member was listed. The configuration member was listed.",
        "Try this command shape, replacing placeholders with fixture values: unzip -l <value>",
        "Use man unzip or unzip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "bundle/app.txt": "app\n",
        "bundle/config.ini": "mode=prod\n"
      },
      "setup": [
        "zip -q -r bundle.zip bundle && rm -rf bundle"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The application member was listed.",
          "expected": "bundle/app.txt"
        },
        {
          "type": "stdout-contains",
          "description": "The configuration member was listed.",
          "expected": "bundle/config.ini"
        }
      ],
      "completion": "unzip -l inventories names, sizes, and timestamps without extracting. You can now use this behavior as part of a larger command-line workflow.",
      "order": 442,
      "command_order": 89,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -l inventories names, sizes, and timestamps without extracting.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "unzip"
      ],
      "prerequisites": [
        "zip"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-unzip-3",
      "section": "09-archives-data-and-automation",
      "command": "unzip",
      "title": "Print one member without extracting",
      "about": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -p streams a selected member directly to standard output.",
      "example": "unzip -p bundle.zip app.conf",
      "example_output": "mode=prod",
      "breakdown": [
        "unzip is the featured command in the worked pattern.",
        "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -p streams a selected member directly to standard output.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print config/app.ini from bundle.zip without extracting it.",
      "solution": "unzip -p bundle.zip config/app.ini",
      "hints": [
        "Use the checker as a specification: config/app.ini is absent. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: unzip -p <values>",
        "Use man unzip or unzip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "config/app.ini": "mode=practice\n",
        "data/large.dat": "other\n"
      },
      "setup": [
        "zip -q -r bundle.zip config data && rm -rf config data"
      ],
      "checks": [
        {
          "type": "path-not-exists",
          "description": "config/app.ini is absent.",
          "path": "config/app.ini"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print one member without extracting”.",
          "expected": "mode=practice",
          "normalize": "trim"
        }
      ],
      "completion": "unzip -p streams a selected member directly to standard output. You can now use this behavior as part of a larger command-line workflow.",
      "order": 443,
      "command_order": 89,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -p streams a selected member directly to standard output.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "unzip"
      ],
      "prerequisites": [
        "zip"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-unzip-4",
      "section": "09-archives-data-and-automation",
      "command": "unzip",
      "title": "Extract one selected member to a directory",
      "about": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. A member operand and -d destination limit extraction to an exact path and location.",
      "example": "unzip bundle.zip app.conf -d restored",
      "example_output": "extracting: restored/app.conf",
      "breakdown": [
        "unzip is the featured command in the worked pattern.",
        "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. A member operand and -d destination limit extraction to an exact path and location.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Extract only docs/readme.md from bundle.zip into restored.",
      "solution": "unzip -q bundle.zip docs/readme.md -d restored",
      "hints": [
        "Use the checker as a specification: restored/docs/readme.md has the expected content. restored/bin/app is absent.",
        "Try this command shape, replacing placeholders with fixture values: unzip -q <values> -d <value>",
        "Use man unzip or unzip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "docs/readme.md": "documentation\n",
        "bin/app": "skip\n"
      },
      "setup": [
        "zip -q -r bundle.zip docs bin && rm -rf docs bin"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "restored/docs/readme.md has the expected content.",
          "path": "restored/docs/readme.md",
          "expected": "documentation\n"
        },
        {
          "type": "path-not-exists",
          "description": "restored/bin/app is absent.",
          "path": "restored/bin/app"
        }
      ],
      "completion": "A member operand and -d destination limit extraction to an exact path and location. You can now use this behavior as part of a larger command-line workflow.",
      "order": 444,
      "command_order": 89,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. A member operand and -d destination limit extraction to an exact path and location.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "unzip"
      ],
      "prerequisites": [
        "zip"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-unzip-5",
      "section": "09-archives-data-and-automation",
      "command": "unzip",
      "title": "Restore an archive without prompts",
      "about": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -o overwrites existing destinations without prompting, suitable only after the archive and target are reviewed.",
      "example": "unzip -o release.zip -d current",
      "example_output": "inflating: current/app.conf",
      "breakdown": [
        "unzip is the featured command in the worked pattern.",
        "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -o overwrites existing destinations without prompting, suitable only after the archive and target are reviewed.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Restore release.zip into current with -o, replacing current/release/app.conf, then print the result.",
      "solution": "unzip -q -o release.zip -d current && cat current/release/app.conf",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: unzip -q -o <value> -d <value> && cat <value>",
        "Use man unzip or unzip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "release/app.conf": "mode=new\n",
        "current/release/app.conf": "mode=old\n"
      },
      "setup": [
        "zip -q -r release.zip release && rm -rf release"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Restore an archive without prompts”.",
          "expected": "mode=new",
          "normalize": "trim"
        }
      ],
      "completion": "unzip -o overwrites existing destinations without prompting, suitable only after the archive and target are reviewed. You can now use this behavior as part of a larger command-line workflow.",
      "order": 445,
      "command_order": 89,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "unzip lists or extracts ZIP archives. This rep creates a synthetic archive, removes the source, and restores it in disposable state. unzip -o overwrites existing destinations without prompting, suitable only after the archive and target are reviewed.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "unzip",
        "command chaining"
      ],
      "prerequisites": [
        "cat",
        "zip"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sha256sum",
      "order": 446,
      "section": "09-archives-data-and-automation",
      "command": "sha256sum",
      "title": "Calculate an exact SHA-256 digest",
      "about": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted.",
      "example": "sha256sum artifact.txt",
      "example_output": "ab1881c8e3c4f2a645b0e9838c61f98b88dca68b27eff9c348676b8281ac43fc  artifact.txt\n",
      "breakdown": [
        "The file bytes are hashed.",
        "The digest and filename are printed."
      ],
      "task": "Calculate the SHA-256 digest of artifact.txt.",
      "solution": "sha256sum artifact.txt",
      "hints": [
        "Pass the filename directly to sha256sum.",
        "Use the checker as a specification: The exact digest and filename were printed.",
        "Try this command shape, replacing placeholders with fixture values: sha256sum <value>"
      ],
      "files": {
        "artifact.txt": "hello linux\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The exact digest and filename were printed.",
          "expected": "ab1881c8e3c4f2a645b0e9838c61f98b88dca68b27eff9c348676b8281ac43fc  artifact.txt",
          "normalize": "trim"
        }
      ],
      "completion": "A hash says nothing about authorship or safety unless the expected digest comes from a trusted channel.",
      "command_order": 90,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "sha256sum"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sha256sum-2",
      "section": "09-archives-data-and-automation",
      "command": "sha256sum",
      "title": "Hash several files",
      "about": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. sha256sum accepts several paths and prints one digest record for each.",
      "example": "sha256sum one.txt two.txt",
      "example_output": "<hash> one.txt\n<hash> two.txt",
      "breakdown": [
        "sha256sum is the featured command in the worked pattern.",
        "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. sha256sum accepts several paths and prints one digest record for each.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print SHA-256 records for api.conf, db.conf, and web.conf in that order.",
      "solution": "sha256sum api.conf db.conf web.conf",
      "hints": [
        "Use the checker as a specification: The API file was hashed. The database file was hashed.",
        "Try this command shape, replacing placeholders with fixture values: sha256sum <values>",
        "Use man sha256sum or sha256sum --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "api.conf": "api\n",
        "db.conf": "db\n",
        "web.conf": "web\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The API file was hashed.",
          "expected": "api.conf"
        },
        {
          "type": "stdout-contains",
          "description": "The database file was hashed.",
          "expected": "db.conf"
        },
        {
          "type": "stdout-contains",
          "description": "The web file was hashed.",
          "expected": "web.conf"
        }
      ],
      "completion": "sha256sum accepts several paths and prints one digest record for each. You can now use this behavior as part of a larger command-line workflow.",
      "order": 447,
      "command_order": 90,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. sha256sum accepts several paths and prints one digest record for each.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "sha256sum"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-sha256sum-3",
      "section": "09-archives-data-and-automation",
      "command": "sha256sum",
      "title": "Create and verify a checksum manifest",
      "about": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. sha256sum -c reads saved digest records and verifies current file contents.",
      "example": "sha256sum artifact > checksums; sha256sum -c checksums",
      "example_output": "artifact: OK",
      "breakdown": [
        "sha256sum is the featured command in the worked pattern.",
        "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. sha256sum -c reads saved digest records and verifies current file contents.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create checksums.sha256 for artifact.bin, then verify it.",
      "solution": "sha256sum artifact.bin > checksums.sha256 && sha256sum -c checksums.sha256",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sha256sum <value> > <value> && sha256sum -c <value>",
        "Use man sha256sum or sha256sum --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact.bin": "verified artifact\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Create and verify a checksum manifest”.",
          "expected": "artifact.bin: OK",
          "normalize": "trim"
        }
      ],
      "completion": "sha256sum -c reads saved digest records and verifies current file contents. You can now use this behavior as part of a larger command-line workflow.",
      "order": 448,
      "command_order": 90,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. sha256sum -c reads saved digest records and verifies current file contents.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "sha256sum",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sha256sum-4",
      "section": "09-archives-data-and-automation",
      "command": "sha256sum",
      "title": "Report only verification failures",
      "about": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. --quiet suppresses successful OK rows while the exit status still reflects overall verification.",
      "example": "sha256sum -c --quiet checksums && echo verified",
      "example_output": "verified",
      "breakdown": [
        "sha256sum is the featured command in the worked pattern.",
        "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. --quiet suppresses successful OK rows while the exit status still reflects overall verification.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create a manifest for two files, verify it quietly, and print all-verified.",
      "solution": "sha256sum one.txt two.txt > checksums.sha256 && sha256sum -c --quiet checksums.sha256 && echo all-verified",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: sha256sum <values> > <value> && sha256sum -c --quiet <value> && echo all-verified",
        "Use man sha256sum or sha256sum --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "one.txt": "one\n",
        "two.txt": "two\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Report only verification failures”.",
          "expected": "all-verified",
          "normalize": "trim"
        }
      ],
      "completion": "--quiet suppresses successful OK rows while the exit status still reflects overall verification. You can now use this behavior as part of a larger command-line workflow.",
      "order": 449,
      "command_order": 90,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. --quiet suppresses successful OK rows while the exit status still reflects overall verification.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "sha256sum",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-sha256sum-5",
      "section": "09-archives-data-and-automation",
      "command": "sha256sum",
      "title": "Detect a changed artifact",
      "about": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. A checksum manifest detects content changes independently of filename or timestamp metadata.",
      "example": "sha256sum file > manifest; echo changed > file; sha256sum -c manifest",
      "example_output": "file: FAILED",
      "breakdown": [
        "sha256sum is the featured command in the worked pattern.",
        "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. A checksum manifest detects content changes independently of filename or timestamp metadata.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Create a checksum for config.ini, change its contents, then run verification and preserve the FAILED output. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "sha256sum config.ini > checksums.sha256; echo 'mode=changed' > config.ini; sha256sum -c checksums.sha256 || true && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: The changed file failed verification.",
        "Try this command shape, replacing placeholders with fixture values: sha256sum <value> > <value> ; echo <value> > <value> ; sha256sum -c <value> | | true",
        "Use man sha256sum or sha256sum --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "config.ini": "mode=original\n",
        ".gym-challenge": "token={{nonce}}\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The changed file failed verification.",
          "expected": "config.ini: FAILED"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "A checksum manifest detects content changes independently of filename or timestamp metadata. You can now use this behavior as part of a larger command-line workflow.",
      "order": 450,
      "command_order": 90,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "sha256sum hashes exact file bytes. A digest supports equality and integrity comparisons when the expected value is trusted. A checksum manifest detects content changes independently of filename or timestamp metadata.",
      "stage_kind": "workflow",
      "mode": "capstone",
      "concepts": [
        "sha256sum",
        "pipelines",
        "redirection",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "cp",
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-ip",
      "order": 451,
      "section": "10-networking-and-services",
      "command": "ip",
      "title": "Inspect network interfaces",
      "about": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary.",
      "example": "ip -brief address",
      "example_output": "lo UNKNOWN 127.0.0.1/8 ::1/128\n",
      "breakdown": [
        "-brief reduces output.",
        "address selects interface addresses."
      ],
      "task": "Print a brief summary of interface addresses.",
      "solution": "ip -brief address",
      "hints": [
        "Use ip brief mode with the address object.",
        "Use the checker as a specification: The loopback interface appears in the address summary.",
        "Try this command shape, replacing placeholders with fixture values: ip -brief <value>"
      ],
      "files": {},
      "packages": [
        "iproute2"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The loopback interface appears in the address summary.",
          "expected": "lo"
        }
      ],
      "completion": "An assigned address does not prove routing, DNS, firewall permission, or application reachability.",
      "command_order": 91,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "ip"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ip-2",
      "section": "10-networking-and-services",
      "command": "ip",
      "title": "Inspect link-layer interfaces",
      "about": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. ip -brief link gives a compact interface, state, and hardware-address summary.",
      "example": "ip -brief link",
      "example_output": "lo UNKNOWN ...\neth0 UP ...",
      "breakdown": [
        "ip is the featured command in the worked pattern.",
        "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. ip -brief link gives a compact interface, state, and hardware-address summary.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the brief link summary.",
      "solution": "ip -brief link",
      "hints": [
        "Use the checker as a specification: The loopback link was included.",
        "Try this command shape, replacing placeholders with fixture values: ip -brief <value>",
        "Use man ip or ip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The loopback link was included.",
          "expected": "lo"
        }
      ],
      "completion": "ip -brief link gives a compact interface, state, and hardware-address summary. You can now use this behavior as part of a larger command-line workflow.",
      "order": 452,
      "command_order": 91,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. ip -brief link gives a compact interface, state, and hardware-address summary.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "ip"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ip-3",
      "section": "10-networking-and-services",
      "command": "ip",
      "title": "Show one interface in detail",
      "about": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. A dev selector narrows ip output to one named interface.",
      "example": "ip address show dev lo",
      "example_output": "1: lo: <LOOPBACK,UP,...>",
      "breakdown": [
        "ip is the featured command in the worked pattern.",
        "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. A dev selector narrows ip output to one named interface.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Show address details only for lo.",
      "solution": "ip address show dev lo",
      "hints": [
        "Use the checker as a specification: The loopback interface details were printed. The IPv4 loopback address was printed.",
        "Try this command shape, replacing placeholders with fixture values: ip <values>",
        "Use man ip or ip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The loopback interface details were printed.",
          "expected": "LOOPBACK"
        },
        {
          "type": "stdout-contains",
          "description": "The IPv4 loopback address was printed.",
          "expected": "127.0.0.1"
        },
        {
          "type": "stdout-not-contains",
          "description": "No non-loopback interface was included.",
          "expected": "eth0"
        }
      ],
      "completion": "A dev selector narrows ip output to one named interface. You can now use this behavior as part of a larger command-line workflow.",
      "order": 453,
      "command_order": 91,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. A dev selector narrows ip output to one named interface.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "ip"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-ip-4",
      "section": "10-networking-and-services",
      "command": "ip",
      "title": "Request JSON interface data",
      "about": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. ip -j emits structured JSON that jq or another parser can consume safely.",
      "example": "ip -j address show dev lo | jq -r '.[0].ifname'",
      "example_output": "lo",
      "breakdown": [
        "ip is the featured command in the worked pattern.",
        "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. ip -j emits structured JSON that jq or another parser can consume safely.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use JSON ip output and jq to print the lo interface name.",
      "solution": "ip -j address show dev lo | jq -r '.[0].ifname'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: ip -j <values> | jq -r <value>",
        "Use man ip or ip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Request JSON interface data”.",
          "expected": "lo",
          "normalize": "trim"
        }
      ],
      "completion": "ip -j emits structured JSON that jq or another parser can consume safely. You can now use this behavior as part of a larger command-line workflow.",
      "order": 454,
      "command_order": 91,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. ip -j emits structured JSON that jq or another parser can consume safely.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "ip",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "jq"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ip-5",
      "section": "10-networking-and-services",
      "command": "ip",
      "title": "Create a loopback address report",
      "about": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. Structured ip JSON lets a workflow select address family and local address without scraping display columns.",
      "example": "ip -j addr show lo | jq -r '.[0].addr_info[] | select(.family==\"inet\") | .local'",
      "example_output": "127.0.0.1",
      "breakdown": [
        "ip is the featured command in the worked pattern.",
        "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. Structured ip JSON lets a workflow select address family and local address without scraping display columns.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print interface=lo ipv4=127.0.0.1 using ip JSON and jq.",
      "solution": "ip -j addr show lo | jq -r '.[0] | \"interface=\\(.ifname) ipv4=\\(.addr_info[] | select(.family==\"inet\") | .local)\"'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: ip -j <values> | jq -r <value> | <values> | <value> | <value>",
        "Use man ip or ip --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Create a loopback address report”.",
          "expected": "interface=lo ipv4=127.0.0.1",
          "normalize": "trim"
        }
      ],
      "completion": "Structured ip JSON lets a workflow select address family and local address without scraping display columns. You can now use this behavior as part of a larger command-line workflow.",
      "order": 455,
      "command_order": 91,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "ip is the modern Linux interface for addresses, links, routes, and neighbors. -brief gives a compact human-readable summary. Structured ip JSON lets a workflow select address family and local address without scraping display columns.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "ip",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "jq"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ss",
      "order": 456,
      "section": "10-networking-and-services",
      "command": "ss",
      "title": "Inspect listening TCP sockets",
      "about": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP.",
      "example": "ss -lnt",
      "example_output": "State Recv-Q Send-Q Local Address:Port Peer Address:Port\n",
      "breakdown": [
        "-l keeps listeners.",
        "-n preserves numeric addresses and ports.",
        "-t selects TCP."
      ],
      "task": "Print listening TCP sockets with numeric addresses and ports.",
      "solution": "ss -lnt",
      "hints": [
        "Combine listener, numeric, and TCP options.",
        "Use the checker as a specification: The socket table header was printed.",
        "Try this command shape, replacing placeholders with fixture values: ss -lnt"
      ],
      "files": {},
      "packages": [
        "iproute2"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The socket table header was printed.",
          "expected": "State"
        }
      ],
      "completion": "A listener proves a local socket state, not that a remote client can reach or successfully use the service.",
      "command_order": 92,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "ss"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ss-2",
      "section": "10-networking-and-services",
      "command": "ss",
      "title": "Show all TCP sockets",
      "about": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -tan includes listening and connected TCP sockets with numeric endpoints.",
      "example": "ss -tan",
      "example_output": "State Recv-Q Send-Q Local Address:Port ...",
      "breakdown": [
        "ss is the featured command in the worked pattern.",
        "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -tan includes listening and connected TCP sockets with numeric endpoints.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the numeric TCP socket table.",
      "solution": "ss -tan",
      "hints": [
        "Use the checker as a specification: The socket table header was printed.",
        "Try this command shape, replacing placeholders with fixture values: ss -tan",
        "Use man ss or ss --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The socket table header was printed.",
          "expected": "State"
        }
      ],
      "completion": "ss -tan includes listening and connected TCP sockets with numeric endpoints. You can now use this behavior as part of a larger command-line workflow.",
      "order": 457,
      "command_order": 92,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -tan includes listening and connected TCP sockets with numeric endpoints.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "ss"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ss-3",
      "section": "10-networking-and-services",
      "command": "ss",
      "title": "Inspect a real loopback listener",
      "about": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -lnt can verify that a TCP service is listening on an expected numeric port.",
      "example": "ss -lnt 'sport = :8080'",
      "example_output": "LISTEN ... 127.0.0.1:8080",
      "breakdown": [
        "ss is the featured command in the worked pattern.",
        "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -lnt can verify that a TCP service is listening on an expected numeric port.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Show the loopback HTTP listener on TCP port 8080.",
      "solution": "ss -lnt 'sport = :8080'",
      "hints": [
        "Use the checker as a specification: The port 8080 listener was shown.",
        "Try this command shape, replacing placeholders with fixture values: ss -lnt <values>",
        "Use man ss or ss --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "site/index.html": "ready\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The port 8080 listener was shown.",
          "expected": ":8080"
        }
      ],
      "completion": "ss -lnt can verify that a TCP service is listening on an expected numeric port. You can now use this behavior as part of a larger command-line workflow.",
      "order": 458,
      "command_order": 92,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -lnt can verify that a TCP service is listening on an expected numeric port.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "ss",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ss-4",
      "section": "10-networking-and-services",
      "command": "ss",
      "title": "Include listening process details",
      "about": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -p adds the process name and PID associated with a socket when permissions allow.",
      "example": "ss -lntp",
      "example_output": "... users:((\"httpd\",pid=...))",
      "breakdown": [
        "ss is the featured command in the worked pattern.",
        "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -p adds the process name and PID associated with a socket when permissions allow.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print process details for the listener on port 8080.",
      "solution": "ss -lntp 'sport = :8080'",
      "hints": [
        "Use the checker as a specification: The listener process details were included.",
        "Try this command shape, replacing placeholders with fixture values: ss -lntp <values>",
        "Use man ss or ss --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "site/index.html": "ready\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The listener process details were included.",
          "expected": "busybox"
        }
      ],
      "completion": "ss -p adds the process name and PID associated with a socket when permissions allow. You can now use this behavior as part of a larger command-line workflow.",
      "order": 459,
      "command_order": 92,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. ss -p adds the process name and PID associated with a socket when permissions allow.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "ss",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ss-5",
      "section": "10-networking-and-services",
      "command": "ss",
      "title": "Build a concise listener audit",
      "about": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. A filtered ss table can feed awk to emit only local endpoints for a monitoring report.",
      "example": "ss -lntH | awk '{print $4}'",
      "example_output": "127.0.0.1:8080",
      "breakdown": [
        "ss is the featured command in the worked pattern.",
        "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. A filtered ss table can feed awk to emit only local endpoints for a monitoring report.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use headerless ss output filtered to port 8080 and print listener=127.0.0.1:8080.",
      "solution": "ss -lntH 'sport = :8080' | awk '{print \"listener=\" $4}'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: ss -lntH <values> | awk <values>",
        "Use man ss or ss --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "site/index.html": "ready\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Build a concise listener audit”.",
          "expected": "listener=127.0.0.1:8080",
          "normalize": "trim"
        }
      ],
      "completion": "A filtered ss table can feed awk to emit only local endpoints for a monitoring report. You can now use this behavior as part of a larger command-line workflow.",
      "order": 460,
      "command_order": 92,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "ss reports socket state from the kernel. -l selects listening sockets, -n avoids name resolution, and -t selects TCP. A filtered ss table can feed awk to emit only local endpoints for a monitoring report.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "ss",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "awk"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ping",
      "order": 461,
      "section": "10-networking-and-services",
      "command": "ping",
      "title": "Test local ICMP echo",
      "about": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network.",
      "example": "ping -c 1 127.0.0.1",
      "example_output": "1 packets transmitted, 1 received\n",
      "breakdown": [
        "-c 1 sends one request.",
        "127.0.0.1 remains inside the local host."
      ],
      "task": "Send one ICMP echo request to IPv4 loopback.",
      "solution": "ping -c 1 127.0.0.1",
      "hints": [
        "Use count 1 and target 127.0.0.1.",
        "Use the checker as a specification: One response was received from loopback. The loopback ping succeeded.",
        "Try this command shape, replacing placeholders with fixture values: ping -c <values>"
      ],
      "files": {},
      "packages": [
        "iputils-ping"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "One response was received from loopback.",
          "expected": "1 received"
        },
        {
          "type": "exit-code",
          "description": "The loopback ping succeeded.",
          "expected": 0
        }
      ],
      "completion": "Ping success proves ICMP echo for one target and moment. Ping failure does not identify the failed network layer by itself.",
      "command_order": 93,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "ping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ping-2",
      "section": "10-networking-and-services",
      "command": "ping",
      "title": "Choose the packet count",
      "about": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -c stops after an exact number of echo requests instead of running continuously.",
      "example": "ping -c 2 127.0.0.1",
      "example_output": "... 2 transmitted, 2 received ...",
      "breakdown": [
        "ping is the featured command in the worked pattern.",
        "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -c stops after an exact number of echo requests instead of running continuously.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Send exactly two echo requests to 127.0.0.1.",
      "solution": "ping -c 2 127.0.0.1",
      "hints": [
        "Use the checker as a specification: Both responses were received.",
        "Try this command shape, replacing placeholders with fixture values: ping -c <values>",
        "Use man ping or ping --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "Both responses were received.",
          "expected": "2 received"
        }
      ],
      "completion": "ping -c stops after an exact number of echo requests instead of running continuously. You can now use this behavior as part of a larger command-line workflow.",
      "order": 462,
      "command_order": 93,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -c stops after an exact number of echo requests instead of running continuously.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "ping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ping-3",
      "section": "10-networking-and-services",
      "command": "ping",
      "title": "Set a response deadline",
      "about": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -W limits how long each request waits for a reply.",
      "example": "ping -c 1 -W 1 127.0.0.1",
      "example_output": "64 bytes from 127.0.0.1 ...",
      "breakdown": [
        "ping is the featured command in the worked pattern.",
        "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -W limits how long each request waits for a reply.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Send one loopback request with a one-second response timeout.",
      "solution": "ping -c 1 -W 1 127.0.0.1",
      "hints": [
        "Use the checker as a specification: The loopback reply was received.",
        "Try this command shape, replacing placeholders with fixture values: ping -c <value> -W <values>",
        "Use man ping or ping --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The loopback reply was received.",
          "expected": "1 received"
        }
      ],
      "completion": "ping -W limits how long each request waits for a reply. You can now use this behavior as part of a larger command-line workflow.",
      "order": 463,
      "command_order": 93,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -W limits how long each request waits for a reply.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "ping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ping-4",
      "section": "10-networking-and-services",
      "command": "ping",
      "title": "Choose the payload size",
      "about": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -s sets the ICMP data payload size so a specific packet size can be tested.",
      "example": "ping -c 1 -s 100 127.0.0.1",
      "example_output": "108 bytes from 127.0.0.1 ...",
      "breakdown": [
        "ping is the featured command in the worked pattern.",
        "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -s sets the ICMP data payload size so a specific packet size can be tested.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Send one loopback request with a 100-byte payload.",
      "solution": "ping -c 1 -s 100 127.0.0.1",
      "hints": [
        "Use the checker as a specification: The enlarged reply size was reported.",
        "Try this command shape, replacing placeholders with fixture values: ping -c <value> -s <values>",
        "Use man ping or ping --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The enlarged reply size was reported.",
          "expected": "108 bytes from"
        }
      ],
      "completion": "ping -s sets the ICMP data payload size so a specific packet size can be tested. You can now use this behavior as part of a larger command-line workflow.",
      "order": 464,
      "command_order": 93,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. ping -s sets the ICMP data payload size so a specific packet size can be tested.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "ping"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ping-5",
      "section": "10-networking-and-services",
      "command": "ping",
      "title": "Extract packet-loss and latency summaries",
      "about": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. A bounded ping run produces stable summary lines that can be retained as a connectivity record.",
      "example": "ping -c 3 127.0.0.1 | tail -n 2",
      "example_output": "3 packets transmitted...\nrtt min/avg/max...",
      "breakdown": [
        "ping is the featured command in the worked pattern.",
        "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. A bounded ping run produces stable summary lines that can be retained as a connectivity record.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Send three loopback requests and print only the final packet and RTT summary lines.",
      "solution": "ping -c 3 127.0.0.1 | tail -n 2",
      "hints": [
        "Use the checker as a specification: Zero packet loss was reported. The RTT summary was included.",
        "Try this command shape, replacing placeholders with fixture values: ping -c <values> | tail -n <value>",
        "Use man ping or ping --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "Zero packet loss was reported.",
          "expected": "0% packet loss"
        },
        {
          "type": "stdout-contains",
          "description": "The RTT summary was included.",
          "expected": "min/avg/max"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 2 nonblank output lines were printed.",
          "expected": 2
        }
      ],
      "completion": "A bounded ping run produces stable summary lines that can be retained as a connectivity record. You can now use this behavior as part of a larger command-line workflow.",
      "order": 465,
      "command_order": 93,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "ping sends ICMP echo requests and measures responses. This rep targets loopback only, requiring no external network. A bounded ping run produces stable summary lines that can be retained as a connectivity record.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "ping",
        "pipelines"
      ],
      "prerequisites": [
        "tail"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-curl",
      "order": 466,
      "section": "10-networking-and-services",
      "command": "curl",
      "title": "Read a local URL",
      "about": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine.",
      "example": "curl -s file://$PWD/message.txt",
      "example_output": "local transfer\n",
      "breakdown": [
        "-s suppresses progress output.",
        "file:// reads a local path rather than making a network request."
      ],
      "task": "Use curl silently to print message.txt through a file URL rooted at $PWD.",
      "solution": "curl -s file://$PWD/message.txt",
      "hints": [
        "Use the file:// scheme and the PWD environment variable.",
        "Use the checker as a specification: The local message was transferred.",
        "Try this command shape, replacing placeholders with fixture values: curl -s <value>"
      ],
      "files": {
        "message.txt": "local curl transfer\n"
      },
      "packages": [
        "curl"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The local message was transferred.",
          "expected": "local curl transfer",
          "normalize": "trim"
        }
      ],
      "completion": "For HTTP work, record method, URL, status, headers, and body deliberately. Silent mode can hide useful diagnostics.",
      "command_order": 94,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "curl",
        "variables"
      ],
      "prerequisites": [
        "pwd",
        "file"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-curl-2",
      "section": "10-networking-and-services",
      "command": "curl",
      "title": "Fetch a loopback HTTP resource",
      "about": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -s retrieves a URL without progress noise while still writing the response body.",
      "example": "curl -s http://127.0.0.1:8080/status.txt",
      "example_output": "ready",
      "breakdown": [
        "curl is the featured command in the worked pattern.",
        "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -s retrieves a URL without progress noise while still writing the response body.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Fetch status.txt from the local HTTP server.",
      "solution": "curl -s http://127.0.0.1:8080/status.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: curl -s <value>",
        "Use man curl or curl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "site/status.txt": "service=ready\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Fetch a loopback HTTP resource”.",
          "expected": "service=ready",
          "normalize": "trim"
        }
      ],
      "completion": "curl -s retrieves a URL without progress noise while still writing the response body. You can now use this behavior as part of a larger command-line workflow.",
      "order": 467,
      "command_order": 94,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -s retrieves a URL without progress noise while still writing the response body.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "curl"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-curl-3",
      "section": "10-networking-and-services",
      "command": "curl",
      "title": "Inspect response headers",
      "about": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -I requests only HTTP response headers rather than downloading the body.",
      "example": "curl -sI http://127.0.0.1:8080/",
      "example_output": "HTTP/1.1 200 OK ...",
      "breakdown": [
        "curl is the featured command in the worked pattern.",
        "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -I requests only HTTP response headers rather than downloading the body.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Request headers for index.html and print the HTTP status headers.",
      "solution": "curl -sI http://127.0.0.1:8080/index.html",
      "hints": [
        "Use the checker as a specification: A successful HTTP status was returned.",
        "Try this command shape, replacing placeholders with fixture values: curl -sI <value>",
        "Use man curl or curl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "site/index.html": "home\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "A successful HTTP status was returned.",
          "expected": "200 OK"
        }
      ],
      "completion": "curl -I requests only HTTP response headers rather than downloading the body. You can now use this behavior as part of a larger command-line workflow.",
      "order": 468,
      "command_order": 94,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -I requests only HTTP response headers rather than downloading the body.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "curl"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-curl-4",
      "section": "10-networking-and-services",
      "command": "curl",
      "title": "Save a response to a chosen file",
      "about": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -o writes the response body to an explicit destination path.",
      "example": "curl -s -o copy.txt http://127.0.0.1:8080/data.txt",
      "example_output": "",
      "breakdown": [
        "curl is the featured command in the worked pattern.",
        "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -o writes the response body to an explicit destination path.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Download artifact.txt to downloads/artifact.copy and print the saved file.",
      "solution": "curl -s -o downloads/artifact.copy http://127.0.0.1:8080/artifact.txt && cat downloads/artifact.copy",
      "hints": [
        "Use the checker as a specification: downloads/artifact.copy has the expected content. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: curl -s -o <values> && cat <value>",
        "Use man curl or curl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "downloads"
      ],
      "files": {
        "site/artifact.txt": "local artifact\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "downloads/artifact.copy has the expected content.",
          "path": "downloads/artifact.copy",
          "expected": "local artifact\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Save a response to a chosen file”.",
          "expected": "local artifact",
          "normalize": "trim"
        }
      ],
      "completion": "curl -o writes the response body to an explicit destination path. You can now use this behavior as part of a larger command-line workflow.",
      "order": 469,
      "command_order": 94,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -o writes the response body to an explicit destination path.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "curl",
        "command chaining"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-curl-5",
      "section": "10-networking-and-services",
      "command": "curl",
      "title": "Emit a response health record",
      "about": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -w can print selected transfer metadata separately from a discarded or saved body.",
      "example": "curl -s -o /dev/null -w 'status=%{http_code} bytes=%{size_download}\\n' URL",
      "example_output": "status=200 bytes=6",
      "breakdown": [
        "curl is the featured command in the worked pattern.",
        "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -w can print selected transfer metadata separately from a discarded or saved body.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Fetch health.txt, discard its body, and print status=<code> bytes=<download-size>.",
      "solution": "curl -s -o /dev/null -w 'status=%{http_code} bytes=%{size_download}\\n' http://127.0.0.1:8080/health.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: curl -s -o <value> -w <values>",
        "Use man curl or curl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "site/health.txt": "ready\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Emit a response health record”.",
          "expected": "status=200 bytes=6",
          "normalize": "trim"
        }
      ],
      "completion": "curl -w can print selected transfer metadata separately from a discarded or saved body. You can now use this behavior as part of a larger command-line workflow.",
      "order": 470,
      "command_order": 94,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "curl transfers data using URL schemes. This rep uses a file URL inside the disposable workspace, so no network request leaves the machine. curl -w can print selected transfer metadata separately from a discarded or saved body.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "curl",
        "quoting"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-wget",
      "order": 471,
      "section": "10-networking-and-services",
      "command": "wget",
      "title": "Inspect the installed wget version",
      "about": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host.",
      "example": "wget --version | head -n 1",
      "example_output": "GNU Wget 1.21 built on linux-gnu.\n",
      "breakdown": [
        "--version reads local program metadata.",
        "head keeps one identifying line."
      ],
      "task": "Print only the first line of wget version information.",
      "solution": "wget --version | head -n 1",
      "hints": [
        "Request version output and bound it with head.",
        "Use the checker as a specification: The GNU Wget version line was printed.",
        "Try this command shape, replacing placeholders with fixture values: wget --version | head -n <value>"
      ],
      "files": {},
      "packages": [
        "wget"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The GNU Wget version line was printed.",
          "expected": "Wget"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 1 nonblank output line was printed.",
          "expected": 1
        }
      ],
      "completion": "Before downloading, decide the destination, overwrite behavior, redirects, trust source, and integrity check.",
      "command_order": 95,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "wget",
        "pipelines"
      ],
      "prerequisites": [
        "head"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-wget-2",
      "section": "10-networking-and-services",
      "command": "wget",
      "title": "Print a loopback response body",
      "about": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -qO- suppresses progress and writes the downloaded body to standard output.",
      "example": "wget -qO- http://127.0.0.1:8080/status.txt",
      "example_output": "ready",
      "breakdown": [
        "wget is the featured command in the worked pattern.",
        "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -qO- suppresses progress and writes the downloaded body to standard output.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print message.txt from the local HTTP server.",
      "solution": "wget -qO- http://127.0.0.1:8080/message.txt",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: wget -qO- <value>",
        "Use man wget or wget --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "site/message.txt": "hello from wget\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Print a loopback response body”.",
          "expected": "hello from wget",
          "normalize": "trim"
        }
      ],
      "completion": "wget -qO- suppresses progress and writes the downloaded body to standard output. You can now use this behavior as part of a larger command-line workflow.",
      "order": 472,
      "command_order": 95,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -qO- suppresses progress and writes the downloaded body to standard output.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "wget"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-wget-3",
      "section": "10-networking-and-services",
      "command": "wget",
      "title": "Save to an explicit destination",
      "about": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -O chooses the local output path instead of deriving it from the URL.",
      "example": "wget -qO copy.txt http://127.0.0.1:8080/data.txt",
      "example_output": "",
      "breakdown": [
        "wget is the featured command in the worked pattern.",
        "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -O chooses the local output path instead of deriving it from the URL.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Download report.txt as downloads/report.copy and print it.",
      "solution": "wget -qO downloads/report.copy http://127.0.0.1:8080/report.txt && cat downloads/report.copy",
      "hints": [
        "Use the checker as a specification: downloads/report.copy has the expected content. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: wget -qO <values> && cat <value>",
        "Use man wget or wget --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "downloads"
      ],
      "files": {
        "site/report.txt": "wget report\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "downloads/report.copy has the expected content.",
          "path": "downloads/report.copy",
          "expected": "wget report\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Save to an explicit destination”.",
          "expected": "wget report",
          "normalize": "trim"
        }
      ],
      "completion": "wget -O chooses the local output path instead of deriving it from the URL. You can now use this behavior as part of a larger command-line workflow.",
      "order": 473,
      "command_order": 95,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -O chooses the local output path instead of deriving it from the URL.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "wget",
        "command chaining"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-wget-4",
      "section": "10-networking-and-services",
      "command": "wget",
      "title": "Check a URL without saving it",
      "about": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget --spider verifies reachability and response status without downloading a response body.",
      "example": "wget -q --spider URL && echo reachable",
      "example_output": "reachable",
      "breakdown": [
        "wget is the featured command in the worked pattern.",
        "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget --spider verifies reachability and response status without downloading a response body.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use spider mode on health.txt and print reachable when it succeeds.",
      "solution": "wget -q --spider http://127.0.0.1:8080/health.txt && echo reachable",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: wget -q --spider <value> && echo <value>",
        "Use man wget or wget --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "site/health.txt": "ready\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Check a URL without saving it”.",
          "expected": "reachable",
          "normalize": "trim"
        }
      ],
      "completion": "wget --spider verifies reachability and response status without downloading a response body. You can now use this behavior as part of a larger command-line workflow.",
      "order": 474,
      "command_order": 95,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget --spider verifies reachability and response status without downloading a response body.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "wget",
        "command chaining"
      ],
      "prerequisites": [
        "echo"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-wget-5",
      "section": "10-networking-and-services",
      "command": "wget",
      "title": "Download URLs from a manifest",
      "about": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -i reads a reviewed URL list and applies the same download behavior to each entry.",
      "example": "wget -q -i urls.txt -P downloads",
      "example_output": "",
      "breakdown": [
        "wget is the featured command in the worked pattern.",
        "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -i reads a reviewed URL list and applies the same download behavior to each entry.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Download every URL in urls.txt into downloads, then print the filenames in sorted order.",
      "solution": "wget -q -i urls.txt -P downloads && find downloads -type f -printf \"%f\n\" | sort",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: wget -q -i <value> -P <value> && find <value> -type <value> -printf <values> | sort",
        "Use man wget or wget --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "downloads"
      ],
      "files": {
        "site/one.txt": "one\n",
        "site/two.txt": "two\n",
        "urls.txt": "http://127.0.0.1:8080/one.txt\nhttp://127.0.0.1:8080/two.txt\n"
      },
      "setup": [
        "pkill -x busybox 2>/dev/null || true",
        "busybox httpd -p 127.0.0.1:8080 -h site"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Download URLs from a manifest”.",
          "expected": "one.txt\ntwo.txt",
          "normalize": "trim"
        }
      ],
      "completion": "wget -i reads a reviewed URL list and applies the same download behavior to each entry. You can now use this behavior as part of a larger command-line workflow.",
      "order": 475,
      "command_order": 95,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "wget is a noninteractive network downloader. To keep this core rep offline, it inspects the local version rather than contacting a remote host. wget -i reads a reviewed URL list and applies the same download behavior to each entry.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "wget",
        "pipelines",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "sort",
        "find"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ssh",
      "order": 476,
      "section": "10-networking-and-services",
      "command": "ssh",
      "title": "Inspect effective SSH client configuration",
      "about": "ssh -G prints the client configuration that would apply to a host without making a connection.",
      "example": "ssh -G example.invalid | grep '^port '",
      "example_output": "port 22\n",
      "breakdown": [
        "-G evaluates configuration only.",
        "grep selects the effective port line.",
        "No SSH connection is attempted."
      ],
      "task": "Print the effective SSH port for host example.invalid without connecting.",
      "solution": "ssh -G example.invalid | grep '^port '",
      "hints": [
        "Use ssh configuration mode -G, then select the port line.",
        "Use the checker as a specification: The effective default SSH port was printed.",
        "Try this command shape, replacing placeholders with fixture values: ssh -G <value> | grep <values>"
      ],
      "files": {},
      "packages": [
        "openssh-client"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The effective default SSH port was printed.",
          "expected": "port 22",
          "normalize": "trim"
        }
      ],
      "completion": "ssh -G is excellent for debugging config precedence. A resolved configuration does not prove network reachability or authentication.",
      "command_order": 96,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "ssh -G prints the client configuration that would apply to a host without making a connection.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "ssh",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "grep"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ssh-2",
      "section": "10-networking-and-services",
      "command": "ssh",
      "title": "Inspect expanded SSH configuration",
      "about": "ssh -G prints the client configuration that would apply to a host without making a connection. ssh -G prints the final client configuration after defaults, files, and command-line options are combined.",
      "example": "ssh -G -p 2222 host | grep -E '^(hostname|port) '",
      "example_output": "hostname host\nport 2222",
      "breakdown": [
        "ssh is the featured command in the worked pattern.",
        "ssh -G prints the client configuration that would apply to a host without making a connection. ssh -G prints the final client configuration after defaults, files, and command-line options are combined.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print the effective hostname and port for example.invalid when -p 2200 is supplied.",
      "solution": "ssh -G -p 2200 example.invalid | grep -E '^(hostname|port) '",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: ssh -G -p <values> | grep -E <value> | <values>",
        "Use man ssh or ssh --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Inspect expanded SSH configuration”.",
          "expected": "hostname example.invalid\nport 2200",
          "normalize": "trim"
        }
      ],
      "completion": "ssh -G prints the final client configuration after defaults, files, and command-line options are combined. You can now use this behavior as part of a larger command-line workflow.",
      "order": 477,
      "command_order": 96,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "ssh -G prints the client configuration that would apply to a host without making a connection. ssh -G prints the final client configuration after defaults, files, and command-line options are combined.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "ssh",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "grep",
        "hostname"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ssh-3",
      "section": "10-networking-and-services",
      "command": "ssh",
      "title": "Apply a client configuration alias",
      "about": "ssh -G prints the client configuration that would apply to a host without making a connection. A Host entry can define hostname, port, user, and identity settings behind a short alias.",
      "example": "ssh -G -F ssh_config lab",
      "example_output": "host lab\nhostname 127.0.0.1\nport 2222",
      "breakdown": [
        "ssh is the featured command in the worked pattern.",
        "ssh -G prints the client configuration that would apply to a host without making a connection. A Host entry can define hostname, port, user, and identity settings behind a short alias.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use ssh -G with ssh_config and print the effective hostname, user, and port for lab.",
      "solution": "ssh -G -F ssh_config lab | grep -E '^(hostname|user|port) '",
      "hints": [
        "Use the checker as a specification: The effective host was printed. The effective user was printed.",
        "Try this command shape, replacing placeholders with fixture values: ssh -G -F <values> | grep -E <value> | user | <values>",
        "Use man ssh or ssh --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "ssh_config": "Host lab\n  HostName 127.0.0.1\n  User root\n  Port 2222\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The effective host was printed.",
          "expected": "hostname 127.0.0.1"
        },
        {
          "type": "stdout-contains",
          "description": "The effective user was printed.",
          "expected": "user root"
        },
        {
          "type": "stdout-contains",
          "description": "The effective port was printed.",
          "expected": "port 2222"
        }
      ],
      "completion": "A Host entry can define hostname, port, user, and identity settings behind a short alias. You can now use this behavior as part of a larger command-line workflow.",
      "order": 478,
      "command_order": 96,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "ssh -G prints the client configuration that would apply to a host without making a connection. A Host entry can define hostname, port, user, and identity settings behind a short alias.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "ssh",
        "pipelines",
        "quoting"
      ],
      "prerequisites": [
        "grep",
        "hostname"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-ssh-4",
      "section": "10-networking-and-services",
      "command": "ssh",
      "title": "Run a command on a real loopback SSH server",
      "about": "ssh -G prints the client configuration that would apply to a host without making a connection. SSH can execute one remote command noninteractively while encryption and authentication still occur normally.",
      "example": "ssh lab \"printf ready\"",
      "example_output": "ready",
      "breakdown": [
        "ssh is the featured command in the worked pattern.",
        "ssh -G prints the client configuration that would apply to a host without making a connection. SSH can execute one remote command noninteractively while encryption and authentication still occur normally.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Connect to the local SSH server and print remote-ready.",
      "solution": "ssh -q -i client_key -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@127.0.0.1 'printf remote-ready'",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: ssh -q -i <value> -p <value> -o <value> -o <values>",
        "Use man ssh or ssh --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "setup": [
        "pkill -f '[s]shd.*sshd_config' 2>/dev/null || true",
        "mkdir -p /run/sshd",
        "ssh-keygen -q -t ed25519 -N '' -f host_key",
        "ssh-keygen -q -t ed25519 -N '' -f client_key",
        "cp client_key.pub authorized_keys",
        "printf '%s\\n' 'Port 2222' 'ListenAddress 127.0.0.1' 'HostKey /work/host_key' 'PidFile /work/sshd.pid' 'AuthorizedKeysFile /work/authorized_keys' 'PermitRootLogin yes' 'PasswordAuthentication no' 'KbdInteractiveAuthentication no' 'StrictModes no' 'Subsystem sftp internal-sftp' 'LogLevel ERROR' > sshd_config",
        "/usr/sbin/sshd -f /work/sshd_config -E /work/sshd.log"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Run a command on a real loopback SSH server”.",
          "expected": "remote-ready",
          "normalize": "trim"
        }
      ],
      "completion": "SSH can execute one remote command noninteractively while encryption and authentication still occur normally. You can now use this behavior as part of a larger command-line workflow.",
      "order": 479,
      "command_order": 96,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "ssh -G prints the client configuration that would apply to a host without making a connection. SSH can execute one remote command noninteractively while encryption and authentication still occur normally.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "ssh",
        "quoting"
      ],
      "prerequisites": [
        "printf"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-ssh-5",
      "section": "10-networking-and-services",
      "command": "ssh",
      "title": "Collect a structured remote fact",
      "about": "ssh -G prints the client configuration that would apply to a host without making a connection. Quoting keeps a command on the remote side, while local formatting can label the returned result.",
      "example": "value=$(ssh lab 'uname -s'); printf 'remote_kernel=%s\\n' \"$value\"",
      "example_output": "remote_kernel=Linux",
      "breakdown": [
        "ssh is the featured command in the worked pattern.",
        "ssh -G prints the client configuration that would apply to a host without making a connection. Quoting keeps a command on the remote side, while local formatting can label the returned result.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Query uname -s through the local SSH server and print remote_kernel=Linux.",
      "solution": "value=$(ssh -q -i client_key -p 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@127.0.0.1 'uname -s'); printf 'remote_kernel=%s\\n' \"$value\"",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: <value> -q -i <value> -p <value> -o <value> -o <values> ; printf <values>",
        "Use man ssh or ssh --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "setup": [
        "pkill -f '[s]shd.*sshd_config' 2>/dev/null || true",
        "mkdir -p /run/sshd",
        "ssh-keygen -q -t ed25519 -N '' -f host_key",
        "ssh-keygen -q -t ed25519 -N '' -f client_key",
        "cp client_key.pub authorized_keys",
        "printf '%s\\n' 'Port 2222' 'ListenAddress 127.0.0.1' 'HostKey /work/host_key' 'PidFile /work/sshd.pid' 'AuthorizedKeysFile /work/authorized_keys' 'PermitRootLogin yes' 'PasswordAuthentication no' 'KbdInteractiveAuthentication no' 'StrictModes no' 'Subsystem sftp internal-sftp' 'LogLevel ERROR' > sshd_config",
        "/usr/sbin/sshd -f /work/sshd_config -E /work/sshd.log"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Collect a structured remote fact”.",
          "expected": "remote_kernel=Linux",
          "normalize": "trim"
        }
      ],
      "completion": "Quoting keeps a command on the remote side, while local formatting can label the returned result. You can now use this behavior as part of a larger command-line workflow.",
      "order": 480,
      "command_order": 96,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "ssh -G prints the client configuration that would apply to a host without making a connection. Quoting keeps a command on the remote side, while local formatting can label the returned result.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "ssh",
        "command chaining",
        "command substitution",
        "variables",
        "quoting"
      ],
      "prerequisites": [
        "printf",
        "uname"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-scp",
      "order": 481,
      "section": "10-networking-and-services",
      "command": "scp",
      "title": "Use scp's local copy form",
      "about": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline.",
      "example": "scp source.txt copied.txt",
      "example_output": "",
      "breakdown": [
        "Neither path includes host: syntax.",
        "The data stays inside the disposable workspace."
      ],
      "task": "Copy artifact.txt to staged.txt using scp without a remote host.",
      "solution": "scp artifact.txt staged.txt",
      "hints": [
        "Use two plain local paths; do not add a colon or host.",
        "Use the checker as a specification: staged.txt contains the artifact.",
        "Try this command shape, replacing placeholders with fixture values: scp <values>"
      ],
      "files": {
        "artifact.txt": "local scp practice\n"
      },
      "packages": [
        "openssh-client"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "staged.txt contains the artifact.",
          "path": "staged.txt",
          "expected": "local scp practice",
          "normalize": "trim"
        }
      ],
      "completion": "Real remote scp adds host-key verification, authentication, network, destination-permission, and integrity considerations.",
      "command_order": 97,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "scp"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-scp-2",
      "section": "10-networking-and-services",
      "command": "scp",
      "title": "Preserve timestamps and modes",
      "about": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. scp -p preserves modification times, access times, and mode bits during a copy.",
      "example": "scp -p source.txt backup.txt",
      "example_output": "",
      "breakdown": [
        "scp is the featured command in the worked pattern.",
        "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. scp -p preserves modification times, access times, and mode bits during a copy.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Set artifact.txt to mode 640, copy it locally to artifact.copy with -p, then print the copy mode.",
      "solution": "chmod 640 artifact.txt && scp -p artifact.txt artifact.copy && stat -c '%a' artifact.copy",
      "hints": [
        "Use the checker as a specification: artifact.copy has the expected content. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: chmod <values> && scp -p <values> && stat -c <values>",
        "Use man scp or scp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact.txt": "artifact\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "artifact.copy has the expected content.",
          "path": "artifact.copy",
          "expected": "artifact\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Preserve timestamps and modes”.",
          "expected": "640",
          "normalize": "trim"
        }
      ],
      "completion": "scp -p preserves modification times, access times, and mode bits during a copy. You can now use this behavior as part of a larger command-line workflow.",
      "order": 482,
      "command_order": 97,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. scp -p preserves modification times, access times, and mode bits during a copy.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "scp",
        "command chaining",
        "quoting"
      ],
      "prerequisites": [
        "stat",
        "chmod"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-scp-3",
      "section": "10-networking-and-services",
      "command": "scp",
      "title": "Copy a directory recursively",
      "about": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. scp -r copies a directory hierarchy and its files.",
      "example": "scp -r project snapshot",
      "example_output": "",
      "breakdown": [
        "scp is the featured command in the worked pattern.",
        "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. scp -r copies a directory hierarchy and its files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Recursively copy project to snapshot.",
      "solution": "scp -r project snapshot",
      "hints": [
        "Use the checker as a specification: snapshot/README.md has the expected content. snapshot/config/app.ini has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: scp -r <values>",
        "Use man scp or scp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "project/README.md": "project\n",
        "project/config/app.ini": "mode=prod\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "snapshot/README.md has the expected content.",
          "path": "snapshot/README.md",
          "expected": "project\n"
        },
        {
          "type": "file-content",
          "description": "snapshot/config/app.ini has the expected content.",
          "path": "snapshot/config/app.ini",
          "expected": "mode=prod\n"
        }
      ],
      "completion": "scp -r copies a directory hierarchy and its files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 483,
      "command_order": 97,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. scp -r copies a directory hierarchy and its files.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "scp"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-scp-4",
      "section": "10-networking-and-services",
      "command": "scp",
      "title": "Upload through a real loopback SSH server",
      "about": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. An scp destination in user@host:path form transfers data through SSH to a remote filesystem.",
      "example": "scp -P 2222 artifact root@127.0.0.1:/work/uploaded",
      "example_output": "",
      "breakdown": [
        "scp is the featured command in the worked pattern.",
        "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. An scp destination in user@host:path form transfers data through SSH to a remote filesystem.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Upload artifact.txt through the local SSH server to /work/uploaded.txt, then print it.",
      "solution": "scp -q -i client_key -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null artifact.txt root@127.0.0.1:/work/uploaded.txt && cat uploaded.txt",
      "hints": [
        "Use the checker as a specification: uploaded.txt has the expected content. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: scp -q -i <value> -P <value> -o <value> -o <values> && cat <value>",
        "Use man scp or scp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "artifact.txt": "remote scp\n"
      },
      "setup": [
        "pkill -f '[s]shd.*sshd_config' 2>/dev/null || true",
        "mkdir -p /run/sshd",
        "ssh-keygen -q -t ed25519 -N '' -f host_key",
        "ssh-keygen -q -t ed25519 -N '' -f client_key",
        "cp client_key.pub authorized_keys",
        "printf '%s\\n' 'Port 2222' 'ListenAddress 127.0.0.1' 'HostKey /work/host_key' 'PidFile /work/sshd.pid' 'AuthorizedKeysFile /work/authorized_keys' 'PermitRootLogin yes' 'PasswordAuthentication no' 'KbdInteractiveAuthentication no' 'StrictModes no' 'Subsystem sftp internal-sftp' 'LogLevel ERROR' > sshd_config",
        "/usr/sbin/sshd -f /work/sshd_config -E /work/sshd.log"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "uploaded.txt has the expected content.",
          "path": "uploaded.txt",
          "expected": "remote scp\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Upload through a real loopback SSH server”.",
          "expected": "remote scp",
          "normalize": "trim"
        }
      ],
      "completion": "An scp destination in user@host:path form transfers data through SSH to a remote filesystem. You can now use this behavior as part of a larger command-line workflow.",
      "order": 484,
      "command_order": 97,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. An scp destination in user@host:path form transfers data through SSH to a remote filesystem.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "scp",
        "command chaining"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-scp-5",
      "section": "10-networking-and-services",
      "command": "scp",
      "title": "Download a remote artifact",
      "about": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. Reversing scp source and destination downloads a remote path through the authenticated SSH channel.",
      "example": "scp host:/path/artifact downloads/",
      "example_output": "",
      "breakdown": [
        "scp is the featured command in the worked pattern.",
        "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. Reversing scp source and destination downloads a remote path through the authenticated SSH channel.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Download /work/remote/report.txt through the local SSH server to downloads/report.txt and print it.",
      "solution": "scp -q -i client_key -P 2222 -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null root@127.0.0.1:/work/remote/report.txt downloads/report.txt && cat downloads/report.txt",
      "hints": [
        "Use the checker as a specification: downloads/report.txt has the expected content. The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: scp -q -i <value> -P <value> -o <value> -o <values> && cat <value>",
        "Use man scp or scp --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "downloads"
      ],
      "files": {
        "remote/report.txt": "downloaded report\n"
      },
      "setup": [
        "pkill -f '[s]shd.*sshd_config' 2>/dev/null || true",
        "mkdir -p /run/sshd",
        "ssh-keygen -q -t ed25519 -N '' -f host_key",
        "ssh-keygen -q -t ed25519 -N '' -f client_key",
        "cp client_key.pub authorized_keys",
        "printf '%s\\n' 'Port 2222' 'ListenAddress 127.0.0.1' 'HostKey /work/host_key' 'PidFile /work/sshd.pid' 'AuthorizedKeysFile /work/authorized_keys' 'PermitRootLogin yes' 'PasswordAuthentication no' 'KbdInteractiveAuthentication no' 'StrictModes no' 'Subsystem sftp internal-sftp' 'LogLevel ERROR' > sshd_config",
        "/usr/sbin/sshd -f /work/sshd_config -E /work/sshd.log"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "downloads/report.txt has the expected content.",
          "path": "downloads/report.txt",
          "expected": "downloaded report\n"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Download a remote artifact”.",
          "expected": "downloaded report",
          "normalize": "trim"
        }
      ],
      "completion": "Reversing scp source and destination downloads a remote path through the authenticated SSH channel. You can now use this behavior as part of a larger command-line workflow.",
      "order": 485,
      "command_order": 97,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "scp copies files using SSH when a path includes a remote host. With two local paths, it performs a local copy, which keeps this rep offline. Reversing scp source and destination downloads a remote path through the authenticated SSH channel.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "scp",
        "command chaining"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rsync",
      "order": 486,
      "section": "10-networking-and-services",
      "command": "rsync",
      "title": "Synchronize local directory content",
      "about": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied.",
      "example": "rsync -a source/ destination/",
      "example_output": "",
      "breakdown": [
        "-a enables archive behavior.",
        "source/ with a trailing slash means copy its contents."
      ],
      "task": "Synchronize the contents of source/ into destination/ with archive mode.",
      "solution": "rsync -a source/ destination/",
      "hints": [
        "Use -a and keep the trailing slash on source/.",
        "Use the checker as a specification: destination/one.txt was copied. The nested file was copied.",
        "Try this command shape, replacing placeholders with fixture values: rsync -a <values>"
      ],
      "files": {
        "source/one.txt": "one\n",
        "source/nested/two.txt": "two\n"
      },
      "packages": [
        "rsync"
      ],
      "checks": [
        {
          "type": "file-content",
          "description": "destination/one.txt was copied.",
          "path": "destination/one.txt",
          "expected": "one",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "The nested file was copied.",
          "path": "destination/nested/two.txt",
          "expected": "two",
          "normalize": "trim"
        }
      ],
      "completion": "rsync --delete and trailing-slash mistakes can remove or reshape data. Dry-run broad synchronization changes first.",
      "command_order": 98,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "rsync"
      ],
      "prerequisites": [
        "source"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rsync-2",
      "section": "10-networking-and-services",
      "command": "rsync",
      "title": "Preview changes with a dry run",
      "about": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync -n reports what would change without writing destination files.",
      "example": "rsync -an source/ destination/",
      "example_output": "... file list ...",
      "breakdown": [
        "rsync is the featured command in the worked pattern.",
        "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync -n reports what would change without writing destination files.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Dry-run an archive synchronization from source to destination and print the itemized change list.",
      "solution": "rsync -ani source/ destination/",
      "hints": [
        "Use the checker as a specification: The changed first file was reported. The missing second file was reported.",
        "Try this command shape, replacing placeholders with fixture values: rsync -ani <values>",
        "Use man rsync or rsync --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "source/one.txt": "new version\n",
        "source/two.txt": "two\n",
        "destination/one.txt": "old\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The changed first file was reported.",
          "expected": "one.txt"
        },
        {
          "type": "stdout-contains",
          "description": "The missing second file was reported.",
          "expected": "two.txt"
        },
        {
          "type": "path-not-exists",
          "description": "destination/two.txt is absent.",
          "path": "destination/two.txt"
        }
      ],
      "completion": "rsync -n reports what would change without writing destination files. You can now use this behavior as part of a larger command-line workflow.",
      "order": 487,
      "command_order": 98,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync -n reports what would change without writing destination files.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "rsync"
      ],
      "prerequisites": [
        "source"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-rsync-3",
      "section": "10-networking-and-services",
      "command": "rsync",
      "title": "Exclude transient files",
      "about": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync --exclude prevents matching paths from being copied into a synchronized tree.",
      "example": "rsync -a --exclude='*.tmp' source/ destination/",
      "example_output": "",
      "breakdown": [
        "rsync is the featured command in the worked pattern.",
        "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync --exclude prevents matching paths from being copied into a synchronized tree.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Synchronize source to destination while excluding .tmp files.",
      "solution": "rsync -a --exclude='*.tmp' source/ destination/",
      "hints": [
        "Use the checker as a specification: destination/app.txt has the expected content. destination/nested/data.txt has the expected content.",
        "Try this command shape, replacing placeholders with fixture values: rsync -a <values>",
        "Use man rsync or rsync --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "source/app.txt": "app\n",
        "source/cache.tmp": "skip\n",
        "source/nested/data.txt": "data\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "destination/app.txt has the expected content.",
          "path": "destination/app.txt",
          "expected": "app\n"
        },
        {
          "type": "file-content",
          "description": "destination/nested/data.txt has the expected content.",
          "path": "destination/nested/data.txt",
          "expected": "data\n"
        },
        {
          "type": "path-not-exists",
          "description": "destination/cache.tmp is absent.",
          "path": "destination/cache.tmp"
        }
      ],
      "completion": "rsync --exclude prevents matching paths from being copied into a synchronized tree. You can now use this behavior as part of a larger command-line workflow.",
      "order": 488,
      "command_order": 98,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync --exclude prevents matching paths from being copied into a synchronized tree.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "rsync",
        "quoting"
      ],
      "prerequisites": [
        "source"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rsync-4",
      "section": "10-networking-and-services",
      "command": "rsync",
      "title": "Delete stale destination files",
      "about": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync --delete removes destination entries absent from the source, making the destination a true mirror.",
      "example": "rsync -a --delete source/ mirror/",
      "example_output": "",
      "breakdown": [
        "rsync is the featured command in the worked pattern.",
        "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync --delete removes destination entries absent from the source, making the destination a true mirror.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Mirror source into destination and remove destination/stale.txt.",
      "solution": "rsync -a --delete source/ destination/",
      "hints": [
        "Use the checker as a specification: destination/current.txt has the expected content. destination/stale.txt is absent.",
        "Try this command shape, replacing placeholders with fixture values: rsync -a --delete <values>",
        "Use man rsync or rsync --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "source/current.txt": "current\n",
        "destination/current.txt": "old\n",
        "destination/stale.txt": "stale\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "destination/current.txt has the expected content.",
          "path": "destination/current.txt",
          "expected": "current\n"
        },
        {
          "type": "path-not-exists",
          "description": "destination/stale.txt is absent.",
          "path": "destination/stale.txt"
        }
      ],
      "completion": "rsync --delete removes destination entries absent from the source, making the destination a true mirror. You can now use this behavior as part of a larger command-line workflow.",
      "order": 489,
      "command_order": 98,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. rsync --delete removes destination entries absent from the source, making the destination a true mirror.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "rsync"
      ],
      "prerequisites": [
        "source"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-rsync-5",
      "section": "10-networking-and-services",
      "command": "rsync",
      "title": "Create and verify an exact mirror",
      "about": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. Archive mode, deletion, and checksums combine into a robust local mirror workflow when metadata alone is insufficient.",
      "example": "rsync -ac --delete source/ mirror/",
      "example_output": "",
      "breakdown": [
        "rsync is the featured command in the worked pattern.",
        "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. Archive mode, deletion, and checksums combine into a robust local mirror workflow when metadata alone is insufficient.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Mirror release into deployed using checksums and deletion, then prove the trees are identical with diff -r.",
      "solution": "rsync -ac --delete release/ deployed/ && diff -r release deployed && echo mirror-verified",
      "hints": [
        "Use the checker as a specification: deployed/app.bin has the expected content. deployed/obsolete.txt is absent.",
        "Try this command shape, replacing placeholders with fixture values: rsync -ac --delete <values> && diff -r <values> && echo mirror-verified",
        "Use man rsync or rsync --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "release/app.bin": "v2\n",
        "release/config/app.ini": "mode=prod\n",
        "deployed/app.bin": "v1\n",
        "deployed/obsolete.txt": "old\n"
      },
      "checks": [
        {
          "type": "file-content",
          "description": "deployed/app.bin has the expected content.",
          "path": "deployed/app.bin",
          "expected": "v2\n"
        },
        {
          "type": "path-not-exists",
          "description": "deployed/obsolete.txt is absent.",
          "path": "deployed/obsolete.txt"
        },
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Create and verify an exact mirror”.",
          "expected": "mirror-verified",
          "normalize": "trim"
        }
      ],
      "completion": "Archive mode, deletion, and checksums combine into a robust local mirror workflow when metadata alone is insufficient. You can now use this behavior as part of a larger command-line workflow.",
      "order": 490,
      "command_order": 98,
      "stage": 5,
      "stage_name": "Workflow",
      "difficulty": "advanced",
      "focus": "rsync copies differences efficiently and can preserve selected metadata. A trailing slash changes whether the source directory itself or its contents are copied. Archive mode, deletion, and checksums combine into a robust local mirror workflow when metadata alone is insufficient.",
      "stage_kind": "workflow",
      "mode": "guided",
      "concepts": [
        "rsync",
        "command chaining"
      ],
      "prerequisites": [
        "echo",
        "diff"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-systemctl",
      "order": 491,
      "section": "10-networking-and-services",
      "command": "systemctl",
      "title": "Identify the installed systemd manager",
      "about": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit.",
      "example": "systemctl --version | head -n 1",
      "example_output": "systemd 256 (256.5-1)\n",
      "breakdown": [
        "--version performs a local read only.",
        "head keeps the identifying line."
      ],
      "task": "Print only the first line of systemctl version information.",
      "solution": "systemctl --version | head -n 1",
      "hints": [
        "Use --version and head.",
        "Use the checker as a specification: A systemd version line was printed.",
        "Try this command shape, replacing placeholders with fixture values: systemctl --version | head -n <value>"
      ],
      "files": {},
      "packages": [
        "systemd"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "A systemd version line was printed.",
          "expected": "systemd"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 1 nonblank output line was printed.",
          "expected": 1
        }
      ],
      "completion": "systemctl is systemd-specific. Other Linux distributions or containers may use a different init/service model.",
      "command_order": 99,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "systemctl",
        "pipelines"
      ],
      "prerequisites": [
        "head"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-systemctl-2",
      "section": "10-networking-and-services",
      "command": "systemctl",
      "title": "List installed unit files",
      "about": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl list-unit-files reads installed unit definitions even when systemd is not PID 1.",
      "example": "systemctl list-unit-files --type=service --no-pager",
      "example_output": "UNIT FILE STATE PRESET ...",
      "breakdown": [
        "systemctl is the featured command in the worked pattern.",
        "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl list-unit-files reads installed unit definitions even when systemd is not PID 1.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "List service unit files without a pager and print the first five lines.",
      "solution": "systemctl list-unit-files --type=service --no-pager | head -n 5",
      "hints": [
        "Use the checker as a specification: The unit-file heading was printed.",
        "Try this command shape, replacing placeholders with fixture values: systemctl list-unit-files --type=service --no-pager | head -n <value>",
        "Use man systemctl or systemctl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {},
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The unit-file heading was printed.",
          "expected": "UNIT FILE"
        }
      ],
      "completion": "systemctl list-unit-files reads installed unit definitions even when systemd is not PID 1. You can now use this behavior as part of a larger command-line workflow.",
      "order": 492,
      "command_order": 99,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl list-unit-files reads installed unit definitions even when systemd is not PID 1.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "systemctl",
        "pipelines"
      ],
      "prerequisites": [
        "head"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-systemctl-3",
      "section": "10-networking-and-services",
      "command": "systemctl",
      "title": "Check an offline unit state",
      "about": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl --root points at an alternate filesystem tree so enablement can be inspected without managing the host.",
      "example": "systemctl --root=/work/rootfs is-enabled gym.service",
      "example_output": "disabled",
      "breakdown": [
        "systemctl is the featured command in the worked pattern.",
        "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl --root points at an alternate filesystem tree so enablement can be inspected without managing the host.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Use systemctl with the alternate root to print the disabled state of gym.service.",
      "solution": "systemctl --root=/work/rootfs is-enabled gym.service || true",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: systemctl <value> is-enabled <value> | | true",
        "Use man systemctl or systemctl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "rootfs/etc/systemd/system/gym.service": "[Unit]\nDescription=Gym Practice Service\n[Install]\nWantedBy=multi-user.target\n[Service]\nExecStart=/usr/bin/true\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Check an offline unit state”.",
          "expected": "disabled",
          "normalize": "trim"
        }
      ],
      "completion": "systemctl --root points at an alternate filesystem tree so enablement can be inspected without managing the host. You can now use this behavior as part of a larger command-line workflow.",
      "order": 493,
      "command_order": 99,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl --root points at an alternate filesystem tree so enablement can be inspected without managing the host.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "systemctl",
        "pipelines"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-systemctl-4",
      "section": "10-networking-and-services",
      "command": "systemctl",
      "title": "Check whether an offline unit is enabled",
      "about": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. is-enabled with --root evaluates symlink-based enablement in an alternate root without contacting a service manager.",
      "example": "systemctl --root=/work/rootfs is-enabled gym.service",
      "example_output": "enabled",
      "breakdown": [
        "systemctl is the featured command in the worked pattern.",
        "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. is-enabled with --root evaluates symlink-based enablement in an alternate root without contacting a service manager.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Enable gym.service in the alternate root, then print its enabled state.",
      "solution": "systemctl --root=/work/rootfs enable gym.service >/dev/null && systemctl --root=/work/rootfs is-enabled gym.service",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: systemctl <values> > <value> && systemctl <value> is-enabled <value>",
        "Use man systemctl or systemctl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "directories": [
        "rootfs/etc/systemd/system/multi-user.target.wants"
      ],
      "files": {
        "rootfs/etc/systemd/system/gym.service": "[Unit]\nDescription=Gym Practice Service\n[Install]\nWantedBy=multi-user.target\n[Service]\nExecStart=/usr/bin/true\n"
      },
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Check whether an offline unit is enabled”.",
          "expected": "enabled",
          "normalize": "trim"
        }
      ],
      "completion": "is-enabled with --root evaluates symlink-based enablement in an alternate root without contacting a service manager. You can now use this behavior as part of a larger command-line workflow.",
      "order": 494,
      "command_order": 99,
      "stage": 4,
      "stage_name": "Composition",
      "difficulty": "intermediate",
      "focus": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. is-enabled with --root evaluates symlink-based enablement in an alternate root without contacting a service manager.",
      "stage_kind": "composition",
      "mode": "guided",
      "concepts": [
        "systemctl",
        "redirection",
        "command chaining"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-systemctl-5",
      "section": "10-networking-and-services",
      "command": "systemctl",
      "title": "Audit selected offline unit properties",
      "about": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl show can emit selected machine-readable properties for a unit when a manager is available; offline unit-file commands provide the safe fallback here.",
      "example": "systemctl --root=/work/rootfs list-unit-files gym.service --no-legend",
      "example_output": "gym.service disabled enabled",
      "breakdown": [
        "systemctl is the featured command in the worked pattern.",
        "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl show can emit selected machine-readable properties for a unit when a manager is available; offline unit-file commands provide the safe fallback here.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "List only gym.service from the alternate root without headings or a pager.",
      "solution": "systemctl --root=/work/rootfs list-unit-files gym.service --no-legend --no-pager",
      "hints": [
        "Use the checker as a specification: The selected unit file was listed.",
        "Try this command shape, replacing placeholders with fixture values: systemctl <value> list-unit-files <value> --no-legend --no-pager",
        "Use man systemctl or systemctl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "rootfs/etc/systemd/system/gym.service": "[Unit]\nDescription=Gym Practice Service\n[Install]\nWantedBy=multi-user.target\n[Service]\nExecStart=/usr/bin/true\n"
      },
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The selected unit file was listed.",
          "expected": "gym.service"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 1 nonblank output line was printed.",
          "expected": 1
        }
      ],
      "completion": "systemctl show can emit selected machine-readable properties for a unit when a manager is available; offline unit-file commands provide the safe fallback here. You can now use this behavior as part of a larger command-line workflow.",
      "order": 495,
      "command_order": 99,
      "stage": 5,
      "stage_name": "Transfer challenge",
      "difficulty": "advanced",
      "focus": "systemctl controls and queries systemd units. This safe core rep prints version information and does not change a unit. systemctl show can emit selected machine-readable properties for a unit when a manager is available; offline unit-file commands provide the safe fallback here.",
      "stage_kind": "transfer",
      "mode": "guided",
      "concepts": [
        "systemctl"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-journalctl",
      "order": 496,
      "section": "10-networking-and-services",
      "command": "journalctl",
      "title": "Identify the systemd journal client",
      "about": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state.",
      "example": "journalctl --version | head -n 1",
      "example_output": "systemd 256 (256.5-1)\n",
      "breakdown": [
        "--version reads program metadata.",
        "No journal records are accessed."
      ],
      "task": "Print only the first line of journalctl version information.",
      "solution": "journalctl --version | head -n 1",
      "hints": [
        "Use --version and keep the first line.",
        "Use the checker as a specification: A systemd version line was printed.",
        "Try this command shape, replacing placeholders with fixture values: journalctl --version | head -n <value>"
      ],
      "files": {},
      "packages": [
        "systemd"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "A systemd version line was printed.",
          "expected": "systemd"
        },
        {
          "type": "stdout-line-count",
          "description": "Exactly 1 nonblank output line was printed.",
          "expected": 1
        }
      ],
      "completion": "Journal availability depends on systemd, permissions, retention, namespaces, and persistent-storage configuration.",
      "command_order": 100,
      "stage": 1,
      "stage_name": "Basics",
      "difficulty": "foundation",
      "focus": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state.",
      "stage_kind": "orientation",
      "mode": "guided",
      "concepts": [
        "journalctl",
        "pipelines"
      ],
      "prerequisites": [
        "head"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "mixed"
    },
    {
      "id": "linux-journalctl-2",
      "section": "10-networking-and-services",
      "command": "journalctl",
      "title": "Read an exported journal file",
      "about": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. journalctl --file queries a specific journal file without depending on the host journal.",
      "example": "journalctl --file gym.journal --no-pager",
      "example_output": "... API started ...",
      "breakdown": [
        "journalctl is the featured command in the worked pattern.",
        "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. journalctl --file queries a specific journal file without depending on the host journal.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print every record from the synthetic gym journal without a pager.",
      "solution": "journalctl --file journal/gym.journal --no-pager",
      "hints": [
        "Use the checker as a specification: The API startup record was shown. The database failure was shown.",
        "Try this command shape, replacing placeholders with fixture values: journalctl --file <value> --no-pager",
        "Use man journalctl or journalctl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "events.export": "__CURSOR=s=one\n__REALTIME_TIMESTAMP=1767358800000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=api\nPRIORITY=6\nMESSAGE=API started\n\n__CURSOR=s=two\n__REALTIME_TIMESTAMP=1767358860000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=db\nPRIORITY=3\nMESSAGE=Database unavailable\n\n__CURSOR=s=three\n__REALTIME_TIMESTAMP=1767358920000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=api\nPRIORITY=4\nMESSAGE=API retrying\n\n"
      },
      "setup": [
        "mkdir -p journal",
        "/usr/lib/systemd/systemd-journal-remote --output=journal/gym.journal events.export >/dev/null 2>&1"
      ],
      "checks": [
        {
          "type": "stdout-contains",
          "description": "The API startup record was shown.",
          "expected": "API started"
        },
        {
          "type": "stdout-contains",
          "description": "The database failure was shown.",
          "expected": "Database unavailable"
        }
      ],
      "completion": "journalctl --file queries a specific journal file without depending on the host journal. You can now use this behavior as part of a larger command-line workflow.",
      "order": 497,
      "command_order": 100,
      "stage": 2,
      "stage_name": "Useful options",
      "difficulty": "foundation",
      "focus": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. journalctl --file queries a specific journal file without depending on the host journal.",
      "stage_kind": "options",
      "mode": "guided",
      "concepts": [
        "journalctl"
      ],
      "prerequisites": [],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "permissive"
    },
    {
      "id": "linux-journalctl-3",
      "section": "10-networking-and-services",
      "command": "journalctl",
      "title": "Filter by syslog identifier",
      "about": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. The -t option selects records carrying one SYSLOG_IDENTIFIER value.",
      "example": "journalctl --file gym.journal -t api",
      "example_output": "... API started ...\n... API retrying ...",
      "breakdown": [
        "journalctl is the featured command in the worked pattern.",
        "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. The -t option selects records carrying one SYSLOG_IDENTIFIER value.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print only api records from the synthetic journal.",
      "solution": "journalctl --file journal/gym.journal -t api --no-pager -o cat",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: journalctl --file <value> -t <value> --no-pager -o <value>",
        "Use man journalctl or journalctl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "events.export": "__CURSOR=s=one\n__REALTIME_TIMESTAMP=1767358800000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=api\nPRIORITY=6\nMESSAGE=API started\n\n__CURSOR=s=two\n__REALTIME_TIMESTAMP=1767358860000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=db\nPRIORITY=3\nMESSAGE=Database unavailable\n\n__CURSOR=s=three\n__REALTIME_TIMESTAMP=1767358920000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=api\nPRIORITY=4\nMESSAGE=API retrying\n\n"
      },
      "setup": [
        "mkdir -p journal",
        "/usr/lib/systemd/systemd-journal-remote --output=journal/gym.journal events.export >/dev/null 2>&1"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Filter by syslog identifier”.",
          "expected": "API started\nAPI retrying",
          "normalize": "trim"
        }
      ],
      "completion": "The -t option selects records carrying one SYSLOG_IDENTIFIER value. You can now use this behavior as part of a larger command-line workflow.",
      "order": 498,
      "command_order": 100,
      "stage": 3,
      "stage_name": "Focused results",
      "difficulty": "intermediate",
      "focus": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. The -t option selects records carrying one SYSLOG_IDENTIFIER value.",
      "stage_kind": "precision",
      "mode": "guided",
      "concepts": [
        "journalctl"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-journalctl-4",
      "section": "10-networking-and-services",
      "command": "journalctl",
      "title": "Filter by priority",
      "about": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. journalctl -p selects a priority or priority range, such as errors and anything more severe.",
      "example": "journalctl -p err --file gym.journal",
      "example_output": "... Database unavailable ...",
      "breakdown": [
        "journalctl is the featured command in the worked pattern.",
        "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. journalctl -p selects a priority or priority range, such as errors and anything more severe.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Print error-or-higher messages from the synthetic journal in cat format.",
      "solution": "journalctl --file journal/gym.journal -p err --no-pager -o cat",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: journalctl --file <value> -p <value> --no-pager -o <value>",
        "Use man journalctl or journalctl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "events.export": "__CURSOR=s=one\n__REALTIME_TIMESTAMP=1767358800000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=api\nPRIORITY=6\nMESSAGE=API started\n\n__CURSOR=s=two\n__REALTIME_TIMESTAMP=1767358860000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=db\nPRIORITY=3\nMESSAGE=Database unavailable\n\n__CURSOR=s=three\n__REALTIME_TIMESTAMP=1767358920000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=api\nPRIORITY=4\nMESSAGE=API retrying\n\n"
      },
      "setup": [
        "mkdir -p journal",
        "/usr/lib/systemd/systemd-journal-remote --output=journal/gym.journal events.export >/dev/null 2>&1"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Filter by priority”.",
          "expected": "Database unavailable",
          "normalize": "trim"
        }
      ],
      "completion": "journalctl -p selects a priority or priority range, such as errors and anything more severe. You can now use this behavior as part of a larger command-line workflow.",
      "order": 499,
      "command_order": 100,
      "stage": 4,
      "stage_name": "Applied use",
      "difficulty": "intermediate",
      "focus": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. journalctl -p selects a priority or priority range, such as errors and anything more severe.",
      "stage_kind": "applied",
      "mode": "guided",
      "concepts": [
        "journalctl"
      ],
      "prerequisites": [
        "cat"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    },
    {
      "id": "linux-journalctl-5",
      "section": "10-networking-and-services",
      "command": "journalctl",
      "title": "Export selected journal fields as JSON",
      "about": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. The json output mode turns each journal entry into structured data that jq can select and reshape.",
      "example": "journalctl -o json --file gym.journal | jq -r '.SYSLOG_IDENTIFIER + \"=\" + .MESSAGE'",
      "example_output": "api=API started",
      "breakdown": [
        "journalctl is the featured command in the worked pattern.",
        "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. The json output mode turns each journal entry into structured data that jq can select and reshape.",
        "The example uses different values; adapt its structure to the mission fixture."
      ],
      "task": "Read the journal as JSON and print identifier=message rows sorted alphabetically. Then copy the one-time token from the workspace-root .gym-challenge into workspace-root .gym-proof.",
      "solution": "journalctl --file journal/gym.journal --no-pager -o json | jq -r '.SYSLOG_IDENTIFIER + \"=\" + .MESSAGE' | sort && cp /work/.gym-challenge /work/.gym-proof",
      "hints": [
        "Use the checker as a specification: The requested result was printed.",
        "Try this command shape, replacing placeholders with fixture values: journalctl --file <value> --no-pager -o <value> | jq -r <values> | sort",
        "Use man journalctl or journalctl --help to inspect the relevant option without revealing this fixture's answer."
      ],
      "files": {
        "events.export": "__CURSOR=s=one\n__REALTIME_TIMESTAMP=1767358800000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=api\nPRIORITY=6\nMESSAGE=API started\n\n__CURSOR=s=two\n__REALTIME_TIMESTAMP=1767358860000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=db\nPRIORITY=3\nMESSAGE=Database unavailable\n\n__CURSOR=s=three\n__REALTIME_TIMESTAMP=1767358920000000\n_BOOT_ID=11111111111111111111111111111111\n_MACHINE_ID=22222222222222222222222222222222\nSYSLOG_IDENTIFIER=api\nPRIORITY=4\nMESSAGE=API retrying\n\n",
        ".gym-challenge": "token={{nonce}}\n"
      },
      "setup": [
        "mkdir -p journal",
        "/usr/lib/systemd/systemd-journal-remote --output=journal/gym.journal events.export >/dev/null 2>&1"
      ],
      "checks": [
        {
          "type": "stdout",
          "description": "The output exactly matched the mission for “Export selected journal fields as JSON”.",
          "expected": "api=API retrying\napi=API started\ndb=Database unavailable",
          "normalize": "trim"
        },
        {
          "type": "file-content",
          "description": "The changing one-time challenge token was copied rather than hard-coded.",
          "path": ".gym-proof",
          "expected": "token={{nonce}}\n",
          "normalize": "exact"
        }
      ],
      "completion": "The json output mode turns each journal entry into structured data that jq can select and reshape. You can now use this behavior as part of a larger command-line workflow.",
      "order": 500,
      "command_order": 100,
      "stage": 5,
      "stage_name": "Blind section capstone",
      "difficulty": "advanced",
      "focus": "journalctl queries systemd's journal. This core rep prints local version information without requiring journal access or changing state. The json output mode turns each journal entry into structured data that jq can select and reshape.",
      "stage_kind": "workflow",
      "mode": "capstone",
      "concepts": [
        "journalctl",
        "pipelines",
        "command chaining",
        "quoting",
        "ordering and grouping"
      ],
      "prerequisites": [
        "cp",
        "sort",
        "jq"
      ],
      "introduced_inline": [],
      "legacy_ids": [],
      "requires_success": true,
      "check_strength": "strict"
    }
  ]
}
