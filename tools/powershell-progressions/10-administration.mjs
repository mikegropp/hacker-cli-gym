import { lesson as l, out, contains, nonempty, exists } from './helpers.mjs';

export default {
  'Get-LocalUser': [
    l({ title: 'Read local-user enabled state', focus: 'Local user objects expose Enabled state without requiring an account change.', example: 'Get-LocalUser | Select Name,Enabled', task: 'Print name=enabled for the first local user sorted by name.', solution: '$u=Get-LocalUser | Sort-Object Name | Select-Object -First 1; "$($u.Name)=$($u.Enabled)"', checks: [nonempty('A local-user state was printed.')] }),
    l({ title: 'Inspect a local-user SID', focus: '-Name retrieves one account and its SID provides the stable security identity.', example: 'Get-LocalUser -Name Administrator | Select Name,SID', task: 'Find the first local user, query it again by -Name, and print name:sid.', solution: '$name=Get-LocalUser | Sort-Object Name | Select-Object -First 1 -ExpandProperty Name; $u=Get-LocalUser -Name $name; "$($u.Name):$($u.SID)"', checks: [contains('S-1-5-')] }),
    l({ title: 'Filter enabled local users', focus: 'Enabled and LastLogon properties support a focused account inventory.', example: 'Get-LocalUser | Where Enabled', task: 'Print enabled-users=<count> and then enabled account names sorted.', solution: '$u=@(Get-LocalUser | Where-Object Enabled | Sort-Object Name); "enabled-users=$($u.Count)"; $u | Select-Object -ExpandProperty Name', checks: [contains('enabled-users=')] }),
    l({ title: 'Create a local-account audit', focus: 'A concise read-only audit can show account name, enabled state, password requirements, and SID.', example: 'Get-LocalUser | Select Name,Enabled,PasswordRequired,SID', task: 'Print name|enabled|password-required|sid for every local user sorted by name.', solution: 'Get-LocalUser | Sort-Object Name | ForEach-Object { "$($_.Name)|$($_.Enabled)|$($_.PasswordRequired)|$($_.SID)" }', checks: [nonempty('A local-account audit was printed.')] }),
  ],
  'Get-LocalGroup': [
    l({ title: 'Read a local-group SID', focus: 'A group SID is stable even when the localized display name differs.', example: 'Get-LocalGroup Administrators | Select Name,SID', task: 'Print the SID for the built-in Administrators group.', solution: 'Get-LocalGroup Administrators | Select-Object -ExpandProperty SID', checks: [contains('S-1-5-32-544')] }),
    l({ title: 'Query a group by SID', focus: '-SID resolves a local group by stable identity instead of display name.', example: 'Get-LocalGroup -SID S-1-5-32-544', task: 'Query S-1-5-32-544 with Get-LocalGroup -SID and print its name.', solution: "Get-LocalGroup -SID 'S-1-5-32-544' | Select-Object -ExpandProperty Name", checks: [nonempty('The Administrators group name was printed.')] }),
    l({ title: 'Filter local-group names', focus: 'Wildcard filtering over group objects creates a focused inventory without changing membership.', example: "Get-LocalGroup | Where Name -like '*Admin*'", task: 'Print local group names containing Admin, sorted.', solution: "Get-LocalGroup | Where-Object Name -like '*Admin*' | Sort-Object Name | Select-Object -ExpandProperty Name", checks: [nonempty('At least one Admin group name was printed.')] }),
    l({ title: 'Create a local-group inventory', focus: 'Name, SID, and Description provide a practical read-only group inventory.', example: 'Get-LocalGroup | Select Name,SID,Description', task: 'Print name|sid|description for every local group sorted by name.', solution: 'Get-LocalGroup | Sort-Object Name | ForEach-Object { "$($_.Name)|$($_.SID)|$($_.Description)" }', checks: [nonempty('A local-group inventory was printed.')] }),
  ],
  'Get-LocalGroupMember': [
    l({ title: 'List member names', focus: 'Group-member objects identify accounts and nested groups assigned to a local group.', example: 'Get-LocalGroupMember Administrators | Select Name', task: 'Print member names of Administrators sorted.', solution: 'Get-LocalGroupMember Administrators | Sort-Object Name | Select-Object -ExpandProperty Name', checks: [nonempty('At least one Administrators member was printed.')] }),
    l({ title: 'Inspect member source and type', focus: 'PrincipalSource and ObjectClass distinguish local, domain, user, and group principals.', example: 'Get-LocalGroupMember Administrators | Select Name,ObjectClass,PrincipalSource', task: 'Print name|object-class|principal-source for Administrators members.', solution: 'Get-LocalGroupMember Administrators | Sort-Object Name | ForEach-Object { "$($_.Name)|$($_.ObjectClass)|$($_.PrincipalSource)" }', checks: [nonempty('An Administrators membership report was printed.')] }),
    l({ title: 'Query a group through the pipeline', focus: 'A LocalGroup object can flow directly into Get-LocalGroupMember.', example: 'Get-LocalGroup Administrators | Get-LocalGroupMember', task: 'Pipe the Administrators group to Get-LocalGroupMember and print members=<count>.', solution: '$m=@(Get-LocalGroup Administrators | Get-LocalGroupMember); "members=$($m.Count)"', checks: [contains('members=')] }),
    l({ title: 'Audit privileged local membership', focus: 'A practical privileged-group audit records stable SIDs and principal classes for every member.', example: 'Get-LocalGroupMember Administrators | Select Name,SID,ObjectClass', task: 'Print class|sid|name rows for Administrators members sorted by name.', solution: 'Get-LocalGroupMember Administrators | Sort-Object Name | ForEach-Object { "$($_.ObjectClass)|$($_.SID)|$($_.Name)" }', checks: [contains('S-1-5-')] }),
  ],
  'Get-ScheduledTask': [
    l({ title: 'Read task state', focus: 'Scheduled task objects expose State and TaskPath without running or changing the task.', example: 'Get-ScheduledTask | Select TaskName,State', task: 'Print task=<name> state=<state> for the first scheduled task sorted by path and name.', solution: '$t=Get-ScheduledTask | Sort-Object TaskPath,TaskName | Select-Object -First 1; "task=$($t.TaskName) state=$($t.State)"', checks: [contains('task='), contains(' state=')] }),
    l({ title: 'Filter by task path', focus: '-TaskPath selects one scheduler folder rather than scanning all folders downstream.', example: "Get-ScheduledTask -TaskPath '\\Microsoft\\Windows\\Defrag\\'", task: 'Find the first task path, query that exact path, and print path=<path> count=<count>.', solution: '$path=Get-ScheduledTask | Select-Object -First 1 -ExpandProperty TaskPath; $tasks=@(Get-ScheduledTask -TaskPath $path); "path=$path count=$($tasks.Count)"', checks: [contains('path='), contains(' count=')] }),
    l({ title: 'Summarize tasks by state', focus: 'Grouping scheduled tasks by State gives a useful scheduler inventory.', example: 'Get-ScheduledTask | Group-Object State', task: 'Print state=count rows for scheduled tasks sorted by state.', solution: 'Get-ScheduledTask | Group-Object State | Sort-Object Name | ForEach-Object { "$($_.Name)=$($_.Count)" }', checks: [nonempty('A scheduled-task state summary was printed.')] }),
    l({ title: 'Create a ready-task inventory', focus: 'Filtering, sorting, and projecting ready tasks creates a concise review list without registering or running tasks.', example: 'Get-ScheduledTask | Where State -eq Ready | Select TaskPath,TaskName', task: 'Print ready-tasks=<count>, then path+name for up to five Ready tasks.', solution: '$ready=@(Get-ScheduledTask | Where-Object State -eq Ready | Sort-Object TaskPath,TaskName); "ready-tasks=$($ready.Count)"; $ready | Select-Object -First 5 | ForEach-Object { "$($_.TaskPath)$($_.TaskName)" }', checks: [contains('ready-tasks=')] }),
  ],
  'Get-WinEvent': [
    l({ title: 'Inspect log metadata', focus: '-ListLog returns configuration and record-count metadata instead of event records.', example: 'Get-WinEvent -ListLog System', task: 'Print Application enabled=<True-or-False> records=<count>.', solution: '$l=Get-WinEvent -ListLog Application; "$($l.LogName) enabled=$($l.IsEnabled) records=$($l.RecordCount)"', checks: [contains('Application enabled='), contains(' records=')] }),
    l({ title: 'Read the newest events', focus: '-MaxEvents bounds a query while the default newest-first order surfaces current records.', example: 'Get-WinEvent -LogName Application -MaxEvents 5', task: 'Print newest-events=<count> after requesting at most five Application events.', solution: '$e=@(Get-WinEvent -LogName Application -MaxEvents 5 -ErrorAction SilentlyContinue); "newest-events=$($e.Count)"', checks: [contains('newest-events=')] }),
    l({ title: 'Use a filter hashtable', focus: '-FilterHashtable sends structured log, provider, level, ID, and time predicates to the event service.', example: 'Get-WinEvent -FilterHashtable @{LogName="Application";Level=2}', task: 'Query up to ten Application warning-or-error events with a filter hashtable and print matched=<count>.', solution: '$e=@(Get-WinEvent -FilterHashtable @{LogName="Application";Level=2,3} -MaxEvents 10 -ErrorAction SilentlyContinue); "matched=$($e.Count)"', checks: [contains('matched=')] }),
    l({ title: 'Create an event-level summary', focus: 'A bounded event query grouped by LevelDisplayName creates a concise recent-log health snapshot.', example: 'Get-WinEvent Application -MaxEvents 100 | Group LevelDisplayName', task: 'Read up to 100 Application events and print level=count rows sorted; print no-events if empty.', solution: '$e=@(Get-WinEvent -LogName Application -MaxEvents 100 -ErrorAction SilentlyContinue); if($e.Count){$e | Group-Object LevelDisplayName | Sort-Object Name | ForEach-Object { "$($_.Name)=$($_.Count)" }}else{"no-events"}', checks: [nonempty('An event-level summary was printed.')] }),
  ],
  'Get-EventLog': [
    l({ title: 'Inspect a classic log', focus: '-List returns classic log metadata including entry counts and size limits.', example: 'Get-EventLog -List | Where Log -eq Application', task: 'Print Application entries=<count> from classic log metadata.', solution: '$l=Get-EventLog -List | Where-Object Log -eq Application; "Application entries=$($l.Entries.Count)"', checks: [contains('Application entries=')] }),
    l({ title: 'Read the newest classic entries', focus: '-Newest bounds the number of records retrieved from a named classic log.', example: 'Get-EventLog Application -Newest 5', task: 'Request up to five newest Application events and print entries=<count>.', solution: '$e=@(Get-EventLog Application -Newest 5 -ErrorAction SilentlyContinue); "entries=$($e.Count)"', checks: [contains('entries=')] }),
    l({ title: 'Filter classic event types', focus: '-EntryType selects categories such as Error and Warning at query time.', example: 'Get-EventLog Application -EntryType Error -Newest 10', task: 'Request up to ten Application Error events and print errors=<count>.', solution: '$e=@(Get-EventLog Application -EntryType Error -Newest 10 -ErrorAction SilentlyContinue); "errors=$($e.Count)"', checks: [contains('errors=')] }),
    l({ title: 'Create a classic event source summary', focus: 'A bounded classic-log query grouped by Source reveals which components produced recent entries.', example: 'Get-EventLog Application -Newest 100 | Group Source', task: 'Read up to 100 Application events and print the five most common source=count rows; print no-events if empty.', solution: '$e=@(Get-EventLog Application -Newest 100 -ErrorAction SilentlyContinue); if($e.Count){$e | Group-Object Source | Sort-Object Count -Descending | Select-Object -First 5 | ForEach-Object { "$($_.Name)=$($_.Count)" }}else{"no-events"}', checks: [nonempty('A classic event-source summary was printed.')] }),
  ],
  'Invoke-Command': [
    l({ title: 'Pass an argument to a script block', focus: '-ArgumentList supplies values to the script block param declaration.', example: 'Invoke-Command { param($n) $n*2 } -ArgumentList 5', task: 'Invoke a local script block with 7 and print 49.', solution: 'Invoke-Command -ScriptBlock { param($n) $n * $n } -ArgumentList 7', checks: out('49') }),
    l({ title: 'Use input data inside a local invocation', focus: 'A local script block can contain a complete pipeline and return its objects.', example: 'Invoke-Command { Get-Content file | Sort-Object }', task: 'Invoke a script block that reads names.txt, sorts it, and prints anna then zoe.', solution: 'Invoke-Command -ScriptBlock { Get-Content names.txt | Sort-Object }', files: { 'names.txt': 'zoe\nanna\n' }, checks: out('anna\nzoe') }),
    l({ title: 'Capture local variables with Using scope', focus: '$using: embeds a caller value into a remotely executable script block; local invocation also demonstrates the syntax contract.', example: '$n=5; Invoke-Command { $using:n * 2 }', task: 'Set value=6 and invoke a script block that prints doubled=12 using the passed argument list.', solution: '$value=6; Invoke-Command -ScriptBlock { param($v) "doubled=$($v*2)" } -ArgumentList $value', checks: out('doubled=12') }),
    l({ title: 'Run a reusable validation block', focus: 'A parameterized script block can validate several files through repeated local Invoke-Command calls.', example: '$check={param($p) ...}; Invoke-Command $check -ArgumentList file', task: 'Invoke one validation script block for app.conf and routes.json and print path=present records.', solution: '$check={param($path) "$path=$(if(Test-Path $path -PathType Leaf){\'present\'}else{\'missing\'})"}; "app.conf","routes.json" | ForEach-Object { Invoke-Command -ScriptBlock $check -ArgumentList $_ }', files: { 'app.conf': '', 'routes.json': '{}' }, checks: out('app.conf=present\nroutes.json=present') }),
  ],
  'Set-StrictMode': [
    l({ title: 'Reject uninitialized variables', focus: 'Strict mode version 1 catches references to variables that have not been assigned.', example: 'Set-StrictMode -Version 1; $missing', task: 'Inside a script block enable strict mode 1, catch an uninitialized-variable error, and print caught.', solution: '& { Set-StrictMode -Version 1; try { $GymMissingVariable } catch { "caught" } }', checks: out('caught') }),
    l({ title: 'Reject missing object properties', focus: 'Strict mode version 2 catches access to properties that are not present.', example: 'Set-StrictMode -Version 2; ([pscustomobject]@{}).missing', task: 'Inside a script block enable strict mode 2, catch access to a missing property, and print missing-property-caught.', solution: '& { Set-StrictMode -Version 2; try { ([pscustomobject]@{name="api"}).status } catch { "missing-property-caught" } }', checks: out('missing-property-caught') }),
    l({ title: 'Disable strict mode locally', focus: '-Off returns the current scope to normal permissive behavior.', example: 'Set-StrictMode -Off', task: 'Inside a script block enable Latest, then turn strict mode Off and print off when an uninitialized variable evaluates to null.', solution: '& { Set-StrictMode -Version Latest; Set-StrictMode -Off; if($GymNeverSet -eq $null){"off"} }', checks: out('off') }),
    l({ title: 'Validate a script under latest rules', focus: 'Running a workflow in a child scope with Latest strict mode catches common scripting mistakes without affecting the parent gym session.', example: '& { Set-StrictMode Latest; ... }', task: 'Run a strict child scope that loads config.json, checks its required mode property, and prints mode=practice.', solution: '& { Set-StrictMode -Version Latest; $c=Get-Content config.json -Raw | ConvertFrom-Json; if([string]::IsNullOrWhiteSpace($c.mode)){throw "mode missing"}; "mode=$($c.mode)" }', files: { 'config.json': '{"mode":"practice"}' }, checks: out('mode=practice') }),
  ],
  'Get-WmiObject': [
    l({ title: 'Select specific WMI properties', focus: '-Property requests only fields required by the workflow.', example: 'Get-WmiObject Win32_OperatingSystem -Property Caption,Version', task: 'Print caption=<value> version=<value> from Win32_OperatingSystem.', solution: '$o=Get-WmiObject Win32_OperatingSystem -Property Caption,Version; "caption=$($o.Caption) version=$($o.Version)"', checks: [contains('caption='), contains(' version=')] }),
    l({ title: 'Filter WMI instances', focus: '-Filter applies a WQL predicate before objects cross into PowerShell.', example: "Get-WmiObject Win32_LogicalDisk -Filter 'DriveType=3'", task: 'Print device IDs of fixed logical disks sorted.', solution: "Get-WmiObject Win32_LogicalDisk -Filter 'DriveType=3' | Sort-Object DeviceID | Select-Object -ExpandProperty DeviceID", checks: [nonempty('At least one fixed logical disk was printed.')] }),
    l({ title: 'Query another namespace explicitly', focus: '-Namespace clarifies which WMI namespace contains the requested class.', example: 'Get-WmiObject -Namespace root\\cimv2 -Class Win32_ComputerSystem', task: 'Query Win32_ComputerSystem in root\cimv2 and print manufacturer=<value> model=<value>.', solution: '$c=Get-WmiObject -Namespace root\cimv2 -Class Win32_ComputerSystem; "manufacturer=$($c.Manufacturer) model=$($c.Model)"', checks: [contains('manufacturer='), contains(' model=')] }),
    l({ title: 'Compare legacy WMI with CIM', focus: 'A migration check can verify that legacy Get-WmiObject and modern Get-CimInstance agree on stable OS fields.', example: '$w=Get-WmiObject Win32_OperatingSystem; $c=Get-CimInstance Win32_OperatingSystem', task: 'Query the OS caption through WMI and CIM and print captions-match=True.', solution: '$w=Get-WmiObject Win32_OperatingSystem; $c=Get-CimInstance Win32_OperatingSystem; "captions-match=$($w.Caption -eq $c.Caption)"', checks: out('captions-match=True') }),
  ],
  'Get-TimeZone': [
    l({ title: 'Read the base UTC offset', focus: 'BaseUtcOffset shows the standard offset before daylight-saving adjustments.', example: '(Get-TimeZone).BaseUtcOffset', task: 'Print id=<zone-id> offset=<base-offset>.', solution: '$z=Get-TimeZone; "id=$($z.Id) offset=$($z.BaseUtcOffset)"', checks: [contains('id='), contains(' offset=')] }),
    l({ title: 'Find an available time zone', focus: '-ListAvailable returns time-zone definitions installed on Windows.', example: 'Get-TimeZone -ListAvailable | Where Id -eq UTC', task: 'Find UTC in the available time-zone list and print its Id.', solution: 'Get-TimeZone -ListAvailable | Where-Object Id -eq UTC | Select-Object -ExpandProperty Id', checks: out('UTC') }),
    l({ title: 'Convert a UTC timestamp to a zone', focus: 'TimeZoneInfo conversion uses the selected zone rules for a specific timestamp.', example: '[TimeZoneInfo]::ConvertTimeBySystemTimeZoneId($utc,$zone.Id)', task: 'Convert 2026-01-01T12:00:00Z to UTC using the UTC zone object and print 2026-01-01T12:00:00.', solution: '$z=Get-TimeZone -Id UTC; $d=[datetime]::Parse("2026-01-01T12:00:00Z").ToUniversalTime(); [TimeZoneInfo]::ConvertTimeFromUtc($d,$z).ToString("yyyy-MM-ddTHH:mm:ss")', checks: out('2026-01-01T12:00:00') }),
    l({ title: 'Create a time-zone comparison report', focus: 'Available time-zone objects can supply offsets for a fixed instant without changing the machine zone.', example: '$zones | ForEach-Object { [TimeZoneInfo]::ConvertTimeFromUtc(...) }', task: 'For UTC and Pacific Standard Time, print zone=local-time at 2026-01-01T12:00:00Z.', solution: '$utc=[datetime]::Parse("2026-01-01T12:00:00Z").ToUniversalTime(); "UTC","Pacific Standard Time" | ForEach-Object { $z=Get-TimeZone -Id $_; "$_=$([TimeZoneInfo]::ConvertTimeFromUtc($utc,$z).ToString(\'yyyy-MM-dd HH:mm\'))" }', checks: out('UTC=2026-01-01 12:00\nPacific Standard Time=2026-01-01 04:00') }),
  ],
};
