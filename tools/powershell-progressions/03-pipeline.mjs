import { lesson as l, out, contains, notContains, content } from './helpers.mjs';

export default {
  'Select-String': [
    l({ title: 'Search case-sensitively', focus: '-CaseSensitive distinguishes values whose letter case carries meaning.', example: "Select-String app.log -Pattern 'ERROR' -CaseSensitive", task: 'Print only the uppercase ERROR line from mixed.log.', solution: "Select-String mixed.log -Pattern 'ERROR' -CaseSensitive | Select-Object -ExpandProperty Line", files: { 'mixed.log': 'error lower\nERROR upper\n' }, checks: out('ERROR upper') }),
    l({ title: 'Show surrounding context', focus: '-Context includes reviewed lines before and after each match.', example: "Select-String app.log -Pattern 'failed' -Context 1,1", task: 'Find FAILED in deploy.log and print the preceding and following context lines as before=<line> and after=<line>.', solution: "$m=Select-String deploy.log -Pattern 'FAILED' -Context 1,1; \"before=$($m.Context.PreContext[0])\"; \"after=$($m.Context.PostContext[0])\"", files: { 'deploy.log': 'prepare\nFAILED validation\nrollback\n' }, checks: out('before=prepare\nafter=rollback') }),
    l({ title: 'Search a file tree', focus: 'File discovery can feed Select-String so only reviewed file types are searched recursively.', example: "Get-ChildItem logs -Recurse -Filter '*.log' | Select-String timeout", task: 'Search every .log file under logs for timeout and print matching relative path:line-number records sorted.', solution: "Get-ChildItem logs -Recurse -File -Filter '*.log' | Select-String -Pattern 'timeout' | ForEach-Object { \"$($_.Path.Substring((Get-Location).Path.Length+1).Replace('\\','/')):$($_.LineNumber)\" } | Sort-Object", files: { 'logs/api.log': 'ready\ntimeout\n', 'logs/nested/web.log': 'timeout\nok\n', 'logs/notes.txt': 'timeout' }, checks: out('logs/api.log:2\nlogs/nested/web.log:1') }),
    l({ title: 'Summarize matches by file', focus: 'Select-String match objects can be grouped to produce a useful incident-search summary.', example: "Select-String logs\*.log ERROR | Group-Object Path", task: 'Count ERROR matches in each .log file and print api.log=2 then web.log=1.', solution: "Select-String -Path logs\*.log -Pattern 'ERROR' | Group-Object Path | Sort-Object Name | ForEach-Object { \"$([IO.Path]::GetFileName($_.Name))=$($_.Count)\" }", files: { 'logs/api.log': 'ERROR one\nok\nERROR two\n', 'logs/web.log': 'ERROR web\n' }, checks: out('api.log=2\nweb.log=1') }),
  ],
  'Measure-Object': [
    l({ title: 'Measure words and characters', focus: '-Word and -Character expose text dimensions beyond a simple line count.', example: "Get-Content note.txt | Measure-Object -Word -Character", task: 'Print words=<count> characters=<count> for the single line one two three.', solution: "$m='one two three' | Measure-Object -Word -Character; \"words=$($m.Words) characters=$($m.Characters)\"", checks: out('words=3 characters=13') }),
    l({ title: 'Sum a numeric property', focus: '-Property and -Sum aggregate one field across incoming objects.', example: 'Import-Csv sales.csv | Measure-Object amount -Sum', task: 'Sum the quantity column in inventory.csv and print total=10.', solution: '$m=Import-Csv inventory.csv | Measure-Object quantity -Sum; "total=$([int]$m.Sum)"', files: { 'inventory.csv': 'item,quantity\na,2\nb,3\nc,5\n' }, checks: out('total=10') }),
    l({ title: 'Calculate a numeric range', focus: 'Minimum, maximum, and average can be requested together for one numeric stream.', example: '1,5,9 | Measure-Object -Minimum -Maximum -Average', task: 'Measure 4, 8, 12, and 16 and print min=4 max=16 average=10.', solution: '$m=4,8,12,16 | Measure-Object -Minimum -Maximum -Average; "min=$($m.Minimum) max=$($m.Maximum) average=$($m.Average)"', checks: out('min=4 max=16 average=10') }),
    l({ title: 'Summarize totals by category', focus: 'Grouping before Measure-Object creates per-category numeric summaries in one pipeline workflow.', example: 'Import-Csv usage.csv | Group-Object team | ForEach-Object { $_.Group | Measure-Object hours -Sum }', task: 'Print blue=7 and green=5 total hours from usage.csv, sorted by team.', solution: "Import-Csv usage.csv | Group-Object team | Sort-Object Name | ForEach-Object { $sum=$_.Group | Measure-Object hours -Sum; \"$($_.Name)=$([int]$sum.Sum)\" }", files: { 'usage.csv': 'team,hours\nblue,3\ngreen,5\nblue,4\n' }, checks: out('blue=7\ngreen=5') }),
  ],
  'Sort-Object': [
    l({ title: 'Sort numbers descending', focus: '-Descending reverses the selected property or value ordering.', example: '3,1,2 | Sort-Object -Descending', task: 'Print 42, 10, 7, and 3 in descending numeric order.', solution: '42,3,10,7 | Sort-Object -Descending', checks: out('42\n10\n7\n3') }),
    l({ title: 'Sort by multiple properties', focus: 'A property list establishes primary and secondary sort keys.', example: 'Import-Csv users.csv | Sort-Object team,name', task: 'Sort people.csv by team then name and print team/name records.', solution: 'Import-Csv people.csv | Sort-Object team,name | ForEach-Object { "$($_.team)/$($_.name)" }', files: { 'people.csv': 'name,team\nzoe,red\nanna,blue\nben,blue\n' }, checks: out('blue/anna\nblue/ben\nred/zoe') }),
    l({ title: 'Sort uniquely', focus: '-Unique removes duplicate values as part of the sorted result.', example: '3,1,3,2 | Sort-Object -Unique', task: 'Print each severity from severities.txt once in alphabetical order.', solution: 'Get-Content severities.txt | Sort-Object -Unique', files: { 'severities.txt': 'warning\nerror\ninfo\nwarning\nerror\n' }, checks: out('error\ninfo\nwarning') }),
    l({ title: 'Rank and select top records', focus: 'A descending property sort followed by Select-Object creates a clear top-N report.', example: 'Import-Csv scores.csv | Sort-Object score -Descending | Select-Object -First 3', task: 'Print the two highest-scoring names from scores.csv; break score ties alphabetically.', solution: 'Import-Csv scores.csv | Sort-Object @{Expression={[int]$_.score};Descending=$true},name | Select-Object -First 2 -ExpandProperty name', files: { 'scores.csv': 'name,score\nzoe,8\nanna,10\nben,10\nlee,7\n' }, checks: out('anna\nben') }),
  ],
  'Group-Object': [
    l({ title: 'Count values without retaining groups', focus: '-NoElement returns concise group counts without embedding every original object.', example: 'Get-Content states.txt | Group-Object -NoElement', task: 'Group states.txt and print name=count rows sorted by name.', solution: 'Get-Content states.txt | Group-Object -NoElement | Sort-Object Name | ForEach-Object { "$($_.Name)=$($_.Count)" }', files: { 'states.txt': 'ready\nfailed\nready\npending\nready\n' }, checks: out('failed=1\npending=1\nready=3') }),
    l({ title: 'Group by a calculated value', focus: 'A script-block property can derive a grouping key from each input value.', example: "1..5 | Group-Object { $_ % 2 }", task: 'Group 1 through 6 as even and odd and print even=3 then odd=3.', solution: "1..6 | Group-Object { if($_ % 2 -eq 0){'even'}else{'odd'} } | Sort-Object Name | ForEach-Object { \"$($_.Name)=$($_.Count)\" }", checks: out('even=3\nodd=3') }),
    l({ title: 'Group by several properties', focus: 'Multiple grouping properties create a composite key for each distinct combination.', example: 'Import-Csv hosts.csv | Group-Object region,status', task: 'Group hosts.csv by region and status, then print composite-name=count records sorted.', solution: 'Import-Csv hosts.csv | Group-Object region,status | Sort-Object Name | ForEach-Object { "$($_.Name)=$($_.Count)" }', files: { 'hosts.csv': 'host,region,status\na,east,up\nb,east,down\nc,east,up\nd,west,up\n' }, checks: out('east, down=1\neast, up=2\nwest, up=1') }),
    l({ title: 'Build a grouped capacity report', focus: 'Each group retains its member objects, allowing a second aggregation inside the group.', example: 'Import-Csv disks.csv | Group-Object server | ForEach-Object { ... Measure-Object size -Sum }', task: 'Print api=150 and db=200 by summing capacity for each server in disks.csv.', solution: "Import-Csv disks.csv | Group-Object server | Sort-Object Name | ForEach-Object { $m=$_.Group | Measure-Object capacity -Sum; \"$($_.Name)=$([int]$m.Sum)\" }", files: { 'disks.csv': 'server,capacity\napi,100\ndb,200\napi,50\n' }, checks: out('api=150\ndb=200') }),
  ],
  'Select-Object': [
    l({ title: 'Select the first records', focus: '-First limits a pipeline after upstream ordering or filtering.', example: 'Get-Process | Select-Object -First 5', task: 'Print only the first two lines of names.txt.', solution: 'Get-Content names.txt | Select-Object -First 2', files: { 'names.txt': 'anna\nben\nzoe\n' }, checks: out('anna\nben') }),
    l({ title: 'Select unique values', focus: '-Unique retains the first occurrence of each adjacent value in the selected stream.', example: '1,1,2,2 | Select-Object -Unique', task: 'Sort teams.txt and print each team once using Select-Object -Unique.', solution: 'Get-Content teams.txt | Sort-Object | Select-Object -Unique', files: { 'teams.txt': 'red\nblue\nred\ngreen\nblue\n' }, checks: out('blue\ngreen\nred') }),
    l({ title: 'Create a calculated property', focus: 'A calculated property renames or derives output while preserving object structure.', example: "Get-Process | Select-Object Name,@{n='MB';e={$_.WorkingSet/1MB}}", task: 'Select name and a calculated doubled property from values.csv, then print name=doubled records.', solution: "Import-Csv values.csv | Select-Object name,@{Name='doubled';Expression={2*[int]$_.value}} | ForEach-Object { \"$($_.name)=$($_.doubled)\" }", files: { 'values.csv': 'name,value\na,3\nb,5\n' }, checks: out('a=6\nb=10') }),
    l({ title: 'Project a clean API report', focus: 'Filtering, sorting, and calculated selection can reshape noisy source records into a stable report contract.', example: 'Import-Csv api.csv | Select-Object Name,Status', task: 'From services.csv select active services, sort by name, and print name=url using a calculated url property http://<host>:<port>.', solution: "Import-Csv services.csv | Where-Object status -eq active | Sort-Object name | Select-Object name,@{Name='url';Expression={\"http://$($_.host):$($_.port)\"}} | ForEach-Object { \"$($_.name)=$($_.url)\" }", files: { 'services.csv': 'name,host,port,status\nweb,web.local,80,active\ndb,db.local,5432,inactive\napi,api.local,8080,active\n' }, checks: out('api=http://api.local:8080\nweb=http://web.local:80') }),
  ],
  'Where-Object': [
    l({ title: 'Filter with a comparison operator', focus: 'Simplified property syntax handles direct comparisons concisely.', example: 'Get-Process | Where-Object CPU -gt 10', task: 'Print item names whose numeric quantity is greater than 3.', solution: 'Import-Csv items.csv | Where-Object quantity -gt 3 | Select-Object -ExpandProperty name', files: { 'items.csv': 'name,quantity\none,2\ntwo,5\nthree,4\n' }, checks: out('two\nthree') }),
    l({ title: 'Filter with a regular expression', focus: '-match selects string properties using regular-expression patterns.', example: "Import-Csv hosts.csv | Where-Object name -match '^web-'", task: 'Print host names beginning with api- and ending in a digit.', solution: "Import-Csv hosts.csv | Where-Object name -match '^api-.*\\d$' | Select-Object -ExpandProperty name", files: { 'hosts.csv': 'name\napi-prod1\napi-test\nweb-prod1\napi-dev2\n' }, checks: out('api-prod1\napi-dev2') }),
    l({ title: 'Combine filter conditions', focus: 'A script block supports boolean combinations across several object properties.', example: 'Where-Object { $_.status -eq "up" -and $_.latency -lt 100 }', task: 'Print active services in the west region whose latency is below 100.', solution: "Import-Csv services.csv | Where-Object { $_.status -eq 'active' -and $_.region -eq 'west' -and [int]$_.latency -lt 100 } | Select-Object -ExpandProperty name", files: { 'services.csv': 'name,status,region,latency\na,active,west,50\nb,active,east,40\nc,inactive,west,20\nd,active,west,120\n' }, checks: out('a') }),
    l({ title: 'Find deployment candidates', focus: 'A practical candidate filter can combine state, semantic version data, and exclusion flags before sorting.', example: 'Import-Csv builds.csv | Where-Object { ... }', task: 'Print ready builds with version 2 or newer that are not blocked, sorted by name.', solution: "Import-Csv builds.csv | Where-Object { $_.status -eq 'ready' -and [int]$_.version -ge 2 -and $_.blocked -eq 'false' } | Sort-Object name | Select-Object -ExpandProperty name", files: { 'builds.csv': 'name,status,version,blocked\napi,ready,2,false\nweb,ready,3,true\ndb,pending,4,false\nworker,ready,5,false\nlegacy,ready,1,false\n' }, checks: out('api\nworker') }),
  ],
  'ForEach-Object': [
    l({ title: 'Transform each pipeline value', focus: 'The process script block applies one expression to every incoming object.', example: '1..3 | ForEach-Object { $_ * 2 }', task: 'Print the square of each number from 1 through 4.', solution: '1..4 | ForEach-Object { $_ * $_ }', checks: out('1\n4\n9\n16') }),
    l({ title: 'Invoke an object method', focus: '-MemberName calls the same method on every pipeline object.', example: "'a','b' | ForEach-Object -MemberName ToUpper", task: 'Trim whitespace from every line in values.txt with -MemberName Trim.', solution: 'Get-Content values.txt | ForEach-Object -MemberName Trim', files: { 'values.txt': ' alpha \n beta  \n' }, checks: out('alpha\nbeta') }),
    l({ title: 'Use begin, process, and end blocks', focus: 'The three script blocks initialize state, process inputs, and emit one final result.', example: '1..3 | ForEach-Object {$sum=0} {$sum+=$_} {$sum}', task: 'Use ForEach-Object begin/process/end blocks to print sum=15 for 1 through 5.', solution: '1..5 | ForEach-Object -Begin { $sum=0 } -Process { $sum += $_ } -End { "sum=$sum" }', checks: out('sum=15') }),
    l({ title: 'Build files from structured rows', focus: 'A per-object workflow can derive a path and content from each structured input record.', example: 'Import-Csv services.csv | ForEach-Object { Set-Content "$($_.name).conf" ... }', task: 'Create generated\api.conf and generated\web.conf from services.csv, each containing port=<port>, then print both files in name order.', solution: "New-Item generated -ItemType Directory | Out-Null; Import-Csv services.csv | ForEach-Object { Set-Content \"generated\\$($_.name).conf\" \"port=$($_.port)\" }; Get-Content generated\api.conf,generated\web.conf", files: { 'services.csv': 'name,port\napi,8080\nweb,80\n' }, checks: [content('generated/api.conf', 'port=8080'), content('generated/web.conf', 'port=80'), ...out('port=8080\nport=80')] }),
  ],
  'Get-Unique': [
    l({ title: 'Remove adjacent duplicate numbers', focus: 'Get-Unique compares each value with the immediately preceding value.', example: '1,1,2,2 | Get-Unique', task: 'Sort 4, 2, 4, 1, 2 and print unique numbers with Get-Unique.', solution: '4,2,4,1,2 | Sort-Object | Get-Unique', checks: out('1\n2\n4') }),
    l({ title: 'Compare values as strings', focus: '-AsString uses each input object string representation when identifying adjacent duplicates.', example: "'A','a' | Get-Unique -AsString", task: 'Sort code strings case-insensitively and print one of each using Get-Unique -AsString.', solution: "'B','A','B','C','A' | Sort-Object | Get-Unique -AsString", checks: out('A\nB\nC') }),
    l({ title: 'Select unique object properties', focus: 'Sorting objects by a property lets Get-Unique -OnType compare the object identities; selecting the property first is clearer for value uniqueness.', example: 'Import-Csv hosts.csv | Sort-Object region | Select-Object -Expand region | Get-Unique', task: 'Print unique regions from hosts.csv alphabetically using Get-Unique.', solution: 'Import-Csv hosts.csv | Sort-Object region | Select-Object -ExpandProperty region | Get-Unique', files: { 'hosts.csv': 'host,region\na,west\nb,east\nc,west\nd,north\n' }, checks: out('east\nnorth\nwest') }),
    l({ title: 'Deduplicate a normalized manifest', focus: 'Normalization before sorting and Get-Unique removes spelling and whitespace differences from an input manifest.', example: 'Get-Content names.txt | ForEach-Object Trim | Sort-Object | Get-Unique', task: 'Trim and lowercase every entry in tags.txt, then print unique normalized tags alphabetically.', solution: 'Get-Content tags.txt | ForEach-Object { $_.Trim().ToLowerInvariant() } | Sort-Object | Get-Unique', files: { 'tags.txt': ' Production \nproduction\nAPI\n api \nDatabase\n' }, checks: out('api\ndatabase\nproduction') }),
  ],
  'Compare-Object': [
    l({ title: 'Show only reference-side values', focus: 'SideIndicator <= identifies values present only in the reference collection.', example: 'Compare-Object expected actual | Where-Object SideIndicator -eq "<="', task: 'Compare expected.txt with actual.txt and print only the expected value that is missing from actual.', solution: '$expected=Get-Content expected.txt; $actual=Get-Content actual.txt; Compare-Object $expected $actual | Where-Object SideIndicator -eq "<=" | Select-Object -ExpandProperty InputObject', files: { 'expected.txt': 'alpha\nbeta\ngamma\n', 'actual.txt': 'alpha\ngamma\n' }, checks: out('beta') }),
    l({ title: 'Include equal values', focus: '-IncludeEqual emits shared values with the == side indicator.', example: 'Compare-Object a b -IncludeEqual', task: 'Compare alpha,beta with beta,gamma using -IncludeEqual and print beta===.', solution: 'Compare-Object alpha,beta beta,gamma -IncludeEqual | Where-Object SideIndicator -eq "==" | ForEach-Object { "$($_.InputObject)=$($_.SideIndicator)" }', checks: out('beta===') }),
    l({ title: 'Compare objects by a property', focus: '-Property compares selected fields instead of complete object representations.', example: 'Compare-Object $old $new -Property name,version', task: 'Compare old.csv and new.csv by name and version; print the changed name/version/side rows.', solution: '$old=Import-Csv old.csv; $new=Import-Csv new.csv; Compare-Object $old $new -Property name,version | Sort-Object SideIndicator | ForEach-Object { "$($_.name):$($_.version):$($_.SideIndicator)" }', files: { 'old.csv': 'name,version\napi,1\nweb,1\n', 'new.csv': 'name,version\napi,2\nweb,1\n' }, checks: [contains('api:1:<='), contains('api:2:=>')] }),
    l({ title: 'Create a deployment drift report', focus: 'A set comparison can label missing and unexpected artifacts for an actionable drift report.', example: 'Compare-Object desired actual | ForEach-Object { ... }', task: 'Compare desired.txt against actual.txt and print missing=db.conf and unexpected=debug.log.', solution: '$d=Get-Content desired.txt; $a=Get-Content actual.txt; Compare-Object $d $a | Sort-Object SideIndicator -Descending | ForEach-Object { if($_.SideIndicator -eq "<="){"missing=$($_.InputObject)"}else{"unexpected=$($_.InputObject)"} }', files: { 'desired.txt': 'api.conf\ndb.conf\nweb.conf\n', 'actual.txt': 'api.conf\ndebug.log\nweb.conf\n' }, checks: out('missing=db.conf\nunexpected=debug.log') }),
  ],
  'Tee-Object': [
    l({ title: 'Capture pipeline values in a variable', focus: '-Variable stores the passing objects while leaving them in the pipeline.', example: 'Get-Process | Tee-Object -Variable processes | Measure-Object', task: 'Pipe 2, 4, and 6 through Tee-Object into values, print their sum from the pipeline, then print captured=3.', solution: '$sum = 2,4,6 | Tee-Object -Variable values | Measure-Object -Sum | Select-Object -ExpandProperty Sum; $sum; "captured=$($values.Count)"', checks: out('12\ncaptured=3') }),
    l({ title: 'Checkpoint transformed values', focus: 'Tee-Object can save a transformed stream before a later aggregation consumes it.', example: '1..3 | ForEach-Object { $_ * $_ } | Tee-Object squares.txt | Measure-Object -Sum', task: 'Square 1 through 4, tee the squares to squares.txt, and print sum=30.', solution: '$sum=1..4 | ForEach-Object { $_ * $_ } | Tee-Object -FilePath squares.txt | Measure-Object -Sum | Select-Object -ExpandProperty Sum; "sum=$sum"', checks: [content('squares.txt', '1\n4\n9\n16'), ...out('sum=30')] }),
    l({ title: 'Inspect and preserve filtered data', focus: 'Tee-Object can save an intermediate pipeline stage before later aggregation changes its shape.', example: 'Import-Csv data.csv | Where status -eq active | Tee-Object active.csv | Measure-Object', task: 'Filter active rows from inventory.csv, tee their names to active.txt, and print count=2.', solution: '$count = Import-Csv inventory.csv | Where-Object status -eq active | Select-Object -ExpandProperty name | Tee-Object -FilePath active.txt | Measure-Object | Select-Object -ExpandProperty Count; "count=$count"', files: { 'inventory.csv': 'name,status\napi,active\ndb,inactive\nweb,active\n' }, checks: [content('active.txt', 'api\nweb'), ...out('count=2')] }),
    l({ title: 'Create a reviewed transformation checkpoint', focus: 'A practical workflow tees normalized data to a checkpoint file before building its final summary.', example: 'Get-Content raw.txt | ForEach Trim | Tee normalized.txt | Group-Object', task: 'Normalize tags.txt to trimmed lowercase values, tee them to normalized.txt, then print unique tags alphabetically.', solution: 'Get-Content tags.txt | ForEach-Object { $_.Trim().ToLowerInvariant() } | Tee-Object normalized.txt | Sort-Object -Unique', files: { 'tags.txt': ' API \nweb\napi\n DB \n' }, checks: [content('normalized.txt', 'api\nweb\napi\ndb'), ...out('api\ndb\nweb')] }),
  ],
};
